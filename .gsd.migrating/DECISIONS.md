# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | migration | migrated-summary | Direct xEdit launch now uses split parsed argv tokens for -autoload and the exact plugin file name, accepting the Phase 6 A2 compatibility assumption documented in research/reviews. | Direct xEdit launch now uses split parsed argv tokens for -autoload and the exact plugin file name, accepting the Phase 6 A2 compatibility assumption documented in research/reviews. | Migrated from legacy summary key-decisions | Yes | agent |
| D002 | migration | migrated-summary | MO2 mode keeps one nested -a payload, but only after the outer MO2 argv is built with ArgumentList. | MO2 mode keeps one nested -a payload, but only after the outer MO2 argv is built with ArgumentList. | Migrated from legacy summary key-decisions | Yes | agent |
| D003 | migration | migrated-summary | ProcessExecutionService now clones ArgumentList entries when present and only falls back to Arguments for legacy callers with an empty ArgumentList. | ProcessExecutionService now clones ArgumentList entries when present and only falls back to Arguments for legacy callers with an empty ArgumentList. | Migrated from legacy summary key-decisions | Yes | agent |
| D004 | migration | migrated-summary | Process-start debug logging reports ArgumentList entry count instead of relying on or disclosing a full argument string. | Process-start debug logging reports ArgumentList entry count instead of relying on or disclosing a full argument string. | Migrated from legacy summary key-decisions | Yes | agent |
| D005 | migration | migrated-summary | Command-build failures now name plugin and launch mode while stating no process was started and pointing users to logs instead of exposing configured paths or command lines. | Command-build failures now name plugin and launch mode while stating no process was started and pointing users to logs instead of exposing configured paths or command lines. | Migrated from legacy summary key-decisions | Yes | agent |
| D006 | migration | migrated-summary | Mocked launch-start failures remain in the existing `xEdit exited with code -1` failed flow without adding a new session policy for Phase 6. | Mocked launch-start failures remain in the existing `xEdit exited with code -1` failed flow without adding a new session policy for Phase 6. | Migrated from legacy summary key-decisions | Yes | agent |
| D007 | migration | migrated-summary | MO2 mode now fails command construction when Mo2ExecutablePath is null, empty, or whitespace instead of falling back to direct xEdit. | MO2 mode now fails command construction when Mo2ExecutablePath is null, empty, or whitespace instead of falling back to direct xEdit. | Migrated from legacy summary key-decisions | Yes | agent |
| D008 | migration | migrated-summary | Unexpected cleaning exceptions are logged with technical details but return a fixed plugin-scoped user message. | Unexpected cleaning exceptions are logged with technical details but return a fixed plugin-scoped user message. | Migrated from legacy summary key-decisions | Yes | agent |
| D009 | migration | migrated-summary | Track BackupFileCopier output ownership with a local createdOutput flag rather than changing backup destination naming or copy policy. | Track BackupFileCopier output ownership with a local createdOutput flag rather than changing backup destination naming or copy policy. | Migrated from legacy summary key-decisions | Yes | agent |
| D010 | migration | migrated-summary | Set ownership through an explicit onDestinationOpened callback immediately after destination FileStream construction succeeds. | Set ownership through an explicit onDestinationOpened callback immediately after destination FileStream construction succeeds. | Migrated from legacy summary key-decisions | Yes | agent |
| D011 | migration | migrated-summary | Keep the existing fixed .autoqac-tmp suffix for atomic restore temp files in this gap-closure plan. | Keep the existing fixed .autoqac-tmp suffix for atomic restore temp files in this gap-closure plan. | Migrated from legacy summary key-decisions | Yes | agent |
| D012 | migration | migrated-summary | Restore services now require an explicit trusted restore root and fail closed when it is missing or invalid. | Restore services now require an explicit trusted restore root and fail closed when it is missing or invalid. | Migrated from legacy summary key-decisions | Yes | agent |
| D013 | migration | migrated-summary | Restore target containment is string-level Path.GetFullPath validation and intentionally does not resolve NTFS reparse points or symlinks. | Restore target containment is string-level Path.GetFullPath validation and intentionally does not resolve NTFS reparse points or symlinks. | Migrated from legacy summary key-decisions | Yes | agent |
| D014 | migration | migrated-summary | RestoreWindow disables Restore Selected and Restore All until LoadSessionsAsync receives the configured game Data folder. | RestoreWindow disables Restore Selected and Restore All until LoadSessionsAsync receives the configured game Data folder. | Migrated from legacy summary key-decisions | Yes | agent |
| D015 | migration | migrated-summary | Regression coverage for the normal ShowProgressAsync lifecycle uses Regex.IsMatch (lambda OR method-group syntax, alternate guard variable names) so future refactors that preserve the contract do not break the test. | Regression coverage for the normal ShowProgressAsync lifecycle uses Regex.IsMatch (lambda OR method-group syntax, alternate guard variable names) so future refactors that preserve the contract do not break the test. | Migrated from legacy summary key-decisions | Yes | agent |
| D016 | migration | migrated-summary | Add explicit normal-path CloseRequested + Closed wiring in ShowProgressAsync as defense-in-depth alongside the pre-existing ProgressWindow.OnDataContextChanged + OnClosed contract; document the dual subscription with a literal `// Defense in depth` comment so future readers do not deduplicate the wiring. | Add explicit normal-path CloseRequested + Closed wiring in ShowProgressAsync as defense-in-depth alongside the pre-existing ProgressWindow.OnDataContextChanged + OnClosed contract; document the dual subscription with a literal `// Defense in depth` comment so future readers do not deduplicate the wiring. | Migrated from legacy summary key-decisions | Yes | agent |
| D017 | migration | migrated-summary | Use a local `var progressDisposed = false` guard plus a local `DisposeProgressViewModel()` function so disposal stays idempotent if ProgressWindow.OnClosed -> DisposeViewModelIfNeeded already disposed first. | Use a local `var progressDisposed = false` guard plus a local `DisposeProgressViewModel()` function so disposal stays idempotent if ProgressWindow.OnClosed -> DisposeViewModelIfNeeded already disposed first. | Migrated from legacy summary key-decisions | Yes | agent |
| D018 | migration | migrated-summary | RestoreWindow Delete Session containment policy lives in IBackupService.DeleteSessionAsync, not in the ViewModel. Filesystem work runs through the existing IBackupSessionDeleter seam. This resolves the cross-AI reviewer consensus (the agent + Codex MEDIUM-HIGH; Gemini accepting) that ViewModels must not call System.IO.Directory.Delete when an injectable service seam already exists. | RestoreWindow Delete Session containment policy lives in IBackupService.DeleteSessionAsync, not in the ViewModel. Filesystem work runs through the existing IBackupSessionDeleter seam. This resolves the cross-AI reviewer consensus (the agent + Codex MEDIUM-HIGH; Gemini accepting) that ViewModels must not call System.IO.Directory.Delete when an injectable service seam already exists. | Migrated from legacy summary key-decisions | Yes | agent |
| D019 | migration | migrated-summary | DeleteSessionAsync delegates string-level containment validation to BackupPathContainment.IsContained from Plan 07-14, eliminating the helper-duplication risk (HIGH from the agent + Codex; Gemini consistent-normalization). | DeleteSessionAsync delegates string-level containment validation to BackupPathContainment.IsContained from Plan 07-14, eliminating the helper-duplication risk (HIGH from the agent + Codex; Gemini consistent-normalization). | Migrated from legacy summary key-decisions | Yes | agent |
| D020 | migration | migrated-summary | DeleteSessionCommand.CanExecute is gated on _backupRoot non-null/non-whitespace AND HasTrustedRestoreRoot AND !IsRestoreActive, with a defense-in-depth top-of-method guard for direct ExecuteAsync(null) callers. | DeleteSessionCommand.CanExecute is gated on _backupRoot non-null/non-whitespace AND HasTrustedRestoreRoot AND !IsRestoreActive, with a defense-in-depth top-of-method guard for direct ExecuteAsync(null) callers. | Migrated from legacy summary key-decisions | Yes | agent |
| D021 | migration | migrated-summary | One canonical sentence ('The selected backup session is outside the configured backup folder.') is shared between StatusText and dialog details for the out-of-root branch; generic 'Technical details were written to the log.' is used for IO failures so exception messages never reach the user (D-04 concise reason pattern). | One canonical sentence ('The selected backup session is outside the configured backup folder.') is shared between StatusText and dialog details for the out-of-root branch; generic 'Technical details were written to the log.' is used for IO failures so exception messages never reach the user (D-04 concise reason pattern). | Migrated from legacy summary key-decisions | Yes | agent |
| D022 | migration | migrated-summary | BackupPathContainment.IsContained is the single shared owner of string-level containment policy for backup/restore/delete safety boundaries; future changes to containment semantics happen in one file. | BackupPathContainment.IsContained is the single shared owner of string-level containment policy for backup/restore/delete safety boundaries; future changes to containment semantics happen in one file. | Migrated from legacy summary key-decisions | Yes | agent |
| D023 | migration | migrated-summary | Helper visibility is internal with explicit InternalsVisibleTo(AutoQAC.Tests) instead of public; ViewModel and service consumers stay inside AutoQAC and tests still cover the helper directly. | Helper visibility is internal with explicit InternalsVisibleTo(AutoQAC.Tests) instead of public; ViewModel and service consumers stay inside AutoQAC and tests still cover the helper directly. | Migrated from legacy summary key-decisions | Yes | agent |
| D024 | migration | migrated-summary | EnsureTrailingDirectorySeparator remains in BackupService.cs because ValidateBackupDestination (line 503) and ValidateRestoreEntry (line 580) still call it; only the IsRestoreTargetInsideTrustedRoot duplication is collapsed in this plan. | EnsureTrailingDirectorySeparator remains in BackupService.cs because ValidateBackupDestination (line 503) and ValidateRestoreEntry (line 580) still call it; only the IsRestoreTargetInsideTrustedRoot duplication is collapsed in this plan. | Migrated from legacy summary key-decisions | Yes | agent |
| D025 | migration | migrated-summary | Plan 07-13 will consume BackupPathContainment.IsContained from RestoreViewModel.DeleteSessionAsync rather than reintroducing duplicate containment logic in the ViewModel layer. | Plan 07-13 will consume BackupPathContainment.IsContained from RestoreViewModel.DeleteSessionAsync rather than reintroducing duplicate containment logic in the ViewModel layer. | Migrated from legacy summary key-decisions | Yes | agent |
| D026 | migration | migrated-summary | Use managed FileStream copy behind IBackupFileCopier so later plans can swap internals without changing service/UI contracts. | Use managed FileStream copy behind IBackupFileCopier so later plans can swap internals without changing service/UI contracts. | Migrated from legacy summary key-decisions | Yes | agent |
| D027 | migration | migrated-summary | Use ReplaceAtomically restore semantics to preserve existing target files until the temporary copy fully succeeds. | Use ReplaceAtomically restore semantics to preserve existing target files until the temporary copy fully succeeds. | Migrated from legacy summary key-decisions | Yes | agent |
| D028 | migration | migrated-summary | Keep SourceMissing as an operation-neutral reason and map it to Missing backup file only in restore contexts. | Keep SourceMissing as an operation-neutral reason and map it to Missing backup file only in restore contexts. | Migrated from legacy summary key-decisions | Yes | agent |
| D029 | migration | migrated-summary | Keep legacy synchronous restore and cleanup APIs on direct synchronous filesystem paths while async callers use structured outcomes. | Keep legacy synchronous restore and cleanup APIs on direct synchronous filesystem paths while async callers use structured outcomes. | Migrated from legacy summary key-decisions | Yes | agent |
| D030 | migration | migrated-summary | Retention cleanup deletes only directories with readable backup session metadata and reports malformed directories as kept. | Retention cleanup deletes only directories with readable backup session metadata and reports malformed directories as kept. | Migrated from legacy summary key-decisions | Yes | agent |
| D031 | migration | migrated-summary | Current session protection is applied before retention counting, so maxSessionCount controls non-current sessions only. | Current session protection is applied before retention counting, so maxSessionCount controls non-current sessions only. | Migrated from legacy summary key-decisions | Yes | agent |
| D032 | migration | migrated-summary | Represent backup and retention cleanup as separate AppState.BackupOperation state so the UI can add a non-xEdit cancel affordance without reusing xEdit Stop semantics. | Represent backup and retention cleanup as separate AppState.BackupOperation state so the UI can add a non-xEdit cancel affordance without reusing xEdit Stop semantics. | Migrated from legacy summary key-decisions | Yes | agent |
| D033 | migration | migrated-summary | Use a short-lived linked CancellationTokenSource only while backup or retention file work is active, guarded separately from the session CTS and process lock. | Use a short-lived linked CancellationTokenSource only while backup or retention file work is active, guarded separately from the session CTS and process lock. | Migrated from legacy summary key-decisions | Yes | agent |
| D034 | migration | migrated-summary | Map canceled backup copies to a skipped plugin result with `Backup canceled` and skip xEdit launch for that plugin while allowing the session to continue. | Map canceled backup copies to a skipped plugin result with `Backup canceled` and skip xEdit launch for that plugin while allowing the session to continue. | Migrated from legacy summary key-decisions | Yes | agent |
| D035 | migration | migrated-summary | RestoreWindow uses inline structured restore results instead of success popups or auto-closing after restore completion. | RestoreWindow uses inline structured restore results instead of success popups or auto-closing after restore completion. | Migrated from legacy summary key-decisions | Yes | agent |
| D036 | migration | migrated-summary | RestoreViewModel owns a short-lived restore CancellationTokenSource and disables restore/delete/refresh commands while it is active. | RestoreViewModel owns a short-lived restore CancellationTokenSource and disables restore/delete/refresh commands while it is active. | Migrated from legacy summary key-decisions | Yes | agent |
| D037 | migration | migrated-summary | Restore byte progress uses decimal units with one fractional digit to match the Phase 07 UI contract. | Restore byte progress uses decimal units with one fractional digit to match the Phase 07 UI contract. | Migrated from legacy summary key-decisions | Yes | agent |
| D038 | migration | migrated-summary | Reuse ICleaningOrchestrator.CancelBackupOperationAsync for both Cancel Backup and Cancel Cleanup so non-xEdit cancellation remains separate from Stop/ForceStop. | Reuse ICleaningOrchestrator.CancelBackupOperationAsync for both Cancel Backup and Cancel Cleanup so non-xEdit cancellation remains separate from Stop/ForceStop. | Migrated from legacy summary key-decisions | Yes | agent |
| D039 | migration | migrated-summary | Show backup/retention operation state as a compact progress band above plugin progress while preserving the existing hang-warning banner. | Show backup/retention operation state as a compact progress band above plugin progress while preserving the existing hang-warning banner. | Migrated from legacy summary key-decisions | Yes | agent |
| D040 | migration | migrated-summary | Use exact required behavioral test names plus a source-level sequential guard to make Phase 7 verification auditable. | Use exact required behavioral test names plus a source-level sequential guard to make Phase 7 verification auditable. | Migrated from legacy summary key-decisions | Yes | agent |
| D041 | migration | migrated-summary | Publish BackupFailureChoice.SkipPlugin as a skipped PluginCleaningResult instead of only mutating SkippedPlugins. | Publish BackupFailureChoice.SkipPlugin as a skipped PluginCleaningResult instead of only mutating SkippedPlugins. | Migrated from legacy summary key-decisions | Yes | agent |
| D042 | migration | migrated-summary | Finalize BackupFailureChoice.AbortSession inside the backup-failure branch because it returns before the normal end-of-method session finalization path. | Finalize BackupFailureChoice.AbortSession inside the backup-failure branch because it returns before the normal end-of-method session finalization path. | Migrated from legacy summary key-decisions | Yes | agent |
| D043 | migration | migrated-summary | Treat backup session metadata as untrusted input: FileName must be a simple non-rooted name and the resolved backup path must stay inside the selected session directory. | Treat backup session metadata as untrusted input: FileName must be a simple non-rooted name and the resolved backup path must stay inside the selected session directory. | Migrated from legacy summary key-decisions | Yes | agent |
| D044 | migration | migrated-summary | Use rooted OriginalPath plus Path.GetFullPath normalization as the restore target safety policy available at the BackupService boundary. | Use rooted OriginalPath plus Path.GetFullPath normalization as the restore target safety policy available at the BackupService boundary. | Migrated from legacy summary key-decisions | Yes | agent |
| D045 | migration | migrated-summary | Carry retention count progress on BackupCopyProgress optional count fields so the existing AppState.BackupOperation UI path can render cleanup progress without a new model. | Carry retention count progress on BackupCopyProgress optional count fields so the existing AppState.BackupOperation UI path can render cleanup progress without a new model. | Migrated from legacy summary key-decisions | Yes | agent |
| D046 | migration | migrated-summary | Restore progress from backup copy callbacks is marshaled through IUiDispatcher.Post before mutating bindable ViewModel state. | Restore progress from backup copy callbacks is marshaled through IUiDispatcher.Post before mutating bindable ViewModel state. | Migrated from legacy summary key-decisions | Yes | agent |
| D047 | migration | migrated-summary | BackupPluginAsync maps expected session-directory creation failures to structured BackupCreateResult failures instead of letting them escape. | BackupPluginAsync maps expected session-directory creation failures to structured BackupCreateResult failures instead of letting them escape. | Migrated from legacy summary key-decisions | Yes | agent |
| D048 | migration | migrated-summary | Retention cancellation at classification/pre-delete gates returns BackupRetentionCleanupResult(Canceled) with rows and counts. | Retention cancellation at classification/pre-delete gates returns BackupRetentionCleanupResult(Canceled) with rows and counts. | Migrated from legacy summary key-decisions | Yes | agent |
| D049 | migration | migrated-summary | Treat PluginInfo.FileName and BackupPluginEntry metadata as untrusted filesystem input until validated as simple non-rooted names. | Treat PluginInfo.FileName and BackupPluginEntry metadata as untrusted filesystem input until validated as simple non-rooted names. | Migrated from legacy summary key-decisions | Yes | agent |
| D050 | migration | migrated-summary | Use the approved Phase 7 restore policy: entry.FileName must equal the OriginalPath file name, and both backup and target names must use .esm/.esp/.esl. | Use the approved Phase 7 restore policy: entry.FileName must equal the OriginalPath file name, and both backup and target names must use .esm/.esp/.esl. | Migrated from legacy summary key-decisions | Yes | agent |
| D051 | migration | migrated-summary | Mixed failed+canceled restore sessions with no restored rows report aggregate Canceled so cancellation remains visible in service and UI summaries. | Mixed failed+canceled restore sessions with no restored rows report aggregate Canceled so cancellation remains visible in service and UI summaries. | Migrated from legacy summary key-decisions | Yes | agent |
| D052 | migration | migrated-summary | File-load-order policy remains local to CleaningPreflight for Phase 8; consolidation remains deferred to REF-02/Phase 9. | File-load-order policy remains local to CleaningPreflight for Phase 8; consolidation remains deferred to REF-02/Phase 9. | Migrated from legacy summary key-decisions | Yes | agent |
| D053 | migration | migrated-summary | Wave 0 locks current CleaningOrchestrator behavior in tests before any Phase 8 production extraction begins. | Wave 0 locks current CleaningOrchestrator behavior in tests before any Phase 8 production extraction begins. | Migrated from legacy summary key-decisions | Yes | agent |
| D054 | migration | migrated-summary | The public-surface snapshot preserves nullable value-type output for LastTerminationResult as TerminationResult? because that is the actual interface signature. | The public-surface snapshot preserves nullable value-type output for LastTerminationResult as TerminationResult? because that is the actual interface signature. | Migrated from legacy summary key-decisions | Yes | agent |
| D055 | migration | migrated-summary | ICleaningPreflight.PrepareAsync is the single source for preflight selection rows used by real cleaning and dry-run projection. | ICleaningPreflight.PrepareAsync is the single source for preflight selection rows used by real cleaning and dry-run projection. | Migrated from legacy summary key-decisions | Yes | agent |
| D056 | migration | migrated-summary | CleaningPreflightPlan carries XEditDirectory so downstream runner/finalizer work can avoid re-deriving the xEdit directory. | CleaningPreflightPlan carries XEditDirectory so downstream runner/finalizer work can avoid re-deriving the xEdit directory. | Migrated from legacy summary key-decisions | Yes | agent |
| D057 | migration | migrated-summary | MO2 executable validation preserves the legacy file-existence guard before consulting IMo2ValidationService so existing MO2 not-found behavior remains stable. | MO2 executable validation preserves the legacy file-existence guard before consulting IMo2ValidationService so existing MO2 not-found behavior remains stable. | Migrated from legacy summary key-decisions | Yes | agent |
| D058 | migration | migrated-summary | BackupSessionCoordinator owns backup-operation CTS/lock state while CleaningOrchestrator retains the xEdit-active guard before delegating CancelBackupOperationAsync. | BackupSessionCoordinator owns backup-operation CTS/lock state while CleaningOrchestrator retains the xEdit-active guard before delegating CancelBackupOperationAsync. | Migrated from legacy summary key-decisions | Yes | agent |
| D059 | migration | migrated-summary | AbortSession remains facade-owned: the coordinator returns PluginBackupOutcomeKind.AbortSession and the facade writes partial metadata, publishes results, logs the session summary, and returns. | AbortSession remains facade-owned: the coordinator returns PluginBackupOutcomeKind.AbortSession and the facade writes partial metadata, publishes results, logs the session summary, and returns. | Migrated from legacy summary key-decisions | Yes | agent |
| D060 | migration | migrated-summary | Backup entries are constructed in the coordinator from BackupCreateResult plus PluginInfo because the current IBackupService contract does not expose a BackupPluginEntry property. | Backup entries are constructed in the coordinator from BackupCreateResult plus PluginInfo because the current IBackupService contract does not expose a BackupPluginEntry property. | Migrated from legacy summary key-decisions | Yes | agent |
| D061 | migration | migrated-summary | CleaningTerminationCoordinator owns all active process, stop escalation, termination result, and hang observable state while CleaningOrchestrator retains only session CTS lifetime. | CleaningTerminationCoordinator owns all active process, stop escalation, termination result, and hang observable state while CleaningOrchestrator retains only session CTS lifetime. | Migrated from legacy summary key-decisions | Yes | agent |
| D062 | migration | migrated-summary | CleaningOrchestrator remains the Stop/ForceStop facade that cancels the session CTS first, then delegates process termination to the coordinator. | CleaningOrchestrator remains the Stop/ForceStop facade that cancels the session CTS first, then delegates process termination to the coordinator. | Migrated from legacy summary key-decisions | Yes | agent |
| D063 | migration | migrated-summary | Wave 3 keeps the per-plugin onProcessStarted lambda inline and changes only its body to call terminationCoordinator.AttachProcess; delegate hoisting remains deferred to 08-05. | Wave 3 keeps the per-plugin onProcessStarted lambda inline and changes only its body to call terminationCoordinator.AttachProcess; delegate hoisting remains deferred to 08-05. | Migrated from legacy summary key-decisions | Yes | agent |
| D064 | migration | migrated-summary | PluginCleaningRunner receives attach/detach delegates instead of depending on ICleaningTerminationCoordinator, preserving the planned no-back-edge dependency shape. | PluginCleaningRunner receives attach/detach delegates instead of depending on ICleaningTerminationCoordinator, preserving the planned no-back-edge dependency shape. | Migrated from legacy summary key-decisions | Yes | agent |
| D065 | migration | migrated-summary | PluginResultFinalizer consumes a TerminationFinalizeContext snapshot captured after runner detach so log reads do not query live coordinator state during async finalization. | PluginResultFinalizer consumes a TerminationFinalizeContext snapshot captured after runner detach so log reads do not query live coordinator state during async finalization. | Migrated from legacy summary key-decisions | Yes | agent |
| D066 | migration | migrated-summary | The existing sequential source guard now checks orchestrator plus runner/finalizer files and verifies RunPluginBackupAsync remains before runner.RunAsync. | The existing sequential source guard now checks orchestrator plus runner/finalizer files and verifies RunPluginBackupAsync remains before runner.RunAsync. | Migrated from legacy summary key-decisions | Yes | agent |
| D067 | migration | migrated-summary | CleaningOrchestrator remains the public sequential facade while preflight, backup, runner, finalizer, and termination policies are isolated behind their collaborators. | CleaningOrchestrator remains the public sequential facade while preflight, backup, runner, finalizer, and termination policies are isolated behind their collaborators. | Migrated from legacy summary key-decisions | Yes | agent |
| D068 | migration | migrated-summary | The final cross-file source guard scans all six Phase 8 cleaning service files for unambiguous parallelization constructs. | The final cross-file source guard scans all six Phase 8 cleaning service files for unambiguous parallelization constructs. | Migrated from legacy summary key-decisions | Yes | agent |
| D069 | migration | migrated-summary | REF-01 is satisfied by collaborator-isolated files/tests plus full solution regression coverage. | REF-01 is satisfied by collaborator-isolated files/tests plus full solution regression coverage. | Migrated from legacy summary key-decisions | Yes | agent |
| D070 | migration | migrated-summary | Create and publish the cleaning session CTS before orphan cleanup/preflight so StopCleaningAsync can cancel the startup window. | Create and publish the cleaning session CTS before orphan cleanup/preflight so StopCleaningAsync can cancel the startup window. | Migrated from legacy summary key-decisions | Yes | agent |
| D071 | migration | migrated-summary | Keep StopCleaningAsync and ForceStopCleaningAsync public behavior unchanged; only StartCleaningAsync ordering changed. | Keep StopCleaningAsync and ForceStopCleaningAsync public behavior unchanged; only StartCleaningAsync ordering changed. | Migrated from legacy summary key-decisions | Yes | agent |
| D072 | migration | migrated-summary | AlreadyClean promotion now requires result.Success && result.Status == CleaningStatus.Cleaned, preventing failed xEdit attempts from being hidden as clean. | AlreadyClean promotion now requires result.Success && result.Status == CleaningStatus.Cleaned, preventing failed xEdit attempts from being hidden as clean. | Migrated from legacy summary key-decisions | Yes | agent |
| D073 | migration | migrated-summary | PluginCleaningResult.Success is now computed from finalStatus (Cleaned or AlreadyClean) after log parsing and exception-log overrides. | PluginCleaningResult.Success is now computed from finalStatus (Cleaned or AlreadyClean) after log parsing and exception-log overrides. | Migrated from legacy summary key-decisions | Yes | agent |
| D074 | migration | migrated-summary | Concurrent StartCleaningAsync calls fail fast with InvalidOperationException instead of waiting or replacing session state. | Concurrent StartCleaningAsync calls fail fast with InvalidOperationException instead of waiting or replacing session state. | Migrated from legacy summary key-decisions | Yes | agent |
| D075 | migration | migrated-summary | Kept QueryPlugins count semantics exact by streaming only the analyzed plugin context and its immediate lower-priority context. | Kept QueryPlugins count semantics exact by streaming only the analyzed plugin context and its immediate lower-priority context. | Migrated from legacy summary key-decisions | Yes | agent |
| D076 | migration | migrated-summary | Propagated OperationCanceledException instead of publishing unavailable or partial rows when exact analysis is canceled. | Propagated OperationCanceledException instead of publishing unavailable or partial rows when exact analysis is canceled. | Migrated from legacy summary key-decisions | Yes | agent |
| D077 | migration | migrated-summary | Phase 09 refresh status uses typed PluginRefreshStatus values with canonical display text helpers until ViewModels map them in later plans. | Phase 09 refresh status uses typed PluginRefreshStatus values with canonical display text helpers until ViewModels map them in later plans. | Migrated from legacy summary key-decisions | Yes | agent |
| D078 | migration | migrated-summary | Wave 0 coordinator tests intentionally reference the not-yet-implemented PluginRefreshCoordinator so Plan 09-03 receives executable RED behavior requirements. | Wave 0 coordinator tests intentionally reference the not-yet-implemented PluginRefreshCoordinator so Plan 09-03 receives executable RED behavior requirements. | Migrated from legacy summary key-decisions | Yes | agent |
| D079 | migration | migrated-summary | Targeted approximation refresh is pinned to StateService.MergePluginApproximation, not MergePluginApproximations, to preserve non-targeted row values. | Targeted approximation refresh is pinned to StateService.MergePluginApproximation, not MergePluginApproximations, to preserve non-targeted row values. | Migrated from legacy summary key-decisions | Yes | agent |
| D080 | migration | migrated-summary | Plugin refresh generation, cancellation, plugin loading, skip-list application, and approximation publication now live in PluginRefreshCoordinator instead of ConfigurationViewModel. | Plugin refresh generation, cancellation, plugin loading, skip-list application, and approximation publication now live in PluginRefreshCoordinator instead of ConfigurationViewModel. | Migrated from legacy summary key-decisions | Yes | agent |
| D081 | migration | migrated-summary | PluginRefreshCapabilityPolicy remains refresh-scoped and only enables issue approximation for Skyrim and Fallout 4 families. | PluginRefreshCapabilityPolicy remains refresh-scoped and only enables issue approximation for Skyrim and Fallout 4 families. | Migrated from legacy summary key-decisions | Yes | agent |
| D082 | migration | migrated-summary | ConfigurationViewModel maps typed PluginRefreshStatus values into UI strings and cancels the coordinator on disposal. | ConfigurationViewModel maps typed PluginRefreshStatus values into UI strings and cancels the coordinator on disposal. | Migrated from legacy summary key-decisions | Yes | agent |
| D083 | migration | migrated-summary | PluginListViewModel owns only selected-row command intent and target snapshotting; approximation workflow remains in IPluginRefreshCoordinator. | PluginListViewModel owns only selected-row command intent and target snapshotting; approximation workflow remains in IPluginRefreshCoordinator. | Migrated from legacy summary key-decisions | Yes | agent |
| D084 | migration | migrated-summary | Refresh selected availability is gated by loaded rows, cleaning state, checked visible rows, current game, and refresh-scoped approximation capability. | Refresh selected availability is gated by loaded rows, cleaning state, checked visible rows, current game, and refresh-scoped approximation capability. | Migrated from legacy summary key-decisions | Yes | agent |
| D085 | migration | migrated-summary | Cancel refresh is a direct coordinator cancellation command with no confirmation dialog and visibility tied to active approximation refresh state. | Cancel refresh is a direct coordinator cancellation command with no confirmation dialog and visibility tied to active approximation refresh state. | Migrated from legacy summary key-decisions | Yes | agent |
| D086 | migration | migrated-summary | CleaningCommandsViewModel cancels active refresh work with CleaningStarted after validation and before progress display or xEdit orchestration. | CleaningCommandsViewModel cancels active refresh work with CleaningStarted after validation and before progress display or xEdit orchestration. | Migrated from legacy summary key-decisions | Yes | agent |
| D087 | migration | migrated-summary | MainWindowViewModel passes the DI-provided IPluginRefreshCoordinator to all child ViewModels so lifecycle and command cancellation share one coordinator instance. | MainWindowViewModel passes the DI-provided IPluginRefreshCoordinator to all child ViewModels so lifecycle and command cancellation share one coordinator instance. | Migrated from legacy summary key-decisions | Yes | agent |
| D088 | migration | migrated-summary | Manual load-order refresh remains coordinator-backed even when no game is selected, preserving file-based plugin loading while Phase 09 moves workflow out of ConfigurationViewModel. | Manual load-order refresh remains coordinator-backed even when no game is selected, preserving file-based plugin loading while Phase 09 moves workflow out of ConfigurationViewModel. | Migrated from legacy summary key-decisions | Yes | agent |
| D089 | migration | migrated-summary | Selected approximation refresh preserves the existing PluginsToClean row set and updates only selected target rows to Pending before analysis results merge back. | Selected approximation refresh preserves the existing PluginsToClean row set and updates only selected target rows to Pending before analysis results merge back. | Migrated from legacy summary key-decisions | Yes | agent |
| D090 | migration | migrated-summary | The empty-current-list selected refresh fallback remains for compatibility, but non-empty row sets use UpdateState instead of SetPluginsToClean(pendingRows). | The empty-current-list selected refresh fallback remains for compatibility, but non-empty row sets use UpdateState instead of SetPluginsToClean(pendingRows). | Migrated from legacy summary key-decisions | Yes | agent |
| D091 | migration | migrated-summary | Disable Skip Lists remains user-driven by ConfigurationViewModel, but the coordinator consumes it only through PluginRefreshRequest so refresh workflow ownership stays in services. | Disable Skip Lists remains user-driven by ConfigurationViewModel, but the coordinator consumes it only through PluginRefreshRequest so refresh workflow ownership stays in services. | Migrated from legacy summary key-decisions | Yes | agent |
| D092 | migration | migrated-summary | The new request field defaults to false to preserve legacy caller behavior and keep the safer skip-list-hiding default. | The new request field defaults to false to preserve legacy caller behavior and keep the safer skip-list-hiding default. | Migrated from legacy summary key-decisions | Yes | agent |
| D093 | migration | migrated-summary | Full-list approximation refresh now has its own FullRefreshCompleted terminal status instead of reusing selected-refresh completion text. | Full-list approximation refresh now has its own FullRefreshCompleted terminal status instead of reusing selected-refresh completion text. | Migrated from legacy summary key-decisions | Yes | agent |
| D094 | migration | migrated-summary | The coordinator publishes FullRefreshCompleted only after successful AnalyzeTargetsAsync completion and only while the generation remains current. | The coordinator publishes FullRefreshCompleted only after successful AnalyzeTargetsAsync completion and only while the generation remains current. | Migrated from legacy summary key-decisions | Yes | agent |
| D095 | migration | migrated-summary | PluginListViewModel keeps the running set intentionally narrow: LoadingPlugins and AnalyzingSelected are running; terminal statuses clear the cancel affordance. | PluginListViewModel keeps the running set intentionally narrow: LoadingPlugins and AnalyzingSelected are running; terminal statuses clear the cancel affordance. | Migrated from legacy summary key-decisions | Yes | agent |
| D096 | migration | migrated-summary | Phase 09 Plan 09 closes remaining verifier blockers with regression tests because PluginRefreshCoordinator already contained the required signal-only CTS, terminal failure status, and variant skip-list implementation. | Phase 09 Plan 09 closes remaining verifier blockers with regression tests because PluginRefreshCoordinator already contained the required signal-only CTS, terminal failure status, and variant skip-list implementation. | Migrated from legacy summary key-decisions | Yes | agent |
| D097 | migration | migrated-summary | Refresh cancellation test doubles use delayed plugin loading so manual, supersede, and cleaning-start cancellation exercise awaited in-flight work. | Refresh cancellation test doubles use delayed plugin loading so manual, supersede, and cleaning-start cancellation exercise awaited in-flight work. | Migrated from legacy summary key-decisions | Yes | agent |
| D098 | migration | migrated-summary | Watcher hash-read exceptions are handled inside ApplyWatcherAsync as typed Watcher/ReadFailed outcomes so transient external writer locks do not become log-only dropped operations. | Watcher hash-read exceptions are handled inside ApplyWatcherAsync as typed Watcher/ReadFailed outcomes so transient external writer locks do not become log-only dropped operations. | Migrated from legacy summary key-decisions | Yes | agent |
| D099 | migration | migrated-summary | The hash-read race is covered through FakeUserConfigFileStore.HashFailure instead of FileSystemWatcher timing, OS locks, or production debounce waits. | The hash-read race is covered through FakeUserConfigFileStore.HashFailure instead of FileSystemWatcher timing, OS locks, or production debounce waits. | Migrated from legacy summary key-decisions | Yes | agent |
| D100 | migration | migrated-summary | Production DI constructs IConfigurationService with the registered shared IConfigPersistenceCoordinator instead of relying on public constructor selection. | Production DI constructs IConfigurationService with the registered shared IConfigPersistenceCoordinator instead of relying on public constructor selection. | Migrated from legacy summary key-decisions | Yes | agent |
| D101 | migration | migrated-summary | The DI regression asserts both coordinator identity and watcher-originated result flow through the IConfigurationService facade. | The DI regression asserts both coordinator identity and watcher-originated result flow through the IConfigurationService facade. | Migrated from legacy summary key-decisions | Yes | agent |
| D102 | migration | migrated-summary | Phase 10 Plan 01 implements model-owned manual Copy() methods and leaves ConfigurationService.CloneConfig unchanged for Plan 03. | Phase 10 Plan 01 implements model-owned manual Copy() methods and leaves ConfigurationService.CloneConfig unchanged for Plan 03. | Migrated from legacy summary key-decisions | Yes | agent |
| D103 | migration | migrated-summary | Behavior parity with the existing YAML round-trip clone is proven through xUnit tests instead of source-regex clone guards. | Behavior parity with the existing YAML round-trip clone is proven through xUnit tests instead of source-regex clone guards. | Migrated from legacy summary key-decisions | Yes | agent |
| D104 | migration | migrated-summary | AutoQAC.csproj does not enable implicit usings, so UserConfiguration.cs explicitly imports System and System.Linq for StringComparer and LINQ copy helpers. | AutoQAC.csproj does not enable implicit usings, so UserConfiguration.cs explicitly imports System and System.Linq for StringComparer and LINQ copy helpers. | Migrated from legacy summary key-decisions | Yes | agent |
| D105 | migration | migrated-summary | ConfigPersistenceCoordinator uses a single-reader Channel<ConfigPersistenceOperation> with barrier TCS completions for deterministic flush/reload results. | ConfigPersistenceCoordinator uses a single-reader Channel<ConfigPersistenceOperation> with barrier TCS completions for deterministic flush/reload results. | Migrated from legacy summary key-decisions | Yes | agent |
| D106 | migration | migrated-summary | InternalsVisibleTo was already available for AutoQAC.Tests, so no csproj change was needed for test access; internal test-only delegates were used to inject Replace/Move failures. | InternalsVisibleTo was already available for AutoQAC.Tests, so no csproj change was needed for test access; internal test-only delegates were used to inject Replace/Move failures. | Migrated from legacy summary key-decisions | Yes | agent |
| D107 | migration | migrated-summary | B16/B17 atomic write sequencing is covered on the production UserConfigFileStore rather than simulated in the fake. | B16/B17 atomic write sequencing is covered on the production UserConfigFileStore rather than simulated in the fake. | Migrated from legacy summary key-decisions | Yes | agent |
| D108 | migration | migrated-summary | ConfigurationService now starts and observes ConfigPersistenceCoordinator, while preserving legacy test construction through a compatibility constructor. | ConfigurationService now starts and observes ConfigPersistenceCoordinator, while preserving legacy test construction through a compatibility constructor. | Migrated from legacy summary key-decisions | Yes | agent |
| D109 | migration | migrated-summary | ConfigWatcherService no longer validates YAML, checks hashes, or defers cleaning-time changes; all such policy is coordinator-owned. | ConfigWatcherService no longer validates YAML, checks hashes, or defers cleaning-time changes; all such policy is coordinator-owned. | Migrated from legacy summary key-decisions | Yes | agent |
| D110 | migration | migrated-summary | Invalid user YAML on facade load is now surfaced through LastFailure instead of throwing from LoadUserConfigAsync. | Invalid user YAML on facade load is now surfaced through LastFailure instead of throwing from LoadUserConfigAsync. | Migrated from legacy summary key-decisions | Yes | agent |
| D111 | migration | migrated-summary | ConfigPersistenceFailureException derives from InvalidOperationException so existing cleaning catch sites remain compatible while preserving typed failure payloads. | ConfigPersistenceFailureException derives from InvalidOperationException so existing cleaning catch sites remain compatible while preserving typed failure payloads. | Migrated from legacy summary key-decisions | Yes | agent |
| D112 | migration | migrated-summary | CleaningPreflight treats Failed and defensive Rejected flush results as hard blockers before validation, game detection, skip-list loading, MO2 validation, or cleaning service calls. | CleaningPreflight treats Failed and defensive Rejected flush results as hard blockers before validation, game detection, skip-list loading, MO2 validation, or cleaning service calls. | Migrated from legacy summary key-decisions | Yes | agent |
| D113 | migration | migrated-summary | Legacy orchestrator/process test substitutes default FlushPendingSavesAsync to NoOp so tests model the production no-pending-save happy path explicitly. | Legacy orchestrator/process test substitutes default FlushPendingSavesAsync to NoOp so tests model the production no-pending-save happy path explicitly. | Migrated from legacy summary key-decisions | Yes | agent |
| D114 | migration | migrated-summary | Settings persistence failures surface as a concise SettingsWindow text banner instead of modal dialogs or retry controls. | Settings persistence failures surface as a concise SettingsWindow text banner instead of modal dialogs or retry controls. | Migrated from legacy summary key-decisions | Yes | agent |
| D115 | migration | migrated-summary | Explicit Settings saves close only after FlushPendingSavesAsync reports Success or NoOp, preventing late async save failures from appearing after the dialog closes. | Explicit Settings saves close only after FlushPendingSavesAsync reports Success or NoOp, preventing late async save failures from appearing after the dialog closes. | Migrated from legacy summary key-decisions | Yes | agent |
| D116 | migration | migrated-summary | ConfigWatcherService depends on IConfigPersistenceCoordinator so DI resolves the watcher against the registered coordinator abstraction. | ConfigWatcherService depends on IConfigPersistenceCoordinator so DI resolves the watcher against the registered coordinator abstraction. | Migrated from legacy summary key-decisions | Yes | agent |
| D117 | migration | migrated-summary | ConfigPersistenceCoordinator now treats observer callbacks as untrusted and catches/logs observer exceptions before completing caller barriers. | ConfigPersistenceCoordinator now treats observer callbacks as untrusted and catches/logs observer exceptions before completing caller barriers. | Migrated from legacy summary key-decisions | Yes | agent |
| D118 | migration | migrated-summary | Explicit reload preserves pending app saves by flushing them before reading disk, making the flushed values the reload source of truth. | Explicit reload preserves pending app saves by flushing them before reading disk, making the flushed values the reload source of truth. | Migrated from legacy summary key-decisions | Yes | agent |
| D119 | migration | migrated-summary | ConfigurationService now treats _stateLock as the single synchronization boundary for facade pending/loaded flags. | ConfigurationService now treats _stateLock as the single synchronization boundary for facade pending/loaded flags. | Migrated from legacy summary key-decisions | Yes | agent |
| D120 | migration | migrated-summary | Facade FlushPendingSavesAsync returns a typed NoOp without calling the coordinator when no app-initiated save is pending. | Facade FlushPendingSavesAsync returns a typed NoOp without calling the coordinator when no app-initiated save is pending. | Migrated from legacy summary key-decisions | Yes | agent |
| D121 | migration | migrated-summary | Explicit reload returns a failed or rejected pending-save flush result directly instead of reading stale disk content. | Explicit reload returns a failed or rejected pending-save flush result directly instead of reading stale disk content. | Migrated from legacy summary key-decisions | Yes | agent |
| D122 | migration | migrated-summary | The regression test proves no disk Read occurs after the failed prerequisite Write in the explicit reload path. | The regression test proves no disk Read occurs after the failed prerequisite Write in the explicit reload path. | Migrated from legacy summary key-decisions | Yes | agent |
| D123 | migration | migrated-summary | ConfigurationService.FlushPendingSavesAsync always delegates to IConfigPersistenceCoordinator.FlushPendingSavesAsync so forced flushes drain queued watcher/reload work even with no facade pending app save. | ConfigurationService.FlushPendingSavesAsync always delegates to IConfigPersistenceCoordinator.FlushPendingSavesAsync so forced flushes drain queued watcher/reload work even with no facade pending app save. | Migrated from legacy summary key-decisions | Yes | agent |
| D124 | migration | migrated-summary | Successful reload tests now assert that later forced flushes remain coordinator barriers instead of expecting a facade-local NoOp. | Successful reload tests now assert that later forced flushes remain coordinator barriers instead of expecting a facade-local NoOp. | Migrated from legacy summary key-decisions | Yes | agent |
| D125 | migration | migrated-summary | CleaningService command-build failures use DiagnosticTextFormatter.SafePluginName(plugin.FileName) before returning failed CleaningResult.Message text. | CleaningService command-build failures use DiagnosticTextFormatter.SafePluginName(plugin.FileName) before returning failed CleaningResult.Message text. | Migrated from legacy summary key-decisions | Yes | agent |
| D126 | migration | migrated-summary | CleaningService unexpected exception failures return DiagnosticTextFormatter.CleaningFailedForPlugin(plugin.FileName), keeping raw exception and unsafe basename details out of result/report boundaries. | CleaningService unexpected exception failures return DiagnosticTextFormatter.CleaningFailedForPlugin(plugin.FileName), keeping raw exception and unsafe basename details out of result/report boundaries. | Migrated from legacy summary key-decisions | Yes | agent |
| D127 | migration | migrated-summary | PluginResultFinalizer keeps raw LogReadResult.Warning text in local logger output but replaces UI-bound LogParseWarning with stable latest-log copy. | PluginResultFinalizer keeps raw LogReadResult.Warning text in local logger output but replaces UI-bound LogParseWarning with stable latest-log copy. | Migrated from legacy summary key-decisions | Yes | agent |
| D128 | migration | migrated-summary | ProgressWindow tooltip binding remains unchanged because the bound PluginCleaningResult.LogParseWarning value is now safe at the model boundary. | ProgressWindow tooltip binding remains unchanged because the bound PluginCleaningResult.LogParseWarning value is now safe at the model boundary. | Migrated from legacy summary key-decisions | Yes | agent |
| D129 | migration | migrated-summary | Timeout retry dialogs keep the existing retry behavior but display only DiagnosticTextFormatter.SafePluginName output. | Timeout retry dialogs keep the existing retry behavior but display only DiagnosticTextFormatter.SafePluginName output. | Migrated from legacy summary key-decisions | Yes | agent |
| D130 | migration | migrated-summary | Backup failure callbacks sanitize both plugin and failure summary before calling IMessageDialogService. | Backup failure callbacks sanitize both plugin and failure summary before calling IMessageDialogService. | Migrated from legacy summary key-decisions | Yes | agent |
| D131 | migration | migrated-summary | MessageDialogService defensively re-sanitizes backup failure dialog inputs because it renders directly to Avalonia TextBlocks. | MessageDialogService defensively re-sanitizes backup failure dialog inputs because it renders directly to Avalonia TextBlocks. | Migrated from legacy summary key-decisions | Yes | agent |
| D132 | migration | migrated-summary | BackupPluginEntry.FileName from session metadata is treated as untrusted display input. | BackupPluginEntry.FileName from session metadata is treated as untrusted display input. | Migrated from legacy summary key-decisions | Yes | agent |
| D133 | migration | migrated-summary | Restore Selected UI copy uses DiagnosticTextFormatter.SafePluginName, but IBackupService.RestorePluginAsync still receives the original BackupPluginEntry. | Restore Selected UI copy uses DiagnosticTextFormatter.SafePluginName, but IBackupService.RestorePluginAsync still receives the original BackupPluginEntry. | Migrated from legacy summary key-decisions | Yes | agent |
| D134 | migration | migrated-summary | Logger properties continue to use original metadata where local troubleshooting value is needed. | Logger properties continue to use original metadata where local troubleshooting value is needed. | Migrated from legacy summary key-decisions | Yes | agent |
| D135 | migration | migrated-summary | DiagnosticTextFormatter lives under AutoQAC.Models.Diagnostics so models, services, ViewModels, and startup code can share safe copy without a service-layer dependency. | DiagnosticTextFormatter lives under AutoQAC.Models.Diagnostics so models, services, ViewModels, and startup code can share safe copy without a service-layer dependency. | Migrated from legacy summary key-decisions | Yes | agent |
| D136 | migration | migrated-summary | Unsafe failure summaries fall back on any detected paths, command flags, exception names, stack markers, executable command markers, or control whitespace. | Unsafe failure summaries fall back on any detected paths, command flags, exception names, stack markers, executable command markers, or control whitespace. | Migrated from legacy summary key-decisions | Yes | agent |
| D137 | migration | migrated-summary | CleaningCommandsViewModel now treats unexpected cleaning and preview failures as latest-log UI copy only; raw exception and stack details stay in logs. | CleaningCommandsViewModel now treats unexpected cleaning and preview failures as latest-log UI copy only; raw exception and stack details stay in logs. | Migrated from legacy summary key-decisions | Yes | agent |
| D138 | migration | migrated-summary | Pre-clean missing-path validation uses DiagnosticTextFormatter.SafeFileIdentifier for xEdit, MO2, and file-load-order paths, preserving basenames while hiding directories. | Pre-clean missing-path validation uses DiagnosticTextFormatter.SafeFileIdentifier for xEdit, MO2, and file-load-order paths, preserving basenames while hiding directories. | Migrated from legacy summary key-decisions | Yes | agent |
| D139 | migration | migrated-summary | Configuration selected load-order technical failures now identify Load Order File (plugins.txt) and point to the latest AutoQAC log instead of showing selected paths or exception text. | Configuration selected load-order technical failures now identify Load Order File (plugins.txt) and point to the latest AutoQAC log instead of showing selected paths or exception text. | Migrated from legacy summary key-decisions | Yes | agent |
| D140 | migration | migrated-summary | Selected game data folder browse failures use game-specific safe folder copy, falling back to selected game data folder when no game context is available. | Selected game data folder browse failures use game-specific safe folder copy, falling back to selected game data folder when no game context is available. | Migrated from legacy summary key-decisions | Yes | agent |
| D141 | migration | migrated-summary | Settings persistence write/read banners preserve ConfigPersistenceFailure.SafeSummary instead of replacing typed safe categories with generic text. | Settings persistence write/read banners preserve ConfigPersistenceFailure.SafeSummary instead of replacing typed safe categories with generic text. | Migrated from legacy summary key-decisions | Yes | agent |
| D142 | migration | migrated-summary | xEdit exception-log content is not copied into AutoQAC result rows, reports, or structured log properties; AutoQAC records only that xEdit reported an exception log for the plugin. | xEdit exception-log content is not copied into AutoQAC result rows, reports, or structured log properties; AutoQAC records only that xEdit reported an exception log for the plugin. | Migrated from legacy summary key-decisions | Yes | agent |
| D143 | migration | migrated-summary | Export report failed rows use DiagnosticTextFormatter.SafeFailureSummary defensively even though finalizer-created messages are already sanitized at source. | Export report failed rows use DiagnosticTextFormatter.SafeFailureSummary defensively even though finalizer-created messages are already sanitized at source. | Migrated from legacy summary key-decisions | Yes | agent |
| D144 | migration | migrated-summary | ProcessExecutionService emits only operation/status/reason/argumentCount/processId fields for launch diagnostics; executable paths and raw arguments remain out of structured log properties. | ProcessExecutionService emits only operation/status/reason/argumentCount/processId fields for launch diagnostics; executable paths and raw arguments remain out of structured log properties. | Migrated from legacy summary key-decisions | Yes | agent |
| D145 | migration | migrated-summary | CleaningService owns caller-side QuickAutoClean launch context logs while preserving XEditCommandBuilder and ProcessStartInfo launch values unchanged. | CleaningService owns caller-side QuickAutoClean launch context logs while preserving XEditCommandBuilder and ProcessStartInfo launch values unchanged. | Migrated from legacy summary key-decisions | Yes | agent |
| D146 | migration | migrated-summary | App.axaml.cs startup diagnostics use DiagnosticTextFormatter.SafeFileIdentifier for xEdit configuration and source guards for private startup copy. | App.axaml.cs startup diagnostics use DiagnosticTextFormatter.SafeFileIdentifier for xEdit configuration and source guards for private startup copy. | Migrated from legacy summary key-decisions | Yes | agent |
| D147 | migration | migrated-summary | Phase-level disclosure guards use one shared unsafe sentinel set so UI, report, and log boundary tests fail on the same path/command/exception regressions. | Phase-level disclosure guards use one shared unsafe sentinel set so UI, report, and log boundary tests fail on the same path/command/exception regressions. | Migrated from legacy summary key-decisions | Yes | agent |
| D148 | migration | migrated-summary | Plan 11-06 keeps behavioral logger capture as the primary log-boundary proof and limits source guards to private startup/known bad template absence. | Plan 11-06 keeps behavioral logger capture as the primary log-boundary proof and limits source guards to private startup/known bad template absence. | Migrated from legacy summary key-decisions | Yes | agent |
| D149 | migration | migrated-summary | Legacy migration warnings now use fixed category copy with latest-log guidance rather than interpolated exception messages. | Legacy migration warnings now use fixed category copy with latest-log guidance rather than interpolated exception messages. | Migrated from legacy summary key-decisions | Yes | agent |
| D150 | migration | migrated-summary | Startup migration warning display defensively applies DiagnosticTextFormatter.SafeFailureSummary before calling ShowMigrationWarning. | Startup migration warning display defensively applies DiagnosticTextFormatter.SafeFailureSummary before calling ShowMigrationWarning. | Migrated from legacy summary key-decisions | Yes | agent |
| D151 | migration | migrated-summary | Successful process-start PID tracking uses sanitized plugin filenames when provided and ExternalProcess when pluginName is omitted; legacy Arguments are never used as labels. | Successful process-start PID tracking uses sanitized plugin filenames when provided and ExternalProcess when pluginName is omitted; legacy Arguments are never used as labels. | Migrated from legacy summary key-decisions | Yes | agent |
| D152 | migration | migrated-summary | Successful-start regression tests inspect PID-store state at the onProcessStarted boundary because normal completion intentionally untracks process entries. | Successful-start regression tests inspect PID-store state at the onProcessStarted boundary because normal completion intentionally untracks process entries. | Migrated from legacy summary key-decisions | Yes | agent |
| D153 | migration | migrated-summary | Generated reports use sanitized plugin basenames for cleaned, already-clean, skipped, and failed row prefixes; raw PluginName remains internal model data. | Generated reports use sanitized plugin basenames for cleaned, already-clean, skipped, and failed row prefixes; raw PluginName remains internal model data. | Migrated from legacy summary key-decisions | Yes | agent |
| D154 | migration | migrated-summary | SafePluginName removes known command-flag suffixes such as -QAC and -autoload when they appear as display-name suffixes before an extension. | SafePluginName removes known command-flag suffixes such as -QAC and -autoload when they appear as display-name suffixes before an extension. | Migrated from legacy summary key-decisions | Yes | agent |
| D155 | migration | migrated-summary | Stop escalation copy lives in AutoQAC.Models.StopTerminationDialogContent so main and Progress stop surfaces can share one exact text contract. | Stop escalation copy lives in AutoQAC.Models.StopTerminationDialogContent so main and Progress stop surfaces can share one exact text contract. | Migrated from legacy summary key-decisions | Yes | agent |
| D156 | migration | migrated-summary | ShowChoiceAsync maps the primary custom button to MessageDialogResult.Yes and the secondary custom button to MessageDialogResult.No, preserving existing dialog result semantics. | ShowChoiceAsync maps the primary custom button to MessageDialogResult.Yes and the secondary custom button to MessageDialogResult.No, preserving existing dialog result semantics. | Migrated from legacy summary key-decisions | Yes | agent |
| D157 | migration | migrated-summary | ProgressViewModel uses the same StopTerminationDialogContent and ShowChoiceAsync contract as main Stop so Progress Stop cannot drift from the shared stop outcome copy. | ProgressViewModel uses the same StopTerminationDialogContent and ShowChoiceAsync contract as main Stop so Progress Stop cannot drift from the shared stop outcome copy. | Migrated from legacy summary key-decisions | Yes | agent |
| D158 | migration | migrated-summary | Hang warning Kill remains an immediate ForceStopCleaningAsync action and only reuses shared failure reporting when the force-kill result is ForceKillFailed. | Hang warning Kill remains an immediate ForceStopCleaningAsync action and only reuses shared failure reporting when the force-kill result is ForceKillFailed. | Migrated from legacy summary key-decisions | Yes | agent |
| D159 | migration | migrated-summary | Phase 12 verification is the current source of truth for SAF-01, SAF-02, REF-04, and TEST-01 closure instead of rewriting historical Phase 5 artifacts. | Phase 12 verification is the current source of truth for SAF-01, SAF-02, REF-04, and TEST-01 closure instead of rewriting historical Phase 5 artifacts. | Migrated from legacy summary key-decisions | Yes | agent |
| D160 | migration | migrated-summary | Full-suite evidence passed with no unrelated failures, so validation was advanced to nyquist_compliant: true and wave_0_complete: true. | Full-suite evidence passed with no unrelated failures, so validation was advanced to nyquist_compliant: true and wave_0_complete: true. | Migrated from legacy summary key-decisions | Yes | agent |
| D161 | migration | migrated-summary | Phase 13 Plan 01 remained evidence-only because targeted command-builder, process-boundary, and cleaning diagnostics tests all passed without code or test changes. | Phase 13 Plan 01 remained evidence-only because targeted command-builder, process-boundary, and cleaning diagnostics tests all passed without code or test changes. | Migrated from legacy summary key-decisions | Yes | agent |
| D162 | migration | migrated-summary | Phase 13 Plan 02 remained evidence-only because targeted and full-suite tests passed without production or test changes. | Phase 13 Plan 02 remained evidence-only because targeted and full-suite tests passed without production or test changes. | Migrated from legacy summary key-decisions | Yes | agent |
| D163 | migration | migrated-summary | Stale Phase 6 and milestone audit findings are closed by current Phase 13 evidence while roadmap/requirements/audit marker reconciliation remains deferred. | Stale Phase 6 and milestone audit findings are closed by current Phase 13 evidence while roadmap/requirements/audit marker reconciliation remains deferred. | Migrated from legacy summary key-decisions | Yes | agent |
| D164 | migration | migrated-summary | Phase 14 is the current REF-01 source of truth; stale Phase 8 verification and milestone marker reconciliation remain out of scope. | Phase 14 is the current REF-01 source of truth; stale Phase 8 verification and milestone marker reconciliation remain out of scope. | Migrated from legacy summary key-decisions | Yes | agent |
| D165 | migration | migrated-summary | Retained the original Process handle in a separate pending force-escalation field instead of adding PID/start-time recovery. | Retained the original Process handle in a separate pending force-escalation field instead of adding PID/start-time recovery. | Migrated from legacy summary key-decisions | Yes | agent |
| D166 | migration | migrated-summary | Kept HasActiveProcess tied only to the active cleaning process so detached pending escalation does not count as active cleaning. | Kept HasActiveProcess tied only to the active cleaning process so detached pending escalation does not count as active cleaning. | Migrated from legacy summary key-decisions | Yes | agent |
| D167 | migration | migrated-summary | Mapped confirmed detached force escalation with no usable target to ForceKillFailed instead of cached GracePeriodExpired. | Mapped confirmed detached force escalation with no usable target to ForceKillFailed instead of cached GracePeriodExpired. | Migrated from legacy summary key-decisions | Yes | agent |
| D168 | migration | migrated-summary | Split termination cleanup into new-session reset versus session-finalization cleanup so unresolved GracePeriodExpired targets survive until user resolution. | Split termination cleanup into new-session reset versus session-finalization cleanup so unresolved GracePeriodExpired targets survive until user resolution. | Migrated from legacy summary key-decisions | Yes | agent |
| D169 | migration | migrated-summary | Kept ProgressViewModel behavior unchanged because existing confirmation and shared force-failure paths already satisfied the detached failure proof. | Kept ProgressViewModel behavior unchanged because existing confirmation and shared force-failure paths already satisfied the detached failure proof. | Migrated from legacy summary key-decisions | Yes | agent |
| D170 | migration | migrated-summary | Phase 15 verification is the current source of truth for SAF-01, SAF-02, TEST-01, INT-STOP-01, and FLOW-STOP-ESCALATION-01 closure. | Phase 15 verification is the current source of truth for SAF-01, SAF-02, TEST-01, INT-STOP-01, and FLOW-STOP-ESCALATION-01 closure. | Migrated from legacy summary key-decisions | Yes | agent |
| D171 | migration | migrated-summary | Phase 15 validation is nyquist_compliant because targeted coordinator, orchestrator, Progress ViewModel, process integration, and full solution evidence all passed. | Phase 15 validation is nyquist_compliant because targeted coordinator, orchestrator, Progress ViewModel, process integration, and full solution evidence all passed. | Migrated from legacy summary key-decisions | Yes | agent |
| D172 | migration | migrated-summary | Milestone marker reconciliation remains deferred to Phase 16; Phase 15 only wrote current evidence artifacts plus required GSD state/roadmap metadata. | Milestone marker reconciliation remains deferred to Phase 16; Phase 15 only wrote current evidence artifacts plus required GSD state/roadmap metadata. | Migrated from legacy summary key-decisions | Yes | agent |
| D173 | migration | migrated-summary | Plan 15-04 closed the stale verification gaps with existing durable PID/start-time source/test proof rather than modifying already-correct production code. | Plan 15-04 closed the stale verification gaps with existing durable PID/start-time source/test proof rather than modifying already-correct production code. | Migrated from legacy summary key-decisions | Yes | agent |
| D174 | migration | migrated-summary | Phase 15 verification now supersedes the old gaps_found report with passed evidence for SAF-01, SAF-02, TEST-01, INT-STOP-01, and FLOW-STOP-ESCALATION-01. | Phase 15 verification now supersedes the old gaps_found report with passed evidence for SAF-01, SAF-02, TEST-01, INT-STOP-01, and FLOW-STOP-ESCALATION-01. | Migrated from legacy summary key-decisions | Yes | agent |
| D175 | migration | migrated-summary | Milestone marker reconciliation remains deferred to Phase 16 per D-18; this plan updated evidence artifacts and normal GSD metadata only. | Milestone marker reconciliation remains deferred to Phase 16 per D-18; this plan updated evidence artifacts and normal GSD metadata only. | Migrated from legacy summary key-decisions | Yes | agent |
| D176 | migration | migrated-summary | Phase 05 receives a passed reconciliation verification artifact based on current Phase 12/15 evidence, not an original historical Phase 05 verification execution. | Phase 05 receives a passed reconciliation verification artifact based on current Phase 12/15 evidence, not an original historical Phase 05 verification execution. | Migrated from legacy summary key-decisions | Yes | agent |
| D177 | migration | migrated-summary | Phase 06 validation metadata remains historical in shape but is marked passed/superseded by row-specific Phase 13 acceptance-criterion evidence. | Phase 06 validation metadata remains historical in shape but is marked passed/superseded by row-specific Phase 13 acceptance-criterion evidence. | Migrated from legacy summary key-decisions | Yes | agent |
| D178 | migration | migrated-summary | Phase 14 receives a Phase 16 override validation artifact for audit/Nyquist discovery, citing existing 14-VERIFICATION evidence instead of rerunning tests. | Phase 14 receives a Phase 16 override validation artifact for audit/Nyquist discovery, citing existing 14-VERIFICATION evidence instead of rerunning tests. | Migrated from legacy summary key-decisions | Yes | agent |
| D179 | migration | migrated-summary | The milestone audit now treats Phase 15 verification/validation as the current source of truth for SAF-01, SAF-02, TEST-01, INT-STOP-01, and FLOW-STOP-ESCALATION-01 closure. | The milestone audit now treats Phase 15 verification/validation as the current source of truth for SAF-01, SAF-02, TEST-01, INT-STOP-01, and FLOW-STOP-ESCALATION-01 closure. | Migrated from legacy summary key-decisions | Yes | agent |
| D180 | migration | migrated-summary | Historical blockers are preserved only in a Closed / Superseded Findings ledger, not in open routing sections. | Historical blockers are preserved only in a Closed / Superseded Findings ledger, not in open routing sections. | Migrated from legacy summary key-decisions | Yes | agent |
| D181 | migration | migrated-summary | No dedicated milestone audit rerun command was discoverable from available GSD CLI help, so Plan 16-02 records manual artifact inspection against 16-SPEC acceptance criteria. | No dedicated milestone audit rerun command was discoverable from available GSD CLI help, so Plan 16-02 records manual artifact inspection against 16-SPEC acceptance criteria. | Migrated from legacy summary key-decisions | Yes | agent |
| D182 | migration | migrated-summary | REQUIREMENTS.md now treats the Plan 16-02 passed milestone audit as the latest audit state: 18 requirements satisfied and 0 pending gap closures. | REQUIREMENTS.md now treats the Plan 16-02 passed milestone audit as the latest audit state: 18 requirements satisfied and 0 pending gap closures. | Migrated from legacy summary key-decisions | Yes | agent |
| D183 | migration | migrated-summary | ROADMAP.md now treats Phase 13 as complete based on current Phase 13 verification evidence for SAF-03 and TEST-02. | ROADMAP.md now treats Phase 13 as complete based on current Phase 13 verification evidence for SAF-03 and TEST-02. | Migrated from legacy summary key-decisions | Yes | agent |
| D184 | migration | migrated-summary | Phase 16 marker reconciliation remained docs-only; production and test directories were verified unchanged. | Phase 16 marker reconciliation remained docs-only; production and test directories were verified unchanged. | Migrated from legacy summary key-decisions | Yes | agent |
