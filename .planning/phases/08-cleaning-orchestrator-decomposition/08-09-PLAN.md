---
phase: 08-cleaning-orchestrator-decomposition
plan: 09
type: tdd
wave: 7
depends_on: [08-08]
files_modified:
  - AutoQAC/Services/Cleaning/CleaningOrchestrator.cs
  - AutoQAC.Tests/Services/CleaningOrchestratorTests.cs
autonomous: true
gap_closure: true
requirements: [REF-01]
user_setup: []
must_haves:
  truths:
    - "A second public StartCleaningAsync call cannot overlap an active cleaning session."
    - "The first active session CTS remains the token canceled by StopCleaningAsync after a rejected concurrent start."
    - "No second cleaning session publishes StartCleaning, reaches preflight completion, or launches xEdit."
  artifacts:
    - path: "AutoQAC/Services/Cleaning/CleaningOrchestrator.cs"
      provides: "Non-blocking active-session guard around the whole StartCleaningAsync workflow"
      contains: "Interlocked.CompareExchange"
    - path: "AutoQAC.Tests/Services/CleaningOrchestratorTests.cs"
      provides: "Concurrent StartCleaningAsync regression coverage"
      contains: "StartCleaningAsync_WhenSessionAlreadyActive_ShouldRejectSecondStartAndKeepFirstSessionCancellable"
  key_links:
    - from: "AutoQAC/Services/Cleaning/CleaningOrchestrator.cs"
      to: "StopCleaningAsync"
      via: "first session keeps _cleaningCts ownership"
      pattern: "Interlocked\.CompareExchange"
    - from: "AutoQAC.Tests/Services/CleaningOrchestratorTests.cs"
      to: "AutoQAC/Services/Cleaning/CleaningOrchestrator.cs"
      via: "blocked first session + rejected second start"
      pattern: "already.*progress|already.*running"
---

<objective>
Close Phase 8 verification gap 1: concurrent public `StartCleaningAsync` calls must not overlap session orchestration or overwrite the active session CTS.

Purpose: preserve sequential cleaning/session state invariants from D-02, D-09, D-10, and REF-01 after the facade decomposition.
Output: one failing-then-passing regression test and a non-blocking active-session guard in `CleaningOrchestrator`.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cleaning-orchestrator-decomposition/08-VERIFICATION.md
@.planning/phases/08-cleaning-orchestrator-decomposition/08-07-SUMMARY.md
@AutoQAC/Services/Cleaning/CleaningOrchestrator.cs
@AutoQAC.Tests/Services/CleaningOrchestratorTests.cs

<interfaces>
From `CleaningOrchestrator.cs`:
```csharp
public Task StartCleaningAsync(CancellationToken ct = default) => StartCleaningAsync(null, null, ct);
public Task StartCleaningAsync(TimeoutRetryCallback? onTimeout, CancellationToken ct = default) =>
    StartCleaningAsync(onTimeout, null, ct);
public async Task StartCleaningAsync(TimeoutRetryCallback? onTimeout, BackupFailureCallback? onBackupFailure, CancellationToken ct = default)
```

Existing CTS helpers that must keep first-session ownership:
```csharp
private CancellationTokenSource CreateSessionCts(CancellationToken ct)
private void CancelSessionCts()
private void DisposeSessionCts()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - add concurrent start rejection regression</name>
  <files>AutoQAC.Tests/Services/CleaningOrchestratorTests.cs</files>
  <read_first>
    - AutoQAC.Tests/Services/CleaningOrchestratorTests.cs
    - AutoQAC/Services/Cleaning/CleaningOrchestrator.cs
    - .planning/phases/08-cleaning-orchestrator-decomposition/08-VERIFICATION.md
  </read_first>
  <behavior>
    - Start first `StartCleaningAsync` and block inside `CleanPluginAsync` after capturing its `CancellationToken`.
    - Call `StartCleaningAsync` a second time while the first session is active.
    - Expected second call behavior: throw `InvalidOperationException` whose message contains `already in progress`.
    - Expected invariant: `IStateService.StartCleaning(...)` is received exactly once, `ICleaningService.CleanPluginAsync(...)` is received exactly once before release, and `StopCleaningAsync()` cancels the first captured token.
  </behavior>
  <action>
    Add `StartCleaningAsync_WhenSessionAlreadyActive_ShouldRejectSecondStartAndKeepFirstSessionCancellable` to the Robustness/Cancellation area of `CleaningOrchestratorTests`. Use existing helpers `CreateSignal`, `WaitForSignalAsync`, and `WaitForCancellationAsync`. Arrange one plugin, valid `AppState`, `ValidateEnvironmentAsync(...).Returns(true)`, and `CleanPluginAsync(...)` that signals first launch then awaits cancellation. Start the first session without awaiting it, wait for the first plugin to start, then assert a second `StartCleaningAsync()` throws `InvalidOperationException` containing `already in progress`. After the rejection, call `StopCleaningAsync()`, wait for the first captured token to cancel, let the first task finish, and assert only one `StartCleaning` and one `CleanPluginAsync` occurred. Do not add production code in this task.
  </action>
  <verify>
    <automated>dotnet test AutoQACSharp.slnx --nologo --filter "FullyQualifiedName~StartCleaningAsync_WhenSessionAlreadyActive_ShouldRejectSecondStartAndKeepFirstSessionCancellable"</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC.Tests/Services/CleaningOrchestratorTests.cs` contains `StartCleaningAsync_WhenSessionAlreadyActive_ShouldRejectSecondStartAndKeepFirstSessionCancellable`.
    - The focused command fails before production changes because the second start is not rejected.
    - The new test asserts `InvalidOperationException` and message substring `already in progress`.
    - The new test asserts `StartCleaning` received exactly 1 call and `CleanPluginAsync` received exactly 1 call before/releasing the first session.
  </acceptance_criteria>
  <done>RED regression proves the existing concurrent-start gap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - guard active cleaning session before mutable session setup</name>
  <files>AutoQAC/Services/Cleaning/CleaningOrchestrator.cs</files>
  <read_first>
    - AutoQAC/Services/Cleaning/CleaningOrchestrator.cs
    - AutoQAC.Tests/Services/CleaningOrchestratorTests.cs
  </read_first>
  <behavior>
    - First start transitions the facade from idle to active before `terminationCoordinator.ResetForNewSession()` or `CreateSessionCts(ct)`.
    - Concurrent starts fail fast without creating/replacing `_cleaningCts`, resetting termination state, calling preflight, publishing `StartCleaning`, or launching xEdit.
    - The active flag is cleared in `finally` after `DisposeSessionCts()` so later legitimate sessions can start.
  </behavior>
  <action>
    In `CleaningOrchestrator`, add `using System.Threading;` is already present; use `Interlocked` without new dependencies. Add `private int _sessionActive;` beside `_cleaningCts`. At the very top of the 3-argument `StartCleaningAsync` method, before `const int maxRetryAttempts`, call a new private helper `EnterSessionOrThrow()` that uses `Interlocked.CompareExchange(ref _sessionActive, 1, 0)` and throws `new InvalidOperationException("A cleaning session is already in progress.")` when the previous value is 1. Add a matching private helper `ExitSession()` using `Volatile.Write(ref _sessionActive, 0)`. Call `ExitSession()` in the existing `finally` after `terminationCoordinator.ResetForNewSession(); DisposeSessionCts();`. Add XML doc comments to both helpers explaining that this guard protects `_cleaningCts` ownership and sequential session orchestration. Do not serialize by waiting; this must be non-blocking fail-fast behavior.
  </action>
  <verify>
    <automated>dotnet test AutoQACSharp.slnx --nologo --filter "FullyQualifiedName~StartCleaningAsync_WhenSessionAlreadyActive_ShouldRejectSecondStartAndKeepFirstSessionCancellable|FullyQualifiedName~StopCleaningAsync_DuringPreflight_CancelsSessionAndDoesNotInvokeCleaningService|FullyQualifiedName~StopCleaningAsync_DuringOrphanCleanup_CancelsSessionBeforePreflightAndDoesNotInvokeCleaningService"</automated>
  </verify>
  <acceptance_criteria>
    - `CleaningOrchestrator.cs` contains `private int _sessionActive;`.
    - `CleaningOrchestrator.cs` contains `Interlocked.CompareExchange(ref _sessionActive, 1, 0)`.
    - `CleaningOrchestrator.cs` contains `Volatile.Write(ref _sessionActive, 0)`.
    - The exception message contains `A cleaning session is already in progress.`.
    - The focused RED test passes and both Plan 08-07 startup Stop regression tests still pass.
  </acceptance_criteria>
  <done>Concurrent StartCleaningAsync is rejected without overwriting the active CTS; Stop still cancels the first session.</done>
</task>

<task type="auto">
  <name>Task 3: Regression sweep and source guard</name>
  <files>AutoQAC/Services/Cleaning/CleaningOrchestrator.cs, AutoQAC.Tests/Services/CleaningOrchestratorTests.cs</files>
  <read_first>
    - AutoQAC/Services/Cleaning/CleaningOrchestrator.cs
    - AutoQAC.Tests/Services/CleaningOrchestratorTests.cs
  </read_first>
  <action>
    Run the focused test plus the existing cleaning orchestrator suite. Confirm the active-session guard did not add `Task.WhenAll`, `Task.WhenAny`, `Task.Run`, `Parallel.ForEach`, or `Parallel.For` to `AutoQAC/Services/Cleaning/CleaningOrchestrator.cs`. If a refactor is needed, keep it limited to names/comments around the guard; do not change the public `ICleaningOrchestrator` interface.
  </action>
  <verify>
    <automated>dotnet test AutoQACSharp.slnx --nologo --filter "FullyQualifiedName~CleaningOrchestratorTests"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test AutoQACSharp.slnx --nologo --filter "FullyQualifiedName~CleaningOrchestratorTests"` exits 0.
    - Non-comment source in `CleaningOrchestrator.cs` contains zero `Task.WhenAll(`, `Task.WhenAny(`, `Task.Run(`, `Parallel.ForEach`, and `Parallel.For` tokens.
    - `ICleaningOrchestrator.cs` is not modified.
  </acceptance_criteria>
  <done>Concurrent-start gap closure is regression-tested against the existing orchestrator suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI/ViewModel→ICleaningOrchestrator | Public service method can be called more than once from UI commands or tests. |
| Orchestrator→xEdit process | Session orchestration eventually launches external xEdit/MO2 processes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09-01 | Denial of Service | `CleaningOrchestrator.StartCleaningAsync` | mitigate | Fail fast on concurrent starts with `Interlocked.CompareExchange` so overlapping sessions cannot corrupt `_cleaningCts` or state. |
| T-08-09-02 | Tampering | `CleaningOrchestrator` session state | mitigate | Regression test proves second start cannot overwrite the first session token and Stop cancels the original active session. |
| T-08-09-03 | Elevation of Privilege | xEdit process launch | accept | No new launch path or permissions are introduced; existing runner/process-service boundaries remain unchanged. |
</threat_model>

<verification>
Run targeted and orchestrator regression tests:
`dotnet test AutoQACSharp.slnx --nologo --filter "FullyQualifiedName~StartCleaningAsync_WhenSessionAlreadyActive_ShouldRejectSecondStartAndKeepFirstSessionCancellable|FullyQualifiedName~CleaningOrchestratorTests"`
</verification>

<success_criteria>
- Second concurrent `StartCleaningAsync` throws `InvalidOperationException` containing `already in progress`.
- First active session remains stoppable via `StopCleaningAsync` and no second xEdit launch occurs.
- `CleaningOrchestratorTests` pass.
</success_criteria>

<output>
After completion, create `.planning/phases/08-cleaning-orchestrator-decomposition/08-09-SUMMARY.md`.
</output>
