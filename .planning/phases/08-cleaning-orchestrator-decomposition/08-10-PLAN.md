---
phase: 08-cleaning-orchestrator-decomposition
plan: 10
type: tdd
wave: 7
depends_on: [08-08]
files_modified:
  - AutoQAC/Services/Cleaning/CleaningPreflight.cs
  - AutoQAC.Tests/Services/Cleaning/CleaningPreflightTests.cs
autonomous: true
gap_closure: true
requirements: [REF-01]
user_setup: []
must_haves:
  truths:
    - "Preflight validates LoadOrderPath after Unknown game detection resolves to Fallout3, FalloutNewVegas, or Oblivion."
    - "Detected file-load-order games cannot proceed to plugin-row construction when LoadOrderPath is null, empty, or points to a missing file."
    - "Mutagen-supported detected games still do not require LoadOrderPath."
  artifacts:
    - path: "AutoQAC/Services/Cleaning/CleaningPreflight.cs"
      provides: "Post-detection file-load-order validation"
      contains: "ValidateDetectedLoadOrderPath"
    - path: "AutoQAC.Tests/Services/Cleaning/CleaningPreflightTests.cs"
      provides: "Unknown-to-file-based-game regression coverage"
      contains: "PrepareAsync_UnknownGameDetectedAsFileLoadOrderGame_WithMissingLoadOrderPath_Throws"
  key_links:
    - from: "CleaningPreflight.PrepareAsync"
      to: "RequiresFileLoadOrder(gameType)"
      via: "post-detection validation before variant detection and plugin-row construction"
      pattern: "ValidateDetectedLoadOrderPath\(gameType"
    - from: "CleaningPreflightTests"
      to: "CleaningPreflight.PrepareAsync"
      via: "Unknown -> Fallout3/FalloutNewVegas/Oblivion parameterized tests"
      pattern: "GameType\.Fallout3|GameType\.FalloutNewVegas|GameType\.Oblivion"
---

<objective>
Close Phase 8 verification gap 2: preflight must revalidate file-load-order requirements for the final detected game before xEdit launch.

Purpose: preserve D-13/D-15 preflight safety and REF-01 after moving validation into `CleaningPreflight`.
Output: post-detection load-order validation plus focused collaborator tests.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cleaning-orchestrator-decomposition/08-VERIFICATION.md
@AutoQAC/Services/Cleaning/CleaningPreflight.cs
@AutoQAC.Tests/Services/Cleaning/CleaningPreflightTests.cs

<interfaces>
Current validation helpers in `CleaningPreflight.cs`:
```csharp
private async Task<bool> ValidateConfigurationAsync(CancellationToken ct)
private static bool RequiresFileLoadOrder(GameType gameType) => gameType switch
{
    GameType.Fallout3 => true,
    GameType.FalloutNewVegas => true,
    GameType.Oblivion => true,
    _ => false
};
```

Current detection flow:
```csharp
var gameType = config.CurrentGameType;
if (gameType == GameType.Unknown)
{
    var detectedGame = gameDetection.DetectFromExecutable(config.XEditExecutablePath ?? string.Empty);
    ...
    gameType = detectedGame;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - add detected file-load-order validation tests</name>
  <files>AutoQAC.Tests/Services/Cleaning/CleaningPreflightTests.cs</files>
  <read_first>
    - AutoQAC.Tests/Services/Cleaning/CleaningPreflightTests.cs
    - AutoQAC/Services/Cleaning/CleaningPreflight.cs
    - .planning/phases/08-cleaning-orchestrator-decomposition/08-VERIFICATION.md
  </read_first>
  <behavior>
    - Case 1: `CurrentGameType = Unknown`, executable detection returns `Fallout3`, `LoadOrderPath = null` -> `PrepareAsync` throws `InvalidOperationException` and does not call `GetSkipListAsync` or `ValidatePluginFile`.
    - Case 2: same for `FalloutNewVegas` with `LoadOrderPath = ""`.
    - Case 3: same for `Oblivion` with `LoadOrderPath` pointing to a missing path.
    - Control case: `CurrentGameType = Unknown`, executable detection returns `Fallout4`, `LoadOrderPath = null` -> `PrepareAsync` succeeds because Fallout4 is Mutagen-supported.
  </behavior>
  <action>
    Add a `[Theory]` named `PrepareAsync_UnknownGameDetectedAsFileLoadOrderGame_WithMissingLoadOrderPath_Throws` with inline data for `GameType.Fallout3`, `GameType.FalloutNewVegas`, and `GameType.Oblivion`. Arrange state with `CurrentGameType = GameType.Unknown`, `XEditExecutablePath = @"C:\Games\xEdit\xEdit.exe"`, one plugin, and the missing load-order path from the inline data. Set `_gameDetectionMock.DetectFromExecutable(...)` to the inline game type and `_cleaningServiceMock.ValidateEnvironmentAsync(...)` to true. Assert `PrepareAsync` throws `InvalidOperationException`, `_configMock.GetSkipListAsync(...)` is not received, and `_validationMock.ValidatePluginFile(...)` is not received. Add a `[Fact]` named `PrepareAsync_UnknownGameDetectedAsMutagenSupportedGame_WithMissingLoadOrderPath_Succeeds` using `GameType.Fallout4` and null load-order path, asserting the plan's `DetectedGameType` is `Fallout4` and one row is returned. Do not add production code in this task.
  </action>
  <verify>
    <automated>dotnet test AutoQACSharp.slnx --nologo --filter "FullyQualifiedName~PrepareAsync_UnknownGameDetectedAsFileLoadOrderGame_WithMissingLoadOrderPath_Throws|FullyQualifiedName~PrepareAsync_UnknownGameDetectedAsMutagenSupportedGame_WithMissingLoadOrderPath_Succeeds"</automated>
  </verify>
  <acceptance_criteria>
    - `CleaningPreflightTests.cs` contains `PrepareAsync_UnknownGameDetectedAsFileLoadOrderGame_WithMissingLoadOrderPath_Throws`.
    - The theory includes `GameType.Fallout3`, `GameType.FalloutNewVegas`, and `GameType.Oblivion` cases.
    - The focused command fails before production changes because detected file-load-order games bypass the second validation.
    - The Mutagen-supported control test passes or is expected to pass without requiring LoadOrderPath.
  </acceptance_criteria>
  <done>RED tests reproduce the Unknown→file-load-order validation gap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - revalidate LoadOrderPath after final game detection</name>
  <files>AutoQAC/Services/Cleaning/CleaningPreflight.cs</files>
  <read_first>
    - AutoQAC/Services/Cleaning/CleaningPreflight.cs
    - AutoQAC.Tests/Services/Cleaning/CleaningPreflightTests.cs
  </read_first>
  <behavior>
    - `PrepareAsync` keeps the existing initial `ValidateConfigurationAsync(ct)` call.
    - After `gameType` is finalized and before `DetectVariant`, `LoadUserConfigAsync`, `GetSkipListAsync`, or plugin-row construction, preflight validates `LoadOrderPath` again when `RequiresFileLoadOrder(gameType)` is true.
    - Invalid post-detection file-load-order state throws `InvalidOperationException("Configuration is invalid")`, matching the existing preflight invalid-config message.
  </behavior>
  <action>
    In `CleaningPreflight.PrepareAsync`, immediately after the `if (gameType == GameType.Unknown)` block and before `var pluginNames = allPlugins.Select(...)`, call a new private helper `ValidateDetectedLoadOrderPath(gameType, config.LoadOrderPath)`. Implement the helper as `private static bool ValidateDetectedLoadOrderPath(GameType gameType, string? loadOrderPath)` returning true for non-file-load-order games and returning `!string.IsNullOrWhiteSpace(loadOrderPath) && File.Exists(loadOrderPath)` for `Fallout3`, `FalloutNewVegas`, and `Oblivion`. If it returns false, log `logger.Error(null, "Configuration is invalid, cannot start cleaning.");` and throw `new InvalidOperationException("Configuration is invalid")`. Add XML doc comments explaining this second validation is needed because `CurrentGameType` can start as `Unknown` and be resolved to a file-load-order game only after executable/load-order detection.
  </action>
  <verify>
    <automated>dotnet test AutoQACSharp.slnx --nologo --filter "FullyQualifiedName~CleaningPreflightTests"</automated>
  </verify>
  <acceptance_criteria>
    - `CleaningPreflight.cs` contains `ValidateDetectedLoadOrderPath(gameType, config.LoadOrderPath)` after game detection and before `DetectVariant`.
    - `CleaningPreflight.cs` contains `private static bool ValidateDetectedLoadOrderPath(GameType gameType, string? loadOrderPath)`.
    - Invalid detected `Fallout3`, `FalloutNewVegas`, and `Oblivion` cases throw before skip-list/plugin validation calls.
    - Detected `Fallout4` with missing load-order path still succeeds.
  </acceptance_criteria>
  <done>Post-detection file-load-order validation blocks unsafe preflight plans.</done>
</task>

<task type="auto">
  <name>Task 3: Regression sweep for preflight and orchestrator integration</name>
  <files>AutoQAC/Services/Cleaning/CleaningPreflight.cs, AutoQAC.Tests/Services/Cleaning/CleaningPreflightTests.cs</files>
  <read_first>
    - AutoQAC/Services/Cleaning/CleaningPreflight.cs
    - AutoQAC.Tests/Services/Cleaning/CleaningPreflightTests.cs
    - AutoQAC.Tests/Services/CleaningOrchestratorTests.cs
  </read_first>
  <action>
    Run all `CleaningPreflightTests` plus the existing orchestrator load-order tests (`StartCleaningAsync_ShouldNotRequireLoadOrderPath_WhenGameTypeIsMutagenSupported` and `StartCleaningAsync_ShouldThrow_WhenNonMutagenGameMissingLoadOrderPath`). Keep the duplicated `RequiresFileLoadOrder` helper local to `CleaningPreflight`; do not consolidate game policy across services because research and D-11 defer that to Phase 9/REF-02.
  </action>
  <verify>
    <automated>dotnet test AutoQACSharp.slnx --nologo --filter "FullyQualifiedName~CleaningPreflightTests|FullyQualifiedName~StartCleaningAsync_ShouldNotRequireLoadOrderPath_WhenGameTypeIsMutagenSupported|FullyQualifiedName~StartCleaningAsync_ShouldThrow_WhenNonMutagenGameMissingLoadOrderPath"</automated>
  </verify>
  <acceptance_criteria>
    - Focused preflight and orchestrator load-order regression command exits 0.
    - No files outside `AutoQAC/Services/Cleaning/CleaningPreflight.cs` and `AutoQAC.Tests/Services/Cleaning/CleaningPreflightTests.cs` are changed by this plan.
    - `CleaningPreflight.cs` still contains the Phase 9 deferral remark about not consolidating duplicated file-load-order policy.
  </acceptance_criteria>
  <done>Detected-game load-order validation is proven without expanding Phase 8 into Phase 9 policy consolidation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local config/state→CleaningPreflight | User-configured paths and detected game type decide whether file-based load-order parsing is required. |
| CleaningPreflight→xEdit launch path | A valid preflight plan allows later xEdit execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10-01 | Tampering | `CleaningPreflight.PrepareAsync` | mitigate | Revalidate `LoadOrderPath` after final game detection so stale `Unknown` state cannot bypass file-load-order requirements. |
| T-08-10-02 | Denial of Service | file-load-order games | mitigate | Fail before skip-list/plugin-row construction with the existing `Configuration is invalid` path when required load-order config is missing. |
| T-08-10-03 | Information Disclosure | path validation error | accept | This plan preserves the existing generic invalid-configuration message and does not add new path details to user-facing text. |
</threat_model>

<verification>
Run preflight-focused regressions:
`dotnet test AutoQACSharp.slnx --nologo --filter "FullyQualifiedName~CleaningPreflightTests|FullyQualifiedName~StartCleaningAsync_ShouldNotRequireLoadOrderPath_WhenGameTypeIsMutagenSupported|FullyQualifiedName~StartCleaningAsync_ShouldThrow_WhenNonMutagenGameMissingLoadOrderPath"`
</verification>

<success_criteria>
- Unknown→Fallout3/FalloutNewVegas/Oblivion with missing load-order path throws before rows are built.
- Unknown→Fallout4 with missing load-order path still succeeds.
- No Phase 9 policy consolidation is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/08-cleaning-orchestrator-decomposition/08-10-SUMMARY.md`.
</output>
