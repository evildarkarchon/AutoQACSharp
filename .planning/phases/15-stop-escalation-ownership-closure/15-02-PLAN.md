---
phase: 15-stop-escalation-ownership-closure
plan: 02
type: tdd
wave: 2
depends_on: [15-01]
files_modified:
  - AutoQAC/Services/Cleaning/CleaningOrchestrator.cs
  - AutoQAC/Services/Cleaning/ICleaningTerminationCoordinator.cs
  - AutoQAC.Tests/Services/CleaningOrchestratorTests.cs
  - AutoQAC.Tests/ViewModels/ProgressViewModelTests.cs
autonomous: true
requirements: [SAF-01, SAF-02, TEST-01]
must_haves:
  truths:
    - "D-02: Normal session finalization preserves an unresolved GracePeriodExpired pending target until the user resolves the confirmation."
    - "D-09: Confirmed detached-target and invalid-target failures use StopTerminationDialogContent.ForceFailureTitle and ForceFailureMessage, with no new user-facing copy."
    - "D-12: Progress Stop does not call ForceStopCleaningAsync before the affirmative confirmation dialog result."
    - "D-13: Progress Stop displays shared safe force-failure dialog and persistent warning when confirmed detached force escalation returns ForceKillFailed."
  artifacts:
    - path: "AutoQAC/Services/Cleaning/CleaningOrchestrator.cs"
      provides: "Correct reset/finalization calls around pending force escalation lifetime"
      contains: "ResetForNewSession"
    - path: "AutoQAC.Tests/Services/CleaningOrchestratorTests.cs"
      provides: "Service-level proof that finalization does not erase unresolved pending escalation target"
      contains: "GracePeriodExpired"
    - path: "AutoQAC.Tests/ViewModels/ProgressViewModelTests.cs"
      provides: "Progress confirmation-order and failure-visibility proof"
      contains: "StopCommand_WhenGracePeriodExpires_ShouldPromptBeforeForceStop"
  key_links:
    - from: "CleaningOrchestrator.StartCleaningAsync finally"
      to: "CleaningTerminationCoordinator pending target"
      via: "session finalization cleanup that preserves unresolved GracePeriodExpired target"
      pattern: "ResetForNewSession"
    - from: "ProgressViewModel.StopAsync"
      to: "ICleaningOrchestrator.ForceStopCleaningAsync"
      via: "ShowChoiceAsync affirmative Force Terminate result"
      pattern: "ShowChoiceAsync"
---

<objective>
Wire the retained coordinator target through session finalization and preserve the Progress-window confirmation/failure proof.

Purpose: Plan 01 fixes the coordinator mechanism; this plan ensures `CleaningOrchestrator` does not erase unresolved pending escalation during normal finalization and that Progress Stop still gates force stop behind user confirmation.
Output: TDD tests and minimal orchestration/ViewModel-adjacent verification for the detached escalation user flow.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-stop-escalation-ownership-closure/15-SPEC.md
@.planning/phases/15-stop-escalation-ownership-closure/15-CONTEXT.md
@.planning/phases/15-stop-escalation-ownership-closure/15-RESEARCH.md
@.planning/phases/15-stop-escalation-ownership-closure/15-01-SUMMARY.md
@.planning/phases/12-process-stop-verification-progress-flow-closure/12-02-SUMMARY.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md

<interfaces>
From `AutoQAC/Services/Cleaning/CleaningOrchestrator.cs`:
```csharp
public async Task<StopCleaningResult> StopCleaningAsync()
{
    CancelSessionCts();
    return await terminationCoordinator.StopAsync().ConfigureAwait(false);
}

public async Task<StopCleaningResult> ForceStopCleaningAsync()
{
    CancelSessionCts();
    return await terminationCoordinator.ForceStopAsync().ConfigureAwait(false);
}
```

From `AutoQAC/ViewModels/ProgressViewModel.cs`:
```csharp
var choice = await _messageDialog.ShowChoiceAsync(
    StopTerminationDialogContent.ConfirmationTitle,
    StopTerminationDialogContent.ConfirmationMessage,
    StopTerminationDialogContent.ForceTerminateButton,
    StopTerminationDialogContent.LeaveRunningButton);
if (choice == MessageDialogResult.Yes)
{
    var forceResult = await _orchestrator.ForceStopCleaningAsync();
    await ReportForceStopFailureIfNeededAsync(forceResult.TerminationResult ?? _orchestrator.LastTerminationResult);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED/GREEN orchestrator finalization lifetime contract</name>
  <files>AutoQAC/Services/Cleaning/CleaningOrchestrator.cs, AutoQAC/Services/Cleaning/ICleaningTerminationCoordinator.cs, AutoQAC.Tests/Services/CleaningOrchestratorTests.cs</files>
  <read_first>
    - AutoQAC/Services/Cleaning/CleaningOrchestrator.cs
    - AutoQAC/Services/Cleaning/ICleaningTerminationCoordinator.cs
    - AutoQAC.Tests/Services/CleaningOrchestratorTests.cs
    - AutoQAC.Tests/Services/Cleaning/CleaningTerminationCoordinatorTests.cs
    - .planning/phases/15-stop-escalation-ownership-closure/15-01-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: a service-level test proves `StartCleaningAsync` finalization uses the coordinator cleanup path that preserves an unresolved pending target after `GracePeriodExpired`, runner detach, and returned execution.
    - Test 2: a service-level test proves the next-session reset boundary releases stale unresolved pending target before new cleaning starts, per D-02, without changing active cleaning semantics.
  </behavior>
  <action>First add failing coverage in `CleaningOrchestratorTests` or the closest existing service-level fixture. The test must model the sequence from SPEC requirement 1: process attached by the runner, `GracePeriodExpired`, runner detach/returned execution, normal finalization, then confirmed `ForceStopCleaningAsync()`. If Plan 01 introduced a new coordinator method or `ResetForNewSession` overload, update `ICleaningTerminationCoordinator` XML docs and `CleaningOrchestrator` calls so the start-of-session path clears stale pending state while the finalization path preserves unresolved `GracePeriodExpired` pending state. Keep `CancelSessionCts()` before both stop calls. Do not add process manipulation to ViewModels or the orchestrator.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~CleaningOrchestratorTests --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `CleaningOrchestratorTests.cs` contains a test name with `GracePeriodExpired`, `Detach`, and `ForceStop` or equivalent exact sequence terms.
    - `CleaningOrchestrator.cs` has distinct calls or arguments for start-of-session reset versus finalization cleanup if Plan 01 made that distinction necessary.
    - `ICleaningTerminationCoordinator.cs` XML docs describe that unresolved pending force-escalation ownership survives normal session finalization until user resolution or next-session reset.
    - Targeted orchestrator tests pass.
  </acceptance_criteria>
  <done>Normal finalization no longer erases the pending target needed by the already-displayed Progress force confirmation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve Progress confirmation order and failure visibility</name>
  <files>AutoQAC.Tests/ViewModels/ProgressViewModelTests.cs</files>
  <read_first>
    - AutoQAC.Tests/ViewModels/ProgressViewModelTests.cs
    - AutoQAC/ViewModels/ProgressViewModel.cs
    - AutoQAC/Models/StopTerminationDialogContent.cs
    - .planning/phases/12-process-stop-verification-progress-flow-closure/12-02-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: `StopCommand_WhenGracePeriodExpires_ShouldPromptBeforeForceStop` continues to hold the dialog `TaskCompletionSource` pending and asserts `ForceStopCleaningAsync()` is not called until the dialog returns `MessageDialogResult.Yes`.
    - Test 2: add or extend a test named `StopCommand_WhenConfirmedDetachedForceTerminationFails_ShouldShowSharedFailureAndPersistWarning` where `StopCleaningAsync()` returns `GracePeriodExpired`, confirmation returns Yes, `ForceStopCleaningAsync()` returns `ForceKillFailed`, `ShowErrorAsync` is called with `StopTerminationDialogContent.ForceFailureTitle` and `ForceFailureMessage`, and `StopOutcomeWarningText` equals `ForceFailureMessage`.
  </behavior>
  <action>Keep `ProgressViewModel` behavior unchanged unless tests reveal a regression from Plan 01/Task 1 changes. Do not add new user-facing copy. The only accepted failure dialog copy for confirmed detached target failure is `StopTerminationDialogContent.ForceFailureTitle` and `StopTerminationDialogContent.ForceFailureMessage` per D-09/D-13. Preserve Hang Kill immediate-force behavior; do not add the Stop confirmation to `KillHungProcessCommand`.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~ProgressViewModelTests --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `ProgressViewModelTests.cs` contains `StopCommand_WhenGracePeriodExpires_ShouldPromptBeforeForceStop`.
    - `ProgressViewModelTests.cs` contains `DidNotReceive().ForceStopCleaningAsync()` before completing the choice task.
    - `ProgressViewModelTests.cs` contains `StopTerminationDialogContent.ForceFailureTitle` and `StopTerminationDialogContent.ForceFailureMessage` assertions for confirmed force failure.
    - Targeted Progress ViewModel tests pass.
  </acceptance_criteria>
  <done>Progress Stop still prompts before force, then shows the shared safe force-failure dialog and persistent warning when confirmed force termination fails.</done>
</task>

</tasks>

<verification>
Run targeted orchestrator and Progress ViewModel tests, then run `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~CleaningTerminationCoordinatorTests|FullyQualifiedName~CleaningOrchestratorTests|FullyQualifiedName~ProgressViewModelTests" --nologo`.
</verification>

<success_criteria>
- Orchestrator reset/finalization semantics preserve pending target through user resolution without treating it as active cleaning.
- Progress confirmation ordering remains intact.
- Confirmed detached force-failure returns `ForceKillFailed` to the existing shared UI warning path.
</success_criteria>

<output>
After completion, create `.planning/phases/15-stop-escalation-ownership-closure/15-02-SUMMARY.md`.
</output>
