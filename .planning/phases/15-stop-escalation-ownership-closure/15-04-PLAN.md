---
phase: 15-stop-escalation-ownership-closure
plan: 04
type: execute
wave: 4
depends_on: [15-03]
files_modified:
  - AutoQAC/Services/Cleaning/CleaningTerminationCoordinator.cs
  - AutoQAC.Tests/Services/Cleaning/CleaningTerminationCoordinatorTests.cs
  - AutoQAC.Tests/Services/CleaningOrchestratorTests.cs
  - .planning/phases/15-stop-escalation-ownership-closure/15-VERIFICATION.md
  - .planning/phases/15-stop-escalation-ownership-closure/15-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [SAF-01, SAF-02, TEST-01]
must_haves:
  truths:
    - "SAF-01 / D-01-D-06: Progress-window Stop retains stable force-termination identity after GracePeriodExpired, runner detach, finalization, and ProcessExecutionService-owned handle disposal."
    - "SAF-02 / D-08-D-09: confirmed force escalation returns ForceKillFailed when the pending target cannot be safely reopened or verified, so ProgressViewModel can show the shared safe warning."
    - "TEST-01 / D-10-D-13: automated regression coverage exercises the production ownership boundary by disposing the original process wrapper before confirmed ForceStopCleaningAsync."
    - "D-15-D-18: 15-VERIFICATION.md supersedes the stale gaps_found report with passed evidence rows for SAF-01, SAF-02, TEST-01, INT-STOP-01, and FLOW-STOP-ESCALATION-01 without performing Phase 16 marker reconciliation."
  artifacts:
    - path: AutoQAC/Services/Cleaning/CleaningTerminationCoordinator.cs
      provides: durable pending force target identity and safe confirmed force-stop mapping
      contains: "private sealed record PendingForceTarget(int ProcessId, DateTime StartTime)"
    - path: AutoQAC.Tests/Services/CleaningOrchestratorTests.cs
      provides: production ownership regression for disposed original Process handle
      contains: "forceUsedDisposedOriginalHandle.Should().BeFalse"
    - path: .planning/phases/15-stop-escalation-ownership-closure/15-VERIFICATION.md
      provides: current gap-closure verification report
      contains: "status: passed"
    - path: .planning/phases/15-stop-escalation-ownership-closure/15-VALIDATION.md
      provides: validation map including this gap closure plan
      contains: "15-04-01"
  key_links:
    - from: AutoQAC/Services/Cleaning/CleaningTerminationCoordinator.cs
      to: AutoQAC/Services/Process/ProcessExecutionService.cs
      via: "pending target captured as PID/start time instead of retaining the borrowed ExecuteAsync Process wrapper"
      pattern: "PendingForceTarget\(int ProcessId, DateTime StartTime\)"
    - from: AutoQAC.Tests/Services/CleaningOrchestratorTests.cs
      to: AutoQAC/Services/Cleaning/CleaningTerminationCoordinator.cs
      via: "disposed original handle assertion after runner detach/finalization"
      pattern: "forceUsedDisposedOriginalHandle\.Should\(\)\.BeFalse"
---

<objective>
Close the Phase 15 verification gaps by proving the confirmed force-stop path no longer depends on a disposed `ProcessExecutionService.ExecuteAsync` wrapper and by replacing the stale `gaps_found` evidence with current passed verification.

Purpose: Phase 15 is not complete until the exact verifier gap is closed: after `GracePeriodExpired`, process execution may return and dispose its wrapper before the user confirms `Force Terminate`. The implementation must use stable target identity or safely fail, and the verification artifact must reflect that proof.
Output: Durable pending-target source/test proof plus updated `15-VERIFICATION.md` and `15-VALIDATION.md`.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-stop-escalation-ownership-closure/15-CONTEXT.md
@.planning/phases/15-stop-escalation-ownership-closure/15-SPEC.md
@.planning/phases/15-stop-escalation-ownership-closure/15-RESEARCH.md
@.planning/phases/15-stop-escalation-ownership-closure/15-VERIFICATION.md
@.planning/phases/15-stop-escalation-ownership-closure/15-REVIEW.md
@.planning/phases/15-stop-escalation-ownership-closure/15-REVIEW-FIX.md
@.planning/phases/15-stop-escalation-ownership-closure/15-01-SUMMARY.md
@.planning/phases/15-stop-escalation-ownership-closure/15-02-SUMMARY.md
@.planning/phases/15-stop-escalation-ownership-closure/15-03-SUMMARY.md
@AutoQAC/Services/Cleaning/CleaningTerminationCoordinator.cs
@AutoQAC/Services/Process/ProcessExecutionService.cs
@AutoQAC.Tests/Services/Cleaning/CleaningTerminationCoordinatorTests.cs
@AutoQAC.Tests/Services/CleaningOrchestratorTests.cs

<interfaces>
<!-- Key contracts the executor must preserve. -->

From `AutoQAC/Services/Cleaning/ICleaningTerminationCoordinator.cs`:
```csharp
void AttachProcess(System.Diagnostics.Process process);
void DetachProcess();
Task<StopCleaningResult> StopAsync();
Task<StopCleaningResult> ForceStopAsync();
StopCleaningResult MarkLeftRunningByUser();
void ResetForNewSession();
void CompleteSessionFinalization();
TerminationResult? LastTerminationResult { get; }
bool HasActiveProcess { get; }
bool ProcessMayStillBeRunning { get; }
```

Required `CleaningTerminationCoordinator` target state for this gap closure:
```csharp
private sealed record PendingForceTarget(int ProcessId, DateTime StartTime);
```

`RetainPendingForceEscalationProcess(System.Diagnostics.Process process)` must capture both `process.Id` and `process.StartTime`; if either cannot be captured, it must log a warning and not retain a force target. `ForceStopAsync()` must reopen pending targets with `System.Diagnostics.Process.GetProcessById(target.ProcessId)`, verify `process.StartTime == target.StartTime`, dispose the reopened handle after use, and return `ForceKillFailed` when the target cannot be reopened or verified.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close durable pending-target ownership gap</name>
  <files>AutoQAC/Services/Cleaning/CleaningTerminationCoordinator.cs, AutoQAC.Tests/Services/Cleaning/CleaningTerminationCoordinatorTests.cs, AutoQAC.Tests/Services/CleaningOrchestratorTests.cs</files>
  <read_first>
    - AutoQAC/Services/Cleaning/CleaningTerminationCoordinator.cs
    - AutoQAC/Services/Process/ProcessExecutionService.cs
    - AutoQAC.Tests/Services/Cleaning/CleaningTerminationCoordinatorTests.cs
    - AutoQAC.Tests/Services/CleaningOrchestratorTests.cs
    - .planning/phases/15-stop-escalation-ownership-closure/15-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: `StartCleaningAsync_AfterGracePeriodExpiredAndDetach_PreservesPendingTargetForForceStopAfterFinalization` disposes the original process wrapper before `ForceStopCleaningAsync()` and asserts the force-kill call uses a fresh reopened process handle, not the disposed original wrapper.
    - Test 2: coordinator confirmed force-stop uses `PendingForceTarget(ProcessId, StartTime)` and rejects unavailable/unverifiable pending targets with `TerminationResult.ForceKillFailed`, not `GracePeriodExpired` and not `AlreadyExited` for an unproven target.
    - Test 3: active cleaning semantics stay separate: `HasActiveProcess` is false after `DetachProcess()` even when pending target identity exists for later confirmation.
  </behavior>
  <action>
    Implement or preserve the exact Phase 15 gap fix for D-01 through D-09: `CleaningTerminationCoordinator` must not retain a borrowed `System.Diagnostics.Process` wrapper as the pending force target. The target state is a private `PendingForceTarget(int ProcessId, DateTime StartTime)` record. `StopAsync()` must call a helper that captures both `process.Id` and `process.StartTime` after `TerminationResult.GracePeriodExpired`; if either value cannot be read, log a warning and do not retain a pending target. `DetachProcess()` and `CompleteSessionFinalization()` may clear `_currentProcess` and stop hang monitoring, but they must preserve unresolved pending identity while `MayProcessStillBeRunning(_lastTerminationResult)` is true. `ForceStopAsync()` must prefer `_currentProcess`, otherwise use the pending target only when the last result may still be running; reopen with `Process.GetProcessById(target.ProcessId)`, require exact start-time equality, dispose mismatched or used reopened handles, and map null/unavailable/unverified pending targets to `TerminationResult.ForceKillFailed`. Do not implement PID-only force termination. Do not change `ProgressViewModel` copy or Phase 16 marker files.
  </action>
  <acceptance_criteria>
    - `AutoQAC/Services/Cleaning/CleaningTerminationCoordinator.cs` contains `private sealed record PendingForceTarget(int ProcessId, DateTime StartTime)`.
    - `AutoQAC/Services/Cleaning/CleaningTerminationCoordinator.cs` contains `DiagnosticsProcess.GetProcessById(target.ProcessId)`.
    - `AutoQAC/Services/Cleaning/CleaningTerminationCoordinator.cs` contains `process.StartTime != target.StartTime`.
    - `AutoQAC/Services/Cleaning/CleaningTerminationCoordinator.cs` contains `return ToStopCleaningResult(TerminationResult.ForceKillFailed)` in the pending-target reopen failure path.
    - `AutoQAC.Tests/Services/CleaningOrchestratorTests.cs` contains `forceUsedDisposedOriginalHandle.Should().BeFalse`.
    - `AutoQAC.Tests/Services/CleaningOrchestratorTests.cs` contains `sleeper.Dispose()` before `ForceStopCleaningAsync()` in the detached-finalization regression.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~CleaningTerminationCoordinatorTests|FullyQualifiedName~CleaningOrchestratorTests" --nologo</automated>
  </verify>
  <done>Confirmed force escalation after runner detach/finalization no longer uses the disposed original process wrapper; it either force-kills a start-time-verified reopened target or returns ForceKillFailed for safe UI reporting.</done>
</task>

<task type="auto">
  <name>Task 2: Replace stale gaps_found evidence with current verification</name>
  <files>.planning/phases/15-stop-escalation-ownership-closure/15-VERIFICATION.md, .planning/phases/15-stop-escalation-ownership-closure/15-VALIDATION.md</files>
  <read_first>
    - .planning/phases/15-stop-escalation-ownership-closure/15-VERIFICATION.md
    - .planning/phases/15-stop-escalation-ownership-closure/15-VALIDATION.md
    - .planning/phases/15-stop-escalation-ownership-closure/15-CONTEXT.md
    - .planning/phases/15-stop-escalation-ownership-closure/15-SPEC.md
    - .planning/phases/15-stop-escalation-ownership-closure/15-REVIEW.md
    - .planning/phases/15-stop-escalation-ownership-closure/15-REVIEW-FIX.md
    - .planning/phases/15-stop-escalation-ownership-closure/15-03-SUMMARY.md
  </read_first>
  <action>
    Rewrite `15-VERIFICATION.md` from the stale `status: gaps_found` report to a current passed gap-closure verification report. Set frontmatter `status: passed` and include a `gap_closure:` section mapping the three failed truths from the old report to the Task 1 durable-target proof. The report must explicitly map `SAF-01`, `SAF-02`, `TEST-01`, `INT-STOP-01`, `FLOW-STOP-ESCALATION-01`, and all 15-SPEC acceptance criteria to source/test evidence. Include concise command rows for: coordinator + orchestrator targeted tests, ProgressViewModel tests, ProcessExecutionIntegrationTests, all four targeted suites together, and the full solution. Update `15-VALIDATION.md` so its per-task map includes `15-04-01` for durable target ownership and `15-04-02` for refreshed verification evidence, both with passed status after commands succeed. Preserve D-18: do not reconcile milestone markers, roadmap completion markers, or requirements markers in this task; Phase 16 owns that.
  </action>
  <acceptance_criteria>
    - `.planning/phases/15-stop-escalation-ownership-closure/15-VERIFICATION.md` frontmatter contains `status: passed`.
    - `.planning/phases/15-stop-escalation-ownership-closure/15-VERIFICATION.md` contains `PendingForceTarget(int ProcessId, DateTime StartTime)`.
    - `.planning/phases/15-stop-escalation-ownership-closure/15-VERIFICATION.md` contains `forceUsedDisposedOriginalHandle.Should().BeFalse`.
    - `.planning/phases/15-stop-escalation-ownership-closure/15-VERIFICATION.md` contains `SAF-01`, `SAF-02`, `TEST-01`, `INT-STOP-01`, and `FLOW-STOP-ESCALATION-01`.
    - `.planning/phases/15-stop-escalation-ownership-closure/15-VALIDATION.md` contains `15-04-01` and `15-04-02`.
    - `.planning/phases/15-stop-escalation-ownership-closure/15-VERIFICATION.md` does not contain `status: gaps_found` outside a quoted historical context line.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~CleaningTerminationCoordinatorTests|FullyQualifiedName~CleaningOrchestratorTests|FullyQualifiedName~ProgressViewModelTests|FullyQualifiedName~ProcessExecutionIntegrationTests" --nologo</automated>
  </verify>
  <done>Phase 15 verification and validation artifacts now prove the gap closure with current command evidence and no Phase 16 marker reconciliation.</done>
</task>

<task type="auto">
  <name>Task 3: Run full-suite closure evidence and summarize</name>
  <files>.planning/phases/15-stop-escalation-ownership-closure/15-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/15-stop-escalation-ownership-closure/15-04-PLAN.md
    - .planning/phases/15-stop-escalation-ownership-closure/15-VERIFICATION.md
    - .planning/phases/15-stop-escalation-ownership-closure/15-VALIDATION.md
  </read_first>
  <action>
    Run the full solution test command and create `15-04-SUMMARY.md` using the standard summary template. The summary must state that Plan 15-04 closes the stale verification gaps by durable PID/start-time pending-target identity and refreshed evidence, list any production/test/doc files actually changed, and explicitly note that milestone marker reconciliation remains deferred to Phase 16 per D-18. If Task 1 required no source changes because the current source already met the target state, record that as an execution note rather than modifying unrelated code.
  </action>
  <acceptance_criteria>
    - `.planning/phases/15-stop-escalation-ownership-closure/15-04-SUMMARY.md` exists.
    - `.planning/phases/15-stop-escalation-ownership-closure/15-04-SUMMARY.md` contains `Self-Check: PASSED`.
    - `.planning/phases/15-stop-escalation-ownership-closure/15-04-SUMMARY.md` contains `Phase 16`.
    - `.planning/phases/15-stop-escalation-ownership-closure/15-04-SUMMARY.md` contains `dotnet test AutoQACSharp.slnx --nologo`.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test AutoQACSharp.slnx --nologo</automated>
  </verify>
  <done>Full-suite evidence is current and a Plan 15-04 summary exists for downstream verify-work and Phase 16 reconciliation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User confirmation -> process termination service | A user-visible Force Terminate choice crosses into privileged local process termination. |
| Stored pending process identity -> OS process lookup | PID/start-time identity is used to reopen a process handle after the original owner may have disposed its wrapper. |
| Local process result -> Progress UI | TerminationResult controls whether the user sees safe force-failure warning copy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-01 | Tampering | `CleaningTerminationCoordinator.TryReopenPendingTarget` | mitigate | Require PID plus exact start-time match before force-killing; PID-only or unverified targets must return ForceKillFailed. |
| T-15-04-02 | Denial of Service | `ForceStopAsync` | mitigate | Dispose reopened process handles and preserve single process-slot/sequential cleaning semantics; do not keep hang monitoring active for detached pending targets. |
| T-15-04-03 | Repudiation | `15-VERIFICATION.md` | mitigate | Record command rows and source/test evidence mapping every failed verifier truth and requirement ID to the closing proof. |
| T-15-04-04 | Information Disclosure | Progress force-failure UI | mitigate | Reuse `StopTerminationDialogContent.ForceFailureTitle` and `ForceFailureMessage`; do not add raw path, PID exception, or command-line copy. |
</threat_model>

<verification>
Run, in order:
1. `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~CleaningTerminationCoordinatorTests|FullyQualifiedName~CleaningOrchestratorTests" --nologo`
2. `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~CleaningTerminationCoordinatorTests|FullyQualifiedName~CleaningOrchestratorTests|FullyQualifiedName~ProgressViewModelTests|FullyQualifiedName~ProcessExecutionIntegrationTests" --nologo`
3. `dotnet test AutoQACSharp.slnx --nologo`
</verification>

<success_criteria>
- The verifier gaps in `15-VERIFICATION.md` lines 7-38 are closed by durable pending target identity or safe ForceKillFailed mapping.
- Confirmed detached force-stop does not reuse a disposed original Process wrapper.
- Phase 15 verification frontmatter is `status: passed` and the validation map contains Plan 15-04 rows.
- No task edits Phase 16-owned milestone marker reconciliation in `.planning/v1.0-MILESTONE-AUDIT.md`, `.planning/ROADMAP.md` requirement completion markers, or `.planning/REQUIREMENTS.md` marker reconciliation beyond normal GSD plan-list metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/15-stop-escalation-ownership-closure/15-04-SUMMARY.md`.
</output>
