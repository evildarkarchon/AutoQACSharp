---
phase: 15-stop-escalation-ownership-closure
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - AutoQAC/Services/Cleaning/CleaningTerminationCoordinator.cs
  - AutoQAC/Services/Cleaning/ICleaningTerminationCoordinator.cs
  - AutoQAC.Tests/Services/Cleaning/CleaningTerminationCoordinatorTests.cs
autonomous: true
requirements: [SAF-01, SAF-02, TEST-01]
must_haves:
  truths:
    - "D-01/D-03: Coordinator retains a separate pending force-escalation Process handle after GracePeriodExpired without counting it as active cleaning."
    - "D-04: HasActiveProcess, hang monitoring, backup-cancel gating, and current-plugin active state continue to represent active cleaning only."
    - "D-05: PID/start-time fallback is not part of the planned primary implementation unless retained-handle evidence fails a required timing edge."
    - "D-06: Confirmed ForceStopAsync after detach returns a terminal result, never cached GracePeriodExpired."
    - "D-07/D-08: Already-exited retained targets return AlreadyExited; unavailable or unprovable targets return ForceKillFailed."
    - "D-10/D-11: Coordinator proof attaches a controlled helper process, drives GracePeriodExpired, detaches, force-stops, and asserts ForceKilled in the controlled success case."
    - "D-14: Tests use existing helper-process and service/ViewModel patterns, with no Avalonia.Headless or real xEdit/MO2 setup."
  artifacts:
    - path: "AutoQAC/Services/Cleaning/CleaningTerminationCoordinator.cs"
      provides: "Pending force-escalation target ownership and terminal force-stop mapping"
      contains: "pending"
    - path: "AutoQAC/Services/Cleaning/ICleaningTerminationCoordinator.cs"
      provides: "Documented reset/finalization lifetime contract for pending escalation targets"
      contains: "GracePeriodExpired"
    - path: "AutoQAC.Tests/Services/Cleaning/CleaningTerminationCoordinatorTests.cs"
      provides: "Coordinator regression tests for detached confirmed force escalation"
      contains: "ForceStopAsync_AfterGracePeriodExpiredAndDetach"
  key_links:
    - from: "CleaningTerminationCoordinator.StopAsync"
      to: "CleaningTerminationCoordinator.ForceStopAsync"
      via: "retained pending Process when graceful stop returns GracePeriodExpired"
      pattern: "GracePeriodExpired"
    - from: "CleaningTerminationCoordinator.DetachProcess"
      to: "CleaningTerminationCoordinator.ForceStopAsync"
      via: "pending escalation target not cleared by active-process detach"
      pattern: "DetachProcess"
---

<objective>
Create the coordinator-owned retained process handle required for confirmed detached force escalation.

Purpose: Phase 15 must close the audit blocker where `DetachProcess()` clears `_currentProcess` and later confirmed `ForceStopAsync()` silently returns cached `GracePeriodExpired` instead of killing or reporting failure.
Output: TDD tests and implementation in the termination coordinator that keep active cleaning semantics separate from pending force-escalation ownership.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-stop-escalation-ownership-closure/15-SPEC.md
@.planning/phases/15-stop-escalation-ownership-closure/15-CONTEXT.md
@.planning/phases/15-stop-escalation-ownership-closure/15-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md

<interfaces>
From `AutoQAC/Services/Cleaning/ICleaningTerminationCoordinator.cs`:
```csharp
void AttachProcess(System.Diagnostics.Process process);
void DetachProcess();
Task<StopCleaningResult> StopAsync();
Task<StopCleaningResult> ForceStopAsync();
StopCleaningResult MarkLeftRunningByUser();
void ResetForNewSession();
bool IsStopRequested { get; }
TerminationResult? LastTerminationResult { get; }
bool HasActiveProcess { get; }
bool ProcessMayStillBeRunning { get; }
IObservable<bool> HangDetected { get; }
```

From `AutoQAC/Services/Cleaning/CleaningTerminationCoordinator.cs`:
```csharp
private System.Diagnostics.Process? _currentProcess;
private TerminationResult? _lastTerminationResult;
public bool HasActiveProcess => _currentProcess is not null;
public bool ProcessMayStillBeRunning => MayProcessStillBeRunning(_lastTerminationResult);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED coordinator tests for detached pending force target</name>
  <files>AutoQAC.Tests/Services/Cleaning/CleaningTerminationCoordinatorTests.cs</files>
  <read_first>
    - AutoQAC.Tests/Services/Cleaning/CleaningTerminationCoordinatorTests.cs
    - AutoQAC/Services/Cleaning/CleaningTerminationCoordinator.cs
    - AutoQAC/Services/Cleaning/ICleaningTerminationCoordinator.cs
    - .planning/phases/15-stop-escalation-ownership-closure/15-SPEC.md
    - .planning/phases/15-stop-escalation-ownership-closure/15-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `ForceStopAsync_AfterGracePeriodExpiredAndDetach_ForceKillsPendingProcess` drives `AttachProcess(process)`, mocked graceful `TerminateProcessAsync(... forceKill: false ...) => GracePeriodExpired`, `StopAsync()`, `DetachProcess()`, then mocked force `TerminateProcessAsync(... forceKill: true ...) => ForceKilled`; result must be `ForceKilled`, `MayStillBeRunning == false`, and the force call must receive the original process.
    - Test 2: `ForceStopAsync_AfterGracePeriodExpiredAndDetach_KeepsHasActiveProcessFalse` proves `DetachProcess()` clears active cleaning semantics while the pending force target remains available: after detach, `HasActiveProcess` is false, then `ForceStopAsync()` still force-kills the original process.
    - Test 3: `ForceStopAsync_AfterGracePeriodExpiredAndDetachedProcessAlreadyExited_ReturnsAlreadyExited` uses a short-lived process after `GracePeriodExpired` and detach; force stop must return `AlreadyExited` and must not call force termination.
    - Test 4: `ForceStopAsync_AfterGracePeriodExpiredAndUnavailablePendingTarget_ReturnsForceKillFailed` simulates retained-target force termination returning `ForceKillFailed` or throwing a process-state exception after confirmation; result must be `ForceKillFailed`, not `GracePeriodExpired`.
  </behavior>
  <action>Add the failing tests first. Use existing `StartSleeperProcess()` / `StartShortLivedProcess()` helpers, `TaskCreationOptions.RunContinuationsAsynchronously` where needed, finite waits, and explicit NSubstitute optional-parameter matches. Reference D-01, D-03, D-06, D-07, D-08, D-10, D-11, and D-13 in comments or assertion because-clauses where they clarify the behavior.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~CleaningTerminationCoordinatorTests --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `CleaningTerminationCoordinatorTests.cs` contains `ForceStopAsync_AfterGracePeriodExpiredAndDetach_ForceKillsPendingProcess`.
    - `CleaningTerminationCoordinatorTests.cs` contains `ForceStopAsync_AfterGracePeriodExpiredAndDetach_KeepsHasActiveProcessFalse`.
    - `CleaningTerminationCoordinatorTests.cs` contains `ForceStopAsync_AfterGracePeriodExpiredAndDetachedProcessAlreadyExited_ReturnsAlreadyExited`.
    - `CleaningTerminationCoordinatorTests.cs` contains `ForceStopAsync_AfterGracePeriodExpiredAndUnavailablePendingTarget_ReturnsForceKillFailed`.
    - The targeted command fails before production changes because existing `ForceStopAsync()` returns cached `GracePeriodExpired` after detach.
  </acceptance_criteria>
  <done>The detached confirmed force-escalation tests exist and fail for the current ownership gap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN retained pending target implementation</name>
  <files>AutoQAC/Services/Cleaning/CleaningTerminationCoordinator.cs, AutoQAC/Services/Cleaning/ICleaningTerminationCoordinator.cs, AutoQAC.Tests/Services/Cleaning/CleaningTerminationCoordinatorTests.cs</files>
  <read_first>
    - AutoQAC/Services/Cleaning/CleaningTerminationCoordinator.cs
    - AutoQAC/Services/Cleaning/ICleaningTerminationCoordinator.cs
    - AutoQAC.Tests/Services/Cleaning/CleaningTerminationCoordinatorTests.cs
    - .planning/phases/15-stop-escalation-ownership-closure/15-RESEARCH.md
  </read_first>
  <action>Implement a separate pending force-escalation target per D-01 through D-04. Add a private `System.Diagnostics.Process?` field for the pending escalation target. When `StopAsync()` stores `TerminationResult.GracePeriodExpired`, also retain the current process into that pending field. Keep `DetachProcess()` focused on active cleaning cleanup: dispose hang monitoring, emit `false`, and clear `_currentProcess`, but do not clear an unresolved pending target. Update `ForceStopAsync()` so a confirmed force request chooses `_currentProcess` first, then the pending escalation target when `_lastTerminationResult` may still be running. If the chosen process has already exited, set `_lastTerminationResult = TerminationResult.AlreadyExited`, release the pending target, and return `AlreadyExited`. If force termination succeeds or returns `ForceKillFailed`, store that terminal result, release the pending target for `ForceKilled`/`AlreadyExited`, and return it. If process APIs throw when checking or terminating a pending target after confirmation and exit cannot be proven, store and return `ForceKillFailed` per D-08/D-09. Add or update XML docs on `DetachProcess()`, `ForceStopAsync()`, and `ResetForNewSession()` so they state the pending target is separate from active cleaning semantics and confirmed force escalation must not return cached `GracePeriodExpired`.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~CleaningTerminationCoordinatorTests --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `CleaningTerminationCoordinator.cs` contains a private pending force-escalation `Process` field distinct from `_currentProcess`.
    - `DetachProcess()` still assigns `_currentProcess = null`.
    - `ForceStopAsync()` contains logic that maps confirmed unavailable/unprovable pending target paths to `TerminationResult.ForceKillFailed`.
    - `ForceStopAsync()` no longer returns `new StopCleaningResult(_lastTerminationResult, MayProcessStillBeRunning(_lastTerminationResult))` for the confirmed pending-target path when `_lastTerminationResult` is `GracePeriodExpired`.
    - Targeted coordinator tests pass.
  </acceptance_criteria>
  <done>Coordinator force-stop behavior returns `ForceKilled`, `AlreadyExited`, or `ForceKillFailed` after confirmed detached escalation and preserves active-cleaning semantics.</done>
</task>

</tasks>

<verification>
Run `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~CleaningTerminationCoordinatorTests --nologo` and confirm all coordinator tests pass.
</verification>

<success_criteria>
- Detached confirmed force escalation no longer silently returns cached `GracePeriodExpired`.
- `HasActiveProcess` remains false after detach even when a pending force target exists.
- Coordinator tests prove terminal outcomes for force-killed, already-exited, and force-failed retained targets.
</success_criteria>

<output>
After completion, create `.planning/phases/15-stop-escalation-ownership-closure/15-01-SUMMARY.md`.
</output>
