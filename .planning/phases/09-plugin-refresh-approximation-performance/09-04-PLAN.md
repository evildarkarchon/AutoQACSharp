---
phase: 09-plugin-refresh-approximation-performance
plan: 04
type: execute
wave: 3
depends_on: [09-03]
files_modified:
  - AutoQAC/ViewModels/MainWindow/PluginListViewModel.cs
  - AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs
  - AutoQAC/ViewModels/MainWindowViewModel.cs
  - AutoQAC/Views/MainWindow.axaml
  - AutoQAC.Tests/ViewModels/PluginListViewModelTests.cs
autonomous: true
requirements: [PERF-01, REF-02]
must_haves:
  truths:
    - "User can refresh checked visible plugin rows from a button near the plugin list controls."
    - "Refresh selected is disabled until a stable plugin list is loaded, approximation is supported, not cleaning, and at least one visible row is checked."
    - "User can cancel an active approximation refresh without a confirmation dialog."
  artifacts:
    - path: "AutoQAC/ViewModels/MainWindow/PluginListViewModel.cs"
      provides: "Refresh selected and cancel refresh commands"
      contains: "RefreshSelectedApproximationsCommand"
    - path: "AutoQAC/Views/MainWindow.axaml"
      provides: "Plugin-list toolbar UI for refresh/cancel actions"
      contains: "Refresh selected approximations"
    - path: "AutoQAC.Tests/ViewModels/PluginListViewModelTests.cs"
      provides: "Command gating and snapshot tests"
      contains: "RefreshSelectedApproximationsCommand"
  key_links:
    - from: "PluginListViewModel"
      to: "IPluginRefreshCoordinator"
      via: "selected target snapshot request"
      pattern: "RefreshSelectedApproximationsAsync"
    - from: "MainWindow.axaml"
      to: "PluginListViewModel"
      via: "button command bindings"
      pattern: "PluginList.RefreshSelectedApproximationsCommand"
---

<objective>
Add the user-facing selected approximation refresh and cancel controls in the plugin list surface.

Purpose: Satisfies D-02, D-03, D-06, D-07, D-08, D-13, D-16, D-32, D-34, D-35, and the approved UI-SPEC without moving business workflow into the ViewModel.
Output: PluginList commands, XAML controls, and ViewModel tests.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-plugin-refresh-approximation-performance/09-CONTEXT.md
@.planning/phases/09-plugin-refresh-approximation-performance/09-UI-SPEC.md
@.planning/phases/09-plugin-refresh-approximation-performance/09-03-SUMMARY.md
@AutoQAC/ViewModels/MainWindow/PluginListViewModel.cs
@AutoQAC/Views/MainWindow.axaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add selected refresh command gating and target snapshot</name>
  <files>AutoQAC/ViewModels/MainWindow/PluginListViewModel.cs, AutoQAC/ViewModels/MainWindowViewModel.cs, AutoQAC.Tests/ViewModels/PluginListViewModelTests.cs</files>
  <read_first>AutoQAC/ViewModels/MainWindow/PluginListViewModel.cs, AutoQAC/ViewModels/MainWindowViewModel.cs, AutoQAC.Tests/ViewModels/PluginListViewModelTests.cs, AutoQAC/Services/Plugin/IPluginRefreshCoordinator.cs, AutoQAC/Services/Plugin/PluginRefreshRequest.cs</read_first>
  <behavior>
    - Command snapshots checked visible rows (`PluginListItem.IsSelected == true`) at invocation, not focused `SelectedPlugin`, per UI-SPEC and D-08.
    - Command disabled when no plugins, cleaning, no selected rows, unstable/unsupported approximation state, or unsupported current game, per D-07, D-34, D-35.
    - Command calls coordinator and does not analyze in the ViewModel, per D-25 and D-32.
  </behavior>
  <action>Inject `IPluginRefreshCoordinator` and `IPluginRefreshCapabilityPolicy` into `PluginListViewModel` through `MainWindowViewModel`. Add observable flags such as `CanRefreshApproximations`, `IsApproximationRefreshRunning`, and `CurrentGameType` derived from `AppState`/coordinator status. Implement `[RelayCommand(CanExecute = nameof(CanRefreshSelectedApproximations))] private async Task RefreshSelectedApproximationsAsync()` that builds an immutable `List<PluginRefreshTarget>` from checked visible rows at command start and calls `RefreshSelectedApproximationsAsync` on the coordinator. If invoked with zero targets despite CanExecute, call coordinator/status path to publish `Select plugins to refresh.` per D-07; do not silently do work. Add tests for command disabled with no selected rows, unsupported game, cleaning, and for snapshot not changing after toggling a row post-invocation.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~PluginListViewModel"</automated>
  </verify>
  <acceptance_criteria>
    - `PluginListViewModel.cs` contains `RefreshSelectedApproximationsCommand`, `CancelApproximationRefreshCommand`, and `CanRefreshSelectedApproximations`.
    - `PluginListViewModelTests.cs` contains tests named `RefreshSelectedApproximationsCommand_WhenNoRowsSelected_ShouldBeDisabled`, `RefreshSelectedApproximationsCommand_ShouldSnapshotCheckedVisibleRows`, and `RefreshSelectedApproximationsCommand_WhenUnsupportedGame_ShouldBeDisabled`.
    - `PluginListViewModel.cs` does not reference `PluginIssueApproximationService` directly.
  </acceptance_criteria>
  <done>Selected refresh/cancel commands are ViewModel intents over coordinator APIs, with behavior covered by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add plugin-list toolbar controls per UI-SPEC</name>
  <files>AutoQAC/Views/MainWindow.axaml</files>
  <read_first>AutoQAC/Views/MainWindow.axaml, .planning/phases/09-plugin-refresh-approximation-performance/09-UI-SPEC.md</read_first>
  <action>Update the plugin-list header grid from `ColumnDefinitions="*,Auto,Auto,Auto"` to include two additional Auto columns. Add a standard `Button` with `Content="Refresh selected approximations"`, `Command="{Binding PluginList.RefreshSelectedApproximationsCommand}"`, `Margin="5,5,2,5"`, `Padding="8,3"`, `FontSize="11"`, and `ToolTip.Tip="Refresh issue approximations for checked visible plugins"`. Add a sibling `Button` with `Content="Cancel refresh"`, `Command="{Binding PluginList.CancelApproximationRefreshCommand}"`, `IsVisible="{Binding PluginList.IsApproximationRefreshRunning}"`, neutral styling, and no confirmation behavior. Keep `All`, `None`, and `Manage Skip List` in the same header area, and do not move `Preview`, `Start Cleaning`, or `Stop`, per UI-SPEC placement.</action>
  <verify>
    <automated>dotnet build AutoQAC/AutoQAC.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `MainWindow.axaml` contains `Content="Refresh selected approximations"`.
    - `MainWindow.axaml` contains `Command="{Binding PluginList.RefreshSelectedApproximationsCommand}"`.
    - `MainWindow.axaml` contains `Content="Cancel refresh"` and `IsVisible="{Binding PluginList.IsApproximationRefreshRunning}"`.
    - `MainWindow.axaml` still contains `Command="{Binding Commands.StartCleaningCommand}"` in the bottom control button section.
  </acceptance_criteria>
  <done>Approved UI controls are present, keyboard-accessible standard buttons, and compile through Avalonia XAML build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User checkbox state → PluginListViewModel | User-selected visible rows become refresh targets. |
| XAML command binding → ViewModel | UI buttons trigger refresh/cancel service requests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | T | target snapshot | mitigate | Snapshot checked visible rows into immutable target list before awaiting coordinator call. |
| T-09-11 | D | refresh command gating | mitigate | Disable command during cleaning/unsupported/no-selection states and provide cancel affordance only while running. |
| T-09-12 | I | disabled/unsupported copy | mitigate | Use UI-SPEC neutral concise strings and tooltips; do not expose local paths in toolbar/status text. |
</threat_model>

<verification>
Run `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~PluginListViewModel"` and `dotnet build AutoQAC/AutoQAC.csproj`.
</verification>

<success_criteria>
Users have a visible, correctly-gated `Refresh selected approximations` action near plugin-list controls, and the ViewModel only delegates target snapshots to the coordinator.
</success_criteria>

<output>
After completion, create `.planning/phases/09-plugin-refresh-approximation-performance/09-04-SUMMARY.md`
</output>
