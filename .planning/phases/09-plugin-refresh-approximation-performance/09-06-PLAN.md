---
phase: 09-plugin-refresh-approximation-performance
plan: 06
type: execute
wave: 5
depends_on: [09-05]
files_modified:
  - AutoQAC/Services/Plugin/PluginRefreshCoordinator.cs
  - AutoQAC.Tests/Services/PluginRefreshCoordinatorTests.cs
autonomous: true
gap_closure: true
requirements: [REF-02, PERF-01]
must_haves:
  truths:
    - "Targeted approximation updates preserve non-targeted row values."
    - "Selected approximation refresh keeps the current visible plugin row set intact."
    - "Selected approximation refresh updates only selected target rows to pending/available/unavailable states."
  artifacts:
    - path: "AutoQAC/Services/Plugin/PluginRefreshCoordinator.cs"
      provides: "Non-destructive selected approximation refresh state publication"
      contains: "UpdateState"
    - path: "AutoQAC.Tests/Services/PluginRefreshCoordinatorTests.cs"
      provides: "Regression coverage for non-target row preservation during selected refresh"
      contains: "RefreshSelectedApproximationsAsync_ShouldPreserveNonSelectedPluginRows"
  key_links:
    - from: "PluginRefreshCoordinator.RefreshSelectedApproximationsAsync"
      to: "IStateService.CurrentState.PluginsToClean"
      via: "in-place UpdateState row mapping for selected targets"
      pattern: "PluginsToClean = rows\.AsReadOnly\(\)"
    - from: "PluginRefreshCoordinator.RefreshSelectedApproximationsAsync"
      to: "IStateService.MergePluginApproximation"
      via: "AnalyzeTargetsAsync callback merges completed selected target results"
      pattern: "MergePluginApproximation\(approximation\)"
---

<objective>
Close Phase 9 verification gaps for destructive selected approximation refresh.

Purpose: Selected refresh must be a targeted approximation update, not a plugin-list replacement. This preserves existing non-targeted row visibility and prior approximation values while still narrowing analysis work to the selected target snapshot per D-02, D-04, D-06, D-08, and D-27.
Output: A coordinator regression test and implementation/source guard ensuring selected refresh marks or merges only target rows and leaves non-targeted rows unchanged.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-plugin-refresh-approximation-performance/09-CONTEXT.md
@.planning/phases/09-plugin-refresh-approximation-performance/09-VERIFICATION.md
@.planning/phases/09-plugin-refresh-approximation-performance/09-03-SUMMARY.md
@.planning/phases/09-plugin-refresh-approximation-performance/09-05-SUMMARY.md
@AutoQAC/Services/Plugin/PluginRefreshCoordinator.cs
@AutoQAC.Tests/Services/PluginRefreshCoordinatorTests.cs
@AutoQAC/Services/State/StateService.cs

<interfaces>
<!-- Key contracts the executor must preserve. -->

From `AutoQAC/Services/State/IStateService.cs`:
```csharp
AppState CurrentState { get; }
void UpdateState(Func<AppState, AppState> updateFunc);
void SetPluginsToClean(List<PluginInfo> plugins);
void MergePluginApproximation(PluginIssueApproximationResult approximation);
void MergePluginApproximations(IReadOnlyList<PluginIssueApproximationResult> approximations);
```

From `AutoQAC/Services/State/StateService.cs`:
```csharp
public void SetPluginsToClean(List<PluginInfo> plugins)
{
    UpdateState(s => s with
    {
        PluginsToClean = new List<PluginInfo>(plugins).AsReadOnly(),
        ExcludedPluginPaths = pruned
    });
}

public void MergePluginApproximation(PluginIssueApproximationResult approximation)
{
    UpdateState(s => s with
    {
        PluginsToClean = merged.AsReadOnly()
    });
}
```

`SetPluginsToClean` is correct for full load-order replacement, but must not be used to replace the current list with selected-only rows during selected approximation refresh. Selected refresh should update existing rows in place and rely on `MergePluginApproximation` for completed selected target results.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock non-destructive selected refresh behavior</name>
  <files>AutoQAC.Tests/Services/PluginRefreshCoordinatorTests.cs, AutoQAC/Services/Plugin/PluginRefreshCoordinator.cs</files>
  <read_first>AutoQAC.Tests/Services/PluginRefreshCoordinatorTests.cs, AutoQAC/Services/Plugin/PluginRefreshCoordinator.cs, AutoQAC/Services/State/StateService.cs, .planning/phases/09-plugin-refresh-approximation-performance/09-VERIFICATION.md</read_first>
  <behavior>
    - Test 1: Preload `StateService` with `Selected.esp` and `Unselected.esp`, where `Unselected.esp` has `PluginIssueApproximation.Available(9, 8, 7)`; run `RefreshSelectedApproximationsAsync` with only `Selected.esp`; assert `PluginsToClean` still has exactly 2 rows.
    - Test 2: Assert `Selected.esp` becomes `PluginIssueApproximationStatus.Available` after selected refresh analysis.
    - Test 3: Assert `Unselected.esp` remains present and its `Approximation` equals the original `PluginIssueApproximation.Available(9, 8, 7)` value.
  </behavior>
  <action>Add or keep a coordinator-level regression test named `RefreshSelectedApproximationsAsync_ShouldPreserveNonSelectedPluginRows` with the exact behavior above. Then update `PluginRefreshCoordinator.RefreshSelectedApproximationsAsync` so selected refresh no longer replaces the whole plugin list with selected-only `pendingRows`. Specifically: build `targetPaths` and `targetNames` from the selected snapshot; call `_stateService.UpdateState` to map over `s.PluginsToClean`; for matching rows set `Approximation = PluginIssueApproximation.Pending`; for non-matching rows return the row unchanged; set `PluginsToClean = rows.AsReadOnly()`. If `s.PluginsToClean.Count == 0`, use `pendingRows.AsReadOnly()` as a compatibility fallback so selected refresh still publishes target rows when no current row list exists. Keep `AnalyzeTargetsAsync` unchanged so completed selected results flow through `_stateService.MergePluginApproximation(approximation)`. Do not call `_stateService.SetPluginsToClean(pendingRows)` in the non-empty selected-refresh path. This implements D-04 and D-27 and closes the verification gaps at `09-VERIFICATION.md` lines 8-34.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~PluginRefreshCoordinator"</automated>
  </verify>
  <acceptance_criteria>
    - `PluginRefreshCoordinatorTests.cs` contains `RefreshSelectedApproximationsAsync_ShouldPreserveNonSelectedPluginRows`.
    - `PluginRefreshCoordinatorTests.cs` contains `PluginIssueApproximation.Available(9, 8, 7)`.
    - `PluginRefreshCoordinatorTests.cs` asserts `PluginsToClean.Should().HaveCount(2` after selected refresh.
    - `PluginRefreshCoordinator.cs` selected refresh path contains `s.PluginsToClean.Select(plugin =>` and `plugin with { Approximation = PluginIssueApproximation.Pending }`.
    - `PluginRefreshCoordinator.cs` selected refresh path contains `PluginsToClean = rows.AsReadOnly()`.
    - `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~PluginRefreshCoordinator"` exits 0.
  </acceptance_criteria>
  <done>Selected approximation refresh preserves non-targeted rows and prior non-targeted approximation values while updating selected targets only.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the destructive selected-refresh gap is closed</name>
  <files>AutoQAC/Services/Plugin/PluginRefreshCoordinator.cs, AutoQAC.Tests/Services/PluginRefreshCoordinatorTests.cs</files>
  <read_first>AutoQAC/Services/Plugin/PluginRefreshCoordinator.cs, AutoQAC.Tests/Services/PluginRefreshCoordinatorTests.cs, .planning/phases/09-plugin-refresh-approximation-performance/09-VERIFICATION.md, .planning/phases/09-plugin-refresh-approximation-performance/09-05-SUMMARY.md</read_first>
  <action>Run a source guard and the relevant automated suites. The source guard must prove there is no selected-refresh list replacement with selected-only pending rows: search `PluginRefreshCoordinator.cs` for `_stateService.SetPluginsToClean(pendingRows)` after filtering comments out. If the filtered count is non-zero, remove that selected-refresh replacement and keep only the in-place `UpdateState` path from Task 1. Then run targeted coordinator tests, targeted plugin-list integration tests that exercise selected refresh command wiring, and the full solution. Do not change `StateService.SetPluginsToClean` or `MergePluginApproximations`; the gap is in coordinator selected-refresh semantics, not the state service full-list replacement behavior.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$matches = Get-Content 'AutoQAC/Services/Plugin/PluginRefreshCoordinator.cs' | Where-Object { $_ -notmatch '^\s*//' } | Select-String '_stateService\.SetPluginsToClean\(pendingRows\)'; if ($matches) { $matches | ForEach-Object { $_.Line }; exit 1 }"</automated>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~PluginRefreshCoordinator|FullyQualifiedName~PluginListViewModel"</automated>
    <automated>dotnet test AutoQACSharp.slnx</automated>
  </verify>
  <acceptance_criteria>
    - Filtered source guard for `_stateService.SetPluginsToClean(pendingRows)` exits 0.
    - `PluginRefreshCoordinator.cs` still contains `MergePluginApproximation(approximation)` in the approximation callback path.
    - `PluginRefreshCoordinator.cs` does not modify `MergePluginApproximations` or `StateService.SetPluginsToClean` semantics.
    - Targeted coordinator and plugin-list tests exit 0.
    - `dotnet test AutoQACSharp.slnx` exits 0.
  </acceptance_criteria>
  <done>The verification blocker no longer reproduces: selected refresh is narrow but non-destructive, and the full solution remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PluginListViewModel → PluginRefreshCoordinator | User-selected target snapshot crosses into service-owned background approximation workflow. |
| PluginRefreshCoordinator → IStateService | Background refresh writes plugin rows and approximation results into UI-observed shared state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-16 | T | `RefreshSelectedApproximationsAsync` | mitigate | Map target rows in place through `UpdateState`; do not replace the current list with selected-only `pendingRows`. |
| T-09-17 | D | selected approximation refresh | mitigate | Keep narrower selected-target analysis but preserve non-targeted rows so the visible plugin list remains stable during background work. |
| T-09-18 | I | refresh status/errors | accept | This gap fix adds no new user-facing error text or path disclosure; existing typed status/logging behavior remains unchanged. |
</threat_model>

<verification>
Run the source guard, targeted AutoQAC tests, and `dotnet test AutoQACSharp.slnx`. Then re-run `/gsd-verify-work 9` to regenerate `09-VERIFICATION.md` and confirm the 16/19 score advances to passed.
</verification>

<success_criteria>
All three verification gaps in `09-VERIFICATION.md` are closed: targeted approximation updates preserve non-targeted row values, selected refresh keeps the visible plugin list consistent, and selected-scope refresh remains narrow without destructive row replacement.
</success_criteria>

<output>
After completion, create `.planning/phases/09-plugin-refresh-approximation-performance/09-06-SUMMARY.md`
</output>
