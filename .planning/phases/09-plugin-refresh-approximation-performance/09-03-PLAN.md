---
phase: 09-plugin-refresh-approximation-performance
plan: 03
type: execute
wave: 2
depends_on: [09-02]
files_modified:
  - AutoQAC/Services/Plugin/PluginRefreshCoordinator.cs
  - AutoQAC/Services/Plugin/PluginRefreshCapabilityPolicy.cs
  - AutoQAC/Infrastructure/ServiceCollectionExtensions.cs
  - AutoQAC/ViewModels/MainWindow/ConfigurationViewModel.cs
  - AutoQAC/ViewModels/MainWindowViewModel.cs
  - AutoQAC.Tests/Services/PluginRefreshCoordinatorTests.cs
  - AutoQAC.Tests/Integration/DependencyInjectionTests.cs
autonomous: true
requirements: [REF-02, PERF-01]
must_haves:
  truths:
    - "Plugin rows load and display before background approximation starts."
    - "Plugin loading, skip-list application, generation/CTS ownership, and approximation refresh no longer live in ConfigurationViewModel."
    - "New refresh requests cancel/replace older refresh work and stale results do not alter current rows."
  artifacts:
    - path: "AutoQAC/Services/Plugin/PluginRefreshCoordinator.cs"
      provides: "Service-owned plugin refresh workflow"
      contains: "sealed class PluginRefreshCoordinator"
    - path: "AutoQAC/Services/Plugin/PluginRefreshCapabilityPolicy.cs"
      provides: "Refresh-scoped game loading/approximation capability policy"
      contains: "SupportsIssueApproximation"
    - path: "AutoQAC/ViewModels/MainWindow/ConfigurationViewModel.cs"
      provides: "UI shell delegating refresh workflow to coordinator"
      contains: "_pluginRefreshCoordinator"
  key_links:
    - from: "ConfigurationViewModel"
      to: "IPluginRefreshCoordinator"
      via: "game/data-folder/load-order refresh requests"
      pattern: "RefreshForGameAsync"
    - from: "PluginRefreshCoordinator"
      to: "IStateService"
      via: "SetPluginsToClean and MergePluginApproximation"
      pattern: "MergePluginApproximation"
---

<objective>
Implement the plugin refresh coordinator and move the game/data-folder/load-order refresh workflow out of `ConfigurationViewModel`.

Purpose: Satisfies REF-02 and the workflow ownership decisions D-25 through D-31 while preserving row-first/stale-result behavior from existing code.
Output: DI-registered refresh coordinator, refresh-scoped capability policy, and a thinner ConfigurationViewModel.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-plugin-refresh-approximation-performance/09-CONTEXT.md
@.planning/phases/09-plugin-refresh-approximation-performance/09-RESEARCH.md
@.planning/phases/09-plugin-refresh-approximation-performance/09-02-SUMMARY.md
@AutoQAC/ViewModels/MainWindow/ConfigurationViewModel.cs
@AutoQAC/Services/Plugin/PluginLoadingService.cs
@AutoQAC/Services/State/StateService.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement coordinator and capability policy</name>
  <files>AutoQAC/Services/Plugin/PluginRefreshCoordinator.cs, AutoQAC/Services/Plugin/PluginRefreshCapabilityPolicy.cs, AutoQAC.Tests/Services/PluginRefreshCoordinatorTests.cs</files>
  <read_first>AutoQAC/Services/Plugin/IPluginRefreshCoordinator.cs, AutoQAC/Services/Plugin/PluginRefreshRequest.cs, AutoQAC/Services/Plugin/PluginRefreshStatus.cs, AutoQAC/Services/Plugin/IPluginRefreshCapabilityPolicy.cs, AutoQAC/Services/Plugin/PluginLoadingService.cs, AutoQAC/Services/Plugin/PluginIssueApproximationService.cs, AutoQAC/Services/State/StateService.cs, AutoQAC.Tests/Services/PluginRefreshCoordinatorTests.cs</read_first>
  <behavior>
    - `RefreshForGameAsync` handles Unknown by clearing rows and publishing `No game selected`, per D-01 and D-25.
    - Mutagen-supported games set rows with Pending approximation, then analyze only selected visible rows when supplied; file-based games set Unavailable rows unless approximation support exists, per D-01, D-02, D-04, D-05, D-35.
    - Coordinator owns one active generation/CTS; `CancelActiveRefresh(Manual)` publishes `Approximation refresh canceled.`, while supersede and cleaning-start cancel reasons are silent, per D-09, D-10, D-13, D-14, D-15, D-26, D-33, D-36.
    - Per-plugin failures call `MergePluginApproximation` with Unavailable for that plugin only and log details, per D-12 and D-21.
  </behavior>
  <action>Implement `PluginRefreshCoordinator` as a singleton-safe service using `Interlocked.Increment` for generation IDs and `Interlocked.Exchange`/`CompareExchange` for CTS replacement/disposal. Port the existing `ConfigurationViewModel.ApplySkipListStatus`, data-folder/load-order resolution, Mutagen/file loading, and stale-result checks into the service. Add a small `PluginRefreshCapabilityPolicy` that delegates loading support to `IPluginLoadingService.IsGameSupportedByMutagen` and returns issue-approximation support only for SkyrimLe, SkyrimSe, SkyrimVr, Fallout4, Fallout4Vr, per D-28 and D-35. Preserve comments explaining non-obvious CTS race handling and add XML docs on public methods.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~PluginRefreshCoordinator"</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Services/Plugin/PluginRefreshCoordinator.cs` contains `Interlocked.Increment`, `Interlocked.Exchange`, and `Interlocked.CompareExchange`.
    - `PluginRefreshCoordinator.cs` contains `SetPluginsToClean` before any selected approximation analysis call in `RefreshForGameAsync`.
    - `PluginRefreshCoordinator.cs` contains `MergePluginApproximation` and does not call `MergePluginApproximations` for selected refresh completion.
    - `PluginRefreshCapabilityPolicy.cs` contains `SupportsIssueApproximation` with Skyrim/Fallout4 families only.
  </acceptance_criteria>
  <done>Coordinator tests from Plan 09-02 pass and workflow behavior is service-owned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register coordinator and make ConfigurationViewModel a refresh shell</name>
  <files>AutoQAC/Infrastructure/ServiceCollectionExtensions.cs, AutoQAC/ViewModels/MainWindow/ConfigurationViewModel.cs, AutoQAC/ViewModels/MainWindowViewModel.cs, AutoQAC.Tests/Integration/DependencyInjectionTests.cs</files>
  <read_first>AutoQAC/Infrastructure/ServiceCollectionExtensions.cs, AutoQAC/ViewModels/MainWindow/ConfigurationViewModel.cs, AutoQAC/ViewModels/MainWindowViewModel.cs, AutoQAC.Tests/Integration/DependencyInjectionTests.cs</read_first>
  <action>Register `IPluginRefreshCapabilityPolicy` and `IPluginRefreshCoordinator` in `AddBusinessLogic`. Replace `ConfigurationViewModel` fields `_pluginIssueApproximationService`, `_pluginLoadingService` workflow use, `_pluginRefreshCts`, `_pluginApproximationCts`, and `_pluginRefreshGeneration` with an injected `IPluginRefreshCoordinator`. Keep path validation, file dialogs, selected-game property changes, reset/settings actions, and status string mapping in the ViewModel. Move `ConfigureLoadOrderAsync` after file validation to call the coordinator path so manual load-order refresh shares service behavior, per D-29. Subscribe to coordinator `StatusChanged` with `CallbackObserver<PluginRefreshStatus>` and map typed status to exact UI-SPEC strings in `StatusText`, per D-31. Dispose the subscription and call `CancelActiveRefresh(PluginRefreshCancelReason.ViewModelDisposed)` on disposal/reset, per D-36.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~PluginRefreshCoordinator|FullyQualifiedName~DependencyInjection"</automated>
  </verify>
  <acceptance_criteria>
    - `ServiceCollectionExtensions.cs` contains `AddSingleton<IPluginRefreshCoordinator, PluginRefreshCoordinator>()` and `AddSingleton<IPluginRefreshCapabilityPolicy, PluginRefreshCapabilityPolicy>()`.
    - `ConfigurationViewModel.cs` contains `_pluginRefreshCoordinator` and does not contain `StartApproximationRefresh`, `RunApproximationRefreshAsync`, `_pluginApproximationCts`, or `_pluginRefreshGeneration`.
    - `ConfigurationViewModel.cs` maps `PluginRefreshStatusKind.SelectPlugins` to `Select plugins to refresh.` and `PluginRefreshStatusKind.Canceled` to `Approximation refresh canceled.`.
    - `DependencyInjectionTests.cs` asserts `IPluginRefreshCoordinator` and `IPluginRefreshCapabilityPolicy` resolve.
  </acceptance_criteria>
  <done>`ConfigurationViewModel` is a UI shell for path/dialog/status concerns and no longer owns plugin refresh workflow decisions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ConfigurationViewModel → PluginRefreshCoordinator | User-selected game/path/load-order inputs enter service workflow. |
| PluginRefreshCoordinator → filesystem/Mutagen | Service loads local plugin lists and analyzes local plugin data. |
| PluginRefreshCoordinator → IStateService | Background workflow updates visible plugin rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | T | `PluginRefreshCoordinator` stale callbacks | mitigate | Check generation and cancellation token before every `IStateService` write and after awaited calls. |
| T-09-08 | I | `ConfigurationViewModel` status mapping | mitigate | Map typed statuses to concise UI-SPEC strings; log technical details in services only. |
| T-09-09 | D | `PluginRefreshCoordinator` active CTS | mitigate | One active generation at a time with cancel/replace semantics; no queuing or concurrent refresh generations. |
</threat_model>

<verification>
Run `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~PluginRefreshCoordinator|FullyQualifiedName~DependencyInjection"`.
</verification>

<success_criteria>
REF-02 is materially satisfied for configuration refresh: plugin loading/approximation workflow lives in services, not `ConfigurationViewModel`, while row-first and stale-result behavior remain covered by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/09-plugin-refresh-approximation-performance/09-03-SUMMARY.md`
</output>
