---
phase: 09-plugin-refresh-approximation-performance
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - QueryPlugins/IPluginQueryService.cs
  - QueryPlugins/PluginQueryService.cs
  - QueryPlugins/Detectors/IItmDetector.cs
  - QueryPlugins/Detectors/ItmDetector.cs
  - QueryPlugins.Tests/Detectors/ItmDetectorTests.cs
  - AutoQAC/Services/Plugin/PluginIssueApproximationService.cs
  - AutoQAC.Tests/Services/PluginIssueApproximationServiceTests.cs
autonomous: true
requirements: [PERF-02, PERF-01]
must_haves:
  truths:
    - "User can cancel large-plugin approximation work while ITM record/context loops are running."
    - "Analyzed plugin counts remain exact; incomplete canceled plugin counts are not published."
    - "ITM detection no longer materializes every override context into an array for each record."
  artifacts:
    - path: "QueryPlugins/Detectors/ItmDetector.cs"
      provides: "Streaming immediate-lower-priority ITM context traversal with cancellation polling"
      contains: "CancellationToken"
    - path: "QueryPlugins/PluginQueryService.cs"
      provides: "Cancellation-aware analysis orchestration"
      contains: "Analyse(IModGetter plugin, ILinkCache linkCache, GameRelease gameRelease, CancellationToken ct = default)"
    - path: "AutoQAC/Services/Plugin/PluginIssueApproximationService.cs"
      provides: "Cancellation token propagation into QueryPlugins analysis"
      contains: "_pluginQueryService.Analyse(target.Plugin, context.LinkCache, context.GameRelease, ct)"
  key_links:
    - from: "AutoQAC/Services/Plugin/PluginIssueApproximationService.cs"
      to: "QueryPlugins/PluginQueryService.cs"
      via: "passes ct into Analyse"
      pattern: "Analyse\(target\.Plugin, context\.LinkCache, context\.GameRelease, ct\)"
    - from: "QueryPlugins/Detectors/ItmDetector.cs"
      to: "Mutagen link cache contexts"
      via: "streaming ResolveAllSimpleContexts enumeration"
      pattern: "foreach .*ResolveAllSimpleContexts"
---

<objective>
Optimize the QueryPlugins ITM hot path so Phase 9 approximation work can be canceled inside record/context loops while preserving exact count semantics.

Purpose: Satisfies PERF-02 and the count-accuracy decisions D-17 through D-24 before the app-level refresh coordinator depends on cancellation-aware analysis.
Output: Cancellation-aware QueryPlugins analysis contracts, streaming ITM traversal, and regression tests.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-plugin-refresh-approximation-performance/09-CONTEXT.md
@.planning/phases/09-plugin-refresh-approximation-performance/09-RESEARCH.md
@QueryPlugins/Detectors/ItmDetector.cs
@QueryPlugins/PluginQueryService.cs
@AutoQAC/Services/Plugin/PluginIssueApproximationService.cs

<interfaces>
Current contracts to extend without removing existing semantics:

```csharp
public interface IPluginQueryService
{
    PluginAnalysisResult Analyse(IModGetter plugin, ILinkCache linkCache, GameRelease gameRelease);
}

public interface IItmDetector
{
    IEnumerable<PluginIssue> FindItmRecords(IModGetter plugin, ILinkCache linkCache);
}

public sealed class PluginIssueApproximationService : IPluginIssueApproximationService
{
    public Task<IReadOnlyList<PluginIssueApproximationResult>> GetApproximationsAsync(
        GameType gameType,
        string dataFolder,
        Action<PluginIssueApproximationResult>? onApproximationReady = null,
        CancellationToken ct = default);
}
```
</interfaces>
</context>

<feature>
  <name>Cancellation-aware streaming ITM analysis</name>
  <files>QueryPlugins/IPluginQueryService.cs, QueryPlugins/PluginQueryService.cs, QueryPlugins/Detectors/IItmDetector.cs, QueryPlugins/Detectors/ItmDetector.cs, QueryPlugins.Tests/Detectors/ItmDetectorTests.cs, AutoQAC/Services/Plugin/PluginIssueApproximationService.cs, AutoQAC.Tests/Services/PluginIssueApproximationServiceTests.cs</files>
  <behavior>
    - Cancellation token canceled before QueryPlugins analysis throws OperationCanceledException and does not invoke per-plugin approximation callback, per D-19 and D-23.
    - `ItmDetector.FindItmRecords(plugin, cache, ct)` polls `ct.ThrowIfCancellationRequested()` inside the major-record loop and inside context traversal, per D-23.
    - Streaming traversal compares the analyzed plugin context with only the immediate lower-priority context and preserves existing tests: middle override, winning override, modified override, deleted override, missing plugin.
    - `ItmDetector.cs` contains no `ResolveAllSimpleContexts(...).ToArray()` call, per D-24.
  </behavior>
  <implementation>
    Extend `IPluginQueryService.Analyse` and `IItmDetector.FindItmRecords` with optional `CancellationToken ct = default` last parameters, add XML docs for cancellation semantics, and update `PluginQueryService` to call `_itmDetector.FindItmRecords(plugin, linkCache, ct)`. In `ItmDetector`, replace `var allContexts = linkCache.ResolveAllSimpleContexts(formLinkInfo).ToArray()` with a streaming helper that tracks whether the plugin context has been seen, throws `ArgumentException` if a cache containing the plugin lacks that plugin context for an override, compares only the immediately next lower-priority context, and returns no issue when the plugin is the lowest-priority context. Keep exact-count behavior; do not add timeouts, caps, partial count modes, or Mutagen submodule changes, per D-17, D-18, D-20, D-21, and D-24.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cancellation and streaming regression tests</name>
  <files>QueryPlugins.Tests/Detectors/ItmDetectorTests.cs, AutoQAC.Tests/Services/PluginIssueApproximationServiceTests.cs</files>
  <read_first>QueryPlugins.Tests/Detectors/ItmDetectorTests.cs, AutoQAC.Tests/Services/PluginIssueApproximationServiceTests.cs, QueryPlugins/Detectors/ItmDetector.cs, AutoQAC/Services/Plugin/PluginIssueApproximationService.cs</read_first>
  <behavior>
    - Test `FindItmRecords_WithCanceledToken_ThrowsOperationCanceledException` using a canceled token.
    - Test `FindItmRecords_MiddleOverride_StreamingRewritePreservesImmediatePreviousComparison` using existing three-mod helper expectations.
    - Test `GetApproximationsAsync_WhenQueryAnalysisCancels_DoesNotPublishPartialResult` where substitute `IPluginQueryService.Analyse(..., ct)` throws OperationCanceledException and callback list stays empty.
  </behavior>
  <action>Write failing tests first for D-17, D-19, D-23, and D-24. Update NSubstitute setups to match optional cancellation parameters explicitly (`Arg.Any<CancellationToken>()`). Do not change production code in this task except for unavoidable compile stubs; if compile stubs are needed, make them throw NotImplementedException so RED remains meaningful.</action>
  <verify>
    <automated>dotnet test QueryPlugins.Tests/QueryPlugins.Tests.csproj --filter "FullyQualifiedName~ItmDetector"</automated>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~PluginIssueApproximationService"</automated>
  </verify>
  <acceptance_criteria>
    - `QueryPlugins.Tests/Detectors/ItmDetectorTests.cs` contains `FindItmRecords_WithCanceledToken_ThrowsOperationCanceledException`.
    - `AutoQAC.Tests/Services/PluginIssueApproximationServiceTests.cs` contains `GetApproximationsAsync_WhenQueryAnalysisCancels_DoesNotPublishPartialResult`.
    - The new tests fail before implementation because cancellation is not yet accepted or propagated.
  </acceptance_criteria>
  <done>Cancellation and streaming expectations are pinned in tests before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement cancellation-aware streaming analysis</name>
  <files>QueryPlugins/IPluginQueryService.cs, QueryPlugins/PluginQueryService.cs, QueryPlugins/Detectors/IItmDetector.cs, QueryPlugins/Detectors/ItmDetector.cs, AutoQAC/Services/Plugin/PluginIssueApproximationService.cs</files>
  <read_first>QueryPlugins/IPluginQueryService.cs, QueryPlugins/PluginQueryService.cs, QueryPlugins/Detectors/IItmDetector.cs, QueryPlugins/Detectors/ItmDetector.cs, AutoQAC/Services/Plugin/PluginIssueApproximationService.cs</read_first>
  <action>Implement the optional cancellation token signatures and pass the token through `PluginIssueApproximationService` into QueryPlugins. In `ItmDetector`, stream `linkCache.ResolveAllSimpleContexts(formLinkInfo)` with `ct.ThrowIfCancellationRequested()` before each record and each context check. Keep the existing comment explaining winner-first ordering, updated to explain why only the immediate lower-priority context is retained. Throw the same `ArgumentException` parameter name `linkCache` when the plugin is absent from cache or absent from a record context set. Do not publish partial results when OperationCanceledException is thrown, per D-19.</action>
  <verify>
    <automated>dotnet test QueryPlugins.Tests/QueryPlugins.Tests.csproj --filter "FullyQualifiedName~ItmDetector"</automated>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~PluginIssueApproximationService"</automated>
  </verify>
  <acceptance_criteria>
    - `QueryPlugins/Detectors/ItmDetector.cs` contains `CancellationToken ct = default` and at least two `ct.ThrowIfCancellationRequested()` calls.
    - `QueryPlugins/Detectors/ItmDetector.cs` does not contain `.ToArray()` on `ResolveAllSimpleContexts`.
    - `QueryPlugins/PluginQueryService.cs` contains `_itmDetector.FindItmRecords(plugin, linkCache, ct)`.
    - `AutoQAC/Services/Plugin/PluginIssueApproximationService.cs` contains `_pluginQueryService.Analyse(target.Plugin, context.LinkCache, context.GameRelease, ct)`.
  </acceptance_criteria>
  <done>All existing and new detector/approximation tests pass with exact-count streaming semantics and cancellation propagation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AutoQAC service → QueryPlugins | App-level cancellation and plugin analysis requests enter the standalone detector library. |
| QueryPlugins → Mutagen link cache | Detector traverses local plugin records and link-cache contexts from filesystem-derived load orders. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | D | `ItmDetector.FindItmRecords` | mitigate | Poll `CancellationToken.ThrowIfCancellationRequested()` in record and context loops so large plugins do not monopolize CPU after cancel. |
| T-09-02 | T | `PluginIssueApproximationService` | mitigate | Propagate OperationCanceledException and avoid adding/publishing partial plugin results when cancellation interrupts exact analysis. |
| T-09-03 | T | `ItmDetector` context ordering | mitigate | Preserve immediate-lower-priority comparison tests so streaming rewrite cannot compare against a non-adjacent master or later winner. |
</threat_model>

<verification>
Run `dotnet test QueryPlugins.Tests/QueryPlugins.Tests.csproj --filter "FullyQualifiedName~ItmDetector"` and `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~PluginIssueApproximationService"`.
</verification>

<success_criteria>
PERF-02 is satisfied at the detector layer: no per-record context arrays, exact counts preserved, and cancellation is observed inside hot loops.
</success_criteria>

<output>
After completion, create `.planning/phases/09-plugin-refresh-approximation-performance/09-01-SUMMARY.md`
</output>
