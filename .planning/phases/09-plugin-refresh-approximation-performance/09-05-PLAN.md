---
phase: 09-plugin-refresh-approximation-performance
plan: 05
type: execute
wave: 4
depends_on: [09-01, 09-04]
files_modified:
  - AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs
  - AutoQAC/ViewModels/MainWindowViewModel.cs
  - AutoQAC/ViewModels/MainWindow/ConfigurationViewModel.cs
  - AutoQAC/ViewModels/MainWindow/PluginListViewModel.cs
  - AutoQAC.Tests/ViewModels/PluginListViewModelTests.cs
  - AutoQAC.Tests/Integration/DependencyInjectionTests.cs
autonomous: true
requirements: [REF-02, PERF-01, PERF-02]
must_haves:
  truths:
    - "Starting cleaning cancels any active approximation refresh before xEdit cleaning begins."
    - "Settings reset, ViewModel disposal, or equivalent teardown cancels active refresh work and prevents stale state writes."
    - "Full solution tests pass after QueryPlugins, coordinator, ViewModel, and XAML integration."
  artifacts:
    - path: "AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs"
      provides: "Cleaning-start refresh cancellation integration"
      contains: "CancelActiveRefresh"
    - path: "AutoQAC/ViewModels/MainWindowViewModel.cs"
      provides: "Shared coordinator injection into child ViewModels"
      contains: "IPluginRefreshCoordinator"
    - path: "AutoQAC.Tests/Integration/DependencyInjectionTests.cs"
      provides: "End-to-end DI registration coverage"
      contains: "IPluginRefreshCoordinator"
  key_links:
    - from: "CleaningCommandsViewModel.StartCleaningAsync"
      to: "IPluginRefreshCoordinator.CancelActiveRefresh"
      via: "cleaning-start cancel reason before orchestrator.StartCleaningAsync"
      pattern: "CancelActiveRefresh\(PluginRefreshCancelReason.CleaningStarted\)"
---

<objective>
Complete cross-ViewModel refresh cancellation/lifecycle wiring and run final Phase 9 verification.

Purpose: Ensures background Mutagen/file I/O cannot compete with sequential xEdit cleaning and that all Phase 9 source decisions are connected in the application shell.
Output: Cleaning-start cancellation, lifecycle cancellation tests, DI assertions, and full solution verification.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-plugin-refresh-approximation-performance/09-CONTEXT.md
@.planning/phases/09-plugin-refresh-approximation-performance/09-01-SUMMARY.md
@.planning/phases/09-plugin-refresh-approximation-performance/09-03-SUMMARY.md
@.planning/phases/09-plugin-refresh-approximation-performance/09-04-SUMMARY.md
@AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs
@AutoQAC/ViewModels/MainWindowViewModel.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cancel refresh before cleaning and on lifecycle teardown</name>
  <files>AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs, AutoQAC/ViewModels/MainWindowViewModel.cs, AutoQAC/ViewModels/MainWindow/ConfigurationViewModel.cs, AutoQAC/ViewModels/MainWindow/PluginListViewModel.cs, AutoQAC.Tests/ViewModels/PluginListViewModelTests.cs</files>
  <read_first>AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs, AutoQAC/ViewModels/MainWindowViewModel.cs, AutoQAC/ViewModels/MainWindow/ConfigurationViewModel.cs, AutoQAC/ViewModels/MainWindow/PluginListViewModel.cs, AutoQAC.Tests/ViewModels/PluginListViewModelTests.cs, AutoQAC/Services/Plugin/IPluginRefreshCoordinator.cs</read_first>
  <behavior>
    - `StartCleaningAsync` calls `CancelActiveRefresh(PluginRefreshCancelReason.CleaningStarted)` before `_showProgressInteraction.Handle` and before `_orchestrator.StartCleaningAsync`, per D-15.
    - `ConfigurationViewModel.ResetSettingsAsync` and ViewModel disposal call coordinator cancellation with lifecycle/reset reasons and do not publish stale canceled status for superseded/cleaning-start reasons, per D-10, D-14, and D-36.
    - Cancel button calls coordinator manual cancel and leaves completed row results intact through coordinator behavior, per D-13 and D-14.
  </behavior>
  <action>Inject `IPluginRefreshCoordinator` into `CleaningCommandsViewModel` through `MainWindowViewModel`. At the top of `StartCleaningAsync`, after validation succeeds and before opening the progress interaction, call `_pluginRefreshCoordinator.CancelActiveRefresh(PluginRefreshCancelReason.CleaningStarted)`. Ensure reset/dispose paths call lifecycle cancellation exactly once per owning ViewModel; keep cancellation idempotent. Add/extend tests using NSubstitute to assert cancel is called with `CleaningStarted` before orchestrator start and that manual cancel command routes `PluginRefreshCancelReason.Manual`.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~PluginListViewModel|FullyQualifiedName~CleaningCommandsViewModel"</automated>
  </verify>
  <acceptance_criteria>
    - `CleaningCommandsViewModel.cs` contains `CancelActiveRefresh(PluginRefreshCancelReason.CleaningStarted)` before `StartCleaningAsync` calls `_orchestrator.StartCleaningAsync`.
    - `PluginListViewModel.cs` contains `CancelActiveRefresh(PluginRefreshCancelReason.Manual)` in the cancel command path.
    - `ConfigurationViewModel.cs` contains `CancelActiveRefresh(PluginRefreshCancelReason.Reset)` or an equivalent reset lifecycle reason during settings reset.
    - No code path starts xEdit cleaning before active approximation refresh cancellation is requested.
  </acceptance_criteria>
  <done>Approximation refresh cancellation is connected to cleaning start and lifecycle teardown.</done>
</task>

<task type="auto">
  <name>Task 2: Final integration verification and source guards</name>
  <files>AutoQAC.Tests/Integration/DependencyInjectionTests.cs, AutoQAC.Tests/ViewModels/PluginListViewModelTests.cs</files>
  <read_first>AutoQAC.Tests/Integration/DependencyInjectionTests.cs, AutoQAC.Tests/ViewModels/PluginListViewModelTests.cs, .planning/phases/09-plugin-refresh-approximation-performance/09-VALIDATION.md</read_first>
  <action>Add final assertions that the DI provider resolves `MainWindowViewModel` with one shared `IPluginRefreshCoordinator` service and that selected refresh tests exercise checked visible rows rather than `SelectedPlugin` focus. Add a source-level guard test if no behavioral test can prove `CleaningCommandsViewModel` cancels refresh before orchestrator start; if using a grep/source guard, filter comments out before counting tokens to avoid self-invalidating grep gates. Run targeted tests and then the full solution. Do not add Avalonia.Headless infrastructure in this phase.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~DependencyInjection|FullyQualifiedName~PluginListViewModel"</automated>
    <automated>dotnet test AutoQACSharp.slnx</automated>
  </verify>
  <acceptance_criteria>
    - `DependencyInjectionTests.cs` asserts `provider.GetService<IPluginRefreshCoordinator>().Should().NotBeNull()`.
    - `DependencyInjectionTests.cs` asserts `provider.GetService<IPluginRefreshCapabilityPolicy>().Should().NotBeNull()`.
    - `PluginListViewModelTests.cs` contains an assertion that changing `SelectedPlugin` alone does not define refresh targets.
    - `dotnet test AutoQACSharp.slnx` exits 0.
  </acceptance_criteria>
  <done>Phase 9 integration is verified across AutoQAC and QueryPlugins with full solution tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CleaningCommandsViewModel → coordinator/orchestrator | User starts xEdit cleaning while background approximation may be active. |
| ViewModel disposal/reset → coordinator | UI lifecycle events stop background work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-13 | D | `CleaningCommandsViewModel.StartCleaningAsync` | mitigate | Cancel active approximation refresh before showing progress and before `ICleaningOrchestrator.StartCleaningAsync`. |
| T-09-14 | T | ViewModel lifecycle callbacks | mitigate | Reset/dispose cancellation prevents stale callbacks from writing rows after UI state changes. |
| T-09-15 | I | final status/errors | mitigate | Use coordinator typed statuses and existing logging; do not add raw path/exception text to user-facing refresh statuses. |
</threat_model>

<verification>
Run targeted AutoQAC tests and `dotnet test AutoQACSharp.slnx`.
</verification>

<success_criteria>
All Phase 9 requirements pass automated verification, cleaning start cancels active approximation refresh, and no background refresh state can outlive reset/disposal lifecycle boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/09-plugin-refresh-approximation-performance/09-05-SUMMARY.md`
</output>
