---
phase: 09-plugin-refresh-approximation-performance
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - AutoQAC/Services/Plugin/IPluginRefreshCoordinator.cs
  - AutoQAC/Services/Plugin/PluginRefreshRequest.cs
  - AutoQAC/Services/Plugin/PluginRefreshStatus.cs
  - AutoQAC/Services/Plugin/IPluginRefreshCapabilityPolicy.cs
  - AutoQAC.Tests/Services/PluginRefreshCoordinatorTests.cs
  - AutoQAC.Tests/Services/StateServiceTests.cs
autonomous: true
requirements: [REF-02, PERF-01]
must_haves:
  truths:
    - "Maintainer has concrete refresh contracts to implement outside ConfigurationViewModel."
    - "Selected refresh target sets are snapshots and do not mutate when row selection changes mid-refresh."
    - "Targeted approximation updates preserve non-targeted row values."
  artifacts:
    - path: "AutoQAC/Services/Plugin/IPluginRefreshCoordinator.cs"
      provides: "Refresh/cancel coordinator contract"
      exports: ["IPluginRefreshCoordinator"]
    - path: "AutoQAC/Services/Plugin/PluginRefreshRequest.cs"
      provides: "Typed refresh request and target DTOs"
      exports: ["PluginRefreshRequest", "PluginRefreshTarget"]
    - path: "AutoQAC/Services/Plugin/PluginRefreshStatus.cs"
      provides: "Typed status/progress outcomes for ViewModel mapping"
      exports: ["PluginRefreshStatus", "PluginRefreshStatusKind"]
    - path: "AutoQAC.Tests/Services/PluginRefreshCoordinatorTests.cs"
      provides: "Wave 0 executable coordinator behavior specification"
      contains: "PluginRefreshCoordinator"
  key_links:
    - from: "IPluginRefreshCoordinator"
      to: "IStateService"
      via: "contract requires coordinator to publish rows/results through state hub"
      pattern: "IStateService"
---

<objective>
Define and test the Phase 9 plugin refresh coordinator contract before implementing the service extraction.

Purpose: Prevents downstream executors from inventing incompatible refresh APIs and pins D-01 through D-16 and D-25 through D-36 in executable tests.
Output: Service contracts, typed request/status DTOs, refresh-scoped capability policy contract, and Wave 0 tests.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-plugin-refresh-approximation-performance/09-CONTEXT.md
@.planning/phases/09-plugin-refresh-approximation-performance/09-RESEARCH.md
@AutoQAC/Services/State/IStateService.cs
@AutoQAC/Services/State/StateService.cs
@AutoQAC/Models/PluginInfo.cs

<interfaces>
New contracts this plan must create for later plans:

```csharp
public interface IPluginRefreshCoordinator
{
    IObservable<PluginRefreshStatus> StatusChanged { get; }
    Task RefreshForGameAsync(PluginRefreshRequest request, CancellationToken ct = default);
    Task RefreshSelectedApproximationsAsync(PluginRefreshRequest request, IReadOnlyList<PluginRefreshTarget> selectedTargets, CancellationToken ct = default);
    void CancelActiveRefresh(PluginRefreshCancelReason reason);
}

public interface IPluginRefreshCapabilityPolicy
{
    bool SupportsPluginLoading(GameType gameType);
    bool SupportsIssueApproximation(GameType gameType);
    bool RequiresLoadOrderFile(GameType gameType);
}
```
</interfaces>
</context>

<feature>
  <name>Refresh coordinator contract and Wave 0 tests</name>
  <files>AutoQAC/Services/Plugin/IPluginRefreshCoordinator.cs, AutoQAC/Services/Plugin/PluginRefreshRequest.cs, AutoQAC/Services/Plugin/PluginRefreshStatus.cs, AutoQAC/Services/Plugin/IPluginRefreshCapabilityPolicy.cs, AutoQAC.Tests/Services/PluginRefreshCoordinatorTests.cs, AutoQAC.Tests/Services/StateServiceTests.cs</files>
  <behavior>
    - Row-first refresh publishes plugin rows before approximation analysis begins, per D-01.
    - Selected refresh uses checked visible target snapshots, does not idle-fill deselected rows, and does nothing clearly when empty, per D-02, D-06, D-07, and UI-SPEC selection contract.
    - New refresh generations cancel/replace old generations and stale callbacks cannot write state, per D-08, D-09, D-10, D-26, D-33, and D-36.
    - Per-plugin analysis failure marks only that target Unavailable and logs details, per D-12 and D-21.
  </behavior>
  <implementation>Create compileable contracts and test scaffolding only; production implementation belongs to Plan 09-03. Use exact DTO/status names from the interface sketch unless implementation requires a tighter name. Include XML docs on public contracts because these are executor-facing service APIs.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create refresh contracts and coordinator behavior tests</name>
  <files>AutoQAC/Services/Plugin/IPluginRefreshCoordinator.cs, AutoQAC/Services/Plugin/PluginRefreshRequest.cs, AutoQAC/Services/Plugin/PluginRefreshStatus.cs, AutoQAC/Services/Plugin/IPluginRefreshCapabilityPolicy.cs, AutoQAC.Tests/Services/PluginRefreshCoordinatorTests.cs</files>
  <read_first>AutoQAC/Services/Plugin/IPluginLoadingService.cs, AutoQAC/Services/Plugin/IPluginIssueApproximationService.cs, AutoQAC/Models/PluginInfo.cs, AutoQAC.Tests/Services/PluginIssueApproximationServiceTests.cs</read_first>
  <action>Create the service interfaces/records listed in `<interfaces>`. Add tests with the exact names `RefreshForGameAsync_PublishesRowsBeforeApproximationResults`, `RefreshSelectedApproximationsAsync_UsesSnapshotAndDoesNotAnalyzeDeselectedRows`, `RefreshSelectedApproximationsAsync_WhenNoTargets_PublishesSelectPluginsStatus`, `RefreshForGameAsync_WhenSuperseded_DoesNotPublishStaleApproximation`, and `CancelActiveRefresh_ManualCancelKeepsCompletedResultsAndPublishesCanceledStatus`. Tests may reference a not-yet-created `PluginRefreshCoordinator` and fail to compile/run until Plan 09-03, which is the intended Wave 0 contract.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~PluginRefreshCoordinator"</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Services/Plugin/IPluginRefreshCoordinator.cs` contains `RefreshForGameAsync`, `RefreshSelectedApproximationsAsync`, `CancelActiveRefresh`, and `StatusChanged`.
    - `AutoQAC/Services/Plugin/PluginRefreshStatus.cs` contains string-producing status data for `Analyzing {current} of {total} selected plugins.`, `Updated {count} selected plugin approximations.`, `Select plugins to refresh.`, `Approximation refresh canceled.`, and `Approximation refresh is not available for this game.` mapping inputs.
    - `AutoQAC.Tests/Services/PluginRefreshCoordinatorTests.cs` contains all five test names listed in the action.
  </acceptance_criteria>
  <done>Downstream plans have fixed coordinator contracts and RED tests for row-first, targeting, generation, and cancellation behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin targeted merge preservation in StateService tests</name>
  <files>AutoQAC.Tests/Services/StateServiceTests.cs</files>
  <read_first>AutoQAC.Tests/Services/StateServiceTests.cs, AutoQAC/Services/State/StateService.cs, AutoQAC/Services/State/IStateService.cs</read_first>
  <action>Add `MergePluginApproximation_ShouldPreserveNonTargetedApproximationValues` proving that a single-row merge updates only the matching plugin and preserves an existing Available approximation on a non-targeted plugin, per D-04 and research Pitfall 1. Do not change `MergePluginApproximations` behavior; targeted refresh must use `MergePluginApproximation` instead.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~StateService"</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC.Tests/Services/StateServiceTests.cs` contains `MergePluginApproximation_ShouldPreserveNonTargetedApproximationValues`.
    - The test asserts target row count changes and non-targeted row count stays unchanged.
    - No production code calls `MergePluginApproximations` for selected-target completion in this plan.
  </acceptance_criteria>
  <done>State merge behavior required by selected refresh is covered by automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ViewModel → refresh coordinator | UI selections and path/game choices become service workflow inputs. |
| Coordinator → shared state | Background refresh work can mutate visible plugin rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | T | `IPluginRefreshCoordinator` generations | mitigate | Contract and tests require generation IDs/CTS ownership and stale callback rejection before state writes. |
| T-09-05 | I | `PluginRefreshStatus` | mitigate | Status DTOs carry concise user-facing states; technical failures are logged rather than placed in status text. |
| T-09-06 | D | selected refresh target handling | mitigate | Snapshot target list at command start and stop after selected target set completes; do not expand into all visible rows. |
</threat_model>

<verification>
Run `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~PluginRefreshCoordinator|FullyQualifiedName~StateService"` after Plan 09-03 implements the coordinator.
</verification>

<success_criteria>
REF-02 and PERF-01 behavior is specified in code-level contracts/tests before implementation, with no plugin refresh workflow added to ViewModels.
</success_criteria>

<output>
After completion, create `.planning/phases/09-plugin-refresh-approximation-performance/09-02-SUMMARY.md`
</output>
