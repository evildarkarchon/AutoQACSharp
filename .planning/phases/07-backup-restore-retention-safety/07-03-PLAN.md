---
phase: 07-backup-restore-retention-safety
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - AutoQAC/Models/AppState.cs
  - AutoQAC/Models/CleaningSessionResult.cs
  - AutoQAC/Services/State/IStateService.cs
  - AutoQAC/Services/State/StateService.cs
  - AutoQAC/Services/Cleaning/ICleaningOrchestrator.cs
  - AutoQAC/Services/Cleaning/CleaningOrchestrator.cs
  - AutoQAC.Tests/Services/CleaningOrchestratorTests.cs
autonomous: true
requirements: [SAF-04, TEST-04, PERF-04]
must_haves:
  truths:
    - "D-05/D-06/D-09: cleaning backups remain per-plugin immediately before xEdit launch and publish progress through existing cleaning state."
    - "D-07/D-08: canceling backup copy prevents that plugin's xEdit launch and deletes the partial backup file."
    - "D-12/D-13/D-16: retention completes, warns, or cancels before final session completion is emitted."
  artifacts:
    - path: "AutoQAC/Services/Cleaning/CleaningOrchestrator.cs"
      provides: "backup/retention progress and cancellation integration in sequential cleaning flow"
      contains: "BackupPluginAsync"
    - path: "AutoQAC/Services/State/IStateService.cs"
      provides: "non-xEdit file-operation progress/cancel state"
      contains: "BackupOperation"
  key_links:
    - from: "CleaningOrchestrator"
      to: "IBackupService.BackupPluginAsync"
      via: "await before CleanPluginAsync inside plugin foreach loop"
      pattern: "await backupService\.BackupPluginAsync[\s\S]*CleanPluginAsync"
---

<objective>
Wire structured backup and retention operations into the sequential cleaning session without changing xEdit launch ordering.

Purpose: Users need visible, cancellable backup/retention work, and retention warnings must be part of the final session outcome rather than hidden log-only cleanup.
Output: State/orchestrator support for backup and retention progress, separate cancellation, retention warnings/cancel outcomes, and integration tests.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-backup-restore-retention-safety/07-CONTEXT.md
@.planning/phases/07-backup-restore-retention-safety/07-RESEARCH.md
@.planning/phases/07-backup-restore-retention-safety/07-UI-SPEC.md
@.planning/phases/07-backup-restore-retention-safety/07-02-SUMMARY.md
@AutoQAC/Services/Cleaning/CleaningOrchestrator.cs
@AutoQAC/Services/State/IStateService.cs
@AutoQAC/Models/CleaningSessionResult.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add non-xEdit backup/retention operation state</name>
  <read_first>AutoQAC/Models/AppState.cs; AutoQAC/Services/State/IStateService.cs; AutoQAC/Services/State/StateService.cs; AutoQAC/ViewModels/ProgressViewModel.cs</read_first>
  <files>AutoQAC/Models/AppState.cs; AutoQAC/Services/State/IStateService.cs; AutoQAC/Services/State/StateService.cs; AutoQAC.Tests/Services/CleaningOrchestratorTests.cs</files>
  <behavior>
    - State can represent `Backing up: {plugin}` and `Cleaning up old backups` without implying xEdit concurrency per UI-SPEC.
    - State includes file counts and byte counts when available per D-10.
    - State distinguishes cancelable file work from xEdit stop per D-11.
  </behavior>
  <action>Add an app model such as `BackupOperationState`/`BackupOperationKind` to `AppState` with fields `Kind`, `Label`, `FileName`, `FilesCompleted`, `TotalFiles`, `BytesCopied`, `TotalBytes`, `IsActive`, and `CanCancel`. Add `IStateService.SetBackupOperation(BackupOperationState? state)` and `ClearBackupOperation()`, implemented in `StateService` through immutable `AppState` updates. Do not remove or repurpose `IsTerminating` or xEdit stop state. Add tests that state changes publish through `StateChanged` and can be cleared after operation completion.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~CleaningOrchestratorTests</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Models/AppState.cs` contains `BackupOperationState` or a property named `BackupOperation`.
    - `AutoQAC/Services/State/IStateService.cs` contains `SetBackupOperation` and `ClearBackupOperation`.
    - `AutoQAC/Services/State/StateService.cs` contains implementations for both methods.
  </acceptance_criteria>
  <done>Existing progress UI can consume a separate backup/retention operation state without overloading the xEdit stop path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate backup/retention progress and cancellation in cleaning</name>
  <read_first>AutoQAC/Services/Cleaning/CleaningOrchestrator.cs; AutoQAC/Services/Cleaning/ICleaningOrchestrator.cs; AutoQAC/Models/CleaningSessionResult.cs; AutoQAC.Tests/Services/CleaningOrchestratorTests.cs</read_first>
  <files>AutoQAC/Services/Cleaning/ICleaningOrchestrator.cs; AutoQAC/Services/Cleaning/CleaningOrchestrator.cs; AutoQAC/Models/CleaningSessionResult.cs; AutoQAC.Tests/Services/CleaningOrchestratorTests.cs</files>
  <behavior>
    - Backup is awaited per plugin before xEdit launch per D-06.
    - Canceling backup copy marks that plugin skipped/canceled and does not call `CleanPluginAsync` for that plugin per D-07/D-08.
    - Retention cleanup is awaited before `FinishCleaningWithResults` per D-12.
    - Retention deletion failure yields successful cleaning with `Backup cleanup warning` counts per D-13.
    - Retention cancellation reports `Backup cleanup canceled` with deleted/skipped/remaining counts per D-16.
  </behavior>
  <action>Add cancellation ownership for non-xEdit file work inside `CleaningOrchestrator` without using the xEdit stop button: expose `CancelBackupOperationAsync` or equivalent on `ICleaningOrchestrator` for ProgressViewModel to call later. Replace synchronous `backupService.BackupPlugin` with `await backupService.BackupPluginAsync(..., progress, cts.Token)` inside the existing plugin loop immediately before `CleanPluginAsync`; preserve MO2 backup skip behavior. Replace `CleanupOldSessions` with `await CleanupOldSessionsAsync(..., progress, cts.Token)` before creating/emitting the final `CleaningSessionResult`. Extend `CleaningSessionResult` with optional retention cleanup result/summary fields so progress UI can display `Backup cleanup warning` or `Backup cleanup canceled`. Keep xEdit launches sequential: no `Task.WhenAll`, no parallel loops, no background xEdit launch.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~CleaningOrchestratorTests</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Services/Cleaning/CleaningOrchestrator.cs` contains `BackupPluginAsync` and `CleanupOldSessionsAsync`.
    - `AutoQAC/Services/Cleaning/CleaningOrchestrator.cs` does not contain `Task.WhenAll`.
    - `AutoQAC.Tests/Services/CleaningOrchestratorTests.cs` contains tests named with `BackupCancellation` and `DoesNotLaunchXEdit`.
    - `AutoQAC/Models/CleaningSessionResult.cs` contains `BackupCleanup` or `Retention` result data.
  </acceptance_criteria>
  <done>Cleaning sessions publish file-operation progress/cancel state and include retention warning/canceled outcomes before final completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file operation→cleaning session | Backup/retention local I/O affects whether xEdit launch should proceed and what final session outcome says. |
| state service→UI | Operation labels and progress values are displayed to users. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | D/T | CleaningOrchestrator | mitigate | Await backup before each `CleanPluginAsync`, skip xEdit on canceled backup, and forbid parallel launch constructs. |
| T-07-05 | R/I | CleaningSessionResult | mitigate | Include structured retention result summaries, not raw exception/path details, before final completion. |
</threat_model>

<verification>
Run `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~CleaningOrchestratorTests`.
</verification>

<success_criteria>
Backup and retention work is visible, cancellable, sequenced before/after xEdit exactly as required, and final session output includes cleanup warnings/cancellations.
</success_criteria>

<output>
After completion, create `.planning/phases/07-backup-restore-retention-safety/07-03-SUMMARY.md`.
</output>
