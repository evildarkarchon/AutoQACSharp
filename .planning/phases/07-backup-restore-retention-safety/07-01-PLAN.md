---
phase: 07-backup-restore-retention-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AutoQAC/Models/BackupOperationResults.cs
  - AutoQAC/Services/Backup/IBackupFileCopier.cs
  - AutoQAC/Services/Backup/BackupFileCopier.cs
  - AutoQAC/Services/Backup/IBackupService.cs
  - AutoQAC/Infrastructure/ServiceCollectionExtensions.cs
  - AutoQAC.Tests/Services/BackupFileCopierTests.cs
  - AutoQAC.Tests/Models/BackupOperationResultTests.cs
autonomous: true
requirements: [SAF-04, TEST-04, PERF-04]
must_haves:
  truths:
    - "D-03/D-05/D-07/D-08: backup and restore file-copy work reports Complete, Partial, Failed, Canceled, and progress-ready outcomes."
    - "D-04: user-facing failure reasons are constrained to concise row-level labels, not raw exception text."
    - "D-07/D-08/D-10: active copy cancellation removes partial files and reports byte progress when file size is available."
  artifacts:
    - path: "AutoQAC/Models/BackupOperationResults.cs"
      provides: "copy, restore, and retention result records/enums shared by later plans"
      contains: "BackupOperationStatus"
    - path: "AutoQAC/Services/Backup/BackupFileCopier.cs"
      provides: "managed async copy with cancellation, byte progress, and partial destination cleanup"
      contains: "CopyAsync"
  key_links:
    - from: "AutoQAC/Services/Backup/IBackupService.cs"
      to: "AutoQAC/Models/BackupOperationResults.cs"
      via: "async method contracts use structured result types"
      pattern: "Task<.*Result>"
---

<objective>
Create the shared backup/restore/retention contracts and cancellable file-copy foundation that all later Phase 7 plans build on.

Purpose: Phase 7 cannot safely report partial restore, retention warning, or copy cancellation outcomes while service APIs only throw or return a boolean-style backup result.
Output: Result records/enums, injectable copier, DI registration, and targeted tests proving cancellation deletes partial files.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-backup-restore-retention-safety/07-CONTEXT.md
@.planning/phases/07-backup-restore-retention-safety/07-RESEARCH.md
@.planning/phases/07-backup-restore-retention-safety/07-VALIDATION.md
@AutoQAC/Models/BackupResult.cs
@AutoQAC/Models/BackupSession.cs
@AutoQAC/Services/Backup/IBackupService.cs
@AutoQAC/Services/Backup/BackupService.cs
@AutoQAC/Infrastructure/ServiceCollectionExtensions.cs

<interfaces>
Existing contracts to preserve while adding async structured APIs:
```csharp
public interface IBackupService
{
    string CreateSessionDirectory(string backupRoot);
    BackupResult BackupPlugin(PluginInfo plugin, string sessionDir);
    Task WriteSessionMetadataAsync(string sessionDir, BackupSession session, CancellationToken ct = default);
    Task<List<BackupSession>> GetBackupSessionsAsync(string backupRoot, CancellationToken ct = default);
    void RestorePlugin(BackupPluginEntry entry, string sessionDir);
    void RestoreSession(BackupSession session);
    void CleanupOldSessions(string backupRoot, int maxSessionCount, string? currentSessionDir = null);
    string GetBackupRoot(string dataFolderPath);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cancellable copy foundation</name>
  <read_first>AutoQAC/Services/Backup/BackupService.cs; AutoQAC/Infrastructure/ServiceCollectionExtensions.cs; AutoQAC.Tests/Services/BackupServiceTests.cs</read_first>
  <files>AutoQAC/Services/Backup/IBackupFileCopier.cs; AutoQAC/Services/Backup/BackupFileCopier.cs; AutoQAC/Infrastructure/ServiceCollectionExtensions.cs; AutoQAC.Tests/Services/BackupFileCopierTests.cs</files>
  <behavior>
    - Copying a file reports at least one progress update with file name, bytes copied, and total bytes.
    - Canceling during a copy returns a canceled result and deletes any partial destination file per D-07/D-08.
    - I/O or access failures return a failed result with concise reason `Target write failed` or `Access denied` and log technical details.
  </behavior>
  <action>Create `IBackupFileCopier.CopyAsync(string sourcePath, string destinationPath, IProgress&lt;BackupCopyProgress&gt;? progress, CancellationToken cancellationToken)`. Implement `BackupFileCopier` with managed `FileStream` async copy using an 81920-byte buffer. Before copy, validate source existence; missing source returns reason `Missing backup file`. On cancellation, catch `OperationCanceledException`, delete `destinationPath` if it exists, and return status `Canceled` with reason `Canceled`. On incomplete `IOException`, delete the partial destination when possible and return `Failed` with reason `Target write failed`. On `UnauthorizedAccessException`, delete the partial destination when possible and return `Failed` with reason `Access denied`. Register `IBackupFileCopier` as a singleton in `AddBusinessLogic`. Add XML doc comments for the new public interface, record, and service methods because this is a new service boundary.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~BackupFileCopierTests</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Services/Backup/IBackupFileCopier.cs` contains `Task&lt;BackupCopyResult&gt; CopyAsync(`.
    - `AutoQAC/Services/Backup/BackupFileCopier.cs` contains `FileStream` and `Delete` and `OperationCanceledException`.
    - `AutoQAC/Infrastructure/ServiceCollectionExtensions.cs` contains `AddSingleton&lt;IBackupFileCopier, BackupFileCopier&gt;`.
    - `AutoQAC.Tests/Services/BackupFileCopierTests.cs` contains tests named with `Cancellation` and `DeletesPartialDestination`.
  </acceptance_criteria>
  <done>Canceled copy leaves no destination file, successful copy returns byte count, failures return concise reasons and no raw exception text in result messages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define structured backup operation results</name>
  <read_first>AutoQAC/Models/BackupResult.cs; AutoQAC/Models/BackupSession.cs; AutoQAC/Services/Backup/IBackupService.cs; .planning/phases/07-backup-restore-retention-safety/07-CONTEXT.md</read_first>
  <files>AutoQAC/Models/BackupOperationResults.cs; AutoQAC/Services/Backup/IBackupService.cs; AutoQAC.Tests/Models/BackupOperationResultTests.cs</files>
  <behavior>
    - Aggregate restore can distinguish Complete, Partial, Failed, and Canceled per D-03.
    - Retention cleanup can distinguish Complete, Warning, Failed, and Canceled per D-13/D-16.
    - Row reasons are restricted to `Missing backup file`, `Access denied`, `Target folder creation failed`, `Target write failed`, `Canceled`, and `Cleanup deletion failed` per D-04/UI-SPEC.
  </behavior>
  <action>Create `AutoQAC/Models/BackupOperationResults.cs` containing enums `BackupOperationStatus { Complete, Partial, Failed, Canceled, Warning }`, `BackupRestoreRowStatus { Restored, Failed, Canceled }`, `BackupFailureReason` with the six required display labels, records `BackupCopyProgress`, `BackupCopyResult`, `BackupRestoreRowResult`, `BackupRestoreResult`, `BackupRetentionRowResult`, and `BackupRetentionCleanupResult`. Include computed count properties for restored/failed/canceled and deleted/skipped/remaining. Extend `IBackupService` with async signatures, without removing legacy sync wrappers yet: `Task<BackupResult> BackupPluginAsync(PluginInfo plugin, string sessionDir, IProgress<BackupCopyProgress>? progress = null, CancellationToken ct = default)`, `Task<BackupRestoreResult> RestorePluginAsync(BackupPluginEntry entry, string sessionDir, IProgress<BackupCopyProgress>? progress = null, CancellationToken ct = default)`, `Task<BackupRestoreResult> RestoreSessionAsync(BackupSession session, IProgress<BackupCopyProgress>? progress = null, CancellationToken ct = default)`, and `Task<BackupRetentionCleanupResult> CleanupOldSessionsAsync(string backupRoot, int maxSessionCount, string? currentSessionDir = null, IProgress<BackupCopyProgress>? progress = null, CancellationToken ct = default)`.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~BackupOperationResultTests</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Models/BackupOperationResults.cs` contains `BackupOperationStatus` and all five values `Complete`, `Partial`, `Failed`, `Canceled`, `Warning`.
    - `AutoQAC/Models/BackupOperationResults.cs` contains all six UI labels from 07-UI-SPEC line 90.
    - `AutoQAC/Services/Backup/IBackupService.cs` contains `RestoreSessionAsync` and `CleanupOldSessionsAsync`.
  </acceptance_criteria>
  <done>Downstream service, orchestrator, and UI plans have concrete result contracts to consume with no interpretation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backup metadata→filesystem | Session metadata paths drive local copy destination and source selection. |
| filesystem exception→UI result | Technical I/O/access exceptions become user-visible row reasons. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | I/T | BackupFileCopier | mitigate | Return only enumerated `BackupFailureReason` labels in result objects and log technical exception details via `ILoggingService`. |
| T-07-02 | D | BackupFileCopier | mitigate | Honor `CancellationToken` inside the read/write loop and delete partial destination files on cancellation or incomplete copy. |
</threat_model>

<verification>
Run `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~BackupFileCopierTests|FullyQualifiedName~BackupOperationResultTests"`.
</verification>

<success_criteria>
The project has async copy and result contracts that cover all required copy/restore/retention statuses, concise reasons, byte progress, and partial-file cleanup.
</success_criteria>

<output>
After completion, create `.planning/phases/07-backup-restore-retention-safety/07-01-SUMMARY.md`.
</output>
