---
phase: 07-backup-restore-retention-safety
plan: 06
type: execute
wave: 5
depends_on: [07-05]
files_modified:
  - AutoQAC/Services/Cleaning/CleaningOrchestrator.cs
  - AutoQAC.Tests/Services/CleaningOrchestratorTests.cs
autonomous: true
gap_closure: true
requirements: [SAF-04, TEST-04, PERF-04]
must_haves:
  truths:
    - "BackupFailureChoice.SkipPlugin creates a skipped PluginCleaningResult, publishes it via AddDetailedCleaningResult, and keeps session/progress accounting complete."
    - "BackupFailureChoice.AbortSession writes partial metadata when present and finalizes through FinishCleaningWithResults before StartCleaningAsync returns."
  artifacts:
    - path: "AutoQAC/Services/Cleaning/CleaningOrchestrator.cs"
      provides: "Backup failure SkipPlugin and AbortSession finalization/accounting fixes"
      contains: "BackupFailureChoice.SkipPlugin"
    - path: "AutoQAC.Tests/Services/CleaningOrchestratorTests.cs"
      provides: "Regression coverage for backup failure skip and abort choices"
      contains: "BackupFailureChoice.SkipPlugin"
  key_links:
    - from: "CleaningOrchestrator backup failure callback"
      to: "stateService.AddDetailedCleaningResult"
      via: "skipped PluginCleaningResult publication"
      pattern: "AddDetailedCleaningResult\\(skipped"
    - from: "CleaningOrchestrator AbortSession branch"
      to: "stateService.FinishCleaningWithResults"
      via: "final session result before return/break"
      pattern: "FinishCleaningWithResults\\(sessionResult\\)"
---

<objective>
Close the verification gap where backup failure decisions can leave cleaning progress/session reporting incomplete.

Purpose: Phase 7 cannot satisfy clear backup failure reporting if SkipPlugin silently drops a plugin result or AbortSession exits before publishing a final session result.
Output: Production fixes plus targeted regression tests for `BackupFailureChoice.SkipPlugin` and `BackupFailureChoice.AbortSession`.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-backup-restore-retention-safety/07-VERIFICATION.md
@.planning/phases/07-backup-restore-retention-safety/07-03-SUMMARY.md
@AutoQAC/Services/Cleaning/CleaningOrchestrator.cs
@AutoQAC.Tests/Services/CleaningOrchestratorTests.cs

<interfaces>
From `CleaningOrchestrator.cs`:
```csharp
public async Task StartCleaningAsync(TimeoutRetryCallback? onTimeout, BackupFailureCallback? onBackupFailure, CancellationToken ct = default)
private async Task<BackupCreateResult> BackupPluginAsync(PluginInfo plugin, string sessionDir, CancellationToken sessionToken)
```

Existing failure branch to fix:
```csharp
case BackupFailureChoice.SkipPlugin:
    stateService.UpdateState(... SkippedPlugins ...);
    continue;
case BackupFailureChoice.AbortSession:
    await backupService.WriteSessionMetadataAsync(...);
    return;
```

Use existing result publication pattern:
```csharp
pluginResults.Add(pluginCleaningResult);
stateService.AddDetailedCleaningResult(pluginCleaningResult);
stateService.FinishCleaningWithResults(sessionResult);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add backup failure choice regression tests</name>
  <read_first>
    - AutoQAC.Tests/Services/CleaningOrchestratorTests.cs
    - AutoQAC/Services/Cleaning/CleaningOrchestrator.cs
    - .planning/phases/07-backup-restore-retention-safety/07-VERIFICATION.md
  </read_first>
  <files>AutoQAC.Tests/Services/CleaningOrchestratorTests.cs</files>
  <behavior>
    - Test 1: when `BackupPluginAsync` returns a failed `BackupCreateResult` and `onBackupFailure` returns `BackupFailureChoice.SkipPlugin`, `CleanPluginAsync` is not called for that plugin, `AddDetailedCleaningResult` receives a `PluginCleaningResult` with `PluginName == "BackupFails.esp"`, `Status == CleaningStatus.Skipped`, `Success == false`, and `Message == "Backup failed - skipped by user"`, and `FinishCleaningWithResults` receives a session whose `PluginResults` contains that skipped result.
    - Test 2: when the callback returns `BackupFailureChoice.AbortSession`, `CleanPluginAsync` is not called for the failed plugin and `FinishCleaningWithResults` is called exactly once with `WasCancelled == true` and no dropped previously processed plugin results.
  </behavior>
  <action>Add two xUnit tests near the existing backup integration tests. Configure `LoadUserConfigAsync` with `Backup.Enabled = true`, rooted plugin paths under a temp directory, `BackupPluginAsync` to return `new BackupCreateResult(BackupOperationStatus.Failed, "BackupFails.esp", 0, null, BackupFailureReason.TargetWriteFailed)`, and the backup failure callback to return the tested choice. Match NSubstitute optional parameters explicitly. Do not use UI dialogs or ViewModel code in these service tests.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~CleaningOrchestratorTests"</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC.Tests/Services/CleaningOrchestratorTests.cs` contains `BackupFailureChoice.SkipPlugin`.
    - `AutoQAC.Tests/Services/CleaningOrchestratorTests.cs` contains `BackupFailureChoice.AbortSession`.
    - `AutoQAC.Tests/Services/CleaningOrchestratorTests.cs` contains `Backup failed - skipped by user`.
    - The targeted `dotnet test` command exits 0.
  </acceptance_criteria>
  <done>The tests fail before the production fix and encode the exact skipped-result and abort-finalization behavior required by the verification gap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finalize SkipPlugin and AbortSession backup failure paths</name>
  <read_first>
    - AutoQAC/Services/Cleaning/CleaningOrchestrator.cs
    - AutoQAC.Tests/Services/CleaningOrchestratorTests.cs
    - .planning/phases/07-backup-restore-retention-safety/07-VERIFICATION.md
  </read_first>
  <files>AutoQAC/Services/Cleaning/CleaningOrchestrator.cs, AutoQAC.Tests/Services/CleaningOrchestratorTests.cs</files>
  <action>In the `BackupFailureChoice.SkipPlugin` branch, create a local `PluginCleaningResult skippedResult = new() { PluginName = plugin.FileName, Status = CleaningStatus.Skipped, Success = false, Message = "Backup failed - skipped by user" }`, add it to `pluginResults`, call `stateService.AddDetailedCleaningResult(skippedResult)`, preserve the existing `SkippedPlugins` state update, then `continue`. In the `BackupFailureChoice.AbortSession` branch, preserve partial metadata writing, set `wasCancelled = true`, create a `CleaningSessionResult` with `StartTime = startTime`, `EndTime = DateTime.Now`, `GameType = gameType`, `WasCancelled = true`, `PluginResults = pluginResults`, and `BackupCleanup = backupCleanup`, then call `stateService.FinishCleaningWithResults(sessionResult)` and `LogSessionSummary(sessionResult)` before returning. Add a short comment explaining that abort finalizes inside the branch because it exits before the normal end-of-method finalization path. Do not call `CleanPluginAsync` after SkipPlugin or AbortSession backup-failure choices.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~CleaningOrchestratorTests"</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Services/Cleaning/CleaningOrchestrator.cs` contains `Message = "Backup failed - skipped by user"`.
    - `AutoQAC/Services/Cleaning/CleaningOrchestrator.cs` contains `stateService.AddDetailedCleaningResult(skippedResult)`.
    - `AutoQAC/Services/Cleaning/CleaningOrchestrator.cs` contains a `BackupFailureChoice.AbortSession` branch with `stateService.FinishCleaningWithResults(sessionResult)` before `return`.
    - The targeted `dotnet test` command exits 0.
  </acceptance_criteria>
  <done>SkipPlugin and AbortSession backup failure choices both produce complete observable session state without parallelizing xEdit or changing backup placement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backup failure callback -> cleaning orchestrator | User/UI choice changes control flow inside the cleaning session. |
| cleaning orchestrator -> state service | Session/result state becomes user-visible progress and final reporting. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Repudiation | `CleaningOrchestrator` backup failure branches | mitigate | Publish explicit skipped/final session results for SkipPlugin and AbortSession so user choices are auditable in app state. |
| T-07-06-02 | Denial of Service | `AbortSession` early exit | mitigate | Finalize session before returning so `IsCleaning` and result state are not left incomplete. |
</threat_model>

<verification>
Run `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~CleaningOrchestratorTests"` and confirm both new backup failure choice tests pass.
</verification>

<success_criteria>
- Backup failure SkipPlugin records and publishes a skipped plugin result.
- Backup failure AbortSession finalizes the cleaning session exactly once before returning.
- Existing sequential xEdit behavior remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/07-backup-restore-retention-safety/07-06-SUMMARY.md`.
</output>
