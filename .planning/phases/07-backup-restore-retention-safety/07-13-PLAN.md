---
phase: 07-backup-restore-retention-safety
plan: 13
type: tdd
wave: 9
depends_on: [07-11]
files_modified:
  - AutoQAC/ViewModels/RestoreViewModel.cs
  - AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs
autonomous: true
gap_closure: true
requirements: [SAF-04, TEST-04]
must_haves:
  truths:
    - "RestoreWindow Delete Session cannot recursively delete a session directory outside the loaded backup root."
    - "Unsafe backup session deletion fails closed with concise user-facing copy while technical path details stay in logs, preserving D-04's concise reason pattern."
    - "Normal Delete Session still removes sessions under the loaded backup root after user confirmation."
  artifacts:
    - path: "AutoQAC/ViewModels/RestoreViewModel.cs"
      provides: "Backup-root containment validation before recursive session deletion"
      contains: "IsSessionDirectoryInsideBackupRoot"
    - path: "AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs"
      provides: "Traversal and sibling-prefix rejection coverage for DeleteSessionAsync"
      contains: "DeleteSessionCommand_UnsafeSessionDirectoryOutsideBackupRoot_FailsClosed"
  key_links:
    - from: "RestoreViewModel.DeleteSessionAsync"
      to: "Directory.Delete"
      via: "backup-root containment check before recursive delete"
      pattern: "IsSessionDirectoryInsideBackupRoot"
    - from: "RestoreViewModel.LoadSessionsAsync"
      to: "DeleteSessionAsync"
      via: "_backupRoot retained from IBackupService.GetBackupRoot(dataFolderPath)"
      pattern: "_backupRoot"
---

<objective>
Close the verification gap where RestoreWindow Delete Session can recursively delete a mutable selected-session path without validating it remains under the loaded backup root.

Purpose: SAF-04 and TEST-04 require restore/backup management safety across failure paths. Delete Session is part of the restore window's backup-management surface and must treat session metadata as untrusted local filesystem input before recursive deletion.
Output: `RestoreViewModel.DeleteSessionAsync` validates selected session directory containment under `_backupRoot`, fails closed with concise user-facing copy and technical logging, and tests prove traversal/sibling-prefix rejection plus normal deletion.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-backup-restore-retention-safety/07-CONTEXT.md
@.planning/phases/07-backup-restore-retention-safety/07-VERIFICATION.md
@.planning/phases/07-backup-restore-retention-safety/07-04-SUMMARY.md
@.planning/phases/07-backup-restore-retention-safety/07-07-SUMMARY.md
@.planning/phases/07-backup-restore-retention-safety/07-11-SUMMARY.md
@AutoQAC/ViewModels/RestoreViewModel.cs
@AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs

<interfaces>
From current `RestoreViewModel`:
```csharp
private string? _backupRoot;

public async Task LoadSessionsAsync(string? dataFolderPath)
{
    _trustedRestoreRoot = string.IsNullOrWhiteSpace(dataFolderPath) ? null : dataFolderPath;
    // ...
    _backupRoot = _backupService.GetBackupRoot(trustedRestoreRoot);
    await LoadSessions();
}
```

Current unsafe delete path:
```csharp
var dirToDelete = SelectedSession.SessionDirectory;
if (System.IO.Directory.Exists(dirToDelete))
{
    System.IO.Directory.Delete(dirToDelete, recursive: true);
}
```

Required behavior after this plan: validate `dirToDelete` is a normalized descendant of `_backupRoot` before `Directory.Exists` or `Directory.Delete`; reject null/empty/invalid backup root, null/empty/invalid selected session directory, parent traversal, and sibling-prefix paths such as `Backup Root 2` when `_backupRoot` is `Backup Root`.
</interfaces>
</context>

<feature>
  <name>Restore backup-session delete containment</name>
  <files>AutoQAC/ViewModels/RestoreViewModel.cs, AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs</files>
  <behavior>
    RED cases:
    - Confirmed Delete Session with a selected session directory outside the loaded backup root does not call `Directory.Delete`, keeps the session in `Sessions`, sets concise status text, shows `ShowErrorAsync("Delete Failed", "Failed to delete the backup session.", "The selected backup session is outside the configured backup folder.")`, and logs the unsafe path.
    - Sibling-prefix rejection: `_backupRoot` `.../AutoQAC Backups` must not allow `.../AutoQAC Backups 2/2026-04-29_07-30-00`.
    - Parent traversal normalization rejection: `_backupRoot/Safe/../OutsideSession` that resolves outside `_backupRoot` is rejected.
    - Normal confirmed deletion for a directory under `_backupRoot` still removes the directory, removes the session from `Sessions`, clears `SelectedSession`, raises `HasSessions`, and sets `StatusText` to `Session deleted`.
  </behavior>
  <implementation>
    Add a private helper such as `IsSessionDirectoryInsideBackupRoot(string? sessionDirectory)` that returns false when `_backupRoot` or `sessionDirectory` is null, empty, whitespace, or cannot be normalized with `Path.GetFullPath`. Normalize `_backupRoot` with a trailing directory separator and normalize `sessionDirectory`; require `sessionDirectory` to start with the normalized backup root using `StringComparison.OrdinalIgnoreCase`. Call this helper in `DeleteSessionAsync` immediately after confirmation and before `Directory.Exists` or `Directory.Delete`. If validation fails, log a warning or error with the technical selected path and backup root, set `StatusText` to `Delete failed -- selected backup session is outside the configured backup folder`, and show concise error copy through `_messageDialog.ShowErrorAsync("Delete Failed", "Failed to delete the backup session.", "The selected backup session is outside the configured backup folder.")`. Do not pass `ex.Message` or raw paths to the user-facing details. Keep normal deletion behavior unchanged for contained directories.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — prove Delete Session rejects out-of-root session paths</name>
  <files>AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs</files>
  <read_first>AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs, AutoQAC/ViewModels/RestoreViewModel.cs, .planning/phases/07-backup-restore-retention-safety/07-VERIFICATION.md, .planning/phases/07-backup-restore-retention-safety/07-11-SUMMARY.md</read_first>
  <behavior>
    - Add `DeleteSessionCommand_UnsafeSessionDirectoryOutsideBackupRoot_FailsClosed`.
    - Add `DeleteSessionCommand_SiblingPrefixBackupRoot_FailsClosed`.
    - Add parent-traversal normalization rejection coverage for a session path that resolves outside `_backupRoot`.
    - Add or extend a positive test proving normal contained session deletion still removes the directory and updates ViewModel state.
  </behavior>
  <action>Add failing tests only. Use real temporary directories under the test temp root so assertions can prove out-of-root directories still exist after rejected deletion. Set up `_backupService.GetBackupRoot(dataFolderPath).Returns(backupRoot)` and `_backupService.GetBackupSessionsAsync(backupRoot, Arg.Any<CancellationToken>()).Returns([session])`; then call `await vm.LoadSessionsAsync(dataFolderPath)`, set `vm.SelectedSession`, confirm deletion with `_messageDialog.ShowConfirmAsync(...).Returns(true)`, and execute `await vm.DeleteSessionCommand.ExecuteAsync(null)`. Match optional parameters explicitly in NSubstitute calls. Expected user-facing error details must be exactly `The selected backup session is outside the configured backup folder.` and must not include raw filesystem paths.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~DeleteSessionCommand_UnsafeSessionDirectoryOutsideBackupRoot_FailsClosed|FullyQualifiedName~DeleteSessionCommand_SiblingPrefixBackupRoot_FailsClosed"</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs` contains `DeleteSessionCommand_UnsafeSessionDirectoryOutsideBackupRoot_FailsClosed`.
    - `AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs` contains `DeleteSessionCommand_SiblingPrefixBackupRoot_FailsClosed`.
    - `AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs` contains a parent-traversal normalization rejection test for Delete Session.
    - Tests assert rejected out-of-root directories still exist after command execution.
    - Tests assert `ShowErrorAsync` receives exact detail text `The selected backup session is outside the configured backup folder.`.
    - Targeted tests fail before production changes because Delete Session still deletes the mutable selected path directly.
  </acceptance_criteria>
  <done>Delete Session safety tests capture traversal, sibling-prefix, and normal contained behavior before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — validate session directory containment before recursive delete</name>
  <files>AutoQAC/ViewModels/RestoreViewModel.cs</files>
  <read_first>AutoQAC/ViewModels/RestoreViewModel.cs, AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs, .planning/phases/07-backup-restore-retention-safety/07-CONTEXT.md</read_first>
  <behavior>
    - `DeleteSessionAsync` rejects unsafe selected session paths before `Directory.Exists` or `Directory.Delete`.
    - Rejected deletes show concise user-facing copy and log technical details.
    - Normal contained session deletion still works after confirmation.
  </behavior>
  <action>In `RestoreViewModel.cs`, add `using System.IO;` if needed and replace fully qualified `System.IO.Directory` calls in `DeleteSessionAsync` with the imported `Directory` after adding validation. Add private helper `IsSessionDirectoryInsideBackupRoot(string? sessionDirectory)` with XML documentation explaining it normalizes backup-root/session paths for string-level containment and does not resolve NTFS reparse points/symlinks. The helper should call `Path.GetFullPath` on `_backupRoot` and `sessionDirectory`, normalize the root with a trailing separator, and return true only when the normalized session path starts with that root using `StringComparison.OrdinalIgnoreCase`. Add a small private trailing-separator helper if no suitable helper is accessible in this class. In `DeleteSessionAsync`, store `var dirToDelete = SelectedSession.SessionDirectory;` immediately after confirmation, call the helper, and on false: `_logger.Warning("Rejected backup session delete outside backup root. BackupRoot={BackupRoot}; SessionDirectory={SessionDirectory}", _backupRoot, dirToDelete);`, set `StatusText = "Delete failed -- selected backup session is outside the configured backup folder";`, call `_messageDialog.ShowErrorAsync("Delete Failed", "Failed to delete the backup session.", "The selected backup session is outside the configured backup folder.");`, then return. Keep the catch block concise too: replace the user-facing third argument `ex.Message` with `"Technical details were written to the log."` while retaining `_logger.Error(ex, "Failed to delete backup session")`.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~RestoreViewModelTests</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/ViewModels/RestoreViewModel.cs` contains `IsSessionDirectoryInsideBackupRoot`.
    - `AutoQAC/ViewModels/RestoreViewModel.cs` calls `IsSessionDirectoryInsideBackupRoot(dirToDelete)` before any `Directory.Exists(dirToDelete)` or `Directory.Delete(dirToDelete` call.
    - `AutoQAC/ViewModels/RestoreViewModel.cs` contains exact user-facing text `The selected backup session is outside the configured backup folder.`.
    - `AutoQAC/ViewModels/RestoreViewModel.cs` catch block for delete failure uses `Technical details were written to the log.` instead of `ex.Message` in `ShowErrorAsync`.
    - `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~RestoreViewModelTests` exits 0.
  </acceptance_criteria>
  <done>RestoreWindow Delete Session is contained to the loaded backup root and has automated regression coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Verify Phase 7 remaining gap closure</name>
  <files>AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs</files>
  <read_first>AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs, AutoQAC/ViewModels/RestoreViewModel.cs, AutoQAC/Views/MainWindow.axaml.cs, .planning/phases/07-backup-restore-retention-safety/07-VERIFICATION.md</read_first>
  <action>After Task 2 passes, run the Phase 7 targeted verification cluster and full solution tests. Also run a source scan under `AutoQAC/Services/Cleaning` for `Task.WhenAll`, `Parallel.ForEachAsync`, or `Task.Run` to reconfirm this restore-window delete safety change did not alter sequential xEdit cleaning. In `07-13-SUMMARY.md`, list both remaining verification gaps from `07-VERIFICATION.md` and note that this plan closes the Delete Session containment gap while Plan 07-12 closes the normal progress lifecycle gap.</action>
  <verify>
    <automated>dotnet test AutoQACSharp.slnx</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~BackupFileCopierTests|FullyQualifiedName~BackupServiceTests|FullyQualifiedName~RestoreViewModelTests|FullyQualifiedName~CleaningOrchestratorTests|FullyQualifiedName~ProgressViewModelTests|FullyQualifiedName~ViewSubscriptionLifecycleTests"` exits 0.
    - `dotnet test AutoQACSharp.slnx` exits 0.
    - Source scan reports no `Task.WhenAll`, `Parallel.ForEachAsync`, or `Task.Run` under `AutoQAC/Services/Cleaning`.
  </acceptance_criteria>
  <done>Remaining Phase 7 backup/restore/progress safety gaps are verified closed with full solution tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backup session metadata -> recursive filesystem delete | `BackupSession.SessionDirectory` is loaded from backup session enumeration and ViewModel state before being passed to `Directory.Delete(recursive: true)`. |
| filesystem exception -> restore UI copy | Delete failures can include raw local paths in exception messages and must not be surfaced directly to users. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13-01 | Tampering | `RestoreViewModel.DeleteSessionAsync` | mitigate | Normalize selected session directory and require containment under `_backupRoot` before recursive delete. |
| T-07-13-02 | Elevation of Privilege | backup-session deletion | mitigate | Reject traversal and sibling-prefix paths before filesystem operations. |
| T-07-13-03 | Information Disclosure | delete failure dialog | mitigate | Show concise fixed copy and keep raw exception/path details in logs. |
</threat_model>

<verification>
Run RestoreViewModel tests, Phase 7 targeted cluster, full solution tests, and sequential xEdit source scan.
</verification>

<success_criteria>
- Delete Session rejects out-of-root, traversal-normalized, and sibling-prefix session paths before recursive delete.
- Delete Session keeps concise user-facing copy and logs technical details.
- Normal contained backup-session deletion still works after confirmation.
- Full solution tests pass and sequential xEdit cleaning remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/07-backup-restore-retention-safety/07-13-SUMMARY.md`.
</output>
