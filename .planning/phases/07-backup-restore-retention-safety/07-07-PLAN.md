---
phase: 07-backup-restore-retention-safety
plan: 07
type: execute
wave: 5
depends_on: [07-05]
files_modified:
  - AutoQAC/Services/Backup/BackupService.cs
  - AutoQAC.Tests/Services/BackupServiceTests.cs
  - AutoQAC.Tests/Services/BackupFileCopierTests.cs
autonomous: true
gap_closure: true
requirements: [SAF-04, TEST-04, PERF-04]
must_haves:
  truths:
    - "Restore rejects unsafe backup metadata FileName values before copying."
    - "Restore rejects unsafe OriginalPath targets that are not rooted under an approved restore root policy."
    - "Retention cleanup reports count progress beyond the initial static band."
    - "TEST-04 includes permission/access-denied and cleanup deletion failure after retry coverage."
  artifacts:
    - path: "AutoQAC/Services/Backup/BackupService.cs"
      provides: "Restore metadata validation and retention progress reporting"
      contains: "ValidateRestoreEntry"
    - path: "AutoQAC.Tests/Services/BackupServiceTests.cs"
      provides: "Malicious metadata and retention deletion warning regression tests"
      contains: "CleanupOldSessionsAsync_DeletionFailureAfterRetry_ReturnsWarning"
    - path: "AutoQAC.Tests/Services/BackupFileCopierTests.cs"
      provides: "Access denied / write failure copy mapping coverage"
      contains: "CopyAsync"
  key_links:
    - from: "BackupService.RestorePluginRowAsync"
      to: "IBackupFileCopier.CopyAsync"
      via: "validated source and target paths"
      pattern: "ValidateRestoreEntry"
    - from: "BackupService.CleanupOldSessionsAsync"
      to: "ProgressViewModel backup-operation progress band"
      via: "IProgress<BackupCopyProgress>.Report"
      pattern: "progress\\?\\.Report"
---

<objective>
Close backup-service verification gaps for unsafe restore metadata, hollow retention cleanup progress, and missing TEST-04 failure coverage.

Purpose: Phase 7 restore and retention work must be safe when reading backup metadata, visibly progressing during cleanup, and test-covered for permission and deletion-failure outcomes.
Output: Backup service validation/progress fixes plus regression tests for traversal, absolute backup filenames, unsafe restore targets, access-denied/write failure mapping, and cleanup deletion warning after retry.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-backup-restore-retention-safety/07-VERIFICATION.md
@.planning/phases/07-backup-restore-retention-safety/07-01-SUMMARY.md
@.planning/phases/07-backup-restore-retention-safety/07-02-SUMMARY.md
@AutoQAC/Services/Backup/BackupService.cs
@AutoQAC.Tests/Services/BackupServiceTests.cs
@AutoQAC.Tests/Services/BackupFileCopierTests.cs

<interfaces>
From `BackupService.cs`:
```csharp
public async Task<BackupRestoreResult> RestorePluginAsync(BackupPluginEntry entry, string sessionDir, IProgress<BackupCopyProgress>? progress = null, CancellationToken ct = default)
public async Task<BackupRetentionCleanupResult> CleanupOldSessionsAsync(string backupRoot, int maxSessionCount, string? currentSessionDir = null, IProgress<BackupCopyProgress>? progress = null, CancellationToken ct = default)
private async Task<BackupRestoreRowResult> RestorePluginRowAsync(BackupPluginEntry entry, string sessionDir, IProgress<BackupCopyProgress>? progress, CancellationToken ct)
```

Existing approved row labels from result contracts:
```csharp
BackupFailureReason.MissingBackupFile      // display: "Missing backup file"
BackupFailureReason.AccessDenied           // display: "Access denied"
BackupFailureReason.TargetFolderCreationFailed
BackupFailureReason.TargetWriteFailed
BackupFailureReason.CleanupDeletionFailed  // display: "Cleanup deletion failed"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add unsafe metadata and deletion-warning regression tests</name>
  <read_first>
    - AutoQAC.Tests/Services/BackupServiceTests.cs
    - AutoQAC/Services/Backup/BackupService.cs
    - .planning/phases/07-backup-restore-retention-safety/07-VERIFICATION.md
  </read_first>
  <files>AutoQAC.Tests/Services/BackupServiceTests.cs</files>
  <behavior>
    - Test 1: `RestorePluginAsync_FileNameTraversal_ReturnsMissingBackupFileAndDoesNotCopy` creates a real file outside `sessionDir`, passes `FileName = "..\\outside.esp"`, and asserts result status Failed, row display reason `Missing backup file`, and target file unchanged/missing.
    - Test 2: `RestorePluginAsync_AbsoluteFileName_ReturnsMissingBackupFileAndDoesNotCopy` passes a rooted `FileName` and asserts the copier is not reached and the row reports `Missing backup file`.
    - Test 3: `RestorePluginAsync_UnrootedOriginalPath_ReturnsTargetFolderCreationFailed` passes `OriginalPath = "relative.esp"` and asserts no copy occurs and the row reports `Target folder creation failed`.
    - Test 4: `CleanupOldSessionsAsync_DeletionFailureAfterRetry_ReturnsWarning` uses `RecordingBackupSessionDeleter(_ => throw new IOException("locked"))`, `maxSessionCount: 0`, and asserts `Status == BackupOperationStatus.Warning`, `Attempts == 2`, one failed row, and `DisplayReason == "Cleanup deletion failed"`.
  </behavior>
  <action>Add the four tests to `BackupServiceTests`. Reuse existing temp root helpers and `RecordingBackupSessionDeleter`. For copier-not-reached assertions, create a small test copier inside the test file that increments a call count and returns success if called; assert call count is 0 for invalid metadata. Do not rely on Windows ACL mutation for these metadata tests.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~BackupServiceTests</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC.Tests/Services/BackupServiceTests.cs` contains `RestorePluginAsync_FileNameTraversal_ReturnsMissingBackupFileAndDoesNotCopy`.
    - `AutoQAC.Tests/Services/BackupServiceTests.cs` contains `RestorePluginAsync_AbsoluteFileName_ReturnsMissingBackupFileAndDoesNotCopy`.
    - `AutoQAC.Tests/Services/BackupServiceTests.cs` contains `RestorePluginAsync_UnrootedOriginalPath_ReturnsTargetFolderCreationFailed`.
    - `AutoQAC.Tests/Services/BackupServiceTests.cs` contains `CleanupOldSessionsAsync_DeletionFailureAfterRetry_ReturnsWarning`.
  </acceptance_criteria>
  <done>Tests capture the restore metadata and deletion-warning gaps before production changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate restore metadata and report retention count progress</name>
  <read_first>
    - AutoQAC/Services/Backup/BackupService.cs
    - AutoQAC.Tests/Services/BackupServiceTests.cs
    - AutoQAC/Models/BackupOperationResults.cs
  </read_first>
  <files>AutoQAC/Services/Backup/BackupService.cs, AutoQAC.Tests/Services/BackupServiceTests.cs</files>
  <action>In `RestorePluginRowAsync`, validate metadata before building/copying paths. Add a private helper such as `ValidateRestoreEntry(BackupPluginEntry entry, string sessionDir, out string backupPath, out string targetPath, out BackupFailureReason? failureReason)`. It must reject `entry.FileName` when `Path.GetFileName(entry.FileName) != entry.FileName`, when `Path.IsPathRooted(entry.FileName)` is true, or when the resolved full backup path is not contained under `Path.GetFullPath(sessionDir)` plus a directory separator. Map these source validation failures to `BackupFailureReason.MissingBackupFile`. Reject `entry.OriginalPath` when null/whitespace or not rooted; map to `BackupFailureReason.TargetFolderCreationFailed`. For rooted targets, normalize with `Path.GetFullPath(entry.OriginalPath)` before `Directory.CreateDirectory` and `CopyAsync`. Add XML doc comments to new non-trivial helpers explaining that backup metadata is untrusted and must not escape the selected session directory. For retention, add a local `ReportRetentionProgress` helper that calls `progress?.Report(new BackupCopyProgress("RetentionCleanup", Path.GetFileName(sessionDirectory), filesCompleted, totalFiles, 0, null))`. Report after classification and after each kept/deleted/failed/canceled row so `FilesCompleted` advances and `TotalFiles` is the number of classified valid sessions plus malformed rows known at that point. Update `CleaningOrchestrator` only if necessary to preserve count fields; otherwise keep this plan scoped to `BackupService`.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~BackupServiceTests</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Services/Backup/BackupService.cs` contains `ValidateRestoreEntry`.
    - `AutoQAC/Services/Backup/BackupService.cs` contains `Path.GetFullPath(sessionDir)`.
    - `AutoQAC/Services/Backup/BackupService.cs` contains `BackupFailureReason.MissingBackupFile` in the restore metadata validation path.
    - `AutoQAC/Services/Backup/BackupService.cs` contains `ReportRetentionProgress`.
    - `AutoQAC/Services/Backup/BackupService.cs` contains `progress?.Report`.
    - The targeted `dotnet test` command exits 0.
  </acceptance_criteria>
  <done>Unsafe restore metadata cannot escape the selected session directory or use unrooted overwrite targets, and retention cleanup emits advancing count progress.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add access-denied/write-failure copy coverage and run gap-cluster verification</name>
  <read_first>
    - AutoQAC.Tests/Services/BackupFileCopierTests.cs
    - AutoQAC.Tests/Services/BackupServiceTests.cs
    - AutoQAC/Services/Backup/BackupFileCopier.cs
    - AutoQAC/Services/Backup/BackupService.cs
  </read_first>
  <files>AutoQAC.Tests/Services/BackupFileCopierTests.cs, AutoQAC.Tests/Services/BackupServiceTests.cs</files>
  <behavior>
    - Test 1: `CopyAsync_DestinationDirectoryMissing_ReturnsTargetWriteFailed` copies an existing source to a destination whose parent directory does not exist and asserts `Status == Failed` and `FailureReason == BackupFailureReason.TargetWriteFailed`.
    - Test 2: If a stable Windows-only UnauthorizedAccess simulation can be made without machine-specific ACL assumptions, add `CopyAsync_AccessDenied_ReturnsAccessDenied`; otherwise use a fake copier in `BackupServiceTests` to assert `RestorePluginAsync` maps `BackupFailureReason.AccessDenied` to row display reason `Access denied`.
    - Verification: targeted backup service and copier tests pass together.
  </behavior>
  <action>Add coverage for target write failure in `BackupFileCopierTests`. Add access-denied restore mapping coverage using the existing `CapturingBackupFileCopier`/fake copier pattern if direct filesystem ACL simulation is not reliable on Windows CI. The access-denied test must assert the restore row display reason is exactly `Access denied`. Do not introduce machine-specific permission changes that can fail on developer machines.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~BackupServiceTests|FullyQualifiedName~BackupFileCopierTests"</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC.Tests/Services/BackupFileCopierTests.cs` contains `CopyAsync_DestinationDirectoryMissing_ReturnsTargetWriteFailed`.
    - Either `AutoQAC.Tests/Services/BackupFileCopierTests.cs` or `AutoQAC.Tests/Services/BackupServiceTests.cs` contains `Access denied`.
    - The combined targeted `dotnet test` command exits 0.
  </acceptance_criteria>
  <done>TEST-04 has automated coverage for restore permission/access-denied mapping, target write failure mapping, malicious metadata, retention progress, and cleanup deletion failure after retry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backup metadata -> filesystem restore | `session.json` fields drive source and destination paths for file copy. |
| retention service -> progress UI | Service progress values become visible operation progress text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07-01 | Tampering | `BackupService.RestorePluginRowAsync` | mitigate | Validate `FileName` is a simple file name and resolved backup path stays under selected `sessionDir` before copying. |
| T-07-07-02 | Elevation of Privilege | `BackupService.RestorePluginRowAsync` | mitigate | Reject unrooted/invalid restore targets and normalize rooted target paths before overwrite attempts. |
| T-07-07-03 | Repudiation | `BackupService.CleanupOldSessionsAsync` | mitigate | Report retention row/count progress and warning rows so cleanup deletion failures are visible and testable. |
</threat_model>

<verification>
Run `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~BackupServiceTests|FullyQualifiedName~BackupFileCopierTests"`, then run the Phase 7 targeted cluster if time allows: `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~BackupServiceTests|FullyQualifiedName~BackupFileCopierTests|FullyQualifiedName~ProgressViewModelTests"`.
</verification>

<success_criteria>
- Restore source paths from backup metadata cannot traverse or use absolute `FileName` values.
- Restore targets are validated before overwrite and unsafe targets fail with concise row reasons.
- Retention cleanup emits data-flowing count progress.
- TEST-04 coverage includes malicious metadata, access/write failure, and cleanup deletion failure after retry.
</success_criteria>

<output>
After completion, create `.planning/phases/07-backup-restore-retention-safety/07-07-SUMMARY.md`.
</output>
