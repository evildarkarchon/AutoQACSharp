---
phase: 07-backup-restore-retention-safety
plan: 09
type: execute
wave: 7
depends_on: [07-08]
files_modified:
  - AutoQAC/Services/Backup/BackupService.cs
  - AutoQAC/ViewModels/RestoreViewModel.cs
  - AutoQAC.Tests/Services/BackupServiceTests.cs
  - AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs
autonomous: true
gap_closure: true
requirements: [SAF-04, TEST-04, PERF-04]
user_setup: []

must_haves:
  truths:
    - "Backup creation rejects rooted, traversing, or multi-segment PluginInfo.FileName values before constructing a destination path."
    - "Restore metadata rejects arbitrary rooted targets by enforcing shared plugin file-name, local-drive rooted target, and extension policy before copy or directory creation."
    - "Legacy synchronous restore callers are audited and either use the shared validation helper or are documented as test-only compatibility paths."
    - "Restore session aggregate status and inline UI copy preserve cancellation visibility when failed and canceled rows are mixed."
  artifacts:
    - path: "AutoQAC/Services/Backup/BackupService.cs"
      provides: "Shared simple plugin-name validation, backup destination containment, restore target metadata validation, and mixed cancellation aggregate status"
      contains: "IsSafeSessionRelativeName"
    - path: "AutoQAC/ViewModels/RestoreViewModel.cs"
      provides: "Accurate inline summary copy for mixed failed+canceled restore sessions"
      contains: "Restore canceled"
    - path: "AutoQAC.Tests/Services/BackupServiceTests.cs"
      provides: "Regression tests for backup traversal, arbitrary restore targets, filename mismatches, positive safe restore, sync compatibility, and mixed failed+canceled status"
      contains: "BackupPluginAsync_FileNameTraversal_ReturnsStructuredFailureAndDoesNotCopy"
    - path: "AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs"
      provides: "ViewModel copy regression for failed+canceled aggregate cancellation"
      contains: "RestoreAllCommand_FailedThenCanceledResult_ShowsCanceledSummaryWithFailedCount"
  key_links:
    - from: "AutoQAC/Services/Backup/BackupService.cs"
      to: "IBackupFileCopier.CopyAsync"
      via: "ValidateBackupDestination / ValidateRestoreEntry using shared IsSafeSessionRelativeName before copy calls"
      pattern: "IsSafeSessionRelativeName.*ValidateBackupDestination|IsSafeSessionRelativeName.*ValidateRestoreEntry"
    - from: "AutoQAC.Tests/Services/BackupServiceTests.cs"
      to: "BackupService.GetRestoreStatus behavior"
      via: "RestoreSessionAsync mixed failed/canceled regression"
      pattern: "RestoreSessionAsync_FailedThenCanceledRows_ReturnsCanceled"
    - from: "AutoQAC/ViewModels/RestoreViewModel.cs"
      to: "BackupRestoreResult.Status"
      via: "BuildRestoreSummaryText canceled branch includes failed/canceled counts"
      pattern: "Restore canceled:.*failed.*canceled"
---

<objective>
Close the remaining Phase 7 filesystem-safety and restore aggregate-status gaps found by verification, incorporating cross-AI review feedback for Plan 07-09 before execution.

Purpose: Phase 7 cannot be considered complete while untrusted plugin names or backup metadata can redirect file operations outside intended locations, while legacy sync behavior can drift from async safety, or while mixed cancellation outcomes are hidden behind a failed aggregate title.
Output: One focused gap-closure implementation and regression test update for `BackupService` and restore inline result copy.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-backup-restore-retention-safety/07-VERIFICATION.md
@.planning/phases/07-backup-restore-retention-safety/07-REVIEWS.md
@.planning/phases/07-backup-restore-retention-safety/07-08-SUMMARY.md
@AutoQAC/Services/Backup/BackupService.cs
@AutoQAC/ViewModels/RestoreViewModel.cs
@AutoQAC.Tests/Services/BackupServiceTests.cs
@AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs

<interfaces>
From `AutoQAC/Services/Backup/IBackupService.cs`:
```csharp
BackupResult BackupPlugin(PluginInfo plugin, string sessionDir);
Task<BackupCreateResult> BackupPluginAsync(PluginInfo plugin, string sessionDir, IProgress<BackupCopyProgress>? progress = null, CancellationToken ct = default);
void RestorePlugin(BackupPluginEntry entry, string sessionDir);
Task<BackupRestoreResult> RestorePluginAsync(BackupPluginEntry entry, string sessionDir, IProgress<BackupCopyProgress>? progress = null, CancellationToken ct = default);
Task<BackupRestoreResult> RestoreSessionAsync(BackupSession session, IProgress<BackupCopyProgress>? progress = null, CancellationToken ct = default);
```

From `AutoQAC/Models/BackupOperationResults.cs`:
```csharp
public enum BackupOperationStatus { Complete, Partial, Failed, Canceled, Warning }
public enum BackupFailureReason { SourceMissing, MissingBackupFile, AccessDenied, TargetFolderCreationFailed, TargetWriteFailed, Canceled, CleanupDeletionFailed }
public sealed record BackupCreateResult(BackupOperationStatus Status, string PluginName, long BytesCopied, long? TotalBytes, BackupFailureReason? FailureReason);
public sealed record BackupRestoreResult(BackupOperationStatus Status, IReadOnlyList<BackupRestoreRowResult> Rows);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject unsafe backup destination file names</name>
  <files>AutoQAC/Services/Backup/BackupService.cs, AutoQAC.Tests/Services/BackupServiceTests.cs</files>
  <read_first>
    - AutoQAC/Services/Backup/BackupService.cs
    - AutoQAC.Tests/Services/BackupServiceTests.cs
    - .planning/phases/07-backup-restore-retention-safety/07-VERIFICATION.md
    - .planning/phases/07-backup-restore-retention-safety/07-REVIEWS.md
  </read_first>
  <behavior>
    - `BackupPlugin` with `PluginInfo.FileName = "..\\outside.esp"` returns `Success == false`, does not create `outside.esp` outside `sessionDir`, and does not copy into the session.
    - `BackupPluginAsync` with `PluginInfo.FileName = "..\\outside.esp"` returns `BackupOperationStatus.Failed`, uses `BackupFailureReason.SourceMissing`, and does not call `IBackupFileCopier.CopyAsync`.
    - `BackupPluginAsync` with a rooted `PluginInfo.FileName` returns failed before copy and leaves the session directory contained.
  </behavior>
  <action>
    Addresses review concerns: centralize path and metadata validation helpers; keep source-grep checks secondary to named behavior tests.

    Add failing tests first in `BackupServiceTests` named exactly `BackupPlugin_FileNameTraversal_ReturnsFailureAndDoesNotEscapeSession`, `BackupPluginAsync_FileNameTraversal_ReturnsStructuredFailureAndDoesNotCopy`, and `BackupPluginAsync_RootedFileName_ReturnsStructuredFailureAndDoesNotCopy`.

    Then update `BackupService` so both `BackupPlugin` and `BackupPluginAsync` validate `plugin.FileName` before `Path.Combine(sessionDir, plugin.FileName)`. Create a private helper with XML docs named `IsSafeSessionRelativeName(string fileName, bool requirePluginExtension)` that returns `false` for null/whitespace names, rooted names, names where `Path.GetFileName(fileName) != fileName`, names containing `Path.DirectorySeparatorChar`, `Path.AltDirectorySeparatorChar`, or `':'`, and names with non-plugin extensions when `requirePluginExtension` is `true`. Create `ValidateBackupDestination` with XML docs and call `IsSafeSessionRelativeName(plugin.FileName, requirePluginExtension: false)` before resolving `Path.GetFullPath(Path.Combine(sessionRoot, plugin.FileName))`. The helper must return the resolved destination path only after it starts with normalized `sessionRoot` plus a trailing separator. Keep user-facing failure text concise (`"Invalid plugin file name for backup."` for sync) and log technical details through `_logger` without exposing a full copy destination to UI results. For async failures return `new BackupCreateResult(BackupOperationStatus.Failed, plugin.FileName, 0, null, BackupFailureReason.SourceMissing)` and do not call `_fileCopier.CopyAsync`. Use `SourceMissing` here as an internal compatibility mapping only; do not add new user-visible failure copy outside the UI-SPEC allow-list.
  </action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~BackupPlugin_FileNameTraversal_ReturnsFailureAndDoesNotEscapeSession|FullyQualifiedName~BackupPluginAsync_FileNameTraversal_ReturnsStructuredFailureAndDoesNotCopy|FullyQualifiedName~BackupPluginAsync_RootedFileName_ReturnsStructuredFailureAndDoesNotCopy"</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Services/Backup/BackupService.cs` contains `IsSafeSessionRelativeName`.
    - `AutoQAC/Services/Backup/BackupService.cs` contains `ValidateBackupDestination`.
    - `AutoQAC/Services/Backup/BackupService.cs` contains `Path.GetFullPath(Path.Combine(sessionRoot, plugin.FileName))`.
    - `AutoQAC/Services/Backup/BackupService.cs` contains `BackupFailureReason.SourceMissing` in the async unsafe-file-name failure path.
    - `AutoQAC.Tests/Services/BackupServiceTests.cs` contains the three exact test method names listed above.
  </acceptance_criteria>
  <done>Backup creation cannot escape the selected session directory through malformed `PluginInfo.FileName`, and both sync and async backup paths have regression tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Constrain restore target metadata before overwrite</name>
  <files>AutoQAC/Services/Backup/BackupService.cs, AutoQAC.Tests/Services/BackupServiceTests.cs</files>
  <read_first>
    - AutoQAC/Services/Backup/BackupService.cs
    - AutoQAC.Tests/Services/BackupServiceTests.cs
    - .planning/phases/07-backup-restore-retention-safety/07-VERIFICATION.md
    - .planning/phases/07-backup-restore-retention-safety/07-REVIEWS.md
  </read_first>
  <behavior>
    - `RestorePluginAsync` rejects an arbitrary rooted `OriginalPath` when `Path.GetFileName(OriginalPath)` does not equal `entry.FileName` before `IBackupFileCopier.CopyAsync` runs.
    - `RestorePluginAsync` rejects non-plugin restore target extensions (`.txt`, `.json`, `.dll`) before copy.
    - `RestorePluginAsync` accepts normal local plugin paths whose `OriginalPath` file name equals `entry.FileName` and extension is `.esm`, `.esp`, or `.esl`.
    - Legacy `RestorePlugin` applies the same metadata safety gate before `File.Exists`, `Directory.CreateDirectory`, or `File.Copy`, while preserving `FileNotFoundException` for valid metadata with a missing backup file.
  </behavior>
  <action>
    Addresses review concerns: audit legacy sync callers before changing `RestorePlugin`; make the stricter restore policy explicit; add positive regression coverage; document unsupported ghosted-plugin and alternate-path metadata cases in the plan summary rather than silently accepting them.

    Add failing tests first in `BackupServiceTests` named exactly `RestorePluginAsync_OriginalPathFileNameMismatch_ReturnsTargetFolderCreationFailedAndDoesNotCopy`, `RestorePluginAsync_NonPluginOriginalPathExtension_ReturnsTargetFolderCreationFailedAndDoesNotCopy`, `RestorePluginAsync_NormalPluginPath_StillRestoresSuccessfully`, `RestorePlugin_FileNameMismatch_ThrowsBeforeCopying`, and `RestorePlugin_MissingBackupFile_WithSafeMetadata_StillThrowsFileNotFoundException`.

    Before production edits, audit remaining sync restore callers with `Select-String -Path AutoQAC/**/*.cs,AutoQAC.Tests/**/*.cs -Pattern 'RestorePlugin\('`. Expected current result: production UI uses async restore paths; direct `RestorePlugin(` calls are only `BackupService` itself and `BackupServiceTests`. If another production caller exists, keep that caller's expected exception behavior by adding a named regression test before changing the helper. Then update `ValidateRestoreEntry` and legacy `RestorePlugin` to call shared validation helpers before any copy or target-directory creation. Keep the existing backup `FileName` session-containment checks and add target checks: `entry.OriginalPath` must be a normal local drive rooted path with `Path.GetPathRoot(targetPath)` matching the Windows drive-root shape `X:\`, must not start with `\\`, `\\?\`, or `\\.\`, `Path.GetFileName(entry.OriginalPath)` must equal `entry.FileName` using `StringComparison.OrdinalIgnoreCase`, and both `entry.FileName` and `entry.OriginalPath` must have an approved plugin extension `.esm`, `.esp`, or `.esl` using case-insensitive comparison. Reject `entry.FileName` values ending in `.esp.ghost` in this phase because the approved Phase 7 restore policy only allows active plugin extensions. Return `BackupFailureReason.TargetFolderCreationFailed` for target metadata policy failures so the existing concise UI label remains `Target folder creation failed`. Apply equivalent safety to legacy `RestorePlugin` by calling the same validation helper before `File.Exists`, `Directory.CreateDirectory`, or `File.Copy`; if validation fails, throw `InvalidOperationException("Backup metadata is not safe to restore.")` and do not copy. Preserve existing `FileNotFoundException` behavior when validation passes but the backup file is missing.
  </action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~RestorePluginAsync_OriginalPathFileNameMismatch_ReturnsTargetFolderCreationFailedAndDoesNotCopy|FullyQualifiedName~RestorePluginAsync_NonPluginOriginalPathExtension_ReturnsTargetFolderCreationFailedAndDoesNotCopy|FullyQualifiedName~RestorePluginAsync_NormalPluginPath_StillRestoresSuccessfully|FullyQualifiedName~RestorePlugin_FileNameMismatch_ThrowsBeforeCopying|FullyQualifiedName~RestorePlugin_MissingBackupFile_WithSafeMetadata_StillThrowsFileNotFoundException"</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Services/Backup/BackupService.cs` contains `IsSafeSessionRelativeName` and restore validation calls it with `requirePluginExtension: true`.
    - `AutoQAC/Services/Backup/BackupService.cs` contains `Path.GetPathRoot` and rejects UNC/device roots before restore copy.
    - `AutoQAC/Services/Backup/BackupService.cs` contains `Path.GetFileName(entry.OriginalPath)` and `StringComparison.OrdinalIgnoreCase` in restore target validation.
    - `AutoQAC/Services/Backup/BackupService.cs` contains `Backup metadata is not safe to restore.` in the legacy restore validation path.
    - `AutoQAC.Tests/Services/BackupServiceTests.cs` contains the five exact restore target safety and compatibility test method names listed above.
  </acceptance_criteria>
  <done>Async and legacy restore reject unsafe target metadata before overwrite attempts, close the arbitrary rooted target and filename-mismatch gaps, preserve missing-backup exception compatibility for safe legacy metadata, and prove a normal plugin restore still succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve mixed cancellation aggregate status and run final verification</name>
  <files>AutoQAC/Services/Backup/BackupService.cs, AutoQAC/ViewModels/RestoreViewModel.cs, AutoQAC.Tests/Services/BackupServiceTests.cs, AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs</files>
  <read_first>
    - AutoQAC/Services/Backup/BackupService.cs
    - AutoQAC/ViewModels/RestoreViewModel.cs
    - AutoQAC.Tests/Services/BackupServiceTests.cs
    - AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs
    - AutoQAC/Models/BackupOperationResults.cs
    - .planning/phases/07-backup-restore-retention-safety/07-VERIFICATION.md
    - .planning/phases/07-backup-restore-retention-safety/07-REVIEWS.md
  </read_first>
  <behavior>
    - A restore session with at least one failed row, at least one canceled row, and zero restored rows returns aggregate `BackupOperationStatus.Canceled`.
    - A restore session with restored rows plus failed/canceled rows still returns `BackupOperationStatus.Partial`.
    - The restore window inline summary for failed+canceled/no-restored rows says `Restore canceled` and includes both failed and canceled counts.
    - Full solution tests remain green after the gap closure.
  </behavior>
  <action>
    Addresses review concerns: enumerate aggregate-status blast radius, update UI copy alongside status semantics, and keep the no-parallel xEdit source scan as a secondary invariant rather than primary proof.

    Add a failing service test first named exactly `RestoreSessionAsync_FailedThenCanceledRows_ReturnsCanceled`. Use a custom `IBackupFileCopier` in the test that returns `BackupCopyResult.Failed(..., BackupFailureReason.TargetWriteFailed)` on the first copy and cancels a shared `CancellationTokenSource`; the second plugin row should then be added as canceled by the loop before copying. Assert `result.Status == BackupOperationStatus.Canceled`, `result.FailedCount == 1`, and `result.CanceledCount == 1`. Before changing `GetRestoreStatus`, inspect existing `BackupServiceTests` and `RestoreViewModelTests` cases that assert `BackupOperationStatus.Failed` or `Restore Failed`; update only cases whose input is failed+canceled/no-restored. Preserve existing tests where every row failed and no row canceled.

    Add a failing ViewModel test in `RestoreViewModelTests` named exactly `RestoreAllCommand_FailedThenCanceledResult_ShowsCanceledSummaryWithFailedCount`. Stub the backup service to return `new BackupRestoreResult(BackupOperationStatus.Canceled, [...failed row..., ...canceled row...])`, execute restore-all through the existing command/test helper pattern, and assert `RestoreOutcomeTitle == "Restore Canceled"` and `RestoreSummaryText` contains `0 restored, 1 failed, 1 canceled`. Update `GetRestoreStatus` so aggregate status rules are exactly: empty/all restored => `Complete`; any restored plus any non-restored => `Partial`; no restored and any canceled => `Canceled`; otherwise => `Failed`. Keep row-level `DisplayReason` mapping unchanged. If `BuildRestoreSummaryText` already includes failed and canceled counts in the canceled branch, leave the copy unchanged; otherwise update the canceled branch to include `{result.RestoredCount} restored, {result.FailedCount} failed, {result.CanceledCount} canceled` and the existing sentence `Partial files were removed and completed/failed/canceled rows remain visible.`

    Run the targeted backup service tests, restore ViewModel tests, and the full solution test suite. Preserve the sequential xEdit invariant: do not edit `CleaningOrchestrator` or introduce `Task.WhenAll`, `Parallel.ForEachAsync`, or `Task.Run` under `AutoQAC/Services/Cleaning`. Treat the source scan for those patterns as a secondary invariant after behavior tests pass; if it matches pre-existing unrelated code, document the match in `07-09-SUMMARY.md` instead of changing unrelated files.
  </action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~BackupServiceTests|FullyQualifiedName~RestoreViewModelTests"; dotnet test AutoQACSharp.slnx</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC.Tests/Services/BackupServiceTests.cs` contains `RestoreSessionAsync_FailedThenCanceledRows_ReturnsCanceled`.
    - `AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs` contains `RestoreAllCommand_FailedThenCanceledResult_ShowsCanceledSummaryWithFailedCount`.
    - `AutoQAC/Services/Backup/BackupService.cs` contains `rows.Any(row => row.Status == BackupRestoreRowStatus.Canceled)` inside `GetRestoreStatus`.
    - `AutoQAC/ViewModels/RestoreViewModel.cs` canceled summary includes restored, failed, and canceled counts.
    - `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~BackupServiceTests"` exits 0.
    - `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~RestoreViewModelTests"` exits 0.
    - `dotnet test AutoQACSharp.slnx` exits 0.
  </acceptance_criteria>
  <done>Mixed failed+canceled restore sessions keep cancellation visible in the aggregate service result and restore-window copy, and all Phase 7 backup service, restore ViewModel, and solution tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backup metadata -> filesystem | `BackupPluginEntry.FileName` and `OriginalPath` from `session.json` influence copy source/target paths. |
| plugin discovery/config -> backup filesystem | `PluginInfo.FileName` influences backup destination path inside the session directory. |
| service results -> UI | Backup/restore failures become concise user-visible labels. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09-01 | Tampering | `BackupService.BackupPlugin*` | mitigate | Validate `PluginInfo.FileName` is simple/non-rooted and resolved destination stays under normalized `sessionDir` before copy. |
| T-07-09-02 | Tampering | `BackupService.ValidateRestoreEntry` | mitigate | Require backup metadata file name and restore target file name to match, reject UNC/device-rooted targets and alternate data stream markers, and use `.esm`/`.esp`/`.esl` before overwrite. |
| T-07-09-03 | Information Disclosure | restore/backup UI results | mitigate | Return concise `BackupFailureReason` labels; keep full path/exception diagnostics in logs only. |
| T-07-09-04 | Denial of Service | restore cancellation aggregation | mitigate | Preserve canceled aggregate status when cancellation stops remaining restore rows after failures. |
| T-07-09-05 | Tampering | symlink/reparse-point targets inside user-writable directories | accept | Phase 7 validates metadata strings and local-drive rooted target policy; resolving NTFS reparse points requires broader filesystem identity checks outside the verified Phase 7 gaps. |
</threat_model>

<verification>
Run, in order:

1. `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~BackupServiceTests"`
2. `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~RestoreViewModelTests"`
3. `dotnet test AutoQACSharp.slnx`
4. Secondary source invariant check in PowerShell: `Select-String -Path AutoQAC/Services/Cleaning/*.cs -Pattern 'Task\.WhenAll|Parallel\.ForEachAsync|Task\.Run'` returns no matches or only documented pre-existing unrelated matches.
</verification>

<success_criteria>
- All three `07-VERIFICATION.md` gaps are covered by tests and implementation.
- Backup creation is session-contained for sync and async paths.
- Restore target metadata cannot redirect overwrites through filename mismatch, UNC/device-rooted paths, alternate-data-stream markers, or non-plugin extension paths.
- Mixed failed+canceled restore sessions return aggregate `Canceled` when no plugins restored and display accurate inline restore-window copy.
- Full solution tests pass.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | Clear restore/backup failure reporting and cancellation while preserving sequential xEdit cleaning | 09 | COVERED | Closes remaining verification blockers without touching cleaning sequence.
REQ | SAF-04 | Restore backups with clear failure reporting for missing/permission/partial cases | 09 | COVERED | Adds target metadata policy and mixed cancellation aggregate status.
REQ | TEST-04 | Verify backup restore safety across missing target, permission, partial, cleanup deletion failures | 09 | COVERED | Adds missing traversal/rooted/mismatch/mixed cancellation regressions.
REQ | PERF-04 | Cancellable visible backup/retention without parallel xEdit cleaning | 09 | COVERED | Preserves existing cancellation and no parallel xEdit invariant.
VERIFICATION | Gap 1 | Backup creation cannot write outside selected backup session directory | 09 | COVERED | Task 1.
VERIFICATION | Gap 2 | Restore metadata cannot redirect restores to arbitrary rooted filesystem targets | 09 | COVERED | Task 2.
VERIFICATION | Gap 3 | Restore cancellation outcomes remain distinguishable when failures and cancellations are mixed | 09 | COVERED | Task 3.
CONTEXT | D-01-D-20 | Phase 7 locked decisions | 01-09 | COVERED | Plan 09 only closes remaining D-03/D-04/D-07/D-08 safety and cancellation verification gaps; no deferred ideas.
REVIEWS | C1 | Legacy sync restore exception/caller audit | 09 | COVERED | Task 2 audits sync callers, preserves safe-metadata missing-backup exception behavior, and shares validation helpers.
REVIEWS | C2 | Mixed failed+canceled aggregate semantics and UI copy | 09 | COVERED | Task 3 updates service aggregation and restore-window summary tests.
REVIEWS | Consensus | Centralized validation and positive safe restore regression | 09 | COVERED | Tasks 1-2 share `IsSafeSessionRelativeName`; Task 2 adds `RestorePluginAsync_NormalPluginPath_StillRestoresSuccessfully`.
REVIEWS | Consensus | Source-grep checks are secondary to behavior tests | 09 | COVERED | Task 3 runs named tests first and treats no-parallel source scan as secondary invariant.
REVIEWS | Consider | Ghosted plugin extensions, reparse-point escape, half-deleted retention sessions, unique temp-file names | 09 | DEFERRED | Not required by 07-VERIFICATION gaps; executor must document these as explicit Phase 7 exclusions/remaining risks in `07-09-SUMMARY.md` if unchanged.
</source_audit>

<output>
After completion, create `.planning/phases/07-backup-restore-retention-safety/07-09-SUMMARY.md`.
</output>
