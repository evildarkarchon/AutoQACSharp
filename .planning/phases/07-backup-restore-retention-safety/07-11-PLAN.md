---
phase: 07-backup-restore-retention-safety
plan: 11
type: tdd
wave: 8
depends_on: [07-09]
files_modified:
  - AutoQAC/Services/Backup/IBackupService.cs
  - AutoQAC/Services/Backup/BackupService.cs
  - AutoQAC/ViewModels/RestoreViewModel.cs
  - AutoQAC/Views/MainWindow.axaml.cs
  - AutoQAC.Tests/Services/BackupServiceTests.cs
  - AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs
autonomous: true
gap_closure: true
requirements: [SAF-04, TEST-04, PERF-04]
must_haves:
  truths:
    - "Restore metadata cannot redirect restores to arbitrary rooted filesystem targets outside the current game Data folder."
    - "Restore UI passes the configured game Data folder as the trusted restore root for selected and all restore operations."
  artifacts:
    - path: "AutoQAC/Services/Backup/IBackupService.cs"
      provides: "Trusted restore root parameter on restore contracts"
      contains: "trustedRestoreRoot"
    - path: "AutoQAC/Services/Backup/BackupService.cs"
      provides: "Normalized trusted-root containment validation before target directory creation or copy"
      contains: "IsRestoreTargetInsideTrustedRoot"
    - path: "AutoQAC/ViewModels/RestoreViewModel.cs"
      provides: "RestoreWindow data-folder root propagation"
      contains: "_trustedRestoreRoot"
    - path: "AutoQAC.Tests/Services/BackupServiceTests.cs"
      provides: "Regression tests for same-name plugin targets outside trusted root"
      contains: "RestorePluginAsync_OriginalPathOutsideTrustedRoot_ReturnsTargetFolderCreationFailedAndDoesNotCopy"
  key_links:
    - from: "MainWindow.axaml.cs"
      to: "RestoreViewModel.LoadSessionsAsync"
      via: "current `Configuration.GameDataFolder`"
      pattern: "LoadSessionsAsync\(dataFolderPath\)"
    - from: "RestoreViewModel"
      to: "IBackupService.RestoreSessionAsync"
      via: "trustedRestoreRoot argument"
      pattern: "RestoreSessionAsync\(session, _trustedRestoreRoot"
    - from: "BackupService.ValidateRestoreEntry"
      to: "Directory.CreateDirectory / IBackupFileCopier.CopyAsync"
      via: "trusted-root containment check before filesystem writes"
      pattern: "IsRestoreTargetInsideTrustedRoot"
---

<objective>
Close the verification gap where tampered restore metadata can overwrite a same-named `.esm`, `.esp`, or `.esl` file outside the approved restore root.

Purpose: Restore operations must only overwrite plugins under the current game Data folder passed from the restore UI, while keeping legacy service callers safe by failing closed when no trusted root is provided.
Output: Restore contracts accept a trusted restore root, `BackupService` enforces containment, `RestoreViewModel` passes the configured Data folder, and tests prove out-of-root same-name targets are rejected before directory creation/copy.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-backup-restore-retention-safety/07-CONTEXT.md
@.planning/phases/07-backup-restore-retention-safety/07-VERIFICATION.md
@.planning/phases/07-backup-restore-retention-safety/07-09-SUMMARY.md
@AutoQAC/Services/Backup/IBackupService.cs
@AutoQAC/Services/Backup/BackupService.cs
@AutoQAC/ViewModels/RestoreViewModel.cs
@AutoQAC/Views/MainWindow.axaml.cs
@AutoQAC.Tests/Services/BackupServiceTests.cs
@AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs

<interfaces>
From `RestoreViewModel.LoadSessionsAsync`:
```csharp
public async Task LoadSessionsAsync(string? dataFolderPath)
```

From current restore calls:
```csharp
await _backupService.RestorePluginAsync(plugin, session.SessionDirectory, progress, cts.Token);
await _backupService.RestoreSessionAsync(session, progress, cts.Token);
```

Required contract after this plan:
```csharp
Task<BackupRestoreResult> RestorePluginAsync(
    BackupPluginEntry entry,
    string sessionDir,
    string? trustedRestoreRoot,
    IProgress<BackupCopyProgress>? progress = null,
    CancellationToken ct = default);

Task<BackupRestoreResult> RestoreSessionAsync(
    BackupSession session,
    string? trustedRestoreRoot,
    IProgress<BackupCopyProgress>? progress = null,
    CancellationToken ct = default);
```
</interfaces>
</context>

<feature>
  <name>Trusted restore root containment</name>
  <files>AutoQAC/Services/Backup/IBackupService.cs, AutoQAC/Services/Backup/BackupService.cs, AutoQAC/ViewModels/RestoreViewModel.cs, AutoQAC/Views/MainWindow.axaml.cs, AutoQAC.Tests/Services/BackupServiceTests.cs, AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs</files>
  <behavior>
    RED cases:
    - Same-name `.esp` target outside `trustedRestoreRoot` returns a failed restore row with `TargetFolderCreationFailed`, does not create the outside target directory, and does not invoke `IBackupFileCopier.CopyAsync`.
    - Same-name `.esp` target inside `trustedRestoreRoot` restores successfully.
    - `RestoreViewModel.LoadSessionsAsync(dataFolderPath)` stores that path and passes it into both selected and all restore service calls.
  </behavior>
  <implementation>
    Add `trustedRestoreRoot` to restore contracts and validation. Normalize with `Path.GetFullPath`, ensure a trailing separator, and require `targetPath.StartsWith(trustedRoot, StringComparison.OrdinalIgnoreCase)`. Treat null/empty/invalid trusted roots as unsafe and return `TargetFolderCreationFailed` before `Directory.CreateDirectory` or `_fileCopier.CopyAsync`. Preserve existing filename, extension, local-drive-root, session-contained backup source, and concise reason behavior.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — prove same-name restore targets outside trusted root are rejected</name>
  <files>AutoQAC.Tests/Services/BackupServiceTests.cs, AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs</files>
  <read_first>AutoQAC.Tests/Services/BackupServiceTests.cs, AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs, AutoQAC/Services/Backup/IBackupService.cs, AutoQAC/ViewModels/RestoreViewModel.cs, .planning/phases/07-backup-restore-retention-safety/07-VERIFICATION.md</read_first>
  <behavior>
    - Add `RestorePluginAsync_OriginalPathOutsideTrustedRoot_ReturnsTargetFolderCreationFailedAndDoesNotCopy`: backup file `Backup.esp` exists in the session, trusted root is `_testRoot/Data`, `OriginalPath` is `_testRoot/Outside/Backup.esp`; expect failed row, `TargetFolderCreationFailed`, copier call count 0, and no outside directory/file created.
    - Add or update a positive test so `RestorePluginAsync_NormalPluginPath_StillRestoresSuccessfully` passes `trustedRestoreRoot: Path.GetDirectoryName(targetPath)!` or a parent Data directory that contains the target.
    - Add RestoreViewModel verification that after `LoadSessionsAsync(dataFolderPath)`, selected restore and restore-all call the backup service overload with that exact `dataFolderPath` as `trustedRestoreRoot`.
  </behavior>
  <action>Add failing tests only. Update test calls to the new intended restore signatures where necessary in test code, but do not implement production behavior yet. Use NSubstitute optional-parameter matching explicitly: match `Arg.Any<string?>()` for `trustedRestoreRoot`, `Arg.Any<IProgress<BackupCopyProgress>>()`, and `Arg.Any<CancellationToken>()`.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~RestorePluginAsync_OriginalPathOutsideTrustedRoot_ReturnsTargetFolderCreationFailedAndDoesNotCopy|FullyQualifiedName~RestoreViewModelTests"</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC.Tests/Services/BackupServiceTests.cs` contains `RestorePluginAsync_OriginalPathOutsideTrustedRoot_ReturnsTargetFolderCreationFailedAndDoesNotCopy`.
    - `AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs` contains assertions that `trustedRestoreRoot` equals the `dataFolderPath` passed to `LoadSessionsAsync`.
    - Targeted tests fail before production changes because the interface/signature/containment implementation is not present.
  </acceptance_criteria>
  <done>Tests describe trusted-root restore containment and UI root propagation before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — enforce trusted restore root before filesystem writes</name>
  <files>AutoQAC/Services/Backup/IBackupService.cs, AutoQAC/Services/Backup/BackupService.cs, AutoQAC/ViewModels/RestoreViewModel.cs, AutoQAC/Views/MainWindow.axaml.cs</files>
  <read_first>AutoQAC/Services/Backup/IBackupService.cs, AutoQAC/Services/Backup/BackupService.cs, AutoQAC/ViewModels/RestoreViewModel.cs, AutoQAC/Views/MainWindow.axaml.cs, AutoQAC.Tests/Services/BackupServiceTests.cs, AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs</read_first>
  <behavior>
    - Async and sync restore paths reject out-of-root metadata before directory creation or copy.
    - RestoreViewModel stores the configured Data folder from `LoadSessionsAsync` in `_trustedRestoreRoot` and passes it to restore selected/all calls.
    - The stricter policy still recreates missing target directories inside the trusted root when possible.
  </behavior>
  <action>Update `IBackupService` and `BackupService` restore signatures to include `string? trustedRestoreRoot` before progress/cancellation parameters for async restore methods and in the legacy sync `RestorePlugin`/`RestoreSession` methods. Update `RestorePluginRowAsync` and `ValidateRestoreEntry` to require the trusted root. Add helper `IsRestoreTargetInsideTrustedRoot(string targetPath, string? trustedRestoreRoot)` that returns false for null/empty/invalid roots and checks the normalized target path starts with the normalized trusted root plus a trailing directory separator. In `RestoreViewModel`, add a private `_trustedRestoreRoot` field, set it in `LoadSessionsAsync(dataFolderPath)`, and pass it to `_backupService.RestorePluginAsync` and `_backupService.RestoreSessionAsync`. `MainWindow.axaml.cs` already passes `Configuration.GameDataFolder` into `LoadSessionsAsync`; keep that flow intact.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~BackupServiceTests|FullyQualifiedName~RestoreViewModelTests|FullyQualifiedName~BackupOperationResultTests"</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Services/Backup/IBackupService.cs` contains `string? trustedRestoreRoot` on restore contracts.
    - `AutoQAC/Services/Backup/BackupService.cs` contains `IsRestoreTargetInsideTrustedRoot` and calls it before `Directory.CreateDirectory` and `_fileCopier.CopyAsync`.
    - `AutoQAC/ViewModels/RestoreViewModel.cs` contains `_trustedRestoreRoot` and passes it into both restore service calls.
    - Targeted BackupService, RestoreViewModel, and BackupOperationResult tests exit 0.
  </acceptance_criteria>
  <done>Restore operations fail closed outside the configured Data folder and still restore safe in-root plugins.</done>
</task>

<task type="auto">
  <name>Task 3: Verify Phase 7 gap closure invariants</name>
  <files>AutoQAC.Tests/Services/BackupServiceTests.cs, AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs</files>
  <read_first>AutoQAC.Tests/Services/BackupServiceTests.cs, AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs, AutoQAC/Services/Backup/BackupService.cs, AutoQAC/ViewModels/RestoreViewModel.cs</read_first>
  <action>Run the targeted restore safety tests and then the full solution test suite. Also run a source scan under `AutoQAC/Services/Cleaning` for `Task.WhenAll`, `Parallel.ForEachAsync`, or `Task.Run` to reconfirm this restore safety change did not alter the sequential xEdit invariant.</action>
  <verify>
    <automated>dotnet test AutoQACSharp.slnx</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test AutoQACSharp.slnx` exits 0.
    - Source scan reports no `Task.WhenAll`, `Parallel.ForEachAsync`, or `Task.Run` under `AutoQAC/Services/Cleaning`.
    - Summary includes the targeted restore-root test names and full-solution test result.
  </acceptance_criteria>
  <done>Both restore trusted-root gap and overall Phase 7 regression suite are verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backup session metadata -> local filesystem overwrite | `BackupPluginEntry.OriginalPath` comes from `session.json` and must be treated as untrusted before restore writes. |
| UI configuration -> restore service | `Configuration.GameDataFolder` is the trusted root supplied by the current app configuration to constrain restore targets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11-01 | Tampering | `BackupService.ValidateRestoreEntry` | mitigate | Require normalized restore target containment under `trustedRestoreRoot` before directory creation or copy. |
| T-07-11-02 | Elevation of Privilege | `RestorePluginAsync` / `RestoreSessionAsync` | mitigate | Fail closed when trusted root is null, empty, invalid, or not a prefix of the normalized target path. |
| T-07-11-03 | Information Disclosure | restore failure UI rows | mitigate | Continue returning concise `TargetFolderCreationFailed` labels; technical path details stay in logs/tests. |
</threat_model>

<verification>
Run targeted service/ViewModel tests, then `dotnet test AutoQACSharp.slnx`, then source-scan cleaning services for parallel xEdit constructs.
</verification>

<success_criteria>
- Same-name `.esp/.esm/.esl` restore targets outside the current game Data folder are rejected before target directory creation/copy.
- Restore selected and restore all pass the configured Data folder into the service as `trustedRestoreRoot`.
- Safe in-root restores still recreate missing target directories and complete successfully.
- Full solution tests pass and sequential xEdit cleaning remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/07-backup-restore-retention-safety/07-11-SUMMARY.md`.
</output>
