---
phase: 07-backup-restore-retention-safety
plan: 04
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - AutoQAC/ViewModels/RestoreViewModel.cs
  - AutoQAC/Views/RestoreWindow.axaml
  - AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs
autonomous: true
requirements: [SAF-04, TEST-04, PERF-04]
must_haves:
  truths:
    - "D-17/D-18: Restore Selected and Restore All require overwrite confirmation with session timestamp and plugin count/name."
    - "D-19/D-20: restore results stay inline in the restore window for success, partial, failed, and canceled outcomes without extra success popups or auto-close."
    - "D-07/D-09/D-11: restore copy progress and Cancel Restore are visible in the restore window."
  artifacts:
    - path: "AutoQAC/ViewModels/RestoreViewModel.cs"
      provides: "restore confirmation, progress, cancellation, and inline result rows"
      contains: "CancelRestore"
    - path: "AutoQAC/Views/RestoreWindow.axaml"
      provides: "lower inline progress/result area preserving two-pane layout"
      contains: "Cancel Restore"
  key_links:
    - from: "RestoreViewModel"
      to: "IBackupService.RestoreSessionAsync"
      via: "Restore All command awaits structured result"
      pattern: "RestoreSessionAsync"
---

<objective>
Implement RestoreWindow confirmation, progress, cancellation, and inline result reporting against the structured restore APIs.

Purpose: Restore safety is user-facing; users need overwrite confirmation and inspectable complete/partial/failed/canceled rows without raw technical details.
Output: Restore ViewModel/window changes plus ViewModel tests.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-backup-restore-retention-safety/07-CONTEXT.md
@.planning/phases/07-backup-restore-retention-safety/07-RESEARCH.md
@.planning/phases/07-backup-restore-retention-safety/07-UI-SPEC.md
@.planning/phases/07-backup-restore-retention-safety/07-02-SUMMARY.md
@AutoQAC/ViewModels/RestoreViewModel.cs
@AutoQAC/Views/RestoreWindow.axaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire restore confirmations and inline result rows</name>
  <read_first>AutoQAC/ViewModels/RestoreViewModel.cs; AutoQAC/Views/RestoreWindow.axaml; AutoQAC/Services/UI/IMessageDialogService.cs; .planning/phases/07-backup-restore-retention-safety/07-UI-SPEC.md</read_first>
  <files>AutoQAC/ViewModels/RestoreViewModel.cs; AutoQAC/Views/RestoreWindow.axaml; AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs</files>
  <behavior>
    - Restore Selected confirmation copy matches UI-SPEC: `Restore Selected: Restore {plugin} from {session timestamp}? This overwrites the current plugin file with the backup copy.`
    - Restore All confirmation copy matches UI-SPEC and states AutoQAC continues past individual failures.
    - Successful/partial/failed rows remain visible inline and no success popup is shown per D-19/D-20.
  </behavior>
  <action>Addresses review concerns: legacy missing-backup test migration, non-fragile title assertions, command-state correctness, and success-popup avoidance. Add bindable restore result state to `RestoreViewModel`: `RestoreOutcomeTitle`, `RestoreSummaryText`, `ObservableCollection<BackupRestoreRowResult> RestoreResults`, `IsRestoreResultVisible`, and `IsRestoreActive`. Before `RestorePluginAsync`, show the Restore Selected confirmation with plugin filename and selected session timestamp per D-17/D-18. Before `RestoreAllAsync`, show the Restore All confirmation with count and timestamp per D-17/D-18 and text that AutoQAC will continue past individual failures. Replace legacy throwing calls with `await RestorePluginAsync`/`RestoreSessionAsync`; update or remove obsolete ViewModel tests that expected `RestorePlugin_MissingBackupFile_ThrowsFileNotFoundException`-style sync exceptions, replacing them with structured-result tests that assert inline `Missing backup file` rows. Map aggregate result to titles exactly `Restore Complete`, `Restore Partial`, `Restore Failed`, or `Restore Canceled` in ViewModel code and tests. Do not call `ShowErrorAsync` for structured partial/failed restore results; show inline row list and a log-reference sentence for failed outcomes. Add tests proving successful restore does not call `ShowErrorAsync` or a success popup, and partial restore keeps `IsRestoreResultVisible == true`. Update AXAML by preserving the two-pane layout and adding a lower result/progress area with ItemsControl row statuses `Restored`, `Failed`, and `Canceled`; the exact title strings may live in ViewModel resources/properties rather than literal AXAML text.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~RestoreViewModelTests</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/ViewModels/RestoreViewModel.cs` contains `RestoreOutcomeTitle` and `RestoreResults`.
    - `AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs` contains tests asserting exact titles `Restore Complete`, `Restore Partial`, `Restore Failed`, and `Restore Canceled` from the ViewModel.
    - `AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs` contains confirmation tests for `Restore Selected` and `Restore All`, plus a structured missing-backup-file row test replacing any sync-exception ViewModel expectation.
  </acceptance_criteria>
  <done>Restore selected/all require confirmation and structured outcomes render inline without success popups or raw exception details.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add restore progress and cancellation behavior</name>
  <read_first>AutoQAC/ViewModels/RestoreViewModel.cs; AutoQAC/Views/RestoreWindow.axaml; AutoQAC/Models/BackupOperationResults.cs</read_first>
  <files>AutoQAC/ViewModels/RestoreViewModel.cs; AutoQAC/Views/RestoreWindow.axaml; AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs</files>
  <behavior>
    - While restore is active, restore/delete/refresh actions are disabled for the session per UI-SPEC.
    - Cancel Restore is visible, has at least 44px effective hit height, and cancels the active CTS per D-07/D-11.
    - Progress text includes counts and bytes when available: `Restoring 2 / 8 plugins — 38.4 MB / 120.0 MB` per D-10/UI-SPEC, using decimal MB with one fractional digit.
    - Disposing the ViewModel disposes the active restore cancellation source per research.
  </behavior>
  <action>Addresses review concerns: byte formatting, non-brittle hit-height verification, refresh race prevention, and CTS lifecycle. Add a private `CancellationTokenSource? _restoreCts` owned by `RestoreViewModel`. Create `CancelRestoreCommand` that calls `_restoreCts.Cancel()` and sets status text to the UI-SPEC cancellation semantics. Disable `RestorePluginCommand`, `RestoreAllCommand`, `DeleteSessionCommand`, and `LoadSessionsCommand` while `IsRestoreActive` is true by updating their `CanExecute` methods and `[NotifyCanExecuteChangedFor]` attributes for every affected command; this prevents refresh/session mutation while an active restore owns `_restoreCts`. Pass an `IProgress<BackupCopyProgress>` to service restore calls and update `RestoreProgressText`, `RestoreBytesCopied`, and `RestoreTotalBytes`. Add a small private/static formatter `FormatBytes(long bytes)` returning decimal units with one fractional digit for MB and use it so progress text matches `Restoring 2 / 8 plugins — 38.4 MB / 120.0 MB`; do not add a new NuGet dependency for byte formatting. In `finally`, dispose `_restoreCts`, set it to null, set `IsRestoreActive = false`, and raise command can-execute changes so a later restore can run. Update AXAML to show a progress panel and `Cancel Restore` button only while restore is active; use spacing multiples from the UI-SPEC for new controls and preserve existing legacy margins elsewhere. Ensure the cancel button has at least a 44px effective hit height via explicit `MinHeight="44"` or padding/row sizing that tests can verify through ViewModel/view text where feasible.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~RestoreViewModelTests</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/ViewModels/RestoreViewModel.cs` contains `CancellationTokenSource` and `CancelRestoreCommand`.
    - `AutoQAC/Views/RestoreWindow.axaml` contains `Cancel Restore`; hit height is implemented with `MinHeight="44"` or documented padding/row sizing that yields at least 44px effective height.
    - `AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs` contains tests named with `CancelRestore`, `DisablesCommands`, `LoadSessionsDisabledWhileRestoreActive`, `FormatBytes`, and `DisposeClearsRestoreCancellationSource`.
  </acceptance_criteria>
  <done>Restore operations are visibly cancellable from RestoreWindow and preserve inline results/progress.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service result→restore UI | File-system failures become visible restore rows. |
| user confirmation→overwrite | User approval gates destructive overwrite actions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | R | RestoreViewModel | mitigate | Require explicit confirmation copy before overwrite for selected/all restore paths. |
| T-07-06 | I | RestoreWindow | mitigate | Bind only concise row reasons from structured result models; do not surface exception text/full paths. |
</threat_model>

<verification>
Run `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~RestoreViewModelTests`.
</verification>

<success_criteria>
RestoreWindow preserves the two-pane layout, adds inline progress/results, confirms destructive restore actions, and supports visible restore cancellation.
</success_criteria>

<output>
After completion, create `.planning/phases/07-backup-restore-retention-safety/07-04-SUMMARY.md`.
</output>
