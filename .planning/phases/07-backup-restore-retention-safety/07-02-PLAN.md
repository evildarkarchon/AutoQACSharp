---
phase: 07-backup-restore-retention-safety
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - AutoQAC/Services/Backup/BackupService.cs
  - AutoQAC/Services/Backup/IBackupService.cs
  - AutoQAC/Services/Backup/IBackupSessionDeleter.cs
  - AutoQAC/Services/Backup/DirectoryBackupSessionDeleter.cs
  - AutoQAC/Infrastructure/ServiceCollectionExtensions.cs
  - AutoQAC.Tests/Services/BackupServiceTests.cs
autonomous: true
requirements: [SAF-04, TEST-04, PERF-04]
must_haves:
  truths:
    - "D-01/D-03: Restore All continues past individual plugin failures and reports Complete, Partial, Failed, or Canceled with counts and failed plugin names."
    - "D-02/D-04: missing target folders are recreated when possible; creation/write failures become concise per-plugin failure reasons."
    - "D-14/D-15/D-16: retention never deletes the current session, keeps newest MaxSessions, retries deletion once, and reports canceled/deletion-failure counts."
  artifacts:
    - path: "AutoQAC/Services/Backup/BackupService.cs"
      provides: "async structured backup, restore, and retention behavior"
      contains: "RestoreSessionAsync"
  key_links:
    - from: "AutoQAC/Services/Backup/BackupService.cs"
      to: "AutoQAC/Services/Backup/IBackupFileCopier.cs"
      via: "restore/backup copy delegates to cancellable copier"
      pattern: "CopyAsync"
---

<objective>
Implement the async structured restore and retention service APIs using the contracts from Plan 07-01.

Purpose: Restore safety and retention cleanup must become recoverable batch operations instead of thrown exceptions and logging-only cleanup failures.
Output: `BackupService` async behavior plus expanded service tests for restore and retention safety.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-backup-restore-retention-safety/07-CONTEXT.md
@.planning/phases/07-backup-restore-retention-safety/07-RESEARCH.md
@.planning/phases/07-backup-restore-retention-safety/07-01-SUMMARY.md
@AutoQAC/Services/Backup/BackupService.cs
@AutoQAC/Services/Backup/IBackupService.cs
@AutoQAC.Tests/Services/BackupServiceTests.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement structured async restore outcomes</name>
  <read_first>AutoQAC/Services/Backup/BackupService.cs; AutoQAC/Models/BackupOperationResults.cs; AutoQAC.Tests/Services/BackupServiceTests.cs</read_first>
  <files>AutoQAC/Services/Backup/BackupService.cs; AutoQAC/Infrastructure/ServiceCollectionExtensions.cs; AutoQAC.Tests/Services/BackupServiceTests.cs</files>
  <behavior>
    - Restore All attempts every `BackupPluginEntry` even when earlier entries fail per D-01.
    - Missing target directories are created when possible per D-02.
    - Aggregate state is `Complete` when all restore, `Partial` when at least one restored and at least one failed/canceled, `Failed` when none restored and at least one failed, and `Canceled` when cancellation is requested before remaining work completes per D-03/D-07.
    - Failure rows contain plugin name and concise reason only per D-04.
    - Canceling or failing a restore overwrite never removes or truncates the pre-existing target plugin file per D-08 and review feedback.
  </behavior>
  <action>Addresses review concerns: atomic restore semantics, legacy wrapper contract/deadlock risk, DI constructor change, and caller-specific failure mapping. Update `BackupService` constructor to accept `IBackupFileCopier` and `ILoggingService`, and update DI registration if constructor dependencies require it. Implement `BackupPluginAsync` using the copier with `BackupCopyExistingTargetPolicy.FailIfExists`, mapping copier `SourceMissing` to the existing plugin-source backup failure semantics instead of the restore-only label `Missing backup file`. Keep legacy `BackupPlugin` on its existing synchronous implementation path until Plan 07-03 migrates the orchestrator; do not bridge sync to async with `.Result`, `.Wait()`, or `.GetAwaiter().GetResult()`. Implement `RestorePluginAsync` and `RestoreSessionAsync`: build backup path with `Path.Combine(sessionDir, entry.FileName)`, create `Path.GetDirectoryName(entry.OriginalPath)` when non-empty, map directory creation failures to `Target folder creation failed`, and call the copier with `BackupCopyExistingTargetPolicy.ReplaceAtomically` so restore copies to `entry.OriginalPath + ".autoqac-tmp"` and atomically moves into place only after a complete temp copy. Cancellation/failure deletes the temp file only and leaves the existing plugin file unchanged. Aggregate rows without throwing for expected file-system failures. Keep legacy `RestorePlugin`/`RestoreSession` compatibility methods preserving existing exception behavior for tests such as `RestorePlugin_MissingBackupFile_ThrowsFileNotFoundException`; implement them through direct synchronous file operations or explicit exception mapping, not by blocking on async tasks. Plan 07-04 migrates UI callers to async structured APIs.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~BackupServiceTests</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Services/Backup/BackupService.cs` contains `RestoreSessionAsync` and `foreach (var entry in session.Plugins)`.
    - `AutoQAC.Tests/Services/BackupServiceTests.cs` contains `RestoreSessionAsync_ContinuesAfterPluginFailure`.
    - `AutoQAC.Tests/Services/BackupServiceTests.cs` contains `RestorePluginAsync_CanceledAtomicRestore_PreservesExistingTarget`.
    - `AutoQAC.Tests/Services/BackupServiceTests.cs` keeps or rewrites `RestorePlugin_MissingBackupFile_ThrowsFileNotFoundException` so legacy exception behavior is explicit until all sync callers are removed.
    - `AutoQAC.Tests/Services/BackupServiceTests.cs` contains assertions for `BackupOperationStatus.Partial`, `Missing backup file`, and `Target folder creation failed`.
  </acceptance_criteria>
  <done>Restore service returns complete/partial/failed/canceled results with per-plugin rows and does not stop at the first recoverable failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement structured async retention cleanup</name>
  <read_first>AutoQAC/Services/Backup/BackupService.cs; AutoQAC/Models/BackupOperationResults.cs; AutoQAC.Tests/Services/BackupServiceTests.cs</read_first>
  <files>AutoQAC/Services/Backup/BackupService.cs; AutoQAC/Services/Backup/IBackupSessionDeleter.cs; AutoQAC/Services/Backup/DirectoryBackupSessionDeleter.cs; AutoQAC/Infrastructure/ServiceCollectionExtensions.cs; AutoQAC.Tests/Services/BackupServiceTests.cs</files>
  <behavior>
    - Current session is never deleted per D-14.
    - Newest configured `MaxSessions` among non-current sessions are kept per D-14.
    - Delete failures retry once and then report kept/not-deleted sessions per D-15.
    - Cancellation stops remaining deletion work and reports deleted/skipped/remaining counts per D-16.
    - Only valid AutoQAC backup session directories are eligible for retention deletion; unrelated/malformed directories under the backup root are kept and reported skipped per review feedback.
  </behavior>
  <action>Addresses review concerns: retention deletion eligibility, retry delay/cancellation handling, deterministic test seam, and `MaxSessions` semantics. Create `IBackupSessionDeleter.DeleteAsync(string sessionDirectory, CancellationToken ct)` plus `DirectoryBackupSessionDeleter` wrapping `Directory.Delete(path, recursive: true)`, and register it with DI. Implement `CleanupOldSessionsAsync` in `BackupService`. Enumerate candidate directories by loading valid backup session metadata only; directories missing readable session metadata are not deletion candidates and produce a skipped/kept row rather than being deleted. Normalize and exclude `currentSessionDir` from deletion candidates before retention counting. Keep the newest configured `maxSessionCount` valid non-current sessions; current session is always additionally protected, so total retained can be `maxSessionCount + 1` when current is outside the newest set. Treat `maxSessionCount <= 0` as keeping no non-current valid sessions while still protecting current. Delete only older valid candidates. For each delete candidate, call `IBackupSessionDeleter.DeleteAsync`; on `IOException` or `UnauthorizedAccessException`, wait `Task.Delay(250, ct)` and retry once to better survive antivirus/indexer locks. If cancellation occurs before or during the retry delay, catch `OperationCanceledException` when `ct.IsCancellationRequested`, stop remaining deletion work, and return `Canceled` with deleted/skipped/remaining counts instead of a cleanup-failed row. If retry fails, add a `BackupRetentionRowResult` with `BackupRetentionRowStatus.Failed` and reason `Cleanup deletion failed`, keep the directory, and return aggregate `Warning` if cleaning otherwise succeeded. Keep legacy `CleanupOldSessions` compatibility behavior without blocking on async UI paths; if it remains sync, use a direct synchronous helper or document it as non-UI compatibility-only and migrate known callers in Plan 07-03.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~BackupServiceTests</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Services/Backup/BackupService.cs` contains `CleanupOldSessionsAsync` and `BackupOperationStatus.Warning`.
    - `AutoQAC/Services/Backup/IBackupSessionDeleter.cs` contains `DeleteAsync` and `AutoQAC/Infrastructure/ServiceCollectionExtensions.cs` registers the deleter implementation.
    - `AutoQAC.Tests/Services/BackupServiceTests.cs` contains tests named `CleanupOldSessionsAsync_ProtectsCurrentSession`, `CleanupOldSessionsAsync_KeepsNewestMaxSessions`, `CleanupOldSessionsAsync_SkipsMalformedSessionDirectory`, `CleanupOldSessionsAsync_RetryDelayCancellation_ReturnsCanceled`, and `CleanupOldSessionsAsync_ReportsCanceled`.
    - `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~BackupServiceTests` exits 0.
  </acceptance_criteria>
  <done>Retention cleanup produces structured complete/warning/canceled results and keeps all sessions required by D-14 through D-16.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backup metadata→restore target | Backup entries specify original paths where files will be overwritten. |
| backup root→retention delete | Directory enumeration decides which local sessions are deleted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | T/I | BackupService.RestoreSessionAsync | mitigate | Recreate only the target entry directory, map expected failures to fixed labels, and log technical exception details without exposing raw paths in results. |
| T-07-03 | T/D | BackupService.CleanupOldSessionsAsync | mitigate | Exclude `currentSessionDir` before delete selection and keep newest `MaxSessions`; retry once then keep failed-delete sessions. |
</threat_model>

<verification>
Run `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~BackupServiceTests`.
</verification>

<success_criteria>
`BackupService` implements structured async restore and retention outcomes satisfying D-01 through D-04 and D-13 through D-16 with targeted tests.
</success_criteria>

<output>
After completion, create `.planning/phases/07-backup-restore-retention-safety/07-02-SUMMARY.md`.
</output>
