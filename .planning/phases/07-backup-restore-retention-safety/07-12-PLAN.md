---
phase: 07-backup-restore-retention-safety
plan: 12
type: tdd
wave: 9
depends_on: [07-11]
files_modified:
  - AutoQAC/Views/MainWindow.axaml.cs
  - AutoQAC.Tests/Views/ViewSubscriptionLifecycleTests.cs
autonomous: true
gap_closure: true
requirements: [PERF-04, TEST-04]
must_haves:
  truths:
    - "Normal cleaning progress window result Close button closes the window instead of leaving a completed progress surface open."
    - "Normal cleaning progress window disposal releases ProgressViewModel subscriptions when the window closes."
    - "Backup/retention progress visibility from D-09 and non-xEdit cancel affordances from D-11 do not leave a stale progress ViewModel alive after completion."
  artifacts:
    - path: "AutoQAC/Views/MainWindow.axaml.cs"
      provides: "Normal ShowProgressAsync lifecycle wiring for ProgressViewModel.CloseRequested and disposal"
      contains: "progressViewModel.CloseRequested +="
    - path: "AutoQAC.Tests/Views/ViewSubscriptionLifecycleTests.cs"
      provides: "Regression coverage for normal progress window lifecycle wiring"
      contains: "MainWindowShowProgressAsync_ShouldWireProgressWindowCloseAndDisposal"
  key_links:
    - from: "ProgressViewModel.CloseCommand"
      to: "normal ProgressWindow instance"
      via: "CloseRequested handler in ShowProgressAsync"
      pattern: "progressViewModel\.CloseRequested \+= .*progressWindow\.Close"
    - from: "normal ProgressWindow close"
      to: "ProgressViewModel.Dispose"
      via: "Closed handler in ShowProgressAsync or ProgressWindow lifecycle contract"
      pattern: "progressViewModel\.Dispose\(\)"
---

<objective>
Close the verification gap where the normal cleaning progress window can display completed results but its Close command does not have explicit normal-path close/disposal wiring.

Purpose: PERF-04 requires visible cancellable backup/retention progress without stale UI subscriptions after completion. This gap closure ensures the normal progress-window path has the same lifecycle guarantees as the preview path and the ProgressWindow lifecycle helper.
Output: Source-level regression coverage plus normal `ShowProgressAsync` lifecycle wiring that closes the window from `ProgressViewModel.CloseRequested` and disposes the ViewModel when the window closes.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-backup-restore-retention-safety/07-CONTEXT.md
@.planning/phases/07-backup-restore-retention-safety/07-VERIFICATION.md
@.planning/phases/07-backup-restore-retention-safety/07-05-SUMMARY.md
@AutoQAC/Views/MainWindow.axaml.cs
@AutoQAC/Views/ProgressWindow.axaml.cs
@AutoQAC/ViewModels/ProgressViewModel.cs
@AutoQAC.Tests/Views/ViewSubscriptionLifecycleTests.cs

<interfaces>
From `ProgressViewModel`:
```csharp
public event EventHandler? CloseRequested;
```

From current `MainWindow.ShowProgressAsync`:
```csharp
var progressViewModel = new ProgressViewModel(_stateService, _orchestrator, _uiDispatcher);
var progressWindow = new ProgressWindow
{
    DataContext = progressViewModel
};

progressWindow.Show(this);
```

Current `ProgressWindow.axaml.cs` already subscribes to `CloseRequested` when `DataContext` changes and disposes the subscribed ViewModel in `OnClosed`. This plan still adds explicit normal-path wiring in `ShowProgressAsync` because `07-VERIFICATION.md` flagged the primary creation path and because the preview path already has explicit `CloseRequested` wiring.
</interfaces>
</context>

<feature>
  <name>Normal progress window lifecycle gap closure</name>
  <files>AutoQAC/Views/MainWindow.axaml.cs, AutoQAC.Tests/Views/ViewSubscriptionLifecycleTests.cs</files>
  <behavior>
    RED cases:
    - `MainWindowShowProgressAsync_ShouldWireProgressWindowCloseAndDisposal` reads `AutoQAC/Views/MainWindow.axaml.cs` and requires `ShowProgressAsync` to contain `progressViewModel.CloseRequested +=` and `progressWindow.Close()` after the normal progress ViewModel/window are created.
    - The same test requires the normal progress path to dispose `progressViewModel` from a `progressWindow.Closed +=` handler, with a local disposal guard so CloseRequested and Closed cannot double-dispose.
    - Existing `ProgressWindow_ShouldUnsubscribePreviousViewModel_AndGuardDoubleDispose` remains green; do not remove or weaken the centralized ProgressWindow cleanup behavior.
  </behavior>
  <implementation>
    Add explicit lifecycle wiring inside `ShowProgressAsync` immediately after constructing `progressWindow` and before `progressWindow.Show(this)`. Use a local `var progressDisposed = false;` and a local function such as `void DisposeProgressViewModel()` that returns when `progressDisposed` is true, then sets the guard and calls `progressViewModel.Dispose()`. Subscribe `progressViewModel.CloseRequested += (_, _) => progressWindow.Close();` so the result Close command closes the normal progress window. Subscribe `progressWindow.Closed += (_, _) => DisposeProgressViewModel();` so closing the normal progress window disposes subscriptions. Preserve the existing `ProgressWindow` DataContext lifecycle helper and preview path behavior; this plan only closes the normal `ShowProgressAsync` verification gap.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — prove normal progress window lifecycle wiring is missing</name>
  <files>AutoQAC.Tests/Views/ViewSubscriptionLifecycleTests.cs</files>
  <read_first>AutoQAC.Tests/Views/ViewSubscriptionLifecycleTests.cs, AutoQAC/Views/MainWindow.axaml.cs, AutoQAC/Views/ProgressWindow.axaml.cs, AutoQAC/ViewModels/ProgressViewModel.cs, .planning/phases/07-backup-restore-retention-safety/07-VERIFICATION.md</read_first>
  <behavior>
    - Add `MainWindowShowProgressAsync_ShouldWireProgressWindowCloseAndDisposal`.
    - The test must read `AutoQAC/Views/MainWindow.axaml.cs` using the existing `GetRepoFilePath` helper.
    - The test must assert the source contains `progressViewModel.CloseRequested +=`, `progressWindow.Close()`, `progressWindow.Closed +=`, `progressViewModel.Dispose()`, and a double-dispose guard string such as `progressDisposed`.
  </behavior>
  <action>Add only the failing lifecycle regression test. Do not modify production code in this task. Keep the existing source-inspection testing style in `ViewSubscriptionLifecycleTests.cs`; this repository does not currently have a separate Avalonia.Headless test project. Reference the verification gap truth exactly: normal `ShowProgressAsync` must wire `CloseRequested` to close the window and dispose the `ProgressViewModel` when the window closes.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~ViewSubscriptionLifecycleTests</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC.Tests/Views/ViewSubscriptionLifecycleTests.cs` contains `MainWindowShowProgressAsync_ShouldWireProgressWindowCloseAndDisposal`.
    - The new test asserts exact strings `progressViewModel.CloseRequested +=`, `progressWindow.Close()`, `progressWindow.Closed +=`, `progressViewModel.Dispose()`, and `progressDisposed`.
    - The targeted test command fails before Task 2 production changes because `ShowProgressAsync` lacks the asserted normal-path lifecycle strings.
  </acceptance_criteria>
  <done>Lifecycle regression coverage exists and fails for the current normal progress-window gap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — wire normal progress window close and disposal</name>
  <files>AutoQAC/Views/MainWindow.axaml.cs</files>
  <read_first>AutoQAC/Views/MainWindow.axaml.cs, AutoQAC/Views/ProgressWindow.axaml.cs, AutoQAC/ViewModels/ProgressViewModel.cs, AutoQAC.Tests/Views/ViewSubscriptionLifecycleTests.cs</read_first>
  <behavior>
    - Normal cleaning progress window closes when `ProgressViewModel.CloseCommand` raises `CloseRequested`.
    - Normal cleaning progress window closing disposes the created `ProgressViewModel` exactly once.
    - Existing `ProgressWindow` lifecycle cleanup remains intact.
  </behavior>
  <action>In `ShowProgressAsync`, after creating `progressViewModel` and `progressWindow`, add a local boolean `progressDisposed` and a local function `DisposeProgressViewModel()` that returns when `progressDisposed` is true, sets it true otherwise, and calls `progressViewModel.Dispose()`. Add `progressViewModel.CloseRequested += (_, _) => progressWindow.Close();` and `progressWindow.Closed += (_, _) => DisposeProgressViewModel();` before `progressWindow.Show(this)`. Add a short WHY comment explaining the local guard: the ViewModel Close command and the window Closed event can both participate in the same close path, and disposal must be idempotent. Do not remove existing comments. Do not alter xEdit Stop/ForceStop semantics or backup/retention cancel command routing.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~ViewSubscriptionLifecycleTests</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Views/MainWindow.axaml.cs` contains `progressViewModel.CloseRequested += (_, _) => progressWindow.Close();`.
    - `AutoQAC/Views/MainWindow.axaml.cs` contains `progressWindow.Closed +=` and `progressViewModel.Dispose();` inside the normal progress path.
    - `AutoQAC/Views/MainWindow.axaml.cs` contains `progressDisposed` guarding disposal.
    - `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~ViewSubscriptionLifecycleTests` exits 0.
  </acceptance_criteria>
  <done>The normal progress-window lifecycle gap from `07-VERIFICATION.md` is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ViewModel event -> Avalonia window lifecycle | `ProgressViewModel.CloseRequested` crosses from testable ViewModel state into view-owned window closing and disposal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12-01 | Denial of Service | `MainWindow.ShowProgressAsync` | mitigate | Dispose the normal progress ViewModel on window close so subscriptions do not keep processing state changes after the surface is gone. |
| T-07-12-02 | Tampering | progress lifecycle event wiring | accept | Local UI event wiring does not mutate filesystem, process launch, or backup metadata; regression test verifies expected wiring. |
</threat_model>

<verification>
Run the source-level lifecycle test cluster, then include this plan in the Phase 7 targeted cluster during final gap verification.
</verification>

<success_criteria>
- Normal progress result Close command closes the window through `CloseRequested`.
- Closing the normal progress window disposes the created `ProgressViewModel` exactly once.
- View lifecycle regression tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/07-backup-restore-retention-safety/07-12-SUMMARY.md`.
</output>
