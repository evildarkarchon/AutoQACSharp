---
phase: 11-user-facing-diagnostics-boundaries
plan: 04
type: tdd
wave: 2
depends_on: [11-01]
files_modified:
  - AutoQAC/Services/Cleaning/PluginResultFinalizer.cs
  - AutoQAC/Models/CleaningSessionResult.cs
  - AutoQAC/Models/PluginCleaningResult.cs
  - AutoQAC.Tests/Services/Cleaning/PluginResultFinalizerTests.cs
  - AutoQAC.Tests/Models/CleaningSessionResultTests.cs
autonomous: true
requirements: [SEC-01]
must_haves:
  truths:
    - "D-09: Failed result rows show plugin filenames and safe summaries/latest-log guidance only."
    - "D-10: Failed PluginCleaningResult.Message values are sanitized at source before reaching result windows or CleaningSessionResult.GenerateReport()."
    - "D-11: xEdit exception-log content is converted to a safe xEdit failure message and never appears in result rows or reports."
    - "D-12: Exported cleaning reports include one short disclaimer and do not repeat technical details."
  artifacts:
    - path: "AutoQAC/Services/Cleaning/PluginResultFinalizer.cs"
      provides: "Source sanitization for PluginCleaningResult.Message and LogParseWarning"
    - path: "AutoQAC/Models/CleaningSessionResult.cs"
      provides: "Defensive report disclaimer and failed-row safe fallback"
    - path: "AutoQAC/Models/PluginCleaningResult.cs"
      provides: "Result-row summary safety projection"
  key_links:
    - from: "PluginResultFinalizer"
      to: "CleaningSessionResult.GenerateReport"
      via: "PluginCleaningResult.Message"
      pattern: "PluginCleaningResult.*Message"
---

<objective>
Harden cleaning result rows and exported reports.

Purpose: D-09 through D-12 require failed plugin messages to be safe at source before they reach result windows or `GenerateReport()`, with report-level defensive checks and a one-line disclaimer.
Output: Tested safe plugin failure messages, xEdit exception-log handling, result summaries, and exported report text.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-SPEC.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-CONTEXT.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-PATTERNS.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-UI-SPEC.md
@.planning/phases/08-cleaning-orchestrator-decomposition/08-05-SUMMARY.md
@.planning/phases/08-cleaning-orchestrator-decomposition/08-08-SUMMARY.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-01-SUMMARY.md
@AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Safe finalizer messages for failed and xEdit exception-log outcomes</name>
  <read_first>
    - AutoQAC/Services/Cleaning/PluginResultFinalizer.cs
    - AutoQAC.Tests/Services/Cleaning/PluginResultFinalizerTests.cs
    - AutoQAC/Models/PluginCleaningResult.cs
    - AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs
    - .planning/phases/11-user-facing-diagnostics-boundaries/11-UI-SPEC.md
  </read_first>
  <files>AutoQAC/Services/Cleaning/PluginResultFinalizer.cs, AutoQAC.Tests/Services/Cleaning/PluginResultFinalizerTests.cs</files>
  <behavior>
    - Runner failure message `Process failed at C:\Users\Alice\Tools\SSEEdit.exe -QAC` becomes `Plugin.esp: Cleaning failed. See the latest AutoQAC log.` in `PluginCleaningResult.Message`.
    - xEdit exception-log content `EAccessViolation at C:\Games\Skyrim\Data\Plugin.esp` forces failed status and sets `Message` and `LogParseWarning` to exactly `xEdit reported an error for Plugin.esp. See the latest AutoQAC log.`.
    - Logger output records only that xEdit reported an error for the plugin filename at this boundary; exception-log content is intentionally omitted from user-facing/result-boundary logs because this phase does not redact xEdit-owned exception-log content at rest.
  </behavior>
  <action>Add failing finalizer tests for unsafe runner failure messages and xEdit exception-log content. Then update `PluginResultFinalizer` so failed result messages preserve already-safe, actionable messages when `DiagnosticTextFormatter.SafeFailureSummary(result.Message, fallback)` returns the original safe summary, and only fall back to `DiagnosticTextFormatter.CleaningFailedForPlugin(plugin.FileName)` when `result.Message` is empty or unsafe. Replace exception-log user-facing `logParseWarning = logResult.ExceptionContent` with `DiagnosticTextFormatter.XEditReportedError(plugin.FileName)` per D-11. Change xEdit exception-log logging to a safe structured message such as `logger.Warning("xEdit reported an exception log for {Plugin}; user-facing details were suppressed.", plugin.FileName);` and do not include `{Content}` or `logResult.ExceptionContent` as a log property in this boundary plan. Addresses review concern: xEdit exception-log content is intentionally not copied into AutoQAC log properties here; the xEdit-owned log remains the local troubleshooting source, while AutoQAC records that the boundary was triggered. Preserve Phase 08 status/success fixes: AlreadyClean promotion still requires successful cleaned result and `Success` derives from final status.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~PluginResultFinalizerTests --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Services/Cleaning/PluginResultFinalizer.cs` contains `DiagnosticTextFormatter.CleaningFailedForPlugin`.
    - `AutoQAC/Services/Cleaning/PluginResultFinalizer.cs` contains `DiagnosticTextFormatter.XEditReportedError`.
    - `AutoQAC/Services/Cleaning/PluginResultFinalizer.cs` does not contain `xEdit exception log for {Plugin}: {Content}`.
    - Finalizer tests assert no result `Message` or `LogParseWarning` contains `C:\Users\Alice`, `-QAC`, or `EAccessViolation`.
    - Focused PluginResultFinalizerTests pass.
  </acceptance_criteria>
  <done>Finalizer creates safe source messages for failed rows and xEdit exception-log outcomes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Defensive report disclaimer and failed-row safety</name>
  <read_first>
    - AutoQAC/Models/CleaningSessionResult.cs
    - AutoQAC/Models/PluginCleaningResult.cs
    - AutoQAC.Tests/Models/CleaningSessionResultTests.cs
    - AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs
    - .planning/phases/11-user-facing-diagnostics-boundaries/11-UI-SPEC.md
  </read_first>
  <files>AutoQAC/Models/CleaningSessionResult.cs, AutoQAC/Models/PluginCleaningResult.cs, AutoQAC.Tests/Models/CleaningSessionResultTests.cs</files>
  <behavior>
    - `GenerateReport()` includes `Technical details are intentionally kept in AutoQAC logs and are not repeated in this report.` exactly once.
    - A failed plugin with unsafe message `System.InvalidOperationException at C:\Users\Alice\Tools\SSEEdit.exe -QAC` renders in reports as one non-duplicated safe failed-row line that includes `Fail.esp` and `Cleaning failed. See the latest AutoQAC log.` but excludes unsafe sentinels.
    - `PluginCleaningResult.Summary` for failed unsafe messages uses a safe fallback and excludes unsafe sentinel text.
  </behavior>
  <action>Add failing model tests for report disclaimer exact-once behavior, defensive failed-row fallback, non-duplicated plugin-name formatting, stack-frame sentinel ` at AutoQAC.`, and `PluginCleaningResult.Summary` unsafe message fallback. Update `CleaningSessionResult.GenerateReport()` to append `DiagnosticTextFormatter.ReportDisclaimer` once near the top of the report before failed plugin sections. When writing failed plugin rows, pass `result.Message` through `DiagnosticTextFormatter.SafeFailureSummary(result.Message, DiagnosticTextFormatter.CleaningFailedForPlugin(result.PluginName))`; if the safe/fallback message already starts with the plugin filename and colon, write it once rather than prefixing `{PluginName}:` again. Update `PluginCleaningResult.Summary` for failed status to use the same safe summary fallback instead of interpolating unsafe `Message` directly. Preserve plugin filenames, counts, durations, status words, and existing report sections.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~CleaningSessionResultTests --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Models/CleaningSessionResult.cs` contains `DiagnosticTextFormatter.ReportDisclaimer`.
    - `AutoQAC/Models/CleaningSessionResult.cs` contains `DiagnosticTextFormatter.SafeFailureSummary`.
    - `AutoQAC/Models/PluginCleaningResult.cs` contains `DiagnosticTextFormatter.SafeFailureSummary`.
    - Model tests assert generated reports do not contain `System.InvalidOperationException`, `C:\Users\Alice`, `-QAC`, or ` at AutoQAC.`.
    - Focused CleaningSessionResultTests pass.
  </acceptance_criteria>
  <done>Reports and result summaries preserve safe facts and omit technical diagnostic details.</done>
</task>

</tasks>

<verification>
Run finalizer and model focused tests, then `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~PluginResultFinalizerTests|FullyQualifiedName~CleaningSessionResultTests" --nologo`.
</verification>

<success_criteria>
Result rows and reports satisfy D-09, D-10, D-11, D-12 and SEC-01: plugin filenames and safe summaries remain, unsafe paths/commands/stacks/raw exception content do not.
</success_criteria>

<output>
After completion, create `.planning/phases/11-user-facing-diagnostics-boundaries/11-04-SUMMARY.md`.
</output>
