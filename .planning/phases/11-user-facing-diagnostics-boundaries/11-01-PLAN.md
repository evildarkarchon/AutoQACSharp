---
phase: 11-user-facing-diagnostics-boundaries
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs
  - AutoQAC.Tests/Models/DiagnosticTextFormatterTests.cs
autonomous: true
requirements: [SEC-01, SEC-02]
must_haves:
  truths:
    - "User-facing diagnostic copy can be built from safe operation, setting, folder, plugin, and report helpers without raw exception text, stack traces, full paths, or command fragments."
    - "Displayed basenames preserve useful names such as Plugin.esp, SSEEdit.exe, and plugins.txt while neutralizing control characters, quotes, backticks, and empty names."
    - "Logs and reports have shared safe wording constants for latest AutoQAC log guidance and the export disclaimer."
  artifacts:
    - path: "AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs"
      provides: "Shared diagnostics formatting boundary used by UI, service, model, and log-call sites"
      exports: ["DiagnosticTextFormatter"]
    - path: "AutoQAC.Tests/Models/DiagnosticTextFormatterTests.cs"
      provides: "Negative-disclosure and positive-actionability tests for shared safe copy"
  key_links:
    - from: "AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs"
      to: "Phase 11 UI/error/log consumers"
      via: "public static helper methods"
      pattern: "DiagnosticTextFormatter\."
---

<objective>
Create the shared safe diagnostics text boundary required by Phase 11.

Purpose: Later plans need one executable source of truth for safe operation messages, sanitized basenames, setting identifiers, plugin failure summaries, latest-log guidance, and export disclaimer wording.
Output: Tested formatter helpers that implement D-01 through D-08, D-09 through D-12 copy primitives, and D-13 through D-16 safe structured-field primitives without changing app behavior yet.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-SPEC.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-CONTEXT.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-RESEARCH.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-PATTERNS.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-UI-SPEC.md

<interfaces>
Create a new public static class in namespace `AutoQAC.Models.Diagnostics` so Models, Services, ViewModels, and startup code can all depend on it without introducing a service-layer dependency into domain models.

Required exported members:
```csharp
public static class DiagnosticTextFormatter
{
    public const string LatestLogDetails = "Technical details were written to the latest AutoQAC log.";
    public const string ReportDisclaimer = "Technical details are intentionally kept in AutoQAC logs and are not repeated in this report.";
    public static string OperationFailed(string operation);
    public static string SafeFileIdentifier(string label, string? path, string fallbackName);
    public static string SafeFolderIssue(string gameDisplayName, string folderLabel = "data folder");
    public static string SafePluginName(string? pluginNameOrPath, string fallbackName = "selected plugin");
    public static string CleaningFailedForPlugin(string? pluginNameOrPath);
    public static string XEditReportedError(string? pluginNameOrPath);
    public static string SafeFailureSummary(string? candidate, string fallback);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — lock shared diagnostic text behavior</name>
  <read_first>
    - .planning/phases/11-user-facing-diagnostics-boundaries/11-SPEC.md
    - .planning/phases/11-user-facing-diagnostics-boundaries/11-CONTEXT.md
    - .planning/phases/11-user-facing-diagnostics-boundaries/11-UI-SPEC.md
    - AutoQAC/Models/BackupOperationResults.cs
    - AutoQAC/Services/Configuration/ConfigPersistenceStatus.cs
    - AutoQAC.Tests/Models/CleaningSessionResultTests.cs
    - AutoQAC.Tests/Models/DiagnosticTextFormatterTests.cs (create)
  </read_first>
  <files>AutoQAC.Tests/Models/DiagnosticTextFormatterTests.cs</files>
  <behavior>
    - `OperationFailed("Cleaning")` returns exactly `Cleaning failed. See the latest AutoQAC log for technical details.` per D-01/D-03 and UI-SPEC.
    - `SafeFileIdentifier("xEdit Path", @"C:\Users\Alice\Tools\SSEEdit.exe", "xEdit executable")` returns exactly `xEdit Path (SSEEdit.exe)` per D-05.
    - `SafeFileIdentifier("Load Order File", @"C:\Users\Alice\AppData\plugins.txt", "load order file")` returns exactly `Load Order File (plugins.txt)` per D-05.
    - `SafeFolderIssue("Skyrim SE")` returns exactly `Skyrim SE data folder is unavailable. Choose a valid Data folder or reset the override.` per D-06 and UI-SPEC.
    - `SafePluginName("Bad\n\"Plugin\`.esp")` excludes newline, quote, and backtick but preserves `.esp` per D-07.
    - `CleaningFailedForPlugin("Plugin.esp")` returns exactly `Plugin.esp: Cleaning failed. See the latest AutoQAC log.` per D-09 and UI-SPEC.
    - `XEditReportedError("Plugin.esp")` returns exactly `xEdit reported an error for Plugin.esp. See the latest AutoQAC log.` per D-11 and UI-SPEC.
    - `SafeFailureSummary(@"System.InvalidOperationException at C:\Users\Alice\xEdit.exe -QAC", "Cleaning failed. See the latest AutoQAC log.")` returns the fallback and excludes `System.InvalidOperationException`, `C:\Users\Alice`, and `-QAC`.
    - `SafeFailureSummary(@"IOException at J:\ModOrganizer\mods\Plugin.esp -autoload", "Cleaning failed. See the latest AutoQAC log.")` returns the fallback and excludes `J:\ModOrganizer`, `Plugin.esp -autoload`, and `-autoload`, proving non-C drive-rooted Windows paths are unsafe per D-07/D-09.
  </behavior>
  <action>Create `DiagnosticTextFormatterTests` with xUnit/FluentAssertions tests for the exact behaviors above. Include a shared unsafe sentinel array containing `C:\Users\Alice\Tools\SSEEdit.exe`, `D:\Games\Skyrim Special Edition\Data\Plugin.esp`, `J:\ModOrganizer\ModOrganizer.exe`, `-QAC`, `-autoload`, and `System.InvalidOperationException: boom at AutoQAC.Services.Cleaning`. Tests must fail to compile because `DiagnosticTextFormatter` does not exist yet. Include a test named `SafeFailureSummary_WhenCandidateContainsNonCDriveWindowsPath_ReturnsFallback` so the boundary detects any drive-rooted Windows path, not only `C:\` paths.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~DiagnosticTextFormatterTests --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC.Tests/Models/DiagnosticTextFormatterTests.cs` contains `OperationFailed_Cleaning_ReturnsUiSpecCopy`.
    - `AutoQAC.Tests/Models/DiagnosticTextFormatterTests.cs` contains `SafeFileIdentifier_XEditPath_ReturnsLabelAndSanitizedBasename`.
    - `AutoQAC.Tests/Models/DiagnosticTextFormatterTests.cs` contains `SafeFailureSummary_WhenCandidateContainsUnsafeDetails_ReturnsFallback`.
    - `AutoQAC.Tests/Models/DiagnosticTextFormatterTests.cs` contains `SafeFailureSummary_WhenCandidateContainsNonCDriveWindowsPath_ReturnsFallback`.
    - RED test command fails before Task 2 because `DiagnosticTextFormatter` is missing.
  </acceptance_criteria>
  <done>Executable failing tests describe the shared safe diagnostic text contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement shared diagnostic text formatter</name>
  <read_first>
    - AutoQAC.Tests/Models/DiagnosticTextFormatterTests.cs
    - AutoQAC/Models/BackupOperationResults.cs
    - AutoQAC/Services/Configuration/ConfigPersistenceStatus.cs
    - AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs (create)
  </read_first>
  <files>AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs, AutoQAC.Tests/Models/DiagnosticTextFormatterTests.cs</files>
  <action>Create `AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs` in namespace `AutoQAC.Models.Diagnostics` with the exact exported members listed in this plan. Use `Path.GetFileName` for basename extraction. Sanitize display names by removing control characters, double quotes, single quotes, backticks, pipe `|`, ampersand `&`, semicolon `;`, angle brackets `<` and `>`, and trimming whitespace. Preserve Unicode letters, spaces inside names, periods, dashes, and underscores. `OperationFailed` must trim the operation and return `{Operation} failed. See the latest AutoQAC log for technical details.` with fallback operation `Operation`. `SafeFailureSummary` must return the fallback when candidate contains `System.`, ` at AutoQAC.`, any drive-rooted Windows path pattern such as `C:\`, `D:\`, or `J:\`, `\\`, `-QAC`, `-autoload`, `.exe `, newline, carriage return, or tab; otherwise return the trimmed candidate. Implement drive-rooted detection generically (for example, letter + colon + slash/backslash) rather than checking only `C:\`. Add XML documentation to every public method per repository instructions.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~DiagnosticTextFormatterTests --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs` contains `public const string LatestLogDetails = "Technical details were written to the latest AutoQAC log."`.
    - `AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs` contains `public const string ReportDisclaimer = "Technical details are intentionally kept in AutoQAC logs and are not repeated in this report."`.
    - `AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs` contains `Path.GetFileName`.
    - `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~DiagnosticTextFormatterTests --nologo` passes.
  </acceptance_criteria>
  <done>Shared formatter compiles, tests pass, and later Phase 11 plans can call it.</done>
</task>

</tasks>

<verification>
Run the focused test command in each task, then `dotnet build AutoQACSharp.slnx --nologo` after GREEN.
</verification>

<success_criteria>
Shared helpers produce the exact UI-SPEC copy for cleaning/preview failure, safe setting identifiers, data-folder issues, failed plugin rows, xEdit exception-log rows, latest-log details, and export disclaimer while excluding configured unsafe sentinel strings.
</success_criteria>

<output>
After completion, create `.planning/phases/11-user-facing-diagnostics-boundaries/11-01-SUMMARY.md`.
</output>
