---
phase: 11-user-facing-diagnostics-boundaries
plan: 09
type: execute
wave: 4
depends_on: [11-06]
files_modified:
  - AutoQAC/Models/CleaningSessionResult.cs
  - AutoQAC/Models/PluginCleaningResult.cs
  - AutoQAC.Tests/Models/CleaningSessionResultTests.cs
  - AutoQAC.Tests/Models/Phase11ReportBoundaryTests.cs
autonomous: true
gap_closure: true
requirements: [SEC-01]
must_haves:
  truths:
    - "SEC-01 / D-07 / D-09: user-facing reports and result summaries sanitize displayed plugin basenames before formatting cleaned, skipped, already-clean, and failed rows."
    - "SEC-01 / D-10 / D-12: reports keep the existing disclaimer and safe failed-message fallback while preserving raw plugin names only in internal model data."
  artifacts:
    - path: "AutoQAC/Models/CleaningSessionResult.cs"
      provides: "Safe plugin display names in generated reports"
      contains: "DiagnosticTextFormatter.SafePluginName"
    - path: "AutoQAC/Models/PluginCleaningResult.cs"
      provides: "Safe failed summary fallback plugin display names"
      contains: "DiagnosticTextFormatter.CleaningFailedForPlugin"
    - path: "AutoQAC.Tests/Models/CleaningSessionResultTests.cs"
      provides: "Report/result tests for plugin names with quotes, backticks, controls, and path separators"
    - path: "AutoQAC.Tests/Models/Phase11ReportBoundaryTests.cs"
      provides: "Phase-level report guard for unsafe plugin-name sentinels"
  key_links:
    - from: "AutoQAC/Models/CleaningSessionResult.cs"
      to: "DiagnosticTextFormatter.SafePluginName"
      via: "report row plugin display names"
      pattern: "SafePluginName\(result\.PluginName"
    - from: "AutoQAC/Models/PluginCleaningResult.cs"
      to: "DiagnosticTextFormatter.CleaningFailedForPlugin"
      via: "failed summary fallback"
      pattern: "CleaningFailedForPlugin\(PluginName\)"
---

<objective>
Close Phase 11 verification gap #3: user-facing reports and failed result summaries must sanitize displayed plugin-name prefixes/fallbacks.

Purpose: SEC-01 and D-07/D-09 require report/result display names to preserve useful plugin filenames while stripping path separators, command-like punctuation, quotes/backticks, and control characters.
Output: Safe plugin display names in report rows and regression tests for malicious/path-like plugin names.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-VERIFICATION.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-CONTEXT.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-04-SUMMARY.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-06-SUMMARY.md
@AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs
@AutoQAC/Models/CleaningSessionResult.cs
@AutoQAC/Models/PluginCleaningResult.cs
@AutoQAC.Tests/Models/CleaningSessionResultTests.cs
@AutoQAC.Tests/Models/Phase11ReportBoundaryTests.cs

<interfaces>
From `AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs`:
```csharp
public static string SafePluginName(string? pluginNameOrPath, string fallbackName = "selected plugin");
public static string CleaningFailedForPlugin(string? pluginNameOrPath);
public static string SafeFailureSummary(string? candidate, string fallback);
```

From `AutoQAC/Models/CleaningSessionResult.cs`:
```csharp
public IEnumerable<PluginCleaningResult> CleanedPlugins =>
    PluginResults.Where(r => r.Status is CleaningStatus.Cleaned or CleaningStatus.AlreadyClean);
public IEnumerable<PluginCleaningResult> AlreadyCleanPlugins =>
    PluginResults.Where(r => r.Status == CleaningStatus.AlreadyClean);
public IEnumerable<PluginCleaningResult> SkippedPlugins =>
    PluginResults.Where(r => r.Status == CleaningStatus.Skipped);
public IEnumerable<PluginCleaningResult> FailedPlugins =>
    PluginResults.Where(r => r.Status == CleaningStatus.Failed);
public string GenerateReport();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove report plugin-name prefixes are sanitized</name>
  <files>AutoQAC.Tests/Models/CleaningSessionResultTests.cs, AutoQAC.Tests/Models/Phase11ReportBoundaryTests.cs</files>
  <read_first>AutoQAC.Tests/Models/CleaningSessionResultTests.cs; AutoQAC.Tests/Models/Phase11ReportBoundaryTests.cs; AutoQAC.Tests/Helpers/DiagnosticSentinels.cs; AutoQAC/Models/CleaningSessionResult.cs; AutoQAC/Models/PluginCleaningResult.cs; AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs</read_first>
  <behavior>
    - Test 1: `GenerateReport_WhenPluginNamesContainUnsafeDisplayCharacters_ShouldUseSafePluginNames` creates cleaned, already-clean, skipped, and failed results whose `PluginName` values contain `C:\Users\Alice\Unsafe"Plugin	here.esp`, backticks, quotes, `|`, `&`, `<`, `>`, and path separators; generated report contains sanitized basenames such as `UnsafePluginhere.esp` and never contains the directory path or unsafe punctuation/control characters.
    - Test 2: failed report rows use the sanitized plugin name both for the prefix and fallback summary, without duplicating raw path-like names.
    - Test 3: `Phase11ReportBoundaryTests` includes a path-like/control-character plugin name and asserts every shared unsafe sentinel is absent from `GenerateReport()` output while `DiagnosticTextFormatter.ReportDisclaimer` is still present exactly once.
  </behavior>
  <action>Add RED tests for verification gap #3. Keep existing model-only test style and do not require Avalonia/UI infrastructure. Use `DiagnosticTextFormatter.SafePluginName` in expected values only if the test still asserts exact absence of raw path/control/command fragments. Include assertions for cleaned, already-clean, skipped, and failed sections because `CleaningSessionResult.GenerateReport()` currently prints raw `result.PluginName` in multiple branches.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~CleaningSessionResultTests|FullyQualifiedName~Phase11ReportBoundaryTests" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC.Tests/Models/CleaningSessionResultTests.cs` contains `GenerateReport_WhenPluginNamesContainUnsafeDisplayCharacters_ShouldUseSafePluginNames`.
    - Tests assert report output does not contain `C:\Users\Alice`, quote/backtick characters from plugin names, `|`, `&`, `<`, `>`, `\t`, `-QAC`, or `-autoload`.
    - Tests assert `DiagnosticTextFormatter.ReportDisclaimer` remains present.
    - The automated command fails before Task 2 because `GenerateReport()` still prints raw `result.PluginName` in report rows.
  </acceptance_criteria>
  <done>Failing tests prove user-facing reports can no longer rely on raw plugin-name prefixes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sanitize plugin display names across report rows</name>
  <files>AutoQAC/Models/CleaningSessionResult.cs, AutoQAC/Models/PluginCleaningResult.cs, AutoQAC.Tests/Models/CleaningSessionResultTests.cs, AutoQAC.Tests/Models/Phase11ReportBoundaryTests.cs</files>
  <read_first>AutoQAC/Models/CleaningSessionResult.cs; AutoQAC/Models/PluginCleaningResult.cs; AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs; AutoQAC.Tests/Models/CleaningSessionResultTests.cs; AutoQAC.Tests/Models/Phase11ReportBoundaryTests.cs</read_first>
  <action>Implement the GREEN fix per SEC-01/D-07/D-09/D-10/D-12. In `CleaningSessionResult.GenerateReport()`, use `DiagnosticTextFormatter.SafePluginName(result.PluginName)` for every plugin display prefix in cleaned, already-clean, skipped, and failed sections. In `FormatFailedPluginReportLine`, compute `var safePluginName = DiagnosticTextFormatter.SafePluginName(result.PluginName);`, use `DiagnosticTextFormatter.CleaningFailedForPlugin(safePluginName)` as fallback, and compare prefixes against `$"{safePluginName}:"` before returning `$"{safePluginName}: {summary}"`. In `PluginCleaningResult.Summary`, keep the raw `PluginName` stored for internal data, but ensure failed summary fallback continues through `DiagnosticTextFormatter.CleaningFailedForPlugin(PluginName)` so the formatter sanitizes the display name. Preserve counts, durations, statuses, report disclaimer, and `PluginResults` model values.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~CleaningSessionResultTests|FullyQualifiedName~Phase11ReportBoundaryTests" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Models/CleaningSessionResult.cs` contains `DiagnosticTextFormatter.SafePluginName(result.PluginName)`.
    - `AutoQAC/Models/CleaningSessionResult.cs` no longer contains `sb.AppendLine($"  {result.PluginName}`.
    - `AutoQAC/Models/CleaningSessionResult.cs` no longer contains `var prefix = $"{result.PluginName}:"`.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>Generated reports and failed result summaries display only sanitized plugin basenames while internal model data remains unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin result model -> report/export text | Raw plugin names or paths can be hand-constructed or introduced by future callers before report generation formats user-facing rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09-01 | I | `CleaningSessionResult.GenerateReport` | mitigate | Sanitize plugin display names in every report section before appending lines. |
| T-11-09-02 | I | `FormatFailedPluginReportLine` | mitigate | Use sanitized plugin prefixes and safe failure-summary fallback to avoid path-like raw names. |
</threat_model>

<verification>
Run `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~CleaningSessionResultTests|FullyQualifiedName~Phase11ReportBoundaryTests" --nologo` and confirm all targeted tests pass.
</verification>

<success_criteria>
- Verification gap #3 in `11-VERIFICATION.md` is closed.
- Reports sanitize plugin display names in cleaned, already-clean, skipped, and failed sections.
- Existing report disclaimer and safe failed-message fallback remain intact.
</success_criteria>

<output>
After completion, create `.planning/phases/11-user-facing-diagnostics-boundaries/11-09-SUMMARY.md`.
</output>
