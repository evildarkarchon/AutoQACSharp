---
phase: 11-user-facing-diagnostics-boundaries
plan: 13
type: execute
wave: 7
depends_on: [11-11]
files_modified:
  - AutoQAC/ViewModels/RestoreViewModel.cs
  - AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs
autonomous: true
gap_closure: true
requirements: [SEC-01]
must_haves:
  truths:
    - "Restore Selected confirmation, progress/status, and error dialog text use sanitized backup metadata filenames per D-07 and D-09."
    - "Restore service calls continue to receive the original BackupPluginEntry and trusted restore root; only user-facing display strings are sanitized."
    - "Restore metadata regressions fail if path, command, control-character, exception, or stack sentinels appear in user-facing restore copy."
  artifacts:
    - path: "AutoQAC/ViewModels/RestoreViewModel.cs"
      provides: "Safe restore-selected display projection for backup metadata filenames"
      contains: "DiagnosticTextFormatter.SafePluginName"
    - path: "AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs"
      provides: "Restore selected unsafe metadata negative-disclosure regression"
      contains: "RestorePluginCommand_WhenBackupMetadataFileNameContainsUnsafeDetails_ShouldShowSanitizedDisplayCopy"
  key_links:
    - from: "AutoQAC/ViewModels/RestoreViewModel.cs"
      to: "IMessageDialogService.ShowConfirmAsync/ShowErrorAsync"
      via: "RestorePluginAsync uses a safe display name in user-facing confirmation and failure copy"
      pattern: "SafePluginName\\(plugin.FileName"
    - from: "AutoQAC/ViewModels/RestoreViewModel.cs"
      to: "IBackupService.RestorePluginAsync"
      via: "RestorePluginAsync still passes the original BackupPluginEntry to restore service"
      pattern: "RestorePluginAsync\\(plugin, session.SessionDirectory"
---

<objective>
Close the Phase 11 SEC-01 restore metadata display gap where Restore Selected confirmation, status, and error text can render raw `BackupPluginEntry.FileName` from editable backup metadata.

Purpose: backup session metadata is untrusted display input; restore operations must preserve original metadata for service safety while projecting safe copy to users.
Output: executable regression tests plus safe restore-selected display formatting in `RestoreViewModel`.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-CONTEXT.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-RESEARCH.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-VERIFICATION.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-09-SUMMARY.md

<interfaces>
From `AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs`:
```csharp
public static string SafePluginName(string? pluginNameOrPath, string fallbackName = "selected plugin");
```

From `AutoQAC/ViewModels/RestoreViewModel.cs` current restore flow:
```csharp
var plugin = SelectedPlugin;
var result = await _backupService.RestorePluginAsync(plugin, session.SessionDirectory, _trustedRestoreRoot, progress, cts.Token);
```

From `AutoQAC.Tests/Helpers/DiagnosticSentinels.cs`:
```csharp
public static string[] UnsafeDiagnosticSentinels { get; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove Restore Selected user copy sanitizes unsafe backup metadata filenames</name>
  <files>AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs</files>
  <read_first>
    AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs
    AutoQAC.Tests/Helpers/DiagnosticSentinels.cs
    AutoQAC/ViewModels/RestoreViewModel.cs
    AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs
    .planning/phases/11-user-facing-diagnostics-boundaries/11-VERIFICATION.md
  </read_first>
  <behavior>
    - Restore Selected confirmation for a `BackupPluginEntry.FileName` shaped like `@"C:\Users\Alice\Backups\Bad` -autoload.esp"` shows a sanitized basename such as `Bad.esp` or `Badautoload.esp` according to `DiagnosticTextFormatter.SafePluginName`, never the path, control characters, quotes/backticks, or command fragments.
    - Restore Selected failure dialog/status for an exception after confirmation uses the same sanitized display name and excludes all shared unsafe sentinels.
    - `IBackupService.RestorePluginAsync` still receives the original `BackupPluginEntry` instance, proving only UI copy is projected.
  </behavior>
  <action>
    Add RED regression coverage to `RestoreViewModelTests.cs`. Add `using AutoQAC.Tests.Helpers;` if needed. Create `RestorePluginCommand_WhenBackupMetadataFileNameContainsUnsafeDetails_ShouldShowSanitizedDisplayCopy` that configures `ShowConfirmAsync` to return true, makes `RestorePluginAsync` throw or return a failed result after invocation, and asserts confirmation message, error dialog strings, and `StatusText` exclude `DiagnosticSentinels.UnsafeDiagnosticSentinels`, `C:\Users\Alice`, `-autoload`, quotes/backticks, and control characters. Also assert `RestorePluginAsync` receives the same `plugin` object so service behavior remains unchanged. Reference D-07/D-09 in assertions or comments.
  </action>
  <acceptance_criteria>
    - `RestoreViewModelTests.cs` contains `RestorePluginCommand_WhenBackupMetadataFileNameContainsUnsafeDetails_ShouldShowSanitizedDisplayCopy`.
    - The new test imports or references `DiagnosticSentinels`.
    - The new test asserts safe user-facing confirmation/status/error copy and original `BackupPluginEntry` service input.
    - The new test fails before production changes because `RestoreViewModel.RestorePluginAsync` displays `plugin.FileName` verbatim.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~RestoreViewModelTests" --nologo</automated>
  </verify>
  <done>The restore metadata disclosure regression is executable and demonstrates the current raw display gap before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project safe restore-selected display names without changing restore service inputs</name>
  <files>AutoQAC/ViewModels/RestoreViewModel.cs</files>
  <read_first>
    AutoQAC/ViewModels/RestoreViewModel.cs
    AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs
    AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs
  </read_first>
  <action>
    Add `using AutoQAC.Models.Diagnostics;` to `RestoreViewModel.cs`. In `RestorePluginAsync`, after `var plugin = SelectedPlugin;`, compute `var safePluginName = DiagnosticTextFormatter.SafePluginName(plugin.FileName);`. Use `safePluginName` in the Restore Selected confirmation message, `StatusText = $"Restoring: {safePluginName}"`, `ShowErrorAsync` message `$"Failed to restore '{safePluginName}'."`, and failure `StatusText = $"Failed to restore: {safePluginName}"`. Keep `_logger` properties and `_backupService.RestorePluginAsync(plugin, ...)` using the original `plugin.FileName`/`plugin` where local troubleshooting and service restore semantics require original metadata. Do not alter Restore All, delete session, backup containment, progress row models, or restore result aggregation.
  </action>
  <acceptance_criteria>
    - `RestoreViewModel.cs` contains `using AutoQAC.Models.Diagnostics;`.
    - `RestoreViewModel.cs` contains `var safePluginName = DiagnosticTextFormatter.SafePluginName(plugin.FileName);` in `RestorePluginAsync`.
    - Confirmation/status/error user-facing strings in `RestorePluginAsync` use `safePluginName`, not raw `plugin.FileName`.
    - `_backupService.RestorePluginAsync(plugin, session.SessionDirectory, _trustedRestoreRoot, progress, cts.Token)` remains unchanged in meaning and still passes the original `plugin` object.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~RestoreViewModelTests|FullyQualifiedName~DiagnosticTextFormatterTests" --nologo</automated>
  </verify>
  <done>Restore Selected user-facing strings use sanitized metadata display names while restore service behavior remains unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backup session metadata → Restore ViewModel | `BackupPluginEntry.FileName` is loaded from session metadata and can be corrupted or edited. |
| Restore ViewModel → dialog/status UI | Restore confirmation, progress, and failure copy becomes user-facing text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-13-01 | I | `RestoreViewModel.RestorePluginAsync` | mitigate | Project `plugin.FileName` through `DiagnosticTextFormatter.SafePluginName` for confirmation/status/error strings. |
| T-11-13-02 | T | `RestoreViewModel.RestorePluginAsync` service call | mitigate | Keep the original `BackupPluginEntry` object for restore service calls so display sanitization cannot alter restore target semantics. |
</threat_model>

<verification>
Run `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~RestoreViewModelTests|FullyQualifiedName~DiagnosticTextFormatterTests" --nologo`, then run the full solution test suite if context remains: `dotnet test AutoQACSharp.slnx --nologo`.
</verification>

<success_criteria>
- SEC-01 restore selected metadata display gap is closed.
- User-facing restore confirmation/status/error strings exclude raw backup metadata path/control/command details.
- Restore service receives unchanged original metadata needed for filesystem safety and restore behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/11-user-facing-diagnostics-boundaries/11-13-SUMMARY.md`.
</output>
