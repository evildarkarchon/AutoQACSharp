---
phase: 11-user-facing-diagnostics-boundaries
plan: 05
type: tdd
wave: 2
depends_on: [11-01]
files_modified:
  - AutoQAC/Services/Process/ProcessExecutionService.cs
  - AutoQAC/Services/Cleaning/CleaningService.cs
  - AutoQAC/App.axaml.cs
  - AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs
  - AutoQAC.Tests/Services/CleaningServiceTests.cs
  - AutoQAC.Tests/Integration/DependencyInjectionTests.cs
autonomous: true
requirements: [SEC-01, SEC-02]
must_haves:
  truths:
    - "D-13: Process-start diagnostics do not log full executable paths, raw Arguments, raw ArgumentList entries, or nested MO2 payloads."
    - "D-13: Cleaning-layer launch diagnostics preserve operation, launch mode, game, plugin filename, argument count, status, and safe reason/category without changing ProcessStartInfo construction."
    - "D-13: Startup diagnostics replace configured executable paths with structured safe configured/basename fields."
    - "D-14: Logs may keep full local paths only when the path is the direct failed resource and omitting it would materially reduce local troubleshooting value."
    - "D-15: Normal workflow logs identify plugins by filename plus game/mode context, not full plugin path unless the plugin file path itself is the direct failing resource."
    - "D-16: Behavioral logger-capture tests prove injectable process/cleaning log boundaries; source guards are reserved for private startup code only."
    - "Legacy migration user warnings use safe latest-log guidance instead of raw exception messages."
  artifacts:
    - path: "AutoQAC/Services/Process/ProcessExecutionService.cs"
      provides: "Safe process-start/failure structured logging boundary"
    - path: "AutoQAC/Services/Cleaning/CleaningService.cs"
      provides: "Caller-side launch diagnostics context with game/mode/plugin fields"
    - path: "AutoQAC/App.axaml.cs"
      provides: "Safe startup log and migration warning boundary"
    - path: "AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs"
      provides: "Behavioral process logger capture regression coverage"
    - path: "AutoQAC.Tests/Services/CleaningServiceTests.cs"
      provides: "Behavioral cleaning-layer logger capture and ProcessStartInfo preservation coverage"
    - path: "AutoQAC.Tests/Integration/DependencyInjectionTests.cs"
      provides: "Startup diagnostics source guard for private startup code"
  key_links:
    - from: "ProcessExecutionService.ExecuteAsync"
      to: "ILoggingService"
      via: "structured safe fields"
      pattern: "ArgumentCount"
---

<objective>
Harden process/startup logs and migration warning copy.

Purpose: SEC-02 and D-13 through D-16 require AutoQAC logs to retain troubleshooting value without unnecessary executable path or command payload exposure, and SEC-01 requires migration warnings to avoid raw exception details.
Output: Tested safe process-start/failure logs, safe cleaning caller diagnostics, safe startup diagnostics, and safe migration warning text.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-SPEC.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-CONTEXT.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-RESEARCH.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-PATTERNS.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-UI-SPEC.md
@.planning/phases/06-command-launch-escaping/06-CONTEXT.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-01-SUMMARY.md
@AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs

<interfaces>
Use `DiagnosticTextFormatter.SafeFileIdentifier("xEdit Path", state.XEditExecutablePath, "not configured")` for startup executable display. Use logger-capture tests for `ProcessExecutionService` and `CleaningService`; do not source-read those files as the primary proof. Source guards are allowed only for private startup code in `App.axaml.cs`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Safe ProcessExecutionService log fields without process-start behavior drift</name>
  <read_first>
    - AutoQAC/Services/Process/ProcessExecutionService.cs
    - AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs
    - AutoQAC/Infrastructure/Logging/ILoggingService.cs
    - .planning/phases/11-user-facing-diagnostics-boundaries/11-SPEC.md
    - .planning/phases/11-user-facing-diagnostics-boundaries/11-CONTEXT.md
  </read_first>
  <files>AutoQAC/Services/Process/ProcessExecutionService.cs, AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs</files>
  <behavior>
    - A `ProcessStartInfo` with `FileName = @"C:\Users\Alice\Tools\SSEEdit.exe"`, `Arguments = "-QAC -autoload C:\\Games\\Skyrim\\Data\\Plugin.esp"`, and populated `ArgumentList` logs `ArgumentCount` and safe reason/status fields but no full filename, raw `Arguments`, `-QAC`, `-autoload`, or plugin full path.
    - Start failure logs a safe reason/category and does not pass `startInfo.FileName` as a structured property.
    - Successful start logs PID when available without changing `ProcessStartInfo.FileName`, `Arguments`, `ArgumentList`, `WorkingDirectory`, or clone/launch behavior.
  </behavior>
  <action>Add failing logger-capture tests to `ProcessExecutionServiceTests` for start failure and, where the existing test seam allows, successful start/PID logging. Assert captured logger templates/arguments include `ArgumentCount`, safe status/reason, and PID when available, and exclude `C:\Users\Alice`, `SSEEdit.exe`, `-QAC`, `-autoload`, and `C:\Games\Skyrim`. Then update only log templates/arguments in `ProcessExecutionService`: replace `Starting process: {FileName} {Arguments}` and `Failed to start process: {FileName}` with safe structured events using operation/status/reason/argumentCount/PID fields. Preserve `CloneStartInfoForLaunch`, `process.StartInfo`, timeout/cancellation, single process slot, and every `ProcessStartInfo` launch value. Addresses review concern: this task is scoped to `ProcessExecutionService` and logger-capture assertions target `ILoggingService`, not a `ProcessStartInfo` substitute.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~ProcessExecutionServiceTests --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Services/Process/ProcessExecutionService.cs` contains `ArgumentCount` or `argumentCount` in process log code.
    - `AutoQAC/Services/Process/ProcessExecutionService.cs` contains `ProcessId` or `pid` in a post-start log path.
    - `AutoQAC/Services/Process/ProcessExecutionService.cs` does not contain `Starting process: {FileName} {Arguments}`.
    - `AutoQAC/Services/Process/ProcessExecutionService.cs` does not contain `Failed to start process: {FileName}`.
    - ProcessExecutionServiceTests assert launch values remain unchanged after logging changes.
    - Focused ProcessExecutionServiceTests pass.
  </acceptance_criteria>
  <done>ProcessExecutionService logs safe process diagnostics without changing process construction or launch semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Safe CleaningService launch diagnostics with explicit logger capture</name>
  <read_first>
    - AutoQAC/Services/Cleaning/CleaningService.cs
    - AutoQAC.Tests/Services/CleaningServiceTests.cs
    - AutoQAC/Services/Process/IProcessExecutionService.cs
    - AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs
    - .planning/phases/06-command-launch-escaping/06-CONTEXT.md
  </read_first>
  <files>AutoQAC/Services/Cleaning/CleaningService.cs, AutoQAC.Tests/Services/CleaningServiceTests.cs</files>
  <behavior>
    - `CleaningService.CleanPluginAsync` logs caller-side safe fields `Operation="QuickAutoClean"`, `LaunchMode`, `Game`, `Plugin="Plugin.esp"`, `ArgumentCount`, and `Status`/`Reason` around `processService.ExecuteAsync`.
    - Logger-capture assertions target the captured `ILoggingService` substitute; the process service substitute only records the `ProcessStartInfo` to prove command construction remains unchanged.
    - Direct and MO2 launches still preserve exact argv intent from Phase 6; no diagnostics code reconstructs or logs raw command payloads.
  </behavior>
  <action>Add failing tests to `CleaningServiceTests` that exercise direct and MO2 launch modes when existing fixtures permit. Use a plugin path under `C:\Games\Skyrim\Data\Plugin.esp` and an app state with `CurrentGameType = GameType.SkyrimSe`. Capture `ILoggingService.Information` calls and assert they include `QuickAutoClean`, launch mode, game display name, plugin filename only, argument count, and safe status/reason; assert they exclude full paths, raw `-QAC`, raw `-autoload`, nested MO2 payloads, and command fragments. Separately capture the `ProcessStartInfo` passed to `IProcessExecutionService.ExecuteAsync` and assert `FileName`, `Arguments`, and `ArgumentList` values are unchanged from the existing command-builder behavior. Then update `CleaningService.CleanPluginAsync` log calls around the existing process-start seam only; compute argument count from `ArgumentList.Count` when populated, otherwise `Arguments` non-empty equals 1, and do not change command construction, launch sequencing, retry handling, result parsing, backup behavior, or sequential cleaning.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~CleaningServiceTests --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Services/Cleaning/CleaningService.cs` logs `QuickAutoClean`, launch mode, game, plugin filename, argument count, status, and safe reason/category near `processService.ExecuteAsync`.
    - `AutoQAC/Services/Cleaning/CleaningService.cs` does not log `command.Arguments`, raw `ArgumentList` entries, `-QAC`, `-autoload`, or plugin full paths in launch diagnostics.
    - CleaningServiceTests assert logger calls and ProcessStartInfo preservation separately.
    - Focused CleaningServiceTests pass.
  </acceptance_criteria>
  <done>CleaningService provides useful safe launch context without touching launch behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Safe startup diagnostics and legacy migration warning</name>
  <read_first>
    - AutoQAC/App.axaml.cs
    - AutoQAC.Tests/Integration/DependencyInjectionTests.cs
    - AutoQAC/ViewModels/MainWindowViewModel.cs
    - AutoQAC/Services/Configuration/LegacyMigrationService.cs
    - AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs
    - .planning/phases/11-user-facing-diagnostics-boundaries/11-UI-SPEC.md
  </read_first>
  <files>AutoQAC/App.axaml.cs, AutoQAC.Tests/Integration/DependencyInjectionTests.cs</files>
  <behavior>
    - Startup log source no longer contains template/property text `xEdit Path: {XEditPath}`.
    - Startup diagnostics log safe configured/basename fields, not the configured executable path.
    - Unexpected legacy migration failure calls `ShowMigrationWarning("Some legacy settings could not be migrated. See the latest AutoQAC log for technical details.")` and excludes `ex.Message`.
  </behavior>
  <action>Add a source guard test in `DependencyInjectionTests` or a new startup diagnostics test file that reads `AutoQAC/App.axaml.cs` and fails if it contains `xEdit Path: {XEditPath}`, `Legacy config migration failed unexpectedly: {ex.Message}`, or `ShowMigrationWarning($"Legacy config migration failed unexpectedly`. Then update `App.axaml.cs`: replace startup `logger.Information("xEdit Path: {XEditPath}", state.XEditExecutablePath ?? "(not configured)")` with safe structured fields using `DiagnosticTextFormatter.SafeFileIdentifier("xEdit Path", state.XEditExecutablePath, "not configured")` plus configured boolean/counts/status fields; never log directories or raw command payloads. Replace unexpected migration warning text with exactly `Some legacy settings could not be migrated. See the latest AutoQAC log for technical details.` while keeping `logger.Error(ex, "[Migration] Unexpected error during legacy migration")`. Do not alter DI construction, config watcher startup, log retention cleanup, or migration execution order. Addresses review concern: source guards are limited to private startup code where behavioral seams are not available.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~DependencyInjectionTests|FullyQualifiedName~Startup" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/App.axaml.cs` does not contain `xEdit Path: {XEditPath}`.
    - `AutoQAC/App.axaml.cs` contains `DiagnosticTextFormatter.SafeFileIdentifier("xEdit Path"`.
    - `AutoQAC/App.axaml.cs` contains `Some legacy settings could not be migrated. See the latest AutoQAC log for technical details.`.
    - `AutoQAC/App.axaml.cs` does not contain `Legacy config migration failed unexpectedly: {ex.Message}`.
    - Startup/source guard test passes.
  </acceptance_criteria>
  <done>Startup logs and migration warning copy satisfy D-13, D-16, UI-SPEC, SEC-01, and SEC-02.</done>
</task>

</tasks>

<verification>
Run task focused tests, then `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~ProcessExecutionServiceTests|FullyQualifiedName~CleaningServiceTests|FullyQualifiedName~DependencyInjectionTests" --nologo`.
</verification>

<success_criteria>
Process/startup logs avoid unnecessary executable path, raw argv, and command-fragment exposure while preserving operation, mode, argument count, PID/reason where available; process construction and sequential launch behavior remain unchanged; legacy migration warnings use safe latest-log guidance.
</success_criteria>

<output>
After completion, create `.planning/phases/11-user-facing-diagnostics-boundaries/11-05-SUMMARY.md`.
</output>
