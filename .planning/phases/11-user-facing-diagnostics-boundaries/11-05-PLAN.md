---
phase: 11-user-facing-diagnostics-boundaries
plan: 05
type: tdd
wave: 2
depends_on: [11-01]
files_modified:
  - AutoQAC/Services/Cleaning/CleaningService.cs
  - AutoQAC/App.axaml.cs
  - AutoQAC/Services/Process/ProcessExecutionService.cs
  - AutoQAC.Tests/Services/CleaningServiceTests.cs
  - AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs
  - AutoQAC.Tests/Integration/DependencyInjectionTests.cs
autonomous: true
requirements: [SEC-01, SEC-02]
must_haves:
  truths:
    - "D-13: Startup diagnostics replace configured executable paths and command payloads with structured safe fields."
    - "D-13: Process-start diagnostics do not log full executable paths, raw Arguments, raw ArgumentList entries, or nested MO2 payloads."
    - "Legacy migration user warnings use safe latest-log guidance instead of raw exception messages."
    - "D-14: Logs may keep full local paths only when the path is the direct failing local resource and materially useful for troubleshooting."
    - "D-15: Normal workflow logs identify plugins by filename plus game/mode context, not full plugin path unless it is the direct failing resource."
    - "D-16: Captured logger/source assertions prove process-start and startup diagnostics exclude full executable paths, raw argv/nested payloads, and command fragments."
    - "D-13: Logs preserve operation, launch mode, game/mode context, plugin filename where available, PID when available, argument count, counts/status, or safe reason/category."
  artifacts:
    - path: "AutoQAC/App.axaml.cs"
      provides: "Safe startup log and migration warning boundary"
    - path: "AutoQAC/Services/Process/ProcessExecutionService.cs"
      provides: "Safe process-start/failure structured logging boundary"
    - path: "AutoQAC/Services/Cleaning/CleaningService.cs"
      provides: "Caller-side launch diagnostics context with game/mode/plugin fields"
    - path: "AutoQAC.Tests/Services/CleaningServiceTests.cs"
      provides: "Cleaning-layer launch diagnostics context regression coverage"
    - path: "AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs"
      provides: "Captured logger process disclosure regressions"
    - path: "AutoQAC.Tests/Integration/DependencyInjectionTests.cs"
      provides: "Startup diagnostics source/behavior guard when private seams are not directly injectable"
  key_links:
    - from: "ProcessExecutionService.ExecuteAsync"
      to: "ILoggingService"
      via: "structured safe fields"
      pattern: "ArgumentCount"
---

<objective>
Harden process/startup logs and migration warning copy.

Purpose: SEC-02 and D-13 through D-16 require AutoQAC logs to retain troubleshooting value without unnecessary executable path or command payload exposure, and SEC-01 requires migration warnings to avoid raw exception details.
Output: Tested safe process-start/failure logs, safe startup diagnostics, and safe migration warning text.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-SPEC.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-CONTEXT.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-RESEARCH.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-PATTERNS.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-UI-SPEC.md
@.planning/phases/06-command-launch-escaping/06-CONTEXT.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-01-SUMMARY.md
@AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Safe process-start and process-failure log fields</name>
  <read_first>
    - AutoQAC/Services/Cleaning/CleaningService.cs
    - AutoQAC.Tests/Services/CleaningServiceTests.cs
    - AutoQAC/Services/Process/ProcessExecutionService.cs
    - AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs
    - AutoQAC/Infrastructure/Logging/ILoggingService.cs
    - AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs
    - .planning/phases/11-user-facing-diagnostics-boundaries/11-SPEC.md
    - .planning/phases/11-user-facing-diagnostics-boundaries/11-CONTEXT.md
  </read_first>
  <files>AutoQAC/Services/Cleaning/CleaningService.cs, AutoQAC/Services/Process/ProcessExecutionService.cs, AutoQAC.Tests/Services/CleaningServiceTests.cs, AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs</files>
  <behavior>
    - A `ProcessStartInfo` with `FileName = @"C:\Users\Alice\Tools\SSEEdit.exe"`, `Arguments = "-QAC -autoload C:\\Games\\Skyrim\\Data\\Plugin.esp"`, and two `ArgumentList` entries logs `ArgumentCount`/safe operation context but no full filename, raw `Arguments`, `-QAC`, `-autoload`, or plugin full path.
    - `CleaningService.CleanPluginAsync` emits caller-side safe launch diagnostics around the cleaning-layer process start with `Operation="QuickAutoClean"`, `LaunchMode` (`"MO2"` or `"direct xEdit"`), `Game`, `Plugin` filename, `ArgumentCount`, and safe `Status`/`Reason` fields where applicable, without full executable paths, raw arguments, `-QAC`, `-autoload`, or plugin full paths per D-13.
    - After the process starts successfully, `ProcessExecutionService.ExecuteAsync` emits a separate safe structured log including the started process PID when available, plus operation/launch mode/argument count, and still excludes full executable paths, raw arguments, `-QAC`, and `-autoload` per D-13.
    - Process start failure logs a safe reason/category and does not pass `startInfo.FileName` as a structured property.
  </behavior>
  <action>Add failing logger-capture assertions to `CleaningServiceTests` and `ProcessExecutionServiceTests`. In `CleaningServiceTests`, exercise `CleaningService.CleanPluginAsync` with a `PluginInfo` whose path is under `C:\Games\Skyrim\Data\Plugin.esp`, an `AppState` using `CurrentGameType = GameType.SkyrimSe`, both direct and MO2 launch modes when practical, and a substitute `IProcessExecutionService` that records the received `ProcessStartInfo` but does not launch. Assert the captured `ILoggingService.Information` calls include structured safe troubleshooting fields `Operation`/`QuickAutoClean`, `LaunchMode`, `Game`, `Plugin` filename only (`Plugin.esp`), `ArgumentCount`, and a safe `Status`/`Reason` such as `Starting`/`Started` or `ProcessReturned`; assert they do not contain `C:\Users\Alice`, `C:\Games\Skyrim`, `C:\ModOrganizer`, `SSEEdit.exe -QAC`, raw `-QAC`, raw `-autoload`, or the plugin full path. Then update `CleaningService.CleanPluginAsync` around the existing process start seam: before `processService.ExecuteAsync(command, timeout, ct, onProcessStarted)`, compute `launchMode = state.Mo2ModeEnabled ? "MO2" : "direct xEdit"`, `gameDisplayName`, `plugin.FileName`, and `argumentCount = command.ArgumentList.Count > 0 ? command.ArgumentList.Count : (string.IsNullOrWhiteSpace(command.Arguments) ? 0 : 1)`, and log a caller-side structured event such as `logger.Information("Starting {Operation}: launchMode={LaunchMode}, game={Game}, plugin={Plugin}, argumentCount={ArgumentCount}, status={Status}", "QuickAutoClean", launchMode, gameDisplayName, plugin.FileName, argumentCount, "Starting")`. After `ExecuteAsync` returns, add a second safe status event with `Status`/`Reason` derived from `result.TimedOut`, `result.ExitCode`, and success/failure (for example `Completed`, `TimedOut`, or `ExitCodeNonZero`) while still logging only operation, launch mode, game, plugin filename, argument count, counts/status, and safe reason/category. In `ProcessExecutionServiceTests`, include an assertion that a successful started-process log contains a structured PID value when `Process.Id` is available. Update `ProcessExecutionService.ExecuteAsync` log calls only: replace `logger.Debug("Starting process: {FileName} {Arguments}", startInfo.FileName, arguments)` with a safe template containing `Operation="ExternalTool"`, `LaunchMode` (`ArgumentList` or `LegacyArguments`), `ArgumentCount`, and `HasWorkingDirectory`; immediately after successful `Process.Start`, add `ProcessId`/PID in a safe post-start structured log; replace `logger.Error(ex, "Failed to start process: {FileName}", startInfo.FileName)` with a safe error template containing `Operation`, `LaunchMode`, `ArgumentCount`, and `Reason="StartFailed"`. Do not log `startInfo.FileName`, `startInfo.Arguments`, or any `ArgumentList` payload in either class. Preserve `CloneStartInfoForLaunch`, single process slot, PID tracking, timeout/cancellation/termination behavior, MO2/direct command construction, and launch argv values exactly.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~ProcessExecutionServiceTests|FullyQualifiedName~CleaningServiceTests" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Services/Process/ProcessExecutionService.cs` contains `argumentCount` or `ArgumentCount` in process-start log templates.
    - `AutoQAC/Services/Process/ProcessExecutionService.cs` contains `pid={ProcessId}` or another structured PID placeholder in a post-start success log.
    - `AutoQAC/Services/Process/ProcessExecutionService.cs` does not contain `Starting process: {FileName} {Arguments}`.
    - `AutoQAC/Services/Process/ProcessExecutionService.cs` does not contain `Failed to start process: {FileName}`.
    - `AutoQAC/Services/Cleaning/CleaningService.cs` logs D-13 caller-side safe troubleshooting fields around `processService.ExecuteAsync`: operation (`QuickAutoClean`), launch mode, game display name, plugin filename, argument count, status, and safe reason/category.
    - `AutoQAC/Services/Cleaning/CleaningService.cs` does not log full executable paths, `command.FileName`, `command.Arguments`, raw `ArgumentList` entries, `-QAC`, `-autoload`, or plugin full paths in launch diagnostics.
    - `CleaningServiceTests` assert captured logger calls include `QuickAutoClean`, `LaunchMode`, `Game`, `Plugin.esp`, `ArgumentCount`, and safe status/reason fields, and exclude full paths, raw argv, nested MO2 payloads, and command fragments.
    - ProcessExecutionServiceTests assert captured successful-start logger calls include a PID when available and do not include `C:\Users\Alice`, `-QAC`, or `-autoload`.
    - Focused ProcessExecutionServiceTests and CleaningServiceTests pass.
  </acceptance_criteria>
  <done>Process logs use safe structured fields and no longer expose executable paths or command payloads unnecessarily.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Safe startup diagnostics and legacy migration warning</name>
  <read_first>
    - AutoQAC/App.axaml.cs
    - AutoQAC.Tests/Integration/DependencyInjectionTests.cs
    - AutoQAC/ViewModels/MainWindowViewModel.cs
    - AutoQAC/Services/Configuration/LegacyMigrationService.cs
    - AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs
    - .planning/phases/11-user-facing-diagnostics-boundaries/11-UI-SPEC.md
  </read_first>
  <files>AutoQAC/App.axaml.cs, AutoQAC.Tests/Integration/DependencyInjectionTests.cs</files>
  <behavior>
    - Startup log source no longer contains template/property text `xEdit Path: {XEditPath}`.
    - Startup diagnostics log safe configuration status/basename fields, not the configured executable path.
    - Unexpected legacy migration failure calls `ShowMigrationWarning("Some legacy settings could not be migrated. See the latest AutoQAC log for technical details.")` and excludes `ex.Message`.
  </behavior>
  <action>Add a source/behavior guard test in `DependencyInjectionTests` or a new startup diagnostics test file that reads `AutoQAC/App.axaml.cs` and fails if it contains `xEdit Path: {XEditPath}`, `Legacy config migration failed unexpectedly: {ex.Message}`, or `ShowMigrationWarning($"Legacy config migration failed unexpectedly`. Then update `App.axaml.cs`: replace startup `logger.Information("xEdit Path: {XEditPath}", state.XEditExecutablePath ?? "(not configured)")` with safe structured fields such as `logger.Information("xEdit configured: {XEditConfigured}, executable={XEditExecutable}", !string.IsNullOrWhiteSpace(state.XEditExecutablePath), DiagnosticTextFormatter.SafePluginName(state.XEditExecutablePath, "not configured"))` or `DiagnosticTextFormatter.SafeFileIdentifier("xEdit Path", state.XEditExecutablePath, "not configured")` without directories. Replace unexpected migration warning text with exactly `Some legacy settings could not be migrated. See the latest AutoQAC log for technical details.` while keeping `logger.Error(ex, "[Migration] Unexpected error during legacy migration")`. Do not alter DI construction, config watcher startup, log retention cleanup, or migration execution order.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~DependencyInjectionTests|FullyQualifiedName~Startup" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/App.axaml.cs` does not contain `xEdit Path: {XEditPath}`.
    - `AutoQAC/App.axaml.cs` contains `Some legacy settings could not be migrated. See the latest AutoQAC log for technical details.`.
    - `AutoQAC/App.axaml.cs` does not contain `Legacy config migration failed unexpectedly: {ex.Message}`.
    - Startup/source guard test passes.
  </acceptance_criteria>
  <done>Startup logs and migration warning copy satisfy D-13, D-16, UI-SPEC, SEC-01, and SEC-02.</done>
</task>

</tasks>

<verification>
Run task focused tests, then `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~ProcessExecutionServiceTests|FullyQualifiedName~CleaningServiceTests|FullyQualifiedName~DependencyInjectionTests" --nologo`.
</verification>

<success_criteria>
Process/startup logs avoid unnecessary executable path, raw argv, and command-fragment exposure while preserving operation, mode, argument count, PID/reason where available; legacy migration warnings use safe latest-log guidance.
</success_criteria>

<output>
After completion, create `.planning/phases/11-user-facing-diagnostics-boundaries/11-05-SUMMARY.md`.
</output>
