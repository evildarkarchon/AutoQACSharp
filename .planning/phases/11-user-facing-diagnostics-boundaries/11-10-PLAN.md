---
phase: 11-user-facing-diagnostics-boundaries
plan: 10
type: execute
wave: 5
depends_on: [11-09]
files_modified:
  - AutoQAC/Services/Cleaning/CleaningService.cs
  - AutoQAC.Tests/Services/CleaningServiceTests.cs
  - AutoQAC.Tests/Services/Cleaning/PluginResultFinalizerTests.cs
  - AutoQAC.Tests/Models/CleaningSessionResultTests.cs
autonomous: true
gap_closure: true
requirements: [SEC-01]
must_haves:
  truths:
    - "SEC-01 / D-07 / D-09 / D-10: failed CleaningResult.Message values created by CleaningService sanitize plugin display names before PluginResultFinalizer, PluginCleaningResult.Summary, or CleaningSessionResult.GenerateReport can show them."
    - "SEC-01 / D-01 / D-02 / D-03: build-command and unexpected cleaning failures preserve concise latest-log guidance without raw plugin basename control characters, quotes, backticks, command fragments, or path detail."
  artifacts:
    - path: "AutoQAC/Services/Cleaning/CleaningService.cs"
      provides: "Source-safe failed CleaningResult messages for command-build and unexpected exception paths"
      contains: "DiagnosticTextFormatter.SafePluginName"
    - path: "AutoQAC.Tests/Services/CleaningServiceTests.cs"
      provides: "CleaningService failed-message regressions for unsafe plugin basenames"
    - path: "AutoQAC.Tests/Services/Cleaning/PluginResultFinalizerTests.cs"
      provides: "End-to-end finalizer regression proving failed service messages stay safe downstream"
    - path: "AutoQAC.Tests/Models/CleaningSessionResultTests.cs"
      provides: "Summary/report regression for service-shaped failed messages with unsafe plugin basenames"
  key_links:
    - from: "AutoQAC/Services/Cleaning/CleaningService.cs"
      to: "DiagnosticTextFormatter.SafePluginName"
      via: "safe plugin display name in command-build failure message and log property"
      pattern: "SafePluginName\(plugin\.FileName\)"
    - from: "AutoQAC/Services/Cleaning/CleaningService.cs"
      to: "PluginResultFinalizer.FinalizeAsync"
      via: "CleaningResult.Message from service LastAttemptResult"
      pattern: "Message = .*safePluginName"
    - from: "AutoQAC/Services/Cleaning/PluginResultFinalizer.cs"
      to: "PluginCleaningResult.Summary and CleaningSessionResult.GenerateReport"
      via: "failed result message propagation"
      pattern: "SafeFailureSummary\(resultMessage, fallback\)"
---

<objective>
Close the remaining Phase 11 verification gap: `CleaningService` must not create failed `CleaningResult.Message` text from raw plugin filenames that can survive into result rows, summaries, or exported reports.

Purpose: SEC-01 and D-10 require failed plugin messages to be sanitized at source before result-window/report boundaries consume them; Plan 11-09 sanitized report prefixes, but verification found service-created failed messages can still carry unsafe basename display characters.
Output: Safe `CleaningService` failed-result messages plus regression coverage through service, finalizer, summary, and report surfaces.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-VERIFICATION.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-CONTEXT.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-04-SUMMARY.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-09-SUMMARY.md
@AutoQAC/Services/Cleaning/CleaningService.cs
@AutoQAC/Services/Cleaning/PluginResultFinalizer.cs
@AutoQAC/Models/PluginCleaningResult.cs
@AutoQAC/Models/CleaningSessionResult.cs
@AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs
@AutoQAC.Tests/Services/CleaningServiceTests.cs
@AutoQAC.Tests/Services/Cleaning/PluginResultFinalizerTests.cs
@AutoQAC.Tests/Models/CleaningSessionResultTests.cs

<interfaces>
From `AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs`:
```csharp
public static string SafePluginName(string? pluginNameOrPath, string fallbackName = "selected plugin");
public static string CleaningFailedForPlugin(string? pluginNameOrPath);
public static string SafeFailureSummary(string? candidate, string fallback);
```

From `AutoQAC/Services/Cleaning/CleaningService.cs`:
```csharp
public async Task<CleaningResult> CleanPluginAsync(
    PluginInfo plugin,
    CancellationToken ct = default,
    Action<System.Diagnostics.Process>? onProcessStarted = null);
```

From `AutoQAC/Services/Cleaning/PluginResultFinalizer.cs`:
```csharp
public async Task<PluginCleaningResult> FinalizeAsync(
    PluginInfo plugin,
    GameType gameType,
    string xEditDir,
    PluginRunnerOutput runnerOutput,
    TerminationFinalizeContext terminationContext,
    CancellationToken ct);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove CleaningService failed messages sanitize unsafe plugin basenames</name>
  <files>AutoQAC.Tests/Services/CleaningServiceTests.cs, AutoQAC.Tests/Services/Cleaning/PluginResultFinalizerTests.cs, AutoQAC.Tests/Models/CleaningSessionResultTests.cs</files>
  <read_first>AutoQAC.Tests/Services/CleaningServiceTests.cs; AutoQAC.Tests/Services/Cleaning/PluginResultFinalizerTests.cs; AutoQAC.Tests/Models/CleaningSessionResultTests.cs; AutoQAC.Tests/Helpers/DiagnosticSentinels.cs; AutoQAC/Services/Cleaning/CleaningService.cs; AutoQAC/Services/Cleaning/PluginResultFinalizer.cs; AutoQAC/Models/PluginCleaningResult.cs; AutoQAC/Models/CleaningSessionResult.cs; AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs</read_first>
  <behavior>
    - Test 1: `CleanPluginAsync_WhenCommandBuildFailsWithUnsafePluginName_ShouldReturnSanitizedFailureMessage` uses plugin filename `Unsafe\"Plugin\t|-QAC-autoload.esp`, forces `IXEditCommandBuilder.BuildCommand` to return null, and asserts `CleaningResult.Message` contains `UnsafePlugin.esp`, `No process was started`, and latest-log guidance while excluding `\"`, backtick, `|`, tab, `-QAC`, and `-autoload`.
    - Test 2: `CleanPluginAsync_WhenUnexpectedLaunchExceptionWithUnsafePluginName_ShouldReturnSanitizedFailureMessage` uses the same unsafe plugin basename, makes `IProcessExecutionService.ExecuteAsync` throw, and asserts the returned failed message is `DiagnosticTextFormatter.CleaningFailedForPlugin(plugin.FileName)` or equivalent safe text without unsafe basename characters.
    - Test 3: `FinalizeAsync_FailedRunnerResultFromCleaningServiceUnsafePluginMessage_ShouldKeepSummaryAndReportSafe` feeds a service-shaped failed `CleaningResult.Message` into `PluginResultFinalizer`, then creates a `CleaningSessionResult` and asserts `result.Message`, `result.Summary`, and `GenerateReport()` exclude raw basename characters while containing the sanitized plugin name and latest-log guidance.
  </behavior>
  <action>Add RED tests for the verified CR-01 gap in `11-VERIFICATION.md`. Keep the tests in existing xUnit/FluentAssertions/NSubstitute style. Use concrete unsafe filename `Unsafe\"Plugin\t|-QAC-autoload.esp` and assert exact forbidden fragments: `\"`, `` ` ``, `|`, `\t`, `-QAC`, `-autoload`, `C:\Users\Alice`, `SSEEdit.exe -QAC`, and `System.InvalidOperationException`. Do not add Avalonia.Headless or new UI infrastructure. The finalizer/report regression must exercise real `PluginResultFinalizer.FinalizeAsync`, real `PluginCleaningResult.Summary`, and real `CleaningSessionResult.GenerateReport()` so the gap is proven across the complete downstream path.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~CleaningServiceTests|FullyQualifiedName~PluginResultFinalizerTests|FullyQualifiedName~CleaningSessionResultTests" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC.Tests/Services/CleaningServiceTests.cs` contains `CleanPluginAsync_WhenCommandBuildFailsWithUnsafePluginName_ShouldReturnSanitizedFailureMessage`.
    - `AutoQAC.Tests/Services/CleaningServiceTests.cs` contains `CleanPluginAsync_WhenUnexpectedLaunchExceptionWithUnsafePluginName_ShouldReturnSanitizedFailureMessage`.
    - `AutoQAC.Tests/Services/Cleaning/PluginResultFinalizerTests.cs` or `AutoQAC.Tests/Models/CleaningSessionResultTests.cs` contains `FinalizeAsync_FailedRunnerResultFromCleaningServiceUnsafePluginMessage_ShouldKeepSummaryAndReportSafe`.
    - The automated command fails before Task 2 because `CleaningService.cs` still interpolates raw `plugin.FileName` in failed messages.
  </acceptance_criteria>
  <done>Failing tests prove the remaining SEC-01 source-boundary gap from `11-VERIFICATION.md` is reproducible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sanitize CleaningService failed result messages at source</name>
  <files>AutoQAC/Services/Cleaning/CleaningService.cs, AutoQAC.Tests/Services/CleaningServiceTests.cs, AutoQAC.Tests/Services/Cleaning/PluginResultFinalizerTests.cs, AutoQAC.Tests/Models/CleaningSessionResultTests.cs</files>
  <read_first>AutoQAC/Services/Cleaning/CleaningService.cs; AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs; AutoQAC/Services/Cleaning/PluginResultFinalizer.cs; AutoQAC/Models/PluginCleaningResult.cs; AutoQAC/Models/CleaningSessionResult.cs; AutoQAC.Tests/Services/CleaningServiceTests.cs; AutoQAC.Tests/Services/Cleaning/PluginResultFinalizerTests.cs; AutoQAC.Tests/Models/CleaningSessionResultTests.cs</read_first>
  <action>Implement the GREEN fix per SEC-01, D-07, D-09, and D-10. In `CleaningService.CleanPluginAsync`, compute `var safePluginName = DiagnosticTextFormatter.SafePluginName(plugin.FileName);` before the command-build branch so every failure path can use it. For the `command == null` branch, change the warning log property from raw `plugin.FileName` to `safePluginName`, and set the returned message to `Could not build {buildFailureLaunchMode} launch command for {safePluginName}. No process was started. See the latest AutoQAC log.` For the unexpected `catch (Exception ex)` branch, change the log property from raw `plugin.FileName` to `safePluginName`, and set the returned message to `DiagnosticTextFormatter.CleaningFailedForPlugin(plugin.FileName)` so the helper sanitizes display text consistently. Preserve process launch values, `ProcessStartInfo`, exit-code/timed-out messages, cancellation behavior, sequential cleaning, and existing structured launch success/failure logs. Do not broaden `SafeFailureSummary` heuristics to reject all punctuation; the source fix belongs in `CleaningService` messages.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~CleaningServiceTests|FullyQualifiedName~PluginResultFinalizerTests|FullyQualifiedName~CleaningSessionResultTests" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Services/Cleaning/CleaningService.cs` contains `var safePluginName = DiagnosticTextFormatter.SafePluginName(plugin.FileName);` before `command == null` is evaluated.
    - `AutoQAC/Services/Cleaning/CleaningService.cs` no longer contains `Message = $"Could not build {buildFailureLaunchMode} launch command for {plugin.FileName}`.
    - `AutoQAC/Services/Cleaning/CleaningService.cs` no longer contains `Message = $"Cleaning failed for {plugin.FileName}`.
    - `AutoQAC/Services/Cleaning/CleaningService.cs` contains `DiagnosticTextFormatter.CleaningFailedForPlugin(plugin.FileName)` in the unexpected exception failure path.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>`CleaningService` creates safe failed messages at source, and downstream finalizer/summary/report surfaces exclude unsafe plugin basename characters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin metadata -> service failed result | Plugin filenames can contain display-unsafe characters before `CleaningService` creates a user-facing `CleaningResult.Message`. |
| service failed result -> finalizer/model/report | `CleaningResult.Message` flows through `PluginResultFinalizer`, `PluginCleaningResult.Summary`, and `CleaningSessionResult.GenerateReport()`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-10-01 | I | `CleaningService.CleanPluginAsync` command-build failure | mitigate | Build failed messages with `DiagnosticTextFormatter.SafePluginName(plugin.FileName)` instead of raw plugin filename interpolation. |
| T-11-10-02 | I | `CleaningService.CleanPluginAsync` unexpected exception catch | mitigate | Return `DiagnosticTextFormatter.CleaningFailedForPlugin(plugin.FileName)` and log only the sanitized plugin display value as a structured property. |
| T-11-10-03 | I | `PluginResultFinalizer` -> `PluginCleaningResult.Summary` -> `CleaningSessionResult.GenerateReport` | mitigate | Add regression coverage that follows service-shaped failed messages through finalizer, summary, and report generation. |
</threat_model>

<verification>
Run `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~CleaningServiceTests|FullyQualifiedName~PluginResultFinalizerTests|FullyQualifiedName~CleaningSessionResultTests" --nologo`, then run `dotnet test AutoQACSharp.slnx --nologo` before phase verification.
</verification>

<success_criteria>
- The `gaps:` item in `11-VERIFICATION.md` is closed.
- `CleaningService` command-build and unexpected-exception failed messages use sanitized plugin display text at source.
- `PluginResultFinalizer`, `PluginCleaningResult.Summary`, and `CleaningSessionResult.GenerateReport()` stay safe when fed a service-shaped failed message with an unsafe plugin basename.
</success_criteria>

<output>
After completion, create `.planning/phases/11-user-facing-diagnostics-boundaries/11-10-SUMMARY.md`.
</output>
