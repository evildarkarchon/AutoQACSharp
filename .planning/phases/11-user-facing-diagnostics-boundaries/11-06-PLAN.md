---
phase: 11-user-facing-diagnostics-boundaries
plan: 06
type: execute
wave: 3
depends_on: [11-02, 11-03, 11-04, 11-05]
files_modified:
  - AutoQAC.Tests/Helpers/DiagnosticSentinels.cs
  - AutoQAC.Tests/ViewModels/Phase11DiagnosticsBoundaryTests.cs
  - AutoQAC.Tests/Models/Phase11ReportBoundaryTests.cs
  - AutoQAC.Tests/Services/Phase11LogBoundaryTests.cs
autonomous: true
requirements: [SEC-01, SEC-02]
must_haves:
  truths:
    - "Covered user-facing surfaces fail tests if stack traces, raw exception messages, full local paths, or command fragments are reintroduced."
    - "Covered log surfaces fail tests if full configured executable paths, raw argv payloads, or nested MO2 payloads are reintroduced."
    - "Phase-level sentinel tests exercise real ViewModel, model, and logger paths instead of only formatter literals or source text."
    - "Full solution tests pass after all Phase 11 diagnostic boundary changes."
  artifacts:
    - path: "AutoQAC.Tests/Helpers/DiagnosticSentinels.cs"
      provides: "Shared unsafe sentinel constants reused by Phase 11 guard tests"
    - path: "AutoQAC.Tests/ViewModels/Phase11DiagnosticsBoundaryTests.cs"
      provides: "Cross-surface user-facing negative-disclosure guard"
    - path: "AutoQAC.Tests/Models/Phase11ReportBoundaryTests.cs"
      provides: "Report/result negative-disclosure guard"
    - path: "AutoQAC.Tests/Services/Phase11LogBoundaryTests.cs"
      provides: "Process/startup log boundary behavior/source guard"
  key_links:
    - from: "Phase11 diagnostics tests"
      to: "Phase 11 acceptance criteria"
      via: "shared unsafe sentinel assertions"
      pattern: "UnsafeDiagnosticSentinels"
---

<objective>
Add phase-level regression guards and run final verification.

Purpose: Requirement 6 in `11-SPEC.md` requires explicit tests that fail if covered UI, export, or log surfaces reintroduce stack/path/command leaks. Earlier plans add focused tests; this plan adds a small cross-surface sentinel net and performs final automated verification.
Output: Shared sentinel helper, phase-level boundary tests, and final solution test pass.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-SPEC.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-CONTEXT.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-VALIDATION.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-UI-SPEC.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-02-SUMMARY.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-03-SUMMARY.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-04-SUMMARY.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cross-surface Phase 11 disclosure sentinels</name>
  <read_first>
    - AutoQAC.Tests/ViewModels/ErrorDialogTests.cs
    - AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs
    - AutoQAC.Tests/Models/CleaningSessionResultTests.cs
    - AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs
    - AutoQAC.Tests/Helpers/DiagnosticSentinels.cs (create)
    - AutoQAC.Tests/ViewModels/Phase11DiagnosticsBoundaryTests.cs (create)
    - AutoQAC.Tests/Models/Phase11ReportBoundaryTests.cs (create)
    - AutoQAC.Tests/Services/Phase11LogBoundaryTests.cs (create)
    - .planning/phases/11-user-facing-diagnostics-boundaries/11-SPEC.md
  </read_first>
  <files>AutoQAC.Tests/Helpers/DiagnosticSentinels.cs, AutoQAC.Tests/ViewModels/Phase11DiagnosticsBoundaryTests.cs, AutoQAC.Tests/Models/Phase11ReportBoundaryTests.cs, AutoQAC.Tests/Services/Phase11LogBoundaryTests.cs</files>
  <action>Create `AutoQAC.Tests/Helpers/DiagnosticSentinels.cs` with a public static `UnsafeDiagnosticSentinels` string array containing exactly: `C:\Users\Alice`, `C:\Games\Skyrim Special Edition`, `C:\ModOrganizer\ModOrganizer.exe`, `SSEEdit.exe -QAC`, `-autoload`, `System.InvalidOperationException`, `Stack Trace`, and ` at AutoQAC.`. Create three small phase-level test files that exercise real production boundaries and reuse this helper instead of duplicating sentinel arrays. These tests must not be satisfiable by asserting only hardcoded safe literals. `Phase11DiagnosticsBoundaryTests` must exercise at least one real ViewModel command catch path, for example `CleaningCommandsViewModel.StartCleaningCommand` with a throwing orchestrator using the existing fixture pattern, before applying sentinel assertions; formatter-only tests are insufficient. `Phase11ReportBoundaryTests` must construct a real `CleaningSessionResult`/`PluginCleaningResult` containing deliberately unsafe failed message input, call `CleaningSessionResult.GenerateReport()`, and assert the generated report includes `Technical details are intentionally kept in AutoQAC logs and are not repeated in this report.` while excluding every sentinel. `Phase11LogBoundaryTests` must prefer captured `ILoggingService` calls from `ProcessExecutionService.ExecuteAsync` and/or `CleaningService.CleanPluginAsync` for injectable service boundaries; source-read assertions are allowed only for `App.axaml.cs` private startup code and for checking known forbidden templates are absent. Assert forbidden templates/properties `Starting process: {FileName} {Arguments}`, `Failed to start process: {FileName}`, `xEdit Path: {XEditPath}`, `Legacy config migration failed unexpectedly: {ex.Message}`, raw `command.Arguments`, and raw `ArgumentList` payload logging are absent. Addresses review concerns: phase-level sentinels use shared helpers, real ViewModel/model/logger paths, and source guards only where behavioral seams are unavailable.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~Phase11 --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC.Tests/Helpers/DiagnosticSentinels.cs` contains `UnsafeDiagnosticSentinels`.
    - `Phase11DiagnosticsBoundaryTests` invokes at least one production ViewModel command catch path before applying sentinel assertions; it does not only assert against formatter output or locally hardcoded safe strings.
    - `AutoQAC.Tests/Models/Phase11ReportBoundaryTests.cs` contains `Technical details are intentionally kept in AutoQAC logs and are not repeated in this report.`.
    - `Phase11ReportBoundaryTests` calls `CleaningSessionResult.GenerateReport()` on a real model containing unsafe input before applying sentinel assertions.
    - `AutoQAC.Tests/Services/Phase11LogBoundaryTests.cs` contains `Starting process: {FileName} {Arguments}` as a forbidden assertion string, not as production code.
    - `Phase11LogBoundaryTests` uses captured logger calls for `ProcessExecutionService` or `CleaningService` boundaries and limits source reads to `App.axaml.cs` private startup checks or known bad-template absence.
    - `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~Phase11 --nologo` passes.
  </acceptance_criteria>
  <done>Phase-level tests cover the acceptance boundary named in `11-SPEC.md` requirement 6.</done>
</task>

<task type="auto">
  <name>Task 2: Final Phase 11 verification sweep</name>
  <read_first>
    - AutoQAC.Tests/Helpers/DiagnosticSentinels.cs
    - AutoQAC.Tests/ViewModels/Phase11DiagnosticsBoundaryTests.cs
    - AutoQAC.Tests/Models/Phase11ReportBoundaryTests.cs
    - AutoQAC.Tests/Services/Phase11LogBoundaryTests.cs
    - .planning/phases/11-user-facing-diagnostics-boundaries/11-VALIDATION.md
  </read_first>
  <files>AutoQAC.Tests/Helpers/DiagnosticSentinels.cs, AutoQAC.Tests/ViewModels/Phase11DiagnosticsBoundaryTests.cs, AutoQAC.Tests/Models/Phase11ReportBoundaryTests.cs, AutoQAC.Tests/Services/Phase11LogBoundaryTests.cs</files>
  <action>Run the final verification commands sequentially, not in parallel, to avoid the recurring MSBuild file-lock issue documented in Phase 08-10 summaries. Execute `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~Phase11|FullyQualifiedName~ErrorDialogTests|FullyQualifiedName~RestoreViewModelTests|FullyQualifiedName~CleaningSessionResultTests|FullyQualifiedName~PluginResultFinalizerTests|FullyQualifiedName~ProcessExecutionServiceTests" --nologo`, then `dotnet test AutoQACSharp.slnx --nologo`. If a failure is caused by a Phase 11 regression, fix it within the touched test/source surface; if failure is unrelated, record it exactly in the summary and do not change unrelated code.</action>
  <verify>
    <automated>dotnet test AutoQACSharp.slnx --nologo</automated>
  </verify>
  <acceptance_criteria>
    - Focused Phase 11-related test command passes.
    - `dotnet test AutoQACSharp.slnx --nologo` passes, or the summary records an unrelated pre-existing failure with command output.
    - No task adds Avalonia.Headless, new diagnostics windows, telemetry, crash reporting, or issue-report export files.
  </acceptance_criteria>
  <done>Phase 11 implementation is covered by focused and full-suite automated verification.</done>
</task>

</tasks>

<verification>
`dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~Phase11 --nologo` and `dotnet test AutoQACSharp.slnx --nologo`.
</verification>

<success_criteria>
All Phase 11 acceptance criteria from `11-SPEC.md` are covered by focused behavioral tests or startup source guards, and the full solution suite passes without new UI infrastructure or behavior changes outside diagnostics boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/11-user-facing-diagnostics-boundaries/11-06-SUMMARY.md`.
</output>
