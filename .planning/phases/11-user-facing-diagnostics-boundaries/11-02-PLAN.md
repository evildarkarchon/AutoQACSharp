---
phase: 11-user-facing-diagnostics-boundaries
plan: 02
type: tdd
wave: 2
depends_on: [11-01]
files_modified:
  - AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs
  - AutoQAC.Tests/ViewModels/ErrorDialogTests.cs
autonomous: true
requirements: [SEC-01]
must_haves:
  truths:
    - "D-01: Unexpected cleaning failures show operation-specific safe copy and latest AutoQAC log guidance, not raw exception details."
    - "D-02: Unexpected cleaning and preview modal dialog details contain safe latest-log guidance only, not stack traces, exception messages, paths, or command fragments."
    - "D-03: Unexpected preview failures set concise status copy and latest AutoQAC log guidance, not ex.Message."
    - "D-05: Pre-clean inline validation identifies xEdit, MO2, and load-order settings by safe basename only."
    - "D-08: Simple missing-path validation provides the safe identifier and fix action without extra latest-log guidance."
  artifacts:
    - path: "AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs"
      provides: "Safe command-boundary dialog/status/validation text"
    - path: "AutoQAC.Tests/ViewModels/ErrorDialogTests.cs"
      provides: "Regression tests for cleaning/preview dialogs and inline validation disclosure boundaries"
  key_links:
    - from: "CleaningCommandsViewModel"
      to: "IMessageDialogService.ShowErrorAsync"
      via: "safe title/message/details values"
      pattern: "ShowErrorAsync\(\s*\"Cleaning Failed\""
---

<objective>
Harden the main cleaning command UI boundary.

Purpose: SEC-01 requires the most visible cleaning and preview failures to stop surfacing `ex.Message`, `ex.StackTrace`, full configured paths, and command fragments.
Output: Tested safe status/dialog text and safe pre-clean validation rows in `CleaningCommandsViewModel`.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-SPEC.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-CONTEXT.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-PATTERNS.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-UI-SPEC.md
@.planning/phases/08-cleaning-orchestrator-decomposition/08-02-SUMMARY.md
@.planning/phases/10-configuration-persistence-hardening/10-04-SUMMARY.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-01-SUMMARY.md
@AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs

<interfaces>
Use `AutoQAC.Models.Diagnostics.DiagnosticTextFormatter` from Plan 11-01 for exact copy:
```csharp
DiagnosticTextFormatter.OperationFailed("Cleaning") == "Cleaning failed. See the latest AutoQAC log for technical details."
DiagnosticTextFormatter.OperationFailed("Preview") == "Preview failed. See the latest AutoQAC log for technical details."
DiagnosticTextFormatter.LatestLogDetails == "Technical details were written to the latest AutoQAC log."
DiagnosticTextFormatter.SafeFileIdentifier("xEdit Path", path, "xEdit executable")
DiagnosticTextFormatter.SafeFileIdentifier("MO2 Path", path, "ModOrganizer.exe")
DiagnosticTextFormatter.SafeFileIdentifier("Load Order File", path, "load order file")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — prove cleaning and preview failures hide unsafe exception details</name>
  <read_first>
    - AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs
    - AutoQAC.Tests/ViewModels/ErrorDialogTests.cs
    - AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs
    - .planning/phases/11-user-facing-diagnostics-boundaries/11-SPEC.md
    - .planning/phases/11-user-facing-diagnostics-boundaries/11-UI-SPEC.md
  </read_first>
  <files>AutoQAC.Tests/ViewModels/ErrorDialogTests.cs</files>
  <behavior>
    - When `ICleaningOrchestrator.StartCleaningAsync` throws an exception with `C:\Users\Alice\Tools\SSEEdit.exe -QAC System.InvalidOperationException: boom at AutoQAC.Services.Cleaning`, dialog title is `Cleaning Failed`, message/status are `Cleaning failed. See the latest AutoQAC log for technical details.`, details are `Technical details were written to the latest AutoQAC log.`, and no captured UI string contains the unsafe sentinel.
    - When `ICleaningOrchestrator.RunDryRunAsync` throws the same unsafe exception, dialog title is `Preview Failed`, message/status are `Preview failed. See the latest AutoQAC log for technical details.`, details are `Technical details were written to the latest AutoQAC log.`, and no captured UI string contains the unsafe sentinel.
  </behavior>
  <action>Add failing tests to `ErrorDialogTests` named `StartCleaningAsync_WhenUnexpectedError_ShouldShowSafeDiagnosticCopy` and `PreviewAsync_WhenUnexpectedError_ShouldShowSafeDiagnosticCopy`. Capture the `ShowErrorAsync` arguments with NSubstitute and assert all title/message/details/status text exclude `C:\Users\Alice`, `SSEEdit.exe -QAC`, `System.InvalidOperationException`, `AutoQAC.Services.Cleaning`, and `Stack Trace` while including `latest AutoQAC log`.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~ErrorDialogTests --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC.Tests/ViewModels/ErrorDialogTests.cs` contains `StartCleaningAsync_WhenUnexpectedError_ShouldShowSafeDiagnosticCopy`.
    - `AutoQAC.Tests/ViewModels/ErrorDialogTests.cs` contains `PreviewAsync_WhenUnexpectedError_ShouldShowSafeDiagnosticCopy`.
    - RED command fails before implementation because current `CleaningCommandsViewModel` uses `ex.Message` and `ex.StackTrace`.
  </acceptance_criteria>
  <done>Failing tests lock D-01, D-02, and D-03 for cleaning and preview unexpected failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — replace raw exception dialog/status copy</name>
  <read_first>
    - AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs
    - AutoQAC.Tests/ViewModels/ErrorDialogTests.cs
    - AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs
  </read_first>
  <files>AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs, AutoQAC.Tests/ViewModels/ErrorDialogTests.cs</files>
  <action>In `StartCleaningAsync` general `catch (Exception ex)`, keep `_logger.Error(ex, "StartCleaningAsync failed")`, set `StatusText` to `DiagnosticTextFormatter.OperationFailed("Cleaning")`, call `_messageDialog.ShowErrorAsync("Cleaning Failed", DiagnosticTextFormatter.OperationFailed("Cleaning"), DiagnosticTextFormatter.LatestLogDetails)`, and remove user-facing `ex.Message`, `ex.StackTrace`, `Error:`, and `Stack Trace:` text. In `PreviewAsync` general `catch (Exception ex)`, keep technical logging, set `StatusText` to `DiagnosticTextFormatter.OperationFailed("Preview")`, and call `_messageDialog.ShowErrorAsync("Preview Failed", DiagnosticTextFormatter.OperationFailed("Preview"), DiagnosticTextFormatter.LatestLogDetails)`. Add `using AutoQAC.Models.Diagnostics;` if required. Do not change `InvalidOperationException` validation branching or cleaning/preview sequencing.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~ErrorDialogTests --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs` contains `DiagnosticTextFormatter.OperationFailed("Cleaning")`.
    - `AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs` contains `DiagnosticTextFormatter.OperationFailed("Preview")`.
    - `AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs` contains `DiagnosticTextFormatter.LatestLogDetails`.
    - `AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs` does not contain `Stack Trace:`.
    - Focused ErrorDialogTests pass.
  </acceptance_criteria>
  <done>Unexpected cleaning and preview failures use safe UI-SPEC copy with latest-log guidance only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Safe pre-clean validation identifiers for configured paths</name>
  <read_first>
    - AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs
    - AutoQAC.Tests/ViewModels/ErrorDialogTests.cs
    - AutoQAC/Models/ValidationError.cs
    - AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs
    - .planning/phases/11-user-facing-diagnostics-boundaries/11-UI-SPEC.md
  </read_first>
  <files>AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs, AutoQAC.Tests/ViewModels/ErrorDialogTests.cs</files>
  <behavior>
    - Missing configured xEdit path produces inline validation message exactly `xEdit Path (SSEEdit.exe) is missing. Choose the correct xEdit executable in Settings.` when state contains `C:\Users\Alice\Tools\SSEEdit.exe`.
    - Missing configured MO2 path produces exactly `MO2 Path (ModOrganizer.exe) is missing. Choose ModOrganizer.exe or disable MO2 Mode.` when MO2 mode is enabled with `C:\Users\Alice\MO2\ModOrganizer.exe`.
    - Missing configured load-order path for file-load-order games produces exactly `Load Order File (plugins.txt) is missing. Choose the current plugins.txt or loadorder.txt file.` for `C:\Users\Alice\AppData\Local\plugins.txt`.
  </behavior>
  <action>Add or tighten validation-row tests, then update `ValidatePreClean()` validation messages to use `DiagnosticTextFormatter.SafeFileIdentifier` and the exact UI-SPEC target strings above per D-05/D-08. Simple missing-path validation must not add latest-log guidance. Keep validation titles stable unless existing tests require exact titles; only replace message/fix text that currently exposes full configured paths. Do not change Mutagen vs file-load-order policy or MO2 validation behavior.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~ErrorDialogTests|FullyQualifiedName~CleaningCommandsViewModel" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs` contains `xEdit Path (`.
    - `AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs` contains `MO2 Path (`.
    - `AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs` contains `Load Order File (`.
    - New/updated tests assert no validation title/message/fix string contains `C:\Users\Alice`.
    - Focused command/error tests pass.
  </acceptance_criteria>
  <done>Inline pre-clean validation identifies settings by safe basename and fix action without full path disclosure.</done>
</task>

</tasks>

<verification>
After all tasks pass focused tests, run `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~ErrorDialogTests|FullyQualifiedName~CleaningCommandsViewModel" --nologo`.
</verification>

<success_criteria>
Cleaning/preview unexpected errors and xEdit/MO2/load-order validation rows satisfy D-01, D-02, D-03, D-05, D-07, D-08, UI-SPEC copy requirements, and SEC-01 without changing cleaning workflow behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/11-user-facing-diagnostics-boundaries/11-02-SUMMARY.md`.
</output>
