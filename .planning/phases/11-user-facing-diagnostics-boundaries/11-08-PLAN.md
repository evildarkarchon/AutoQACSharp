---
phase: 11-user-facing-diagnostics-boundaries
plan: 08
type: execute
wave: 4
depends_on: [11-06]
files_modified:
  - AutoQAC/Services/Process/ProcessExecutionService.cs
  - AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs
  - AutoQAC.Tests/Services/Phase11LogBoundaryTests.cs
autonomous: true
gap_closure: true
requirements: [SEC-02]
must_haves:
  truths:
    - "SEC-02 / D-13 / D-16: successful process starts never persist or log raw legacy Arguments when pluginName is omitted."
    - "SEC-02 / D-15: PID tracking labels are sanitized plugin names when available and `ExternalProcess` otherwise."
  artifacts:
    - path: "AutoQAC/Services/Process/ProcessExecutionService.cs"
      provides: "Safe PID tracking labels for successful process starts"
      contains: "ExternalProcess"
    - path: "AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs"
      provides: "Successful-start regression coverage with legacy Arguments and no pluginName"
    - path: "AutoQAC.Tests/Services/Phase11LogBoundaryTests.cs"
      provides: "Phase-level log boundary coverage for successful starts"
  key_links:
    - from: "AutoQAC/Services/Process/ProcessExecutionService.cs"
      to: "IPidStore.UpdateAsync"
      via: "TrackProcessAsync safe tracking label"
      pattern: "TrackProcessAsync\(process, .*ExternalProcess"
    - from: "AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs"
      to: "ProcessExecutionService.ExecuteAsync"
      via: "legacy ProcessStartInfo.Arguments success path"
      pattern: "Arguments = .*SSEEdit.exe -QAC"
---

<objective>
Close Phase 11 verification gap #2: successful process-start diagnostics must not log or persist raw legacy `Arguments` payloads.

Purpose: SEC-02 and D-13 require process/startup logs and PID tracking data to use safe structured fields and labels without raw command payload exposure.
Output: Safe PID tracking label selection and successful-start regression tests for legacy `ProcessStartInfo.Arguments` with omitted `pluginName`.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-VERIFICATION.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-CONTEXT.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-05-SUMMARY.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-06-SUMMARY.md
@AutoQAC/Services/Process/ProcessExecutionService.cs
@AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs
@AutoQAC.Tests/Services/Phase11LogBoundaryTests.cs

<interfaces>
From `AutoQAC/Services/Process/ProcessExecutionService.cs`:
```csharp
public async Task<ProcessResult> ExecuteAsync(
    ProcessStartInfo startInfo,
    TimeSpan? timeout = null,
    CancellationToken ct = default,
    Action<System.Diagnostics.Process>? onProcessStarted = null,
    string? pluginName = null)

public async Task TrackProcessAsync(System.Diagnostics.Process process, string pluginName, CancellationToken ct = default)
```

From `AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs`:
```csharp
private ProcessExecutionService CreateService(IProcessExitWaiter? processExitWaiter = null) =>
    new(_mockLogger, _pidStore, _sessionProvider, processExitWaiter);

private static string DotNetHostPath => RuntimeInformation.IsOSPlatform(OSPlatform.Windows) ? "dotnet.exe" : "dotnet";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove successful legacy-Arguments starts keep PID tracking safe</name>
  <files>AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs, AutoQAC.Tests/Services/Phase11LogBoundaryTests.cs</files>
  <read_first>AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs; AutoQAC.Tests/Services/Phase11LogBoundaryTests.cs; AutoQAC.Tests/Helpers/DiagnosticSentinels.cs; AutoQAC/Services/Process/ProcessExecutionService.cs</read_first>
  <behavior>
    - Test 1: `ExecuteAsync_WhenProcessStartsWithLegacyArgumentsAndNoPluginName_ShouldTrackSafeExternalProcessLabel` launches `dotnet --info` through `ProcessStartInfo.Arguments`, passes no `pluginName`, exits 0, and asserts `_pidStore.LoadAsync()` contains no `-QAC`, `-autoload`, `C:\Users\Alice`, `SSEEdit.exe`, or raw argument payload in `TrackedProcess.PluginName`.
    - Test 2: The same test asserts the tracked label is exactly `ExternalProcess`, and captured non-exception logger templates/arguments exclude the unsafe fragments.
    - Test 3: `Phase11LogBoundaryTests` adds a successful-start guard that uses legacy `Arguments` and no pluginName, then rejects `DiagnosticSentinels.UnsafeDiagnosticSentinels` from captured log text and PID-store entries.
  </behavior>
  <action>Add RED tests for CR-02 / verification gap #2. Use the existing `RealProcessExitWaiter`, `DotNetHostPath`, `_pidStore`, and `AssertCapturedLogTextExcludes` helpers in `ProcessExecutionServiceTests`. To exercise legacy `Arguments` while still launching a real process, set `FileName = DotNetHostPath`, `WorkingDirectory = Environment.CurrentDirectory`, `Arguments = "--info"`, and append the unsafe payload only to the test's expected-forbidden checks by using a second string such as `legacyRawArguments = @"SSEEdit.exe -QAC -autoload C:\Users\Alice\Data\Unsafe.esp"` where the production code currently would use raw `Arguments`; if a real process must receive only `--info`, also add a direct safe-label unit test for any helper introduced in Task 2. Do not alter process launch construction, single-slot behavior, timeout/cancellation, or orphan cleanup semantics.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~ProcessExecutionServiceTests|FullyQualifiedName~Phase11LogBoundaryTests" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs` contains `ExecuteAsync_WhenProcessStartsWithLegacyArgumentsAndNoPluginName_ShouldTrackSafeExternalProcessLabel`.
    - The new process test asserts `PluginName == "ExternalProcess"` or `.PluginName.Should().Be("ExternalProcess")`.
    - The tests contain negative assertions for `-QAC`, `-autoload`, and `C:\Users\Alice`.
    - The automated command fails before Task 2 because `TrackProcessAsync(process, pluginName ?? arguments, ct)` can use raw legacy arguments.
  </acceptance_criteria>
  <done>Failing tests prove successful process starts no longer have coverage blind spots for raw legacy argument tracking.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Use safe tracking labels for successful process starts</name>
  <files>AutoQAC/Services/Process/ProcessExecutionService.cs, AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs, AutoQAC.Tests/Services/Phase11LogBoundaryTests.cs</files>
  <read_first>AutoQAC/Services/Process/ProcessExecutionService.cs; AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs; AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs; AutoQAC.Tests/Services/Phase11LogBoundaryTests.cs</read_first>
  <action>Implement the GREEN fix per SEC-02/D-13/D-15/D-16. In `ProcessExecutionService.cs`, import `AutoQAC.Models.Diagnostics` and replace `await TrackProcessAsync(process, pluginName ?? arguments, ct)` with safe label selection. Add a private helper with XML docs, e.g. `private static string GetSafeTrackingLabel(string? pluginName)`, that returns `DiagnosticTextFormatter.SafePluginName(pluginName)` when `pluginName` is not null/whitespace and returns exactly `ExternalProcess` otherwise. Do not pass `GetArgumentSummary(startInfo)` or `startInfo.Arguments` into PID tracking. Keep `GetArgumentSummary` only if still needed for non-user-facing debug context; remove it if it becomes unused. Keep `GetArgumentCount`, launch cloning, process start info preservation, PID cleanup, and cancellation semantics unchanged.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~ProcessExecutionServiceTests|FullyQualifiedName~Phase11LogBoundaryTests" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Services/Process/ProcessExecutionService.cs` does not contain `pluginName ?? arguments`.
    - `AutoQAC/Services/Process/ProcessExecutionService.cs` contains `ExternalProcess` in the safe tracking label path.
    - `AutoQAC/Services/Process/ProcessExecutionService.cs` contains `DiagnosticTextFormatter.SafePluginName(pluginName`.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>Successful process starts track only sanitized plugin labels or `ExternalProcess`, and process/log boundary tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller ProcessStartInfo -> PID store/logger | Raw launch arguments can originate from xEdit/MO2 command construction and must not become PID tracking labels or log payloads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08-01 | I | `ProcessExecutionService.ExecuteAsync` | mitigate | Select PID tracking labels from sanitized plugin names or `ExternalProcess`, never legacy `Arguments`. |
| T-11-08-02 | I | process log boundary tests | mitigate | Add successful-start logger/PID-store regression coverage with omitted `pluginName`. |
</threat_model>

<verification>
Run `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~ProcessExecutionServiceTests|FullyQualifiedName~Phase11LogBoundaryTests" --nologo` and confirm all targeted tests pass.
</verification>

<success_criteria>
- Verification gap #2 in `11-VERIFICATION.md` is closed.
- `ProcessExecutionService` no longer uses raw legacy `Arguments` as a PID tracking label.
- Successful-start tests cover both captured logs and PID-store labels.
</success_criteria>

<output>
After completion, create `.planning/phases/11-user-facing-diagnostics-boundaries/11-08-SUMMARY.md`.
</output>
