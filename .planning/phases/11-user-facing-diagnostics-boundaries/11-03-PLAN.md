---
phase: 11-user-facing-diagnostics-boundaries
plan: 03
type: tdd
wave: 2
depends_on: [11-01]
files_modified:
  - AutoQAC/ViewModels/MainWindow/ConfigurationViewModel.cs
  - AutoQAC/ViewModels/RestoreViewModel.cs
  - AutoQAC.Tests/ViewModels/MainWindowViewModelTests.cs
  - AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs
  - AutoQAC.Tests/ViewModels/SettingsViewModelTests.cs
autonomous: true
requirements: [SEC-01]
must_haves:
  truths:
    - "D-06: Configuration browse failures use safe game-folder labels rather than selected full folder paths."
    - "D-07: Configuration and persistence diagnostics use sanitized display names when basenames are shown."
    - "D-08: Restore session load and technical configuration failures use concise latest-log guidance rather than raw exception messages, while simple missing-path validation omits extra log guidance."
    - "D-04: Settings persistence write/read failures preserve ConfigPersistenceFailure.SafeSummary and never surface unsafe path-bearing exception text."
    - "D-04: Existing typed safe labels from backup and persistence flows remain preserved."
  artifacts:
    - path: "AutoQAC/ViewModels/MainWindow/ConfigurationViewModel.cs"
      provides: "Safe browse-path diagnostics for load-order and game-data folder surfaces"
    - path: "AutoQAC/ViewModels/RestoreViewModel.cs"
      provides: "Safe backup-session loading failure status"
    - path: "AutoQAC.Tests/ViewModels/MainWindowViewModelTests.cs"
      provides: "Configuration path disclosure regressions"
    - path: "AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs"
      provides: "Restore session load disclosure regression"
    - path: "AutoQAC.Tests/ViewModels/SettingsViewModelTests.cs"
      provides: "Settings persistence write/read failure disclosure regression preserving ConfigPersistenceFailure.SafeSummary"
  key_links:
    - from: "ConfigurationViewModel"
      to: "DiagnosticTextFormatter.SafeFileIdentifier/SafeFolderIssue"
      via: "browse failure catch blocks"
      pattern: "Safe(FileIdentifier|FolderIssue)"
---

<objective>
Harden configuration browse-path and restore session-loading diagnostics.

Purpose: Phase 11 must cover inline/status/dialog surfaces beyond the cleaning command path, especially selected load-order files, selected data folders, and restore session loading.
Output: Tested safe labels and latest-log guidance for configuration technical failures and restore session load failures, preserving already-safe backup labels per D-04.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-SPEC.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-CONTEXT.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-PATTERNS.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-UI-SPEC.md
@.planning/phases/09-plugin-refresh-approximation-performance/09-03-SUMMARY.md
@.planning/phases/10-configuration-persistence-hardening/10-05-SUMMARY.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-01-SUMMARY.md
@AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Safe configuration browse failure copy</name>
  <read_first>
    - AutoQAC/ViewModels/MainWindow/ConfigurationViewModel.cs
    - AutoQAC.Tests/ViewModels/MainWindowViewModelTests.cs
    - AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs
    - .planning/phases/11-user-facing-diagnostics-boundaries/11-UI-SPEC.md
  </read_first>
  <files>AutoQAC/ViewModels/MainWindow/ConfigurationViewModel.cs, AutoQAC.Tests/ViewModels/MainWindowViewModelTests.cs</files>
  <behavior>
    - Invalid selected load-order file technical failures use `Load Order File (plugins.txt)` plus `See the latest AutoQAC log for technical details.` and exclude `C:\Users\Alice` and raw exception text.
    - Selected game data folder failures use exactly `{Game} data folder is unavailable. Choose a valid Data folder or reset the override.` and exclude the selected folder path.
    - Simple missing-path validation/browse copy omits latest-log guidance unless the catch represents read/parse/unexpected technical failure per D-08.
  </behavior>
  <action>Add failing tests around existing configuration ViewModel browse/load-order/data-folder error paths using sentinel paths `C:\Users\Alice\AppData\Local\Skyrim Special Edition\plugins.txt` and `C:\Games\Skyrim Special Edition\Data`. Then update `ConfigurationViewModel` catch/status/dialog text to use `DiagnosticTextFormatter.SafeFileIdentifier("Load Order File", path, "load order file")`, `DiagnosticTextFormatter.SafeFolderIssue(SelectedGameDisplayName or "selected game")`, and `DiagnosticTextFormatter.OperationFailed("Load order selection")` only for unexpected technical failures. Copy must include `latest AutoQAC log` for read/parse/unexpected failures and must not include `ex.Message` in user-facing text. Do not change plugin refresh coordinator calls, skip-list behavior, selected game persistence, or file-dialog selection behavior.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~MainWindowViewModelTests|FullyQualifiedName~Configuration" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/ViewModels/MainWindow/ConfigurationViewModel.cs` contains `DiagnosticTextFormatter.SafeFileIdentifier("Load Order File"`.
    - `AutoQAC/ViewModels/MainWindow/ConfigurationViewModel.cs` contains `DiagnosticTextFormatter.SafeFolderIssue`.
    - New/updated tests assert no captured configuration status/dialog text contains `C:\Users\Alice`.
    - Focused configuration-related tests pass.
  </acceptance_criteria>
  <done>Configuration path-related user text uses safe identifiers and exact UI-SPEC action guidance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Safe restore session loading failure status</name>
  <read_first>
    - AutoQAC/ViewModels/RestoreViewModel.cs
    - AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs
    - AutoQAC/Models/BackupOperationResults.cs
    - AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs
    - .planning/phases/11-user-facing-diagnostics-boundaries/11-UI-SPEC.md
  </read_first>
  <files>AutoQAC/ViewModels/RestoreViewModel.cs, AutoQAC.Tests/ViewModels/RestoreViewModelTests.cs</files>
  <behavior>
    - `LoadSessions()` exception with message `Access denied reading C:\Users\Alice\Backups\session.json` sets `StatusText` to exactly `Backup sessions could not be loaded. See the latest AutoQAC log for technical details.`.
    - Existing restore/delete safe category labels from `BackupFailureReason.ToDisplayLabel()` remain unchanged.
  </behavior>
  <action>Add a failing restore ViewModel test for backup session load failure with a path-bearing exception. Update `RestoreViewModel.LoadSessions()` general catch to keep `_logger.Error(ex, "Failed to load backup sessions")` and set `StatusText` to exactly `Backup sessions could not be loaded. See the latest AutoQAC log for technical details.` per UI-SPEC. Do not change restore, delete, retention, or `BackupFailureReason` display-label mappings; D-04 requires preserving those already-safe labels.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~RestoreViewModelTests --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/ViewModels/RestoreViewModel.cs` contains `Backup sessions could not be loaded. See the latest AutoQAC log for technical details.`.
    - `AutoQAC/ViewModels/RestoreViewModel.cs` no longer contains `Error loading sessions: {ex.Message}`.
    - `RestoreViewModelTests` includes an assertion that `StatusText` does not contain `C:\Users\Alice`.
    - Focused RestoreViewModelTests pass.
  </acceptance_criteria>
  <done>Restore session loading status no longer surfaces raw exception/path text.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Settings persistence failure safe-summary coverage</name>
  <read_first>
    - AutoQAC/ViewModels/SettingsViewModel.cs
    - AutoQAC.Tests/ViewModels/SettingsViewModelTests.cs
    - AutoQAC/Services/Configuration/ConfigPersistenceStatus.cs
    - .planning/phases/10-configuration-persistence-hardening/10-05-SUMMARY.md
    - .planning/phases/11-user-facing-diagnostics-boundaries/11-SPEC.md
  </read_first>
  <files>AutoQAC.Tests/ViewModels/SettingsViewModelTests.cs</files>
  <behavior>
    - A persistence write failure carrying unsafe detail such as `UnauthorizedAccessException: denied J:\Users\Alice\AutoQAC Settings.yaml` surfaces the existing `ConfigPersistenceFailure.SafeSummary` category text and excludes `J:\Users\Alice`, `AutoQAC Settings.yaml`, and the raw exception type/message.
    - A persistence read failure carrying unsafe detail such as `IOException reading D:\Profiles\AutoQAC Main.yaml` surfaces the existing `ConfigPersistenceFailure.SafeSummary` category text and excludes `D:\Profiles`, `AutoQAC Main.yaml`, and the raw exception type/message.
    - Tests assert the SettingsViewModel banner/status path preserves the safe typed category from D-04 rather than replacing it with generic failure text.
  </behavior>
  <action>Add targeted SettingsViewModel tests that inject or publish `ConfigPersistenceStatus`/`ConfigPersistenceFailure` write and read failures with unsafe path-bearing detail, then assert the displayed persistence banner/status equals or contains the expected `ConfigPersistenceFailure.SafeSummary` value and excludes every unsafe sentinel. This is a coverage-only task for SPEC requirement 4 and D-04: do not change `SettingsViewModel` unless the tests expose an actual raw-detail leak; if implementation is needed, map from the existing `ConfigPersistenceFailure.SafeSummary` property and keep all configuration save/load behavior unchanged.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~SettingsViewModelTests --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC.Tests/ViewModels/SettingsViewModelTests.cs` contains a write-failure test with `J:\Users\Alice` or another non-C drive-rooted sentinel.
    - `AutoQAC.Tests/ViewModels/SettingsViewModelTests.cs` contains a read-failure test with `D:\Profiles` or another non-C drive-rooted sentinel.
    - Settings persistence failure tests assert user-visible text contains `SafeSummary` and does not contain unsafe path-bearing input.
    - Focused SettingsViewModelTests pass.
  </acceptance_criteria>
  <done>Settings persistence write/read failure diagnostics satisfy SPEC requirement 4 and D-04 with automated negative-disclosure coverage.</done>
</task>

</tasks>

<verification>
Run all task-level focused test commands, then `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~MainWindowViewModelTests|FullyQualifiedName~RestoreViewModelTests|FullyQualifiedName~SettingsViewModelTests|FullyQualifiedName~Configuration" --nologo`.
</verification>

<success_criteria>
Configuration selected path problems, backup-session loading failures, and Settings persistence write/read failure banners satisfy D-04, D-06, D-07, D-08, UI-SPEC copy, SPEC requirement 4, and SEC-01 with automated negative-disclosure tests.
</success_criteria>

<output>
After completion, create `.planning/phases/11-user-facing-diagnostics-boundaries/11-03-SUMMARY.md`.
</output>
