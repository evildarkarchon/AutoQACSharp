---
phase: 11-user-facing-diagnostics-boundaries
plan: 07
type: execute
wave: 4
depends_on: [11-06]
files_modified:
  - AutoQAC/App.axaml.cs
  - AutoQAC/Services/Configuration/LegacyMigrationService.cs
  - AutoQAC.Tests/Services/LegacyMigrationServiceTests.cs
  - AutoQAC.Tests/Integration/DependencyInjectionTests.cs
autonomous: true
gap_closure: true
requirements: [SEC-01]
must_haves:
  truths:
    - "SEC-01 / D-01 / D-02 / D-03: legacy migration warnings shown through MainWindowViewModel.ShowMigrationWarning use concise safe latest-log guidance, not raw exception messages."
    - "SEC-01 / D-14: migration implementation may log local exception objects/details, but warning text that reaches UI does not include path, command, exception, or stack sentinels."
  artifacts:
    - path: "AutoQAC/Services/Configuration/LegacyMigrationService.cs"
      provides: "Safe migration warning messages for parse, write, backup, and delete outcomes"
      contains: "See the latest AutoQAC log"
    - path: "AutoQAC/App.axaml.cs"
      provides: "Defensive UI warning boundary before ShowMigrationWarning"
      contains: "DiagnosticTextFormatter.SafeFailureSummary"
    - path: "AutoQAC.Tests/Services/LegacyMigrationServiceTests.cs"
      provides: "Regression coverage for path-bearing migration warnings"
    - path: "AutoQAC.Tests/Integration/DependencyInjectionTests.cs"
      provides: "Startup source guard for migration warning forwarding"
  key_links:
    - from: "AutoQAC/Services/Configuration/LegacyMigrationService.cs"
      to: "AutoQAC/App.axaml.cs"
      via: "MigrationResult.WarningMessage"
      pattern: "WarningMessage.*See the latest AutoQAC log"
    - from: "AutoQAC/App.axaml.cs"
      to: "MainWindowViewModel.ShowMigrationWarning"
      via: "SafeFailureSummary before warning display"
      pattern: "ShowMigrationWarning\(.*SafeFailureSummary"
---

<objective>
Close Phase 11 verification gap #1: legacy migration user warnings must use safe latest-log guidance instead of raw exception messages.

Purpose: SEC-01 and decisions D-01 through D-03 require migration warning UI to avoid raw exception/path/command details while keeping actionable guidance.
Output: Safe migration result warning copy, a defensive startup display boundary, and regression tests proving path-bearing migration warnings are not shown.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-VERIFICATION.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-CONTEXT.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-RESEARCH.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-01-SUMMARY.md
@.planning/phases/11-user-facing-diagnostics-boundaries/11-05-SUMMARY.md
@AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs
@AutoQAC/App.axaml.cs
@AutoQAC/Services/Configuration/LegacyMigrationService.cs
@AutoQAC.Tests/Services/LegacyMigrationServiceTests.cs
@AutoQAC.Tests/Integration/DependencyInjectionTests.cs

<interfaces>
From `AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs`:
```csharp
public const string LatestLogDetails = "Technical details were written to the latest AutoQAC log.";
public static string OperationFailed(string operation);
public static string SafeFailureSummary(string? candidate, string fallback);
```

From `AutoQAC/App.axaml.cs`:
```csharp
private static async Task RunMigrationAsync(
    ILegacyMigrationService migrationService,
    MainWindowViewModel viewModel,
    ILoggingService logger)
```

From `AutoQAC/Services/Configuration/LegacyMigrationService.cs`:
```csharp
public async Task<MigrationResult> MigrateIfNeededAsync(CancellationToken ct = default)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove legacy migration warnings exclude raw exception details</name>
  <files>AutoQAC.Tests/Services/LegacyMigrationServiceTests.cs, AutoQAC.Tests/Integration/DependencyInjectionTests.cs</files>
  <read_first>AutoQAC.Tests/Services/LegacyMigrationServiceTests.cs; AutoQAC.Tests/Integration/DependencyInjectionTests.cs; AutoQAC.Tests/Helpers/DiagnosticSentinels.cs; AutoQAC/Services/Configuration/LegacyMigrationService.cs; AutoQAC/App.axaml.cs</read_first>
  <behavior>
    - Test 1: invalid legacy YAML returns a non-empty `WarningMessage` that contains "Legacy config could not be migrated" and "See the latest AutoQAC log", and does not contain `C:\Users\Alice`, `SSEEdit.exe -QAC`, `System.InvalidOperationException`, `Stack Trace`, or ` at AutoQAC.` per SEC-01/D-01/D-03.
    - Test 2: write failure caused by a directory at `AutoQAC Settings.yaml` returns a safe warning containing "new settings file could not be written" and latest-log guidance without the blocked target path or exception text per SEC-01/D-02.
    - Test 3: backup failure caused by a file named `migration_backup` returns a safe warning containing "could not back up the original file" and "Original file was kept for safety" without the temp directory path or exception text per D-04 preservation of safety semantics.
    - Test 4: `AppStartupSource_ShouldUseSafeDiagnosticFieldsAndMigrationWarningCopy` asserts `App.axaml.cs` calls `DiagnosticTextFormatter.SafeFailureSummary` before `ShowMigrationWarning`, and does not pass `result.WarningMessage` directly.
  </behavior>
  <action>Add RED regression tests for the verification gap. In `LegacyMigrationServiceTests`, add helper assertions that inspect `result.WarningMessage` and reject the shared unsafe sentinel fragments plus `_testDirectory`, `IOException`, `UnauthorizedAccess`, and `Exception`. Keep the existing filesystem-isolated temp-directory pattern. In `DependencyInjectionTests.AppStartupSource_ShouldUseSafeDiagnosticFieldsAndMigrationWarningCopy`, add source assertions that `appSource` contains `DiagnosticTextFormatter.SafeFailureSummary(result.WarningMessage` and does not contain `ShowMigrationWarning(result.WarningMessage)`. Do not change production code in this task.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~LegacyMigrationServiceTests|FullyQualifiedName~DependencyInjectionTests" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC.Tests/Services/LegacyMigrationServiceTests.cs` contains `WarningMessage.Should().Contain("See the latest AutoQAC log")`.
    - `AutoQAC.Tests/Services/LegacyMigrationServiceTests.cs` contains `WarningMessage.Should().NotContain(_testDirectory)`.
    - `AutoQAC.Tests/Integration/DependencyInjectionTests.cs` contains `DiagnosticTextFormatter.SafeFailureSummary(result.WarningMessage`.
    - The automated command fails before Task 2 because production still forwards/directly creates unsafe warning text.
  </acceptance_criteria>
  <done>Failing tests capture the path-bearing migration warning gap without production changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace migration warning UI copy with safe categories</name>
  <files>AutoQAC/App.axaml.cs, AutoQAC/Services/Configuration/LegacyMigrationService.cs, AutoQAC.Tests/Services/LegacyMigrationServiceTests.cs, AutoQAC.Tests/Integration/DependencyInjectionTests.cs</files>
  <read_first>AutoQAC/App.axaml.cs; AutoQAC/Services/Configuration/LegacyMigrationService.cs; AutoQAC/Models/Diagnostics/DiagnosticTextFormatter.cs; AutoQAC.Tests/Services/LegacyMigrationServiceTests.cs; AutoQAC.Tests/Integration/DependencyInjectionTests.cs</read_first>
  <action>Implement the GREEN fix per SEC-01/D-01/D-02/D-03/D-04. In `LegacyMigrationService.cs`, replace all `WarningMessage` interpolations that include `ex.Message` with exact safe copy: parse failure -> `Legacy config could not be migrated because it could not be parsed. See the latest AutoQAC log for technical details.`; write failure -> `Legacy config could not be migrated because the new settings file could not be written. See the latest AutoQAC log for technical details.`; backup failure -> `Legacy config was migrated, but AutoQAC could not back up the original file. Original file was kept for safety. See the latest AutoQAC log for technical details.`; delete failure -> `Legacy config was migrated, but AutoQAC could not remove the original legacy file. See the latest AutoQAC log for technical details.` Keep logger calls with the exception object/message unchanged where they are local logs. In `App.axaml.cs`, defensively compute `var warningMessage = DiagnosticTextFormatter.SafeFailureSummary(result.WarningMessage, "Some legacy settings could not be migrated. See the latest AutoQAC log for technical details.");` and pass `warningMessage` to `ShowMigrationWarning`, so future service warnings that accidentally include paths/commands are blocked at the UI boundary. Preserve migration success/failure behavior and backup-then-delete order.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~LegacyMigrationServiceTests|FullyQualifiedName~DependencyInjectionTests" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Services/Configuration/LegacyMigrationService.cs` has no `WarningMessage: $` occurrences that interpolate `ex.Message`.
    - `AutoQAC/Services/Configuration/LegacyMigrationService.cs` contains the exact four safe warning strings listed in the task action.
    - `AutoQAC/App.axaml.cs` contains `DiagnosticTextFormatter.SafeFailureSummary(result.WarningMessage`.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>Legacy migration warnings shown to the user are safe latest-log guidance and tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy config file -> migration warning UI | Local YAML parse/write/backup/delete failures can contain user paths or exception text before warning text reaches MainWindowViewModel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07-01 | I | `LegacyMigrationService.MigrationResult.WarningMessage` | mitigate | Replace raw `ex.Message` warning strings with safe typed latest-log copy and add negative-disclosure tests. |
| T-11-07-02 | I | `App.RunMigrationAsync` | mitigate | Wrap migration warning display with `DiagnosticTextFormatter.SafeFailureSummary` before `ShowMigrationWarning`. |
</threat_model>

<verification>
Run `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~LegacyMigrationServiceTests|FullyQualifiedName~DependencyInjectionTests" --nologo` and confirm all targeted tests pass.
</verification>

<success_criteria>
- Verification gap #1 in `11-VERIFICATION.md` is closed.
- `ShowMigrationWarning` no longer receives raw `result.WarningMessage` directly.
- Migration warning tests reject path, command, exception, and stack sentinel fragments.
</success_criteria>

<output>
After completion, create `.planning/phases/11-user-facing-diagnostics-boundaries/11-07-SUMMARY.md`.
</output>
