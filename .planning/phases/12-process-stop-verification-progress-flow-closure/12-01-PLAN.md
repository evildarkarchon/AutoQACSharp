---
phase: 12-process-stop-verification-progress-flow-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AutoQAC/Models/StopTerminationDialogContent.cs
  - AutoQAC/Services/UI/IMessageDialogService.cs
  - AutoQAC/Services/UI/MessageDialogService.cs
  - AutoQAC/ViewModels/MessageDialogViewModel.cs
  - AutoQAC/Views/MessageDialog.axaml
  - AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs
  - AutoQAC.Tests/ViewModels/MainWindowViewModelTests.cs
autonomous: true
requirements:
  - SAF-01
  - SAF-02
must_haves:
  truths:
    - "D-01/D-02: Main Stop and Progress Stop have one shared exact text contract for confirmation, force-failure, and leave-running outcomes."
    - "D-03: Stop confirmation presents action labels exactly as Force Terminate and Leave Running, not generic Yes/No."
    - "D-04: Stop outcome copy is Phase 11-safe and excludes raw exceptions, stack traces, full paths, and command lines."
    - "D-08: Main Stop status and dialogs use the shared outcome language so they do not contradict Progress Stop wording."
  artifacts:
    - path: "AutoQAC/Models/StopTerminationDialogContent.cs"
      provides: "Shared exact stop outcome title, messages, and button labels"
      contains: "Force Terminate"
    - path: "AutoQAC/Services/UI/IMessageDialogService.cs"
      provides: "Dialog API for custom confirmation labels"
      contains: "ShowChoiceAsync"
    - path: "AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs"
      provides: "Main Stop uses shared text/labels"
      contains: "StopTerminationDialogContent"
    - path: "AutoQAC.Tests/ViewModels/MainWindowViewModelTests.cs"
      provides: "Exact shared copy and label assertions for main Stop"
      contains: "Force Terminate"
  key_links:
    - from: "AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs"
      to: "AutoQAC/Models/StopTerminationDialogContent.cs"
      via: "shared constants"
      pattern: "StopTerminationDialogContent\."
    - from: "AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs"
      to: "IMessageDialogService.ShowChoiceAsync"
      via: "explicit action-label confirmation"
      pattern: "ShowChoiceAsync"
---

<objective>
Create the shared stop outcome text and dialog-label contract used by every Stop surface.

Purpose: Phase 12 decisions D-01 through D-04 require main Stop and Progress Stop to share exact, Phase 11-safe copy and explicit `Force Terminate` / `Leave Running` action labels so the two surfaces cannot drift.
Output: Shared stop copy constants, a narrow dialog service custom-choice API, updated main Stop caller, and tests proving the main Stop path uses the shared contract.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-process-stop-verification-progress-flow-closure/12-SPEC.md
@.planning/phases/12-process-stop-verification-progress-flow-closure/12-CONTEXT.md
@.planning/phases/12-process-stop-verification-progress-flow-closure/12-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs
@AutoQAC/Services/UI/IMessageDialogService.cs
@AutoQAC/Services/UI/MessageDialogService.cs
@AutoQAC/ViewModels/MessageDialogViewModel.cs
@AutoQAC/Views/MessageDialog.axaml
@AutoQAC.Tests/ViewModels/MainWindowViewModelTests.cs

<interfaces>
From `AutoQAC/Services/UI/IMessageDialogService.cs`:
```csharp
public enum MessageDialogResult
{
    None,
    Ok,
    Cancel,
    Yes,
    No,
    Retry
}

public interface IMessageDialogService
{
    Task<MessageDialogResult> ShowAsync(string title, string message, MessageDialogButtons buttons = MessageDialogButtons.Ok, MessageDialogIcon icon = MessageDialogIcon.None, string? details = null);
    Task<bool> ShowConfirmAsync(string title, string message);
}
```

From `AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs`:
```csharp
private async Task StopCleaningAsync()
{
    StatusText = "Stopping...";
    var stopResult = await _orchestrator.StopCleaningAsync();
    var terminationResult = stopResult.TerminationResult ?? _orchestrator.LastTerminationResult;
    // GracePeriodExpired currently uses ShowConfirmAsync and hardcoded copy.
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared stop copy contract and custom choice dialog API</name>
  <files>AutoQAC/Models/StopTerminationDialogContent.cs, AutoQAC/Services/UI/IMessageDialogService.cs, AutoQAC/Services/UI/MessageDialogService.cs, AutoQAC/ViewModels/MessageDialogViewModel.cs, AutoQAC/Views/MessageDialog.axaml, AutoQAC.Tests/ViewModels/MainWindowViewModelTests.cs</files>
  <read_first>
    - AutoQAC/Services/UI/IMessageDialogService.cs
    - AutoQAC/Services/UI/MessageDialogService.cs
    - AutoQAC/ViewModels/MessageDialogViewModel.cs
    - AutoQAC/Views/MessageDialog.axaml
    - AutoQAC.Tests/ViewModels/MainWindowViewModelTests.cs
  </read_first>
  <behavior>
    - Test 1: A main Stop confirmation assertion can verify the exact labels `Force Terminate` and `Leave Running` are supplied through the dialog service per D-03.
    - Test 2: Shared constants expose the exact title/message strings listed in this task so D-01/D-02 copy drift is testable.
  </behavior>
  <action>Create `AutoQAC/Models/StopTerminationDialogContent.cs` as a public/static shared text contract with XML docs. It must define exact constants: `ConfirmationTitle = "Force Terminate xEdit?"`, `ConfirmationMessage = "xEdit did not exit after the stop request. AutoQAC can leave xEdit running, or force terminate it now. Force terminating can interrupt remaining file or log writes."`, `ForceTerminateButton = "Force Terminate"`, `LeaveRunningButton = "Leave Running"`, `ForceFailureTitle = "Could Not Force Terminate xEdit"`, `ForceFailureMessage = "AutoQAC could not force terminate xEdit. xEdit may still be running; close it manually before starting another cleaning session. Technical details are in the latest AutoQAC log."`, `LeftRunningTitle = "Cleaning Stopped"`, and `LeftRunningMessage = "AutoQAC stopped the cleaning session. xEdit was left running by your choice; close it manually when it is safe."` per D-01 through D-04. Extend `IMessageDialogService` with `Task<MessageDialogResult> ShowChoiceAsync(string title, string message, string primaryButtonText, string secondaryButtonText, MessageDialogIcon icon = MessageDialogIcon.Question, string? details = null);`. Implement it in `MessageDialogService` by creating the existing `MessageDialogViewModel`, setting the custom labels, and returning `MessageDialogResult.Yes` for the primary button and `MessageDialogResult.No` for the secondary button. Add `YesButtonText` and `NoButtonText` bindable properties defaulting to `"Yes"` and `"No"` in `MessageDialogViewModel`; bind the Yes/No button `Content` in `MessageDialog.axaml` to those properties. Preserve `ShowConfirmAsync` behavior by leaving its public signature and Yes/No defaults intact.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~MainWindowViewModelTests</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Models/StopTerminationDialogContent.cs` contains `ForceTerminateButton = "Force Terminate"` and `LeaveRunningButton = "Leave Running"`.
    - `AutoQAC/Services/UI/IMessageDialogService.cs` contains `ShowChoiceAsync(string title, string message, string primaryButtonText, string secondaryButtonText`.
    - `AutoQAC/ViewModels/MessageDialogViewModel.cs` contains `YesButtonText` and `NoButtonText` properties defaulting to `Yes` and `No`.
    - `AutoQAC/Views/MessageDialog.axaml` binds Yes and No button `Content` to `YesButtonText` and `NoButtonText`.
  </acceptance_criteria>
  <done>Dialog service can render custom confirmation labels without changing existing Yes/No callers, and the shared stop copy contract exists with the exact Phase 12 text.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update main Stop to use the shared choice contract</name>
  <files>AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs, AutoQAC.Tests/ViewModels/MainWindowViewModelTests.cs</files>
  <read_first>
    - AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs
    - AutoQAC/Models/StopTerminationDialogContent.cs
    - AutoQAC.Tests/ViewModels/MainWindowViewModelTests.cs
  </read_first>
  <behavior>
    - Test 1: `StopCleaningCommand_GracePeriodExpired_UserConfirms_ShouldForceStop` configures `ShowChoiceAsync(...).Returns(MessageDialogResult.Yes)` and verifies `ForceStopCleaningAsync()` is called once.
    - Test 2: `StopCleaningCommand_GracePeriodExpired_UserDeclines_ShouldNotForceStop` configures `ShowChoiceAsync(...).Returns(MessageDialogResult.No)`, verifies `MarkLeftRunningByUser()` is called once, and verifies `ForceStopCleaningAsync()` is not called.
    - Test 3: `StopCleaningCommand_ForceKillFailed_ShouldShowErrorCopy` asserts `ShowErrorAsync` receives `StopTerminationDialogContent.ForceFailureTitle` and `StopTerminationDialogContent.ForceFailureMessage`.
  </behavior>
  <action>Replace `CleaningCommandsViewModel.StopCleaningAsync` hardcoded `ShowConfirmAsync` usage with `ShowChoiceAsync(StopTerminationDialogContent.ConfirmationTitle, StopTerminationDialogContent.ConfirmationMessage, StopTerminationDialogContent.ForceTerminateButton, StopTerminationDialogContent.LeaveRunningButton, MessageDialogIcon.Question)`. Treat `MessageDialogResult.Yes` as confirmed force termination and every other result as leave-running. Replace the hardcoded force-failure and leave-running dialog copy with `StopTerminationDialogContent.ForceFailureTitle`, `StopTerminationDialogContent.ForceFailureMessage`, `StopTerminationDialogContent.LeftRunningTitle`, and `StopTerminationDialogContent.LeftRunningMessage` per D-01/D-04/D-08. Keep existing orchestration semantics unchanged: stop first, then force only after affirmative confirmation; decline calls `MarkLeftRunningByUser()` and sets `StatusText = "Cleaning stopped; xEdit left running."`.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~MainWindowViewModelTests</automated>
  </verify>
  <acceptance_criteria>
    - `CleaningCommandsViewModel.cs` contains `ShowChoiceAsync(StopTerminationDialogContent.ConfirmationTitle`.
    - `CleaningCommandsViewModel.cs` no longer contains `ShowConfirmAsync(` inside `StopCleaningAsync`.
    - `MainWindowViewModelTests.cs` contains assertions for `StopTerminationDialogContent.ForceTerminateButton` and `StopTerminationDialogContent.LeaveRunningButton`.
    - The targeted test command exits 0.
  </acceptance_criteria>
  <done>Main Stop preserves existing two-stage behavior while consuming the exact shared Stop text and explicit action-label API required by D-01 through D-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ViewModel→Dialog UI | User-facing text crosses from ViewModels/models into Avalonia TextBlocks. |
| User Stop→Process control | A UI action decides whether AutoQAC escalates from graceful stop to force termination. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | `CleaningCommandsViewModel.StopCleaningAsync` | mitigate | Use shared constants and exact tests so main/progress copy cannot silently drift from D-01/D-02. |
| T-12-02 | Information Disclosure | `StopTerminationDialogContent` user copy | mitigate | Use fixed Phase 11-safe strings with latest-log guidance and no raw exception/path/command interpolation per D-04. |
| T-12-03 | Elevation of Privilege | Stop confirmation action labels | mitigate | Preserve force kill only behind affirmative `MessageDialogResult.Yes` from explicit `Force Terminate` label per D-03. |
</threat_model>

<verification>
Run `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter FullyQualifiedName~MainWindowViewModelTests` and confirm it exits 0. Confirm source search shows no `ShowConfirmAsync(` call remains in `CleaningCommandsViewModel.StopCleaningAsync`.
</verification>

<success_criteria>
- Shared stop copy exists in one model/helper file and contains all exact strings listed in D-01 through D-04.
- Main Stop uses `ShowChoiceAsync` with `Force Terminate` and `Leave Running`.
- Main Stop tests assert exact labels and shared failure/leave-running copy.
- Existing Yes/No dialog callers continue to compile and pass tests.
</success_criteria>

<output>
After completion, create `.planning/phases/12-process-stop-verification-progress-flow-closure/12-01-SUMMARY.md`
</output>
