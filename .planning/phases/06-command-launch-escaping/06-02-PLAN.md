---
phase: 06-command-launch-escaping
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - AutoQAC/Services/Process/ProcessExecutionService.cs
  - AutoQAC.Tests/Services/ProcessExecutionIntegrationTests.cs
  - AutoQAC.Tests/TestProcessHelper/Program.cs
autonomous: true
requirements:
  - SAF-03
  - TEST-02
must_haves:
  truths:
    - "D-13: ProcessExecutionService preserves ProcessStartInfo.ArgumentList through its internal clone and real process launch."
    - "D-14: AutoQAC.TestProcessHelper exposes an argv-echo mode instead of adding a second helper process."
    - "D-03/D-04: Integration tests prove Unicode and shell-sensitive characters arrive as literal argv text under UseShellExecute=false."
    - "D-01/D-15/D-16: Quote/parser and combined worst-case cases are verified through parsed argv rather than raw direct command strings."
    - "Reviews: Process-boundary tests assert helper exit code 0 plus exact parsed JSON argv, and they preserve legacy `Arguments` fallback when `ArgumentList` is empty."
  artifacts:
    - path: "AutoQAC.Tests/TestProcessHelper/Program.cs"
      provides: "argv-echo helper mode"
      contains: "argv-echo"
    - path: "AutoQAC/Services/Process/ProcessExecutionService.cs"
      provides: "ArgumentList-preserving process start clone"
      contains: "startInfo.ArgumentList"
    - path: "AutoQAC.Tests/Services/ProcessExecutionIntegrationTests.cs"
      provides: "Real helper-process argv preservation tests"
      contains: "JsonSerializer.Deserialize"
  key_links:
    - from: "AutoQAC/Services/Process/ProcessExecutionService.cs"
      to: "System.Diagnostics.ProcessStartInfo.ArgumentList"
      via: "ExecuteAsync copies each provided ArgumentList entry into the processStartInfo used by Process.Start"
      pattern: "foreach.*ArgumentList"
    - from: "AutoQAC.Tests/Services/ProcessExecutionIntegrationTests.cs"
      to: "AutoQAC.Tests/TestProcessHelper/Program.cs"
      via: "helper process argv-echo mode writes JSON stdout that integration tests parse"
      pattern: "argv-echo"
---

<objective>
Prove and implement preservation of `ProcessStartInfo.ArgumentList` through the actual process-start boundary.

Purpose: A command-builder fix is incomplete if `ProcessExecutionService` drops `ArgumentList` while cloning `ProcessStartInfo`; users need the actual launched process to receive the intended argv.
Output: UTF-8 JSON argv echo helper, integration tests, and process-layer clone changes.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-command-launch-escaping/06-CONTEXT.md
@.planning/phases/06-command-launch-escaping/06-RESEARCH.md
@.planning/phases/06-command-launch-escaping/06-REVIEWS.md
@.planning/phases/06-command-launch-escaping/06-VALIDATION.md
@.planning/phases/05-process-stop-pid-safety/05-04-SUMMARY.md
@AutoQAC/Services/Process/ProcessExecutionService.cs
@AutoQAC.Tests/Services/ProcessExecutionIntegrationTests.cs
@AutoQAC.Tests/TestProcessHelper/Program.cs

<interfaces>
From `AutoQAC/Services/Process/IProcessExecutionService.cs`:
```csharp
Task<ProcessResult> ExecuteAsync(
    ProcessStartInfo startInfo,
    TimeSpan? timeout = null,
    CancellationToken ct = default,
    Action<System.Diagnostics.Process>? onProcessStarted = null,
    string? pluginName = null);
```

From `AutoQAC.Tests/Services/ProcessExecutionIntegrationTests.cs` helper pattern:
```csharp
private static ProcessStartInfo HelperStartInfo(string arguments)
{
    var helperPath = Path.Combine(AppContext.BaseDirectory, "AutoQAC.TestProcessHelper.exe");
    return new ProcessStartInfo { FileName = helperPath, Arguments = arguments, UseShellExecute = false, CreateNoWindow = true };
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add argv-echo helper mode and failing preservation tests</name>
  <files>AutoQAC.Tests/TestProcessHelper/Program.cs, AutoQAC.Tests/Services/ProcessExecutionIntegrationTests.cs</files>
  <read_first>
    - AutoQAC.Tests/TestProcessHelper/Program.cs
    - AutoQAC.Tests/Services/ProcessExecutionIntegrationTests.cs
    - .planning/phases/05-process-stop-pid-safety/05-04-SUMMARY.md
    - .planning/phases/06-command-launch-escaping/06-CONTEXT.md
  </read_first>
  <behavior>
    - `argv-echo` mode writes `args.Skip(1).ToArray()` as UTF-8 JSON to stdout and returns exit code 0.
    - Integration test launches helper using `ProcessStartInfo.ArgumentList` entries: `argv-echo`, `Quote " literal`, `Résumé 測試 🚀`, `Shell & | ; ( ) ^ value`, and exact combined worst-case value `Quote" Résumé 測試 🚀 & | ; ( ) ^ Spaces.esp`.
    - Test redirects stdout, sets `StandardOutputEncoding = Encoding.UTF8`, captures stdout through one reader in `onProcessStarted` because `ProcessResult` does not expose stdout, parses JSON with `JsonSerializer.Deserialize<string[]>`, and asserts `result.ExitCode.Should().Be(0)` plus exact sequence equality.
    - Add a legacy fallback regression that launches helper with `Arguments = "argv-echo legacy-token"`, empty `ArgumentList`, and asserts exit code 0 plus JSON `new[] { "legacy-token" }` so non-Phase-6 callers keep working.
  </behavior>
  <action>Extend the existing `AutoQAC.TestProcessHelper` switch with `case "argv-echo"` per D-14; do not create a new helper project. In `ProcessExecutionIntegrationTests`, add a helper factory overload that accepts `params string[] argumentList` and populates `ProcessStartInfo.ArgumentList` rather than `Arguments`. Add a local async helper such as `ReadHelperJsonAsync(Process process)` used from `onProcessStarted` so only one consumer reads redirected stdout. Add failing tests that prove the current `ProcessExecutionService` clone drops `ArgumentList` entries; each failing assertion must check both `result.ExitCode.Should().Be(0)` and `JsonSerializer.Deserialize<string[]>(stdout).Should().Equal(expectedArgs)` to address the review concern about weak red assertions.</action>
  <verify>
    <automated>dotnet test AutoQACSharp.slnx --filter FullyQualifiedName~ProcessExecutionIntegrationTests</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC.Tests/TestProcessHelper/Program.cs` contains `case "argv-echo"`.
    - `AutoQAC.Tests/TestProcessHelper/Program.cs` contains `JsonSerializer.Serialize`.
    - `AutoQAC.Tests/Services/ProcessExecutionIntegrationTests.cs` contains `StandardOutputEncoding = Encoding.UTF8`.
    - `AutoQAC.Tests/Services/ProcessExecutionIntegrationTests.cs` contains `Quote \" literal` and `Shell &amp; | ; ( ) ^`.
    - `AutoQAC.Tests/Services/ProcessExecutionIntegrationTests.cs` contains `ExitCode.Should().Be(0)` and `JsonSerializer.Deserialize<string[]>` in the argv preservation test.
    - `AutoQAC.Tests/Services/ProcessExecutionIntegrationTests.cs` contains `legacy-token`.
  </acceptance_criteria>
  <done>Helper mode exists and integration tests define exact parsed-argv preservation expectations for D-03, D-04, D-13, D-14, D-15, and D-16.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve ArgumentList in ProcessExecutionService clone</name>
  <files>AutoQAC/Services/Process/ProcessExecutionService.cs, AutoQAC.Tests/Services/ProcessExecutionIntegrationTests.cs</files>
  <read_first>
    - AutoQAC/Services/Process/ProcessExecutionService.cs
    - AutoQAC.Tests/Services/ProcessExecutionIntegrationTests.cs
    - .planning/phases/06-command-launch-escaping/06-RESEARCH.md
  </read_first>
  <action>Update `ExecuteAsync` so the internal `processStartInfo` copies `ArgumentList` entries when `startInfo.ArgumentList.Count > 0`; only copy `startInfo.Arguments` when no `ArgumentList` entries are present. Prefer extracting a private clone helper in `ProcessExecutionService` if it keeps field preservation explicit. Preserve every currently copied setting exactly: `FileName`, `WorkingDirectory`, `UseShellExecute=false`, `CreateNoWindow`, `RedirectStandardInput`, `RedirectStandardOutput`, `RedirectStandardError`, `StandardInputEncoding`, `StandardOutputEncoding`, and `StandardErrorEncoding`. Keep PID tracking, timeout, user-stop, and force-kill semantics unchanged from Phase 5. Update the debug log so it does not rely on `startInfo.Arguments` as the only argument source; do not add full command-line disclosure beyond existing debug-level process-start information. Address review concern about mutually exclusive APIs by documenting and testing that `ArgumentList` entries take precedence in the clone when present, while legacy `Arguments` are copied only when `ArgumentList.Count == 0`.</action>
  <verify>
    <automated>dotnet test AutoQACSharp.slnx --filter FullyQualifiedName~ProcessExecutionIntegrationTests</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Services/Process/ProcessExecutionService.cs` contains `startInfo.ArgumentList.Count > 0`.
    - `AutoQAC/Services/Process/ProcessExecutionService.cs` contains `processStartInfo.ArgumentList.Add(argument)`.
    - `AutoQAC/Services/Process/ProcessExecutionService.cs` contains `else` followed by `processStartInfo.Arguments = startInfo.Arguments` or equivalent legacy fallback.
    - `AutoQAC/Services/Process/ProcessExecutionService.cs` still contains `UseShellExecute = false`.
    - `dotnet test AutoQACSharp.slnx --filter FullyQualifiedName~ProcessExecutionIntegrationTests` exits 0.
  </acceptance_criteria>
  <done>Real helper-process tests pass, proving the actual launched process receives the exact difficult argv strings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service-built ProcessStartInfo → OS process start | AutoQAC's internal clone becomes the authoritative `Process.Start` input. |
| test process stdout → test assertions | Helper process emits argv evidence consumed by tests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | `ProcessExecutionService.ExecuteAsync` | mitigate | Copy `ArgumentList` exactly when present and verify with a real helper process. |
| T-06-02-02 | Tampering | helper argv verification | mitigate | Serialize received argv as UTF-8 JSON to avoid delimiter and code-page ambiguity. |
| T-06-02-03 | Denial of Service | integration helper processes | mitigate | Keep existing bounded timeouts and `finally` cleanup/kill patterns from Phase 5 tests. |
</threat_model>

<verification>
Run `dotnet test AutoQACSharp.slnx --filter FullyQualifiedName~ProcessExecutionIntegrationTests` after each task, then full suite after Wave 1 completes.
</verification>

<success_criteria>
- `ProcessExecutionService` preserves `ArgumentList` without regressing Phase 5 stop/timeout/PID behavior.
- `AutoQAC.TestProcessHelper` supports `argv-echo` as the only helper extension for this phase.
- Integration tests prove exact parsed argv receipt for quote, Unicode, shell-sensitive, and combined cases.
</success_criteria>

<output>
After completion, create `.planning/phases/06-command-launch-escaping/06-02-SUMMARY.md`.
</output>
