---
phase: 06-command-launch-escaping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AutoQAC/Services/Cleaning/XEditCommandBuilder.cs
  - AutoQAC.Tests/Services/XEditCommandBuilderTests.cs
autonomous: true
requirements:
  - SAF-03
  - TEST-02
must_haves:
  truths:
    - "D-01/D-15: Curated direct and MO2 command-builder tests cover quote/parser, Unicode, shell-sensitive, and combined worst-case inputs."
    - "D-02: Tests cover every launch-bound input: xEdit executable path, MO2 executable path, plugin file name, and nested xEdit arguments."
    - "D-05/D-06/D-07/D-08: MO2 command shape remains `run`, xEdit path, `-a`, one nested xEdit payload, and file-name-only `-autoload`."
    - "Reviews: Direct xEdit `-autoload` argv shape is explicitly locked as parsed tokens `-autoload`, exact plugin filename, preserving the parsed intent of the old `-autoload \"Plugin.esp\"` command string."
    - "Reviews: MO2 nested `-a` payload uses a documented quote/backslash formatter with direct formatter tests for quotes and trailing backslashes."
    - "D-10/D-16: Builder passes authoritative inputs as argv data and asserts parsed ArgumentList entries rather than direct raw command strings."
  artifacts:
    - path: "AutoQAC/Services/Cleaning/XEditCommandBuilder.cs"
      provides: "Direct xEdit and MO2 ProcessStartInfo construction using ArgumentList"
      contains: "ArgumentList.Add"
    - path: "AutoQAC.Tests/Services/XEditCommandBuilderTests.cs"
      provides: "Curated command escaping regression matrix"
      contains: "ArgumentList"
  key_links:
    - from: "AutoQAC/Services/Cleaning/XEditCommandBuilder.cs"
      to: "System.Diagnostics.ProcessStartInfo.ArgumentList"
      via: "BuildCommand populates argv tokens and leaves direct Arguments empty"
      pattern: "ArgumentList\\.Add"
    - from: "AutoQAC.Tests/Services/XEditCommandBuilderTests.cs"
      to: "AutoQAC/Services/Cleaning/XEditCommandBuilder.cs"
      via: "BuildCommand assertions over FileName, WorkingDirectory, Arguments, and ArgumentList"
      pattern: "BuildCommand"
---

<objective>
Replace fragile direct xEdit and MO2 command construction with explicit argv contracts in `XEditCommandBuilder`.

Purpose: Users must be able to clean plugins whose launch-bound paths and names contain quotes, Unicode, spaces, and shell-sensitive punctuation without AutoQAC corrupting the target plugin or MO2 wrapper arguments.
Output: Command-builder tests and implementation proving direct xEdit argv tokens plus the locked MO2 `run <xEdit> -a <payload>` contract.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-command-launch-escaping/06-CONTEXT.md
@.planning/phases/06-command-launch-escaping/06-RESEARCH.md
@.planning/phases/06-command-launch-escaping/06-REVIEWS.md
@.planning/phases/06-command-launch-escaping/06-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
@AutoQAC/Services/Cleaning/XEditCommandBuilder.cs
@AutoQAC.Tests/Services/XEditCommandBuilderTests.cs

<interfaces>
From `AutoQAC/Services/Cleaning/XEditCommandBuilder.cs`:
```csharp
public interface IXEditCommandBuilder
{
    ProcessStartInfo? BuildCommand(PluginInfo plugin, GameType gameType);
}
```

From `AutoQAC/Models/PluginInfo.cs`:
```csharp
public sealed record PluginInfo
{
    public required string FileName { get; init; }
    public required string FullPath { get; init; }
    public bool IsInSkipList { get; init; }
    public GameType DetectedGameType { get; init; }
}
```

From `AutoQAC/Models/AppState.cs`:
```csharp
public sealed record AppState
{
    public string? Mo2ExecutablePath { get; init; }
    public string? XEditExecutablePath { get; init; }
    public int CleaningTimeout { get; init; } = 300;
    public bool Mo2ModeEnabled { get; init; }
    public bool PartialFormsEnabled { get; init; }
    public GameType CurrentGameType { get; init; } = GameType.Unknown;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add command-builder argv contract tests</name>
  <files>AutoQAC.Tests/Services/XEditCommandBuilderTests.cs</files>
  <read_first>
    - AutoQAC.Tests/Services/XEditCommandBuilderTests.cs
    - AutoQAC/Services/Cleaning/XEditCommandBuilder.cs
    - .planning/phases/06-command-launch-escaping/06-CONTEXT.md
    - .planning/phases/06-command-launch-escaping/06-RESEARCH.md
  </read_first>
  <behavior>
    - Direct xEdit mode returns `FileName == xEditPath`, `UseShellExecute == false`, empty `Arguments`, and parsed `ArgumentList` entries containing optional game flag, `-QAC`, `-autoexit`, `-autoload`, exact plugin file name, and optional partial-form flags. This addresses the review concern about Open Question A2 by explicitly locking the intended parsed argv contract instead of leaving `-autoload` shape implicit.
    - Add one exact ordering assertion for universal xEdit with partial forms: `result!.ArgumentList.Should().Equal("-FO4", "-QAC", "-autoexit", "-autoload", "Plugin.esp", "-iknowwhatimdoing", "-allowmakepartial");`.
    - Define and reuse `private const string WorstCasePluginName = "Quote\" Résumé 測試 🚀 & | ; ( ) ^ Spaces.esp";` in this test file.
    - The curated matrix covers `Simple Plugin.esp`, `Quote & Parser "Case".esp` as a synthetic parser case, `Résumé 測試 🚀.esp`, `Shell & | ; ( ) ^ Name.esp`, and `WorstCasePluginName`.
    - D-02 coverage is explicit: at least one test uses an xEdit executable path containing spaces and Unicode, one MO2 executable path containing spaces and Unicode, every matrix row varies plugin file name, and MO2 nested payload assertions inspect nested xEdit argument text.
    - MO2 mode returns `FileName == mo2Path`, empty `Arguments`, and `ArgumentList` exactly: `run`, xEdit path, `-a`, one nested xEdit payload string.
    - The MO2 nested payload contains file-name-only `-autoload` target text, not `plugin.FullPath`, and remains one `ArgumentList` entry.
    - Add direct tests for the MO2 nested formatter covering embedded quotes (`Quote "Case".esp`) and trailing backslashes in a synthetic nested value; assert the resulting payload keeps one `-a` value and round-trips through the same Windows-style parser assumptions used by the formatter where the test harness exposes that parser helper.
  </behavior>
  <action>Add focused tests before implementation. Replace string-fragment assertions with `result.ArgumentList.Should().Equal(...)` and `result.Arguments.Should().BeEmpty()` assertions. Cover D-01 through D-08, D-15, and D-16 explicitly in test names or assertion reason strings. Address review concern "direct -autoload argv shape" by naming the direct-mode test `BuildCommand_DirectMode_ShouldUseParsedAutoloadArgumentAndExactPluginFileName` or equivalent and asserting separate `"-autoload"` and exact `plugin.FileName` entries. Address review concern "MO2 nested escaping" by adding tests for a private nested-argument formatter before implementation. Preserve existing tests for null on missing xEdit path and `GameType.Unknown`; update their expectations only where `ArgumentList` changes the contract.</action>
  <verify>
    <automated>dotnet test AutoQACSharp.slnx --filter FullyQualifiedName~XEditCommandBuilderTests</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC.Tests/Services/XEditCommandBuilderTests.cs` contains `ArgumentList.Should()`.
    - `AutoQAC.Tests/Services/XEditCommandBuilderTests.cs` contains `Shell &amp; | ; ( ) ^`.
    - `AutoQAC.Tests/Services/XEditCommandBuilderTests.cs` contains `private const string WorstCasePluginName = "Quote\" Résumé 測試 🚀 &amp; | ; ( ) ^ Spaces.esp";`.
    - `AutoQAC.Tests/Services/XEditCommandBuilderTests.cs` contains `Résumé` and `測試`.
    - `AutoQAC.Tests/Services/XEditCommandBuilderTests.cs` contains `run`, `-a`, and an assertion that MO2 has exactly one nested payload entry after `-a`.
    - `AutoQAC.Tests/Services/XEditCommandBuilderTests.cs` contains an equality assertion for `"-FO4", "-QAC", "-autoexit", "-autoload", "Plugin.esp", "-iknowwhatimdoing", "-allowmakepartial"`.
  </acceptance_criteria>
  <done>Tests fail against the current manual `Arguments` implementation and precisely define the Phase 6 command-builder contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ArgumentList-based direct and MO2 command construction</name>
  <files>AutoQAC/Services/Cleaning/XEditCommandBuilder.cs, AutoQAC.Tests/Services/XEditCommandBuilderTests.cs</files>
  <read_first>
    - AutoQAC/Services/Cleaning/XEditCommandBuilder.cs
    - AutoQAC.Tests/Services/XEditCommandBuilderTests.cs
    - .planning/phases/06-command-launch-escaping/06-RESEARCH.md
  </read_first>
  <action>Update `BuildCommand` to construct `ProcessStartInfo` with `UseShellExecute = false`, `WorkingDirectory = Path.GetDirectoryName(...)`, and `ArgumentList` entries instead of direct `Arguments` concatenation. Direct xEdit mode must add optional universal game flag, `-QAC`, `-autoexit`, `-autoload`, the exact `plugin.FileName`, then partial-form flags in that order; add a short code comment explaining that this locks the parsed argv intent of the old `-autoload "Plugin.esp"` command string and avoids shell quoting at AutoQAC's process boundary. MO2 mode must preserve the locked D-05/D-06 contract by setting `FileName = config.Mo2ExecutablePath` and adding `run`, exact `xEditPath`, `-a`, and one nested xEdit payload string. Implement a private formatter such as `FormatMo2NestedArgument(...)` or `QuoteMo2NestedArgument(...)` that applies Windows CRT quote/backslash escaping only for the MO2-owned second parser boundary; add an XML doc comment or concise comment explaining why MO2 `-a` is intentionally different from direct mode. Direct mode must not quote-decorate argv fragments. Do not normalize, strip, sanitize, or rewrite any configured path or plugin name per D-10.</action>
  <verify>
    <automated>dotnet test AutoQACSharp.slnx --filter FullyQualifiedName~XEditCommandBuilderTests</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Services/Cleaning/XEditCommandBuilder.cs` contains `ArgumentList.Add("-autoload")`.
    - `AutoQAC/Services/Cleaning/XEditCommandBuilder.cs` contains `ArgumentList.Add(plugin.FileName)` for direct mode.
    - `AutoQAC/Services/Cleaning/XEditCommandBuilder.cs` contains `ArgumentList.Add("run")`, `ArgumentList.Add("-a")`, and `ArgumentList.Add(xEditPath)` for MO2 mode.
    - `AutoQAC/Services/Cleaning/XEditCommandBuilder.cs` contains `MO2 owns a second parser boundary` or equivalent comment/doc text beside the nested formatter.
    - `AutoQAC/Services/Cleaning/XEditCommandBuilder.cs` does not contain `$"-autoload \"{plugin.FileName}\""`.
    - `dotnet test AutoQACSharp.slnx --filter FullyQualifiedName~XEditCommandBuilderTests` exits 0.
  </acceptance_criteria>
  <done>Direct xEdit and MO2 command builder tests pass, and production code no longer relies on direct-mode manual command-line escaping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user config/plugin metadata → process argv | Configured executable paths and plugin names become process launch data. |
| AutoQAC → MO2 nested parser | AutoQAC passes one `-a` payload that MO2 later interprets as xEdit arguments. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | `XEditCommandBuilder.BuildCommand` direct mode | mitigate | Use `ProcessStartInfo.ArgumentList` tokens and tests for quotes, Unicode, spaces, and shell-sensitive punctuation. |
| T-06-01-02 | Tampering | `XEditCommandBuilder.BuildCommand` MO2 mode | mitigate | Preserve one audited `-a` payload and assert MO2 argv contract without spreading nested xEdit args into MO2 argv. |
| T-06-01-03 | Information Disclosure | command-builder tests/log-adjacent behavior | accept | This plan does not add user-facing output or logging; Plan 06-03 covers user-facing failure copy. |
</threat_model>

<verification>
Run `dotnet test AutoQACSharp.slnx --filter FullyQualifiedName~XEditCommandBuilderTests` after each task, then full suite after Wave 1 completes.
</verification>

<success_criteria>
- `XEditCommandBuilder` uses `ArgumentList` for every AutoQAC-controlled process argv boundary.
- Direct mode passes plugin names and paths as exact strings without shell escaping or sanitization.
- MO2 mode preserves `run`, xEdit path, `-a`, and one nested payload containing file-name-only `-autoload`.
- Curated tests cover D-01 through D-08, D-10, D-15, and D-16.
</success_criteria>

<output>
After completion, create `.planning/phases/06-command-launch-escaping/06-01-SUMMARY.md`.
</output>
