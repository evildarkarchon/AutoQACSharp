---
phase: 05-process-stop-pid-safety
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - AutoQAC/Models/TerminationResult.cs
  - AutoQAC/Services/Process/IProcessExecutionService.cs
  - AutoQAC/Services/Process/ProcessExecutionService.cs
  - AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs
autonomous: true
requirements:
  - SAF-01
  - SAF-02
  - REF-04
must_haves:
  truths:
    - "User-initiated Stop reports `GracePeriodExpired` instead of auto-force-killing inside `ExecuteAsync` (D-01, D-02, SAF-01)."
    - "Timeout handling may still auto-force after the grace period (D-05)."
    - "Failed `Kill(true)` or post-kill wait returns `ForceKillFailed`, never `ForceKilled` (D-06, SAF-02)."
    - "`ForceKilled` means kill was invoked and the tracked/root process exited (D-07)."
    - "Process service uses injected PID store/session abstractions from Plan 01 (D-10, D-13, REF-04)."
  artifacts:
    - path: "AutoQAC/Models/TerminationResult.cs"
      provides: "ForceKillFailed termination outcome"
      contains: "ForceKillFailed"
    - path: "AutoQAC/Services/Process/ProcessExecutionService.cs"
      provides: "Explicit timeout vs user-stop termination behavior"
      contains: "ProcessStopReason"
    - path: "AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs"
      provides: "Unit tests for cancellation intent and force-kill failures"
  key_links:
    - from: "AutoQAC/Services/Process/ProcessExecutionService.cs"
      to: "AutoQAC/Services/Process/IPidStore.cs"
      via: "constructor-injected store"
      pattern: "IPidStore"
    - from: "AutoQAC/Services/Process/ProcessExecutionService.cs"
      to: "AutoQAC/Models/TerminationResult.cs"
      via: "force-kill result mapping"
      pattern: "ForceKillFailed"
---

<objective>
Correct low-level process termination semantics while preserving the single process slot.

Purpose: The process service currently conflates user cancellation with timeout and reports failed force-kill attempts as success.
Output: Explicit cancellation intent, `ForceKillFailed`, PID store integration, and focused process-service regression tests.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-process-stop-pid-safety/05-CONTEXT.md
@.planning/phases/05-process-stop-pid-safety/05-RESEARCH.md
@.planning/phases/05-process-stop-pid-safety/05-01-SUMMARY.md
@AutoQAC/Services/Process/IProcessExecutionService.cs
@AutoQAC/Services/Process/ProcessExecutionService.cs
@AutoQAC/Models/TerminationResult.cs
@AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs

<interfaces>
Existing process-service signatures:
```csharp
Task<ProcessResult> ExecuteAsync(ProcessStartInfo startInfo, TimeSpan? timeout = null, CancellationToken ct = default, Action<Process>? onProcessStarted = null, string? pluginName = null);
Task<TerminationResult> TerminateProcessAsync(Process process, bool forceKill = false, CancellationToken ct = default);
```

New internal intent model:
```csharp
private enum ProcessStopReason
{
    Timeout,
    UserRequestedStop
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrate PID abstractions into process execution</name>
  <files>AutoQAC/Services/Process/ProcessExecutionService.cs, AutoQAC/Services/Process/IProcessExecutionService.cs, AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs</files>
  <read_first>AutoQAC/Services/Process/ProcessExecutionService.cs, AutoQAC/Services/Process/IProcessExecutionService.cs, AutoQAC/Services/Process/IPidStore.cs, AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs</read_first>
  <behavior>
    - Test 1: `TrackProcessAsync` writes through injected `IPidStore.UpdateAsync` with current session ID.
    - Test 2: `UntrackProcessAsync` removes only the matching PID through `IPidStore.UpdateAsync`.
    - Test 3: `CleanOrphanedProcessesAsync` loads through `IPidStore`, filters prior-session entries, and does not clear current-session entries blindly.
  </behavior>
  <action>Change `ProcessExecutionService` constructor to accept `ILoggingService logger`, `IPidStore pidStore`, and `IProcessSessionIdProvider sessionIdProvider`. Replace private `GetPidFilePath`, `LoadTrackedProcessesAsync`, and `SaveTrackedProcessesAsync` calls with `pidStore.LoadAsync` and `pidStore.UpdateAsync`. `TrackProcessAsync` must set `SessionId = sessionIdProvider.CurrentSessionId` per D-13. `CleanOrphanedProcessesAsync` must inspect only entries whose `SessionId` is empty or differs from the current session, then remove only processed prior-session entries; do not clear the entire PID file. Preserve `SemaphoreSlim(1,1)` and do not change xEdit sequential execution.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~ProcessExecutionService"</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Services/Process/ProcessExecutionService.cs` constructor contains `IPidStore pidStore` and `IProcessSessionIdProvider sessionIdProvider`.
    - `AutoQAC/Services/Process/ProcessExecutionService.cs` no longer contains `GetPidFilePath(`.
    - `AutoQAC/Services/Process/ProcessExecutionService.cs` contains `SessionId = sessionIdProvider.CurrentSessionId`.
    - Existing reflection access to `GetPidFilePath` is removed from `AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs`.
  </acceptance_criteria>
  <done>PID tracking in `ProcessExecutionService` is injectable, session-aware, and no longer tested through private reflection (D-10, D-13, REF-04).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Split user-stop and timeout termination results</name>
  <files>AutoQAC/Models/TerminationResult.cs, AutoQAC/Services/Process/IProcessExecutionService.cs, AutoQAC/Services/Process/ProcessExecutionService.cs, AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs</files>
  <read_first>AutoQAC/Models/TerminationResult.cs, AutoQAC/Services/Process/ProcessExecutionService.cs, .planning/phases/05-process-stop-pid-safety/05-RESEARCH.md</read_first>
  <behavior>
    - Test 1: user cancellation that reaches graceful timeout does not call force kill inside `ExecuteAsync`; result path preserves `GracePeriodExpired` for caller handling.
    - Test 2: timeout cancellation still attempts force kill after grace expiration.
    - Test 3: `TerminateProcessAsync(forceKill: true)` returns `ForceKillFailed` when `Kill(true)` or `WaitForExitAsync` fails with documented exceptions.
  </behavior>
  <action>Add `ForceKillFailed` to `TerminationResult` with XML documentation per D-06. Add an explicit internal stop-reason branch in `ExecuteAsync` so timeout-driven interruption may force-kill after `GracePeriodExpired` per D-05, while user-driven cancellation returns/leaves `GracePeriodExpired` for the orchestrator/ViewModel confirmation flow per D-01 and D-02. In `TerminateProcessAsync(forceKill: true)`, return `ForceKilled` only after `Kill(entireProcessTree: true)` is invoked and `WaitForExitAsync` completes for the tracked/root process per D-07. Catch `Win32Exception`, `NotSupportedException`, `AggregateException`, and post-kill `OperationCanceledException`; log details and return `ForceKillFailed` rather than `ForceKilled` per D-06. Do not introduce blocking `.Wait()`/`.Result` calls.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~ProcessExecutionService"</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Models/TerminationResult.cs` contains `ForceKillFailed`.
    - `AutoQAC/Services/Process/ProcessExecutionService.cs` contains `ProcessStopReason.UserRequestedStop` and `ProcessStopReason.Timeout`.
    - `AutoQAC/Services/Process/ProcessExecutionService.cs` contains `return TerminationResult.ForceKillFailed` in force-kill exception handling.
    - The automated test command exits 0.
  </acceptance_criteria>
  <done>User Stop no longer auto-force-kills before confirmation, timeout auto-force remains allowed, and force-kill failures are represented accurately (D-01, D-02, D-05, D-06, D-07, SAF-01, SAF-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user cancellation→process service | Caller cancellation intent must not be misinterpreted as permission to force kill. |
| Windows process API→AutoQAC result model | `Kill(true)` and `WaitForExitAsync` can fail or only prove root-process exit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Denial of Service | `ExecuteAsync` | mitigate | Separate timeout and user-stop reasons so user Stop cannot force-kill before confirmation. |
| T-05-02-02 | Repudiation | `TerminateProcessAsync` | mitigate | Return `ForceKillFailed` on kill/wait exceptions and log exception details. |
| T-05-02-03 | Tampering | orphan cleanup | mitigate | Filter PID cleanup by session ID and process identity instead of clearing all entries. |
</threat_model>

<verification>
Run `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~ProcessExecutionService"`, then run `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~JsonPidStore"` to ensure Plan 01 behavior still passes.
</verification>

<success_criteria>
Process execution preserves the single process slot while distinguishing timeout, user stop, force-kill success, and force-kill failure.
</success_criteria>

<output>
After completion, create `.planning/phases/05-process-stop-pid-safety/05-02-SUMMARY.md`.
</output>
