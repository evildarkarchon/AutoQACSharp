---
phase: 05-process-stop-pid-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AutoQAC/Models/TrackedProcess.cs
  - AutoQAC/Services/Process/IPidStore.cs
  - AutoQAC/Services/Process/JsonPidStore.cs
  - AutoQAC/Services/Process/IPidStorePathProvider.cs
  - AutoQAC/Services/Process/DefaultPidStorePathProvider.cs
  - AutoQAC/Services/Process/IProcessSessionIdProvider.cs
  - AutoQAC/Services/Process/ProcessSessionIdProvider.cs
  - AutoQAC/Infrastructure/ServiceCollectionExtensions.cs
  - AutoQAC.Tests/Services/JsonPidStoreTests.cs
autonomous: true
requirements:
  - REF-04
must_haves:
  truths:
    - "Maintainer can test PID tracking through injected storage/path abstractions without reflection (D-10, REF-04)."
    - "PID JSON read-modify-write operations are protected by an interprocess file lock (D-11)."
    - "Corrupt PID JSON is preserved as a timestamped copy before a clean store is recreated (D-12)."
    - "Tracked PID entries include a session ID so current-run and prior-run entries are distinguishable (D-13)."
  artifacts:
    - path: "AutoQAC/Services/Process/IPidStore.cs"
      provides: "Injected PID store abstraction"
      exports: ["IPidStore"]
    - path: "AutoQAC/Services/Process/JsonPidStore.cs"
      provides: "File-lock-protected JSON PID store"
      contains: "FileStream"
    - path: "AutoQAC.Tests/Services/JsonPidStoreTests.cs"
      provides: "PID store abstraction, lock, session ID, and corrupt-file tests"
    - path: "AutoQAC/Infrastructure/ServiceCollectionExtensions.cs"
      provides: "Singleton DI registrations for PID store services"
      contains: "IPidStore"
  key_links:
    - from: "AutoQAC/Models/TrackedProcess.cs"
      to: "AutoQAC/Services/Process/JsonPidStore.cs"
      via: "SessionId serialized with PID entries"
      pattern: "SessionId"
    - from: "AutoQAC.Tests/Services/JsonPidStoreTests.cs"
      to: "AutoQAC/Services/Process/JsonPidStore.cs"
      via: "temp-path injected path provider"
      pattern: "JsonPidStore"
---

<objective>
Create the PID storage foundation for Phase 5.

Purpose: Process/PID cleanup cannot be tested or made process-safe while the PID path and JSON read/write logic are private helpers inside `ProcessExecutionService`.
Output: Injectable PID store/path/session abstractions, locked JSON implementation, session-aware `TrackedProcess`, and direct unit tests.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-process-stop-pid-safety/05-CONTEXT.md
@.planning/phases/05-process-stop-pid-safety/05-RESEARCH.md
@.planning/phases/05-process-stop-pid-safety/05-VALIDATION.md
@AutoQAC/Models/TrackedProcess.cs
@AutoQAC/Services/Process/ProcessExecutionService.cs
@AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs

<interfaces>
Existing contracts to preserve:
```csharp
public sealed record TrackedProcess
{
    public int Pid { get; init; }
    public DateTime StartTime { get; init; }
    public string PluginName { get; init; } = string.Empty;
}
```

New contracts to create:
```csharp
public interface IPidStore
{
    Task<IReadOnlyList<TrackedProcess>> LoadAsync(CancellationToken ct = default);
    Task UpdateAsync(Func<IReadOnlyList<TrackedProcess>, IReadOnlyList<TrackedProcess>> update, CancellationToken ct = default);
}

public interface IPidStorePathProvider
{
    string PidFilePath { get; }
}

public interface IProcessSessionIdProvider
{
    string CurrentSessionId { get; }
}

public sealed class JsonPidStore(IPidStorePathProvider pathProvider, ILoggingService logger) : IPidStore;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define session-aware PID store contracts</name>
  <files>AutoQAC/Models/TrackedProcess.cs, AutoQAC/Services/Process/IPidStore.cs, AutoQAC/Services/Process/IPidStorePathProvider.cs, AutoQAC/Services/Process/IProcessSessionIdProvider.cs</files>
  <read_first>AutoQAC/Models/TrackedProcess.cs, AutoQAC/Services/Process/ProcessExecutionService.cs, .planning/phases/05-process-stop-pid-safety/05-CONTEXT.md</read_first>
  <behavior>
    - Test 1: constructing `TrackedProcess` can set and read `SessionId` without changing existing `Pid`, `StartTime`, or `PluginName` semantics.
    - Test 2: interface signatures expose async load and locked update seams that tests can substitute without reflection.
  </behavior>
  <action>Create `IPidStore`, `IPidStorePathProvider`, and `IProcessSessionIdProvider` in `AutoQAC/Services/Process/`. Add `public string SessionId { get; init; } = string.Empty;` to `TrackedProcess` per D-13. Add tight XML doc comments to each new interface and to the new property. Do not remove existing comments. Do not move PID storage into SQLite or another dependency; keep JSON per D-10.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~ProcessExecutionService"</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Services/Process/IPidStore.cs` contains `Task&lt;IReadOnlyList&lt;TrackedProcess&gt;&gt; LoadAsync`.
    - `AutoQAC/Services/Process/IPidStore.cs` contains `Task UpdateAsync(Func&lt;IReadOnlyList&lt;TrackedProcess&gt;, IReadOnlyList&lt;TrackedProcess&gt;&gt; update`.
    - `AutoQAC/Models/TrackedProcess.cs` contains `public string SessionId { get; init; } = string.Empty;`.
  </acceptance_criteria>
  <done>PID storage can be referenced through public interfaces and tracked entries carry a session ID (D-10, D-13, REF-04).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement locked JSON PID store and providers</name>
  <files>AutoQAC/Services/Process/JsonPidStore.cs, AutoQAC/Services/Process/DefaultPidStorePathProvider.cs, AutoQAC/Services/Process/ProcessSessionIdProvider.cs, AutoQAC/Infrastructure/ServiceCollectionExtensions.cs, AutoQAC.Tests/Services/JsonPidStoreTests.cs</files>
  <read_first>AutoQAC/Services/Process/ProcessExecutionService.cs, AutoQAC/Infrastructure/ServiceCollectionExtensions.cs, AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs, .planning/codebase/TESTING.md</read_first>
  <behavior>
    - Test 1: `JsonPidStore.UpdateAsync` serializes entries to the injected temp path.
    - Test 2: concurrent `UpdateAsync` calls preserve all updates rather than losing one.
    - Test 3: invalid JSON is copied to `autoqac-pids.corrupt-*.json` and `LoadAsync` returns an empty list.
    - Test 4: `ProcessSessionIdProvider.CurrentSessionId` is non-empty and stable for the provider instance.
    - Test 5: DI registration resolves one singleton `IProcessSessionIdProvider` and one singleton `IPidStore` instance.
  </behavior>
  <action>Addresses review concern: PID store locking and DI registrations were underspecified. Create `JsonPidStore` with exact constructor `JsonPidStore(IPidStorePathProvider pathProvider, ILoggingService logger)`. Use a private process-local `SemaphoreSlim(1, 1)` plus bounded retry/backoff around `FileStream.Lock(0, 1)` so same-process and cross-process contention do not lose updates. `LoadAsync` and `UpdateAsync` must both open the PID file with `FileMode.OpenOrCreate`, `FileAccess.ReadWrite`, `FileShare.ReadWrite`, async enabled, acquire `stream.Lock(0, 1)` before reading, and release it in `finally`; recovery writes also happen under that same lock. In `finally`, call `stream.Unlock(0, 1)` and include a one-line WHY comment if unlock exceptions are intentionally swallowed. Use a static `JsonSerializerOptions` instance with indented output. Recover only `JsonException`/JSON format failures: preserve original bytes at `autoqac-pids.corrupt-{yyyyMMddHHmmssfff}.json`, log corruption metadata through `ILoggingService`, truncate/rewrite a clean empty JSON array, and return an empty list per D-12. Do not convert `UnauthorizedAccessException`, `IOException` sharing failures, or other access errors into an empty store; let them surface after logging. Create `DefaultPidStorePathProvider` with the same production path resolution currently in `GetPidFilePath`, and create `ProcessSessionIdProvider` with a per-app-run GUID string. Register PID services in `AutoQAC/Infrastructure/ServiceCollectionExtensions.cs` before `ProcessExecutionService` can depend on them: `services.AddSingleton&lt;IPidStorePathProvider, DefaultPidStorePathProvider&gt;();`, `services.AddSingleton&lt;IProcessSessionIdProvider, ProcessSessionIdProvider&gt;();`, and `services.AddSingleton&lt;IPidStore, JsonPidStore&gt;();`. `IProcessSessionIdProvider` MUST be singleton so one app run has one session ID. Write XML doc comments for new public classes/methods.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~JsonPidStore"</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Services/Process/JsonPidStore.cs` contains `stream.Lock(0, 1)` and `stream.Unlock(0, 1)`.
    - `AutoQAC/Services/Process/JsonPidStore.cs` contains `SemaphoreSlim` and a bounded retry/backoff for lock contention.
    - `AutoQAC/Services/Process/JsonPidStore.cs` contains `corrupt-` and `JsonException`.
    - `AutoQAC/Infrastructure/ServiceCollectionExtensions.cs` contains `AddSingleton&lt;IPidStorePathProvider, DefaultPidStorePathProvider&gt;`, `AddSingleton&lt;IProcessSessionIdProvider, ProcessSessionIdProvider&gt;`, and `AddSingleton&lt;IPidStore, JsonPidStore&gt;`.
    - `AutoQAC.Tests/Services/JsonPidStoreTests.cs` contains tests named with `Corrupt`, `Concurrent`, and `SessionId`.
    - The automated test command exits 0.
  </acceptance_criteria>
  <done>PID JSON updates are process-safe, corrupt files are preserved, and PID store behavior is directly testable without reflection (D-10, D-11, D-12, REF-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem→PID store | Local JSON may be stale, corrupt, or concurrently modified. |
| process table→orphan cleanup | PIDs may be reused or inaccessible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | `JsonPidStore` | mitigate | Lock PID store reads, recovery writes, and updates with `FileStream.Lock(0, 1)`, plus process-local `SemaphoreSlim` and bounded retry/backoff for deterministic contention handling. |
| T-05-01-02 | Repudiation | corrupt PID JSON | mitigate | Preserve timestamped corrupt copy and log corruption metadata before recreating clean store. |
| T-05-01-03 | Denial of Service | PID reuse | mitigate | Add `SessionId` to entries; later process-service integration filters current/prior sessions. |
</threat_model>

<verification>
Run `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~JsonPidStore|FullyQualifiedName~ProcessExecutionService"`.
</verification>

<success_criteria>
PID storage is injectable, file-lock protected, corruption-preserving, session-aware, and covered by direct tests.
</success_criteria>

<output>
After completion, create `.planning/phases/05-process-stop-pid-safety/05-01-SUMMARY.md`.
</output>
