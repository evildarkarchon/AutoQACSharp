---
phase: 05
slug: process-stop-pid-safety
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-28
---

# Phase 05 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — Avalonia FluentTheme desktop UI |
| Preset | not applicable |
| Component library | Avalonia 12 Fluent controls; existing `MessageDialog`, `ProgressWindow`, `MainWindow`, and `CleaningResultsWindow` patterns |
| Icon library | none — existing glyph text icons (`ℹ`, `⚠`, `❌`, `?`, `!`) only |
| Font | Avalonia FluentTheme default UI font; do not introduce a new font for this phase |

**Existing design-system evidence:** `AutoQAC/App.axaml` uses `<FluentTheme />`, the Avalonia DataGrid Fluent include, and a shared `Button.hyperlink` style. No React, Next.js, Vite, Tailwind, `components.json`, or shadcn setup exists, so the shadcn gate is not applicable.

**Avalonia/MVVM contract:** Implement UI changes through existing ViewModel/service boundaries. ViewModels may request stop/confirmation through `CleaningCommandsViewModel`, `ProgressViewModel`, `ICleaningOrchestrator`, and `IMessageDialogService`; views/code-behind must remain limited to window/dialog ownership and binding setup.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding, row separators |
| sm | 8px | Compact element spacing, banner content padding |
| md | 16px | Default element spacing, dialog message spacing |
| lg | 24px | Section padding, modal outer margin for new Phase 5 dialogs/panels |
| xl | 32px | Large separation before destructive confirmation actions |
| 2xl | 48px | Reserved for major page breaks; not expected in Phase 5 changes |
| 3xl | 64px | Reserved for page-level spacing; not expected in Phase 5 changes |

Exceptions: Preserve existing window-local spacing already present in `MainWindow.axaml`, `ProgressWindow.axaml`, and `MessageDialog.axaml` (notably 5px/10px/15px/20px values) when the phase only changes behavior or copy. Any new Phase 5 UI surface must use the 4-point scale above. Existing Stop buttons keep their current `MinWidth` (`120px` in MainWindow, `100px` in ProgressWindow); do not shrink destructive/stop touch targets below the existing size.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 | 1.5 |
| Label | 12px | 400 | 1.4 |
| Heading | 16px | 600 | 1.2 |
| Display | 20px | 600 | 1.2 |

**Typography rules:** Use exactly these Phase 5 text sizes for new or changed stop/PID-safety copy. Keep font weights to regular (`400`) and semibold (`600`) for new copy; existing `Bold` text may remain only where already present. Error details in expandable dialog details may retain the existing 12px monospace style.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | Avalonia FluentTheme default window background | Main window, progress window, and dialog base surfaces |
| Secondary (30%) | `{DynamicResource SystemControlBackgroundBaseLowBrush}` / `{DynamicResource SystemControlBackgroundAltHighBrush}` | Summary cards, dialog detail blocks, progress summary panels, low-emphasis grouped surfaces |
| Accent (10%) | `{DynamicResource SystemAccentColor}` / existing blue `#0078D4` | Information/question icons, existing hyperlink-style text buttons, non-destructive default dialog affordance only |
| Destructive | `#D13438` for error icon/text; existing red `#F44336` may remain for validation glyphs | Force-kill failure, destructive termination/error state only |

Accent reserved for: question/info dialog icon, existing hyperlink-style buttons, and default non-destructive confirmation focus. Do **not** use accent blue for force-kill, failure, or warning states.

**Warning color:** Use existing warning palette for escalation prompts and “left running” outcomes: banner/dialog icon `#FF8C00`, warning foreground `#BF360C` or `#8B4513`, warning surface `#FFF3E0`/`#FFF2CC`, border `#E65100`/`#E6A817`.

**State-to-color mapping:**
- First Stop requested / graceful stop pending: neutral status text plus existing indeterminate progress indicator.
- Grace period expired / force-kill prompt: warning icon and warning copy, not destructive red until the user confirms force termination or a force-kill failure occurs.
- User declines force termination / xEdit left running: warning state.
- Force kill succeeds: neutral completion/cancelled state; avoid celebratory success styling.
- Force kill fails: destructive/error state with concise message and optional expandable details/log reference.

**Visual hierarchy / focal point:** The primary visual anchor for Phase 5 is the Stop / Force Terminate status row in the main and progress windows. During escalation, the confirmation prompt draws attention in this order: warning icon/title first, risk copy second, and the destructive `Force Terminate` action last; force-kill failure dialogs draw attention first to the error title, then the manual-close instruction, then expandable/log details.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | `Stop Cleaning` for first user stop request; `Force Terminate` only inside the explicit escalation prompt |
| Empty state heading | `No active cleaning process` |
| Empty state body | `Start a cleaning session before using stop controls.` |
| Error state | `AutoQAC could not force terminate xEdit. xEdit may still be running; close it manually or check the log for details before starting another cleaning session.` |
| Destructive confirmation | `Force Terminate xEdit`: `xEdit did not exit after the stop request. Force terminating can interrupt any remaining file or log writes. Do you want AutoQAC to force terminate xEdit now?` |

### Required Interaction Copy

| Situation | UI Surface | Title | Body | Buttons / Outcome |
|-----------|------------|-------|------|-------------------|
| First Stop click while cleaning | MainWindow status and ProgressWindow termination row | n/a | Status text: `Stopping... waiting for xEdit to exit` | Disable start/preview controls as already done during cleaning. Keep Stop available if it is the explicit second-click escalation path; otherwise show existing indeterminate stopping indicator. |
| Grace period expires after user Stop | Existing `MessageDialog` through `IMessageDialogService` | `Force Terminate xEdit?` | `xEdit did not exit after the stop request. Force terminating can interrupt any remaining file or log writes. Do you want AutoQAC to force terminate xEdit now?` | Primary/default: `Yes` only if existing `YesNo` dialog remains. Preferred copy if button labels become customizable: `Force Terminate` and `Leave Running`. |
| User declines force termination | Progress/result/status state | `Cleaning Stopped` | `AutoQAC stopped the cleaning session. xEdit was left running by your choice; close it manually when it is safe.` | Preferred close label: `Close Warning`. Do not parse post-exit logs for the affected plugin. |
| Second Stop click during grace/confirmation path | MainWindow/ProgressWindow Stop button behavior | n/a | Treat as explicit escalation. If surfaced, status text: `Force terminating xEdit...` | No second prompt. |
| Force kill succeeds | Progress/result/status state | `Cleaning Stopped` | `xEdit was force terminated and the cleaning session was stopped.` | Preferred close label: `Close Stop Notice`. |
| Force kill fails | Existing `MessageDialog` error | `Could Not Force Terminate xEdit` | `AutoQAC could not force terminate xEdit. xEdit may still be running; close it manually or check the log for details before starting another cleaning session.` | Preferred close label: `Close Warning`; expandable details may contain sanitized exception type/message or log reference, but no stack trace in primary body. |
| Duplicate AutoQAC instance blocked | Startup message dialog or equivalent app-level error | `AutoQAC Is Already Running` | `Another AutoQAC window is already running. Close the existing window before starting AutoQAC again.` | Preferred close label: `Close AutoQAC Notice`; no technical mutex/PID details in primary body. |
| PID file corrupt and recreated | Log plus optional warning only if surfaced during cleanup | `Process Tracking Reset` | `AutoQAC reset its process tracking file because it could not be read. Cleaning can continue, but previous orphan-process tracking may be incomplete.` | Preferred close label: `Close Process Tracking Notice`; detailed corrupt-file path belongs in logs, not primary UI. |

### Interaction Rules

- Preserve locked decisions from `05-CONTEXT.md`: first user Stop must not force-kill before confirmation, declining the prompt leaves xEdit running, and a second Stop click during grace/confirmation is explicit escalation.
- Force-kill failure must be a first-class user-visible outcome, never reported as `ForceKilled` or as a normal cancellation.
- When xEdit may still be running (`GracePeriodExpired` declined or `ForceKillFailed`), the affected plugin must not display log-derived success/cleaned results.
- Keep detailed exception information in logs or expandable details. The primary dialog body must be concise and action-oriented.
- Legacy constraint: if the existing `MessageDialog` API cannot customize single-button labels yet, legacy `OK` may remain only on untouched dialog plumbing. Any Phase 5-touched or newly customizable single-button outcome must use the preferred labels above instead of generic `OK`.
- Maintain dialog ownership through existing `MessageDialog`/window-owner patterns; do not introduce an Avalonia Headless dependency or new UI framework.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — Avalonia project, shadcn not initialized |
| third-party | none | no third-party registries or UI blocks declared; no registry vetting required |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

## Source Traceability

| Source | Decisions Used |
|--------|----------------|
| `.planning/phases/05-process-stop-pid-safety/05-CONTEXT.md` | Stop confirmation ownership, second-click escalation, decline leaves xEdit running, force-kill failure visibility, PID/single-instance scope |
| `.planning/phases/05-process-stop-pid-safety/05-RESEARCH.md` | Avalonia MVVM boundaries, state/dialog propagation, no new UI packages, concise failure dialog and blocked log parsing on unsafe termination |
| `.planning/ROADMAP.md` | Phase 5 goal, UI hint, SAF-01/SAF-02/REF-04/TEST-01 success criteria |
| `.planning/REQUIREMENTS.md` | User-visible stop confirmation and force-kill failure requirements |
| Existing AXAML | FluentTheme, spacing/typography/color conventions, ProgressWindow stop/termination affordances, MessageDialog structure |
