---
phase: 05-process-stop-pid-safety
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - AutoQAC/Services/Cleaning/ICleaningOrchestrator.cs
  - AutoQAC/Services/Cleaning/StopCleaningResult.cs
  - AutoQAC/Services/Cleaning/CleaningOrchestrator.cs
  - AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs
  - AutoQAC.Tests/Services/CleaningOrchestratorTests.cs
  - AutoQAC.Tests/ViewModels/CleaningCommandsViewModelTests.cs
autonomous: true
requirements:
  - SAF-01
  - SAF-02
must_haves:
  truths:
    - "First Stop shows the confirmation path before force-kill is requested (D-01, D-02, SAF-01)."
    - "Declining the force-terminate prompt leaves xEdit running and reports that choice clearly (D-03)."
    - "Second Stop during the grace/confirmation path force-kills immediately without another prompt (D-04)."
    - "Force-kill failure is user-visible, concise, logged in detail, and blocks post-exit log parsing (D-08, D-09, SAF-02)."
    - "Stop/force-stop APIs return structured outcomes so the ViewModel does not rely only on a fragile mutable side channel (review consensus)."
  artifacts:
    - path: "AutoQAC/Services/Cleaning/CleaningOrchestrator.cs"
      provides: "Stop/force-stop state machine and unsafe-log-read guard"
      contains: "ForceKillFailed"
    - path: "AutoQAC/Services/Cleaning/StopCleaningResult.cs"
      provides: "Structured stop outcome returned by orchestrator stop APIs"
      contains: "TerminationResult"
    - path: "AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs"
      provides: "Stop confirmation, decline, and force-kill failure dialogs"
      contains: "Force Terminate xEdit?"
    - path: "AutoQAC.Tests/ViewModels/CleaningCommandsViewModelTests.cs"
      provides: "ViewModel stop interaction tests"
  key_links:
    - from: "AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs"
      to: "AutoQAC/Services/Cleaning/CleaningOrchestrator.cs"
      via: "StopCleaningAsync then optional ForceStopCleaningAsync"
      pattern: "LastTerminationResult"
    - from: "AutoQAC/Services/Cleaning/CleaningOrchestrator.cs"
      to: "AutoQAC/Services/Cleaning/IXEditLogFileService"
      via: "skip ReadLogContentAsync when xEdit may still be running"
      pattern: "ForceKillFailed|GracePeriodExpired"
---

<objective>
Wire the corrected process termination semantics into user-visible stop behavior.

Purpose: Phase 5 is only complete if users see the explicit confirmation, decline, second-click escalation, and force-kill failure outcomes described in the context and UI contract.
Output: Orchestrator/ViewModel state-machine updates and tests proving prompt ordering, decline, second stop, failure reporting, and unsafe log-read blocking.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-process-stop-pid-safety/05-CONTEXT.md
@.planning/phases/05-process-stop-pid-safety/05-RESEARCH.md
@.planning/phases/05-process-stop-pid-safety/05-UI-SPEC.md
@.planning/phases/05-process-stop-pid-safety/05-02-SUMMARY.md
@AutoQAC/Services/Cleaning/ICleaningOrchestrator.cs
@AutoQAC/Services/Cleaning/CleaningOrchestrator.cs
@AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs
@AutoQAC/Services/UI/IMessageDialogService.cs
@AutoQAC.Tests/Services/CleaningOrchestratorTests.cs

<interfaces>
Stop UI surface:
```csharp
Task<bool> ShowConfirmAsync(string title, string message);
Task ShowWarningAsync(string title, string message, string? details = null);
Task ShowErrorAsync(string title, string message, string? details = null);

public sealed record StopCleaningResult(TerminationResult? TerminationResult, bool MayStillBeRunning, string? Message = null);
```

Required copy from UI-SPEC:
```text
Force Terminate xEdit?
xEdit did not exit after the stop request. Force terminating can interrupt any remaining file or log writes. Do you want AutoQAC to force terminate xEdit now?
AutoQAC stopped the cleaning session. xEdit was left running by your choice; close it manually when it is safe.
AutoQAC could not force terminate xEdit. xEdit may still be running; close it manually or check the log for details before starting another cleaning session.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist stop outcomes in orchestrator state</name>
  <files>AutoQAC/Services/Cleaning/ICleaningOrchestrator.cs, AutoQAC/Services/Cleaning/StopCleaningResult.cs, AutoQAC/Services/Cleaning/CleaningOrchestrator.cs, AutoQAC.Tests/Services/CleaningOrchestratorTests.cs</files>
  <read_first>AutoQAC/Services/Cleaning/ICleaningOrchestrator.cs, AutoQAC/Services/Cleaning/CleaningOrchestrator.cs, AutoQAC.Tests/Services/CleaningOrchestratorTests.cs, AutoQAC/Models/TerminationResult.cs</read_first>
  <behavior>
    - Test 1: first Stop stores `GracePeriodExpired` and does not force-kill.
    - Test 2: second Stop after `GracePeriodExpired` calls `TerminateProcessAsync(..., true, ...)` exactly once.
    - Test 3: `ForceStopCleaningAsync` stores `ForceKillFailed` when process service returns it.
    - Test 4: log reading is skipped when last termination result is `GracePeriodExpired` or `ForceKillFailed`.
    - Test 5: `StopCleaningAsync` and `ForceStopCleaningAsync` return `StopCleaningResult` with `MayStillBeRunning == true` for `GracePeriodExpired`, `LeftRunningByUser`, and `ForceKillFailed`.
  </behavior>
  <action>Addresses review concern: `LastTerminationResult` alone is a fragile side channel. Add `StopCleaningResult` with exact shape `public sealed record StopCleaningResult(TerminationResult? TerminationResult, bool MayStillBeRunning, string? Message = null);`. Update `ICleaningOrchestrator.StopCleaningAsync` and `ForceStopCleaningAsync` to return `Task&lt;StopCleaningResult&gt;` while preserving existing command semantics. Update `CleaningOrchestrator` so `_lastTerminationResult` records `GracePeriodExpired`, `ForceKilled`, `ForceKillFailed`, and `LeftRunningByUser`, but callers also receive the same outcome in `StopCleaningResult`. `ForceStopCleaningAsync` must store and return the result from `TerminateProcessAsync(proc, forceKill: true, ...)` instead of discarding it. Preserve the ordering contract: `CleaningCommandsViewModel` reads the returned `StopCleaningResult` immediately after `await StopCleaningAsync()`/`await ForceStopCleaningAsync()`; `_lastTerminationResult` must not be reset before the stop command completes. Preserve D-04: if `_isStopRequested` is already true, `StopCleaningAsync` escalates immediately through `ForceStopCleaningAsync` without a second prompt. Add an orchestrator method or explicit branch to mark user-decline as `TerminationResult.LeftRunningByUser` after the ViewModel decline path, so the unsafe-log-read guard has a concrete signal. Update the log-read guard so any termination outcome where xEdit may still be running (`GracePeriodExpired` awaiting user decision, `LeftRunningByUser`, or `ForceKillFailed`) blocks `ReadLogContentAsync` per D-09. Keep sequential plugin processing unchanged.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~CleaningOrchestrator"</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/Services/Cleaning/CleaningOrchestrator.cs` contains `ForceKillFailed`.
    - `AutoQAC/Services/Cleaning/StopCleaningResult.cs` contains `MayStillBeRunning`.
    - `AutoQAC/Services/Cleaning/CleaningOrchestrator.cs` assigns `_lastTerminationResult = result` after force-stop.
    - `AutoQAC/Services/Cleaning/CleaningOrchestrator.cs` contains `LeftRunningByUser`.
    - `AutoQAC.Tests/Services/CleaningOrchestratorTests.cs` contains tests referencing `ForceKillFailed` and `ReadLogContentAsync`.
    - The automated test command exits 0.
  </acceptance_criteria>
  <done>Orchestrator state accurately records stop/force outcomes and prevents unsafe log parsing when xEdit may still be active (D-04, D-08, D-09, SAF-02).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface confirmation, decline, and failure copy in ViewModel</name>
  <files>AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs, AutoQAC.Tests/ViewModels/CleaningCommandsViewModelTests.cs</files>
  <read_first>AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs, AutoQAC/Services/Cleaning/ICleaningOrchestrator.cs, AutoQAC/Services/UI/IMessageDialogService.cs, .planning/phases/05-process-stop-pid-safety/05-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: `StopCleaningAsync` shows confirmation with title `Force Terminate xEdit?` after `GracePeriodExpired`.
    - Test 2: when confirmation returns false, `ForceStopCleaningAsync` is not called and warning copy says xEdit was left running by user choice.
    - Test 3: when force stop returns `ForceKillFailed`, ViewModel shows `Could Not Force Terminate xEdit` with the UI-SPEC error body.
    - Test 4: ViewModel uses the returned `StopCleaningResult` snapshot, not a post-await `LastTerminationResult` read, to decide confirmation/failure UI.
  </behavior>
  <action>Addresses review concern: user-decline and force-kill failure must not depend on a race-prone `LastTerminationResult` read. Update `CleaningCommandsViewModel.StopCleaningAsync` to store the returned `StopCleaningResult` from `await _orchestrator.StopCleaningAsync()` in a local variable and branch on `result.TerminationResult`. Use `LastTerminationResult` only as a compatibility fallback if an existing test/helper path still returns null. Copy must match `05-UI-SPEC.md`: title `Force Terminate xEdit?` and body `xEdit did not exit after the stop request. Force terminating can interrupt any remaining file or log writes. Do you want AutoQAC to force terminate xEdit now?` per D-01/D-02. If the user declines, do not call `ForceStopCleaningAsync`; call the orchestrator's user-decline marker from Task 1 so `TerminationResult.LeftRunningByUser` is stored, set status to a warning outcome, and call `ShowWarningAsync` with `Cleaning Stopped` plus `AutoQAC stopped the cleaning session. xEdit was left running by your choice; close it manually when it is safe.` per D-03. If the user confirms, branch on the returned `StopCleaningResult` from `await _orchestrator.ForceStopCleaningAsync()`; when it is `ForceKillFailed`, call `ShowErrorAsync` with title `Could Not Force Terminate xEdit` and body `AutoQAC could not force terminate xEdit. xEdit may still be running; close it manually or check the log for details before starting another cleaning session.` per D-08. Do not rewrite `IMessageDialogService` for custom button labels in this phase; existing Yes/No maps to Force Terminate/Leave Running per UI-SPEC legacy constraint, and add a code comment documenting that mapping. Keep ViewModel free of direct process/control manipulation; all termination remains through orchestrator and dialogs through `IMessageDialogService`.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~CleaningCommandsViewModel"</automated>
  </verify>
  <acceptance_criteria>
    - `AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs` contains `Force Terminate xEdit?`.
    - `AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs` contains `xEdit was left running by your choice`.
    - `AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs` contains `Could Not Force Terminate xEdit`.
    - `AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs` contains `StopCleaningResult` or `stopResult`.
    - `AutoQAC.Tests/ViewModels/CleaningCommandsViewModelTests.cs` contains tests for confirm false, confirm true, and `ForceKillFailed`.
  </acceptance_criteria>
  <done>User-facing stop copy and behavior match locked decisions and UI-SPEC requirements (D-01, D-02, D-03, D-08, SAF-01, SAF-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ViewModel→orchestrator | User intent crosses from UI command to process-control service. |
| process outcome→user dialog | Low-level process failures must be summarized without leaking stack traces in primary copy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Denial of Service | Stop command | mitigate | Require confirmation before force kill on first user Stop; second Stop is explicit escalation. |
| T-05-03-02 | Information Disclosure | force-kill failure dialog | mitigate | Show concise UI-SPEC body and keep detailed exception data in logs/details. |
| T-05-03-03 | Integrity | log parsing after unsafe termination | mitigate | Skip log parsing when termination result indicates xEdit may still be running or flushing logs. |
</threat_model>

<verification>
Run `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~CleaningOrchestrator|FullyQualifiedName~CleaningCommandsViewModel"`.
</verification>

<success_criteria>
First Stop prompts before force kill, declining leaves xEdit running with a clear warning, second Stop escalates, and force-kill failure is a distinct user-visible outcome.
</success_criteria>

<output>
After completion, create `.planning/phases/05-process-stop-pid-safety/05-03-SUMMARY.md`.
</output>
