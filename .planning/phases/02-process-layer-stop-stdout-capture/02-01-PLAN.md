---
phase: 02-process-layer-stop-stdout-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AutoQAC/Services/Process/ProcessExecutionService.cs
  - AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs
autonomous: true
requirements: [PRC-01, PRC-02]

must_haves:
  truths:
    - "ProcessExecutionService.ExecuteAsync no longer sets RedirectStandardOutput or RedirectStandardError to true"
    - "ProcessExecutionService.ExecuteAsync no longer calls BeginOutputReadLine or BeginErrorReadLine"
    - "ProcessExecutionService.ExecuteAsync no longer creates ConcurrentQueue buffers or OutputDataReceived/ErrorDataReceived handlers"
    - "ProcessResult.OutputLines and ErrorLines are returned as empty lists (fields kept for Phase 4 interface cleanup)"
    - "UseShellExecute remains false (needed for WaitForExitAsync, PID tracking, CreateNoWindow)"
    - "CreateNoWindow remains false (xEdit is a GUI app)"
    - "All PID tracking, timeout, termination, and MO2 wrapping behavior is unchanged"
    - "The old concurrent stdout/stderr capture test is deleted"
    - "All remaining tests pass"
  artifacts:
    - path: "AutoQAC/Services/Process/ProcessExecutionService.cs"
      provides: "Process executor without stdout/stderr redirection"
      contains: "UseShellExecute = false"
    - path: "AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs"
      provides: "Tests without dead stdout capture test"
  key_links:
    - from: "AutoQAC/Services/Cleaning/CleaningService.cs"
      to: "ProcessExecutionService.ExecuteAsync"
      via: "IProcessExecutionService"
      pattern: "processService\\.ExecuteAsync"
    - from: "AutoQAC/Services/Cleaning/XEditCommandBuilder.cs"
      to: "ProcessExecutionService.ExecuteAsync"
      via: "ProcessStartInfo consumed by ExecuteAsync"
      pattern: "new ProcessStartInfo"
---

<objective>
Remove dead stdout/stderr redirection from ProcessExecutionService so xEdit launches without capturing empty output streams.

Purpose: xEdit writes its output to log files, not stdout. The current code redirects stdout/stderr and captures nothing, creating a broken data pipeline. This phase removes the dead capture plumbing while preserving all process lifecycle behavior (PID tracking, timeouts, termination, MO2 wrapping).

Output: Modified ProcessExecutionService without redirect plumbing, updated test file without the dead capture test.
</objective>

<execution_context>
@C:/Users/evild/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From AutoQAC/Services/Process/IProcessExecutionService.cs (NOT modified in this phase):
```csharp
public interface IProcessExecutionService
{
    Task<ProcessResult> ExecuteAsync(
        ProcessStartInfo startInfo,
        IProgress<string>? outputProgress = null,
        TimeSpan? timeout = null,
        CancellationToken ct = default,
        Action<System.Diagnostics.Process>? onProcessStarted = null,
        string? pluginName = null);
    // ... other methods unchanged
}

public sealed record ProcessResult
{
    public int ExitCode { get; init; }
    public List<string> OutputLines { get; init; } = new();
    public List<string> ErrorLines { get; init; } = new();
    public bool TimedOut { get; init; }
}
```

From AutoQAC/Services/Cleaning/XEditCommandBuilder.cs (NOT modified, MO2 wrapping reference):
```csharp
// MO2 wrapping returns ProcessStartInfo with MO2 as FileName, xEdit as inner arg
// ProcessExecutionService consumes this -- no stdout dependency in MO2 path
if (config.Mo2ModeEnabled && !string.IsNullOrEmpty(config.Mo2ExecutablePath))
{
    return new ProcessStartInfo
    {
        FileName = config.Mo2ExecutablePath,
        Arguments = mo2Args,
        WorkingDirectory = Path.GetDirectoryName(config.Mo2ExecutablePath)
    };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove stdout/stderr redirect plumbing from ProcessExecutionService.ExecuteAsync</name>
  <files>AutoQAC/Services/Process/ProcessExecutionService.cs</files>
  <read_first>
    - AutoQAC/Services/Process/ProcessExecutionService.cs (current implementation -- lines 50-161 are the target area)
    - AutoQAC/Services/Process/IProcessExecutionService.cs (interface contract -- NOT modified, confirms ProcessResult shape stays)
    - AutoQAC/Services/Cleaning/XEditCommandBuilder.cs (MO2 wrapping -- read-only reference, confirms no stdout dependency)
  </read_first>
  <action>
Modify `ProcessExecutionService.ExecuteAsync()` to remove all stdout/stderr capture plumbing while preserving process lifecycle behavior. Per D-01, this is minimal scope -- interface stays intact for Phase 4.

Specific changes to the `ExecuteAsync` method body:

1. **Remove redirect flags** (currently lines 55-56):
   - Delete `RedirectStandardOutput = true,`
   - Delete `RedirectStandardError = true,`
   - Keep `UseShellExecute = false` (per D-02, required for PID tracking, WaitForExitAsync, CreateNoWindow)
   - Keep `CreateNoWindow = false` (xEdit is a GUI app)

2. **Remove capture infrastructure** (currently lines 64-77):
   - Delete `var outputLines = new ConcurrentQueue<string>();`
   - Delete `var errorLines = new ConcurrentQueue<string>();`
   - Delete the entire `process.OutputDataReceived += ...` handler block (lines 67-72)
   - Delete the entire `process.ErrorDataReceived += ...` handler block (lines 74-77)

3. **Remove begin-read calls** (currently lines 84-85):
   - Delete `process.BeginOutputReadLine();`
   - Delete `process.BeginErrorReadLine();`

4. **Update return value** (currently lines 155-161):
   - Change `OutputLines = outputLines.ToList(),` to `OutputLines = [],`
   - Change `ErrorLines = errorLines.ToList(),` to `ErrorLines = [],`
   - Keep `ExitCode` and `TimedOut` logic exactly as-is

5. **Update startup failure return** (currently line 90):
   - Change `ErrorLines = [ex.Message]` to `ErrorLines = []` -- the error is already logged via `logger.Error(ex, ...)` and the `-1` exit code signals failure. Per D-01, error lines are dead output (Phase 4 cleanup). Alternatively, keeping `[ex.Message]` is also acceptable since it doesn't harm anything -- use discretion here. The key point is that `ConcurrentQueue` is gone.
   - Actually, keep `ErrorLines = [ex.Message]` on the startup failure path -- this is a useful diagnostic that costs nothing and the field is already in the record. Only the runtime capture queues are removed.

6. **Remove dead using** (line 2):
   - Delete `using System.Collections.Concurrent;` -- no longer needed since `ConcurrentQueue` is removed
   - Keep `using System.Linq;` -- still used by PID tracking code (`Where`, `Any`, `ToList` on lines 284, 360)

7. **Do NOT modify** (per D-01):
   - The `IProgress<string>? outputProgress` parameter (stays in signature, just not called -- Phase 4 cleanup)
   - Any PID tracking code
   - Any timeout/cancellation logic
   - Any termination logic (TerminateProcessAsync)
   - Any orphan cleanup code (CleanOrphanedProcessesAsync)
  </action>
  <verify>
    <automated>cd J:/AutoQACSharp && dotnet build AutoQAC/AutoQAC.csproj</automated>
  </verify>
  <acceptance_criteria>
    - ProcessExecutionService.cs does NOT contain the string `RedirectStandardOutput`
    - ProcessExecutionService.cs does NOT contain the string `RedirectStandardError`
    - ProcessExecutionService.cs does NOT contain the string `BeginOutputReadLine`
    - ProcessExecutionService.cs does NOT contain the string `BeginErrorReadLine`
    - ProcessExecutionService.cs does NOT contain the string `OutputDataReceived`
    - ProcessExecutionService.cs does NOT contain the string `ErrorDataReceived`
    - ProcessExecutionService.cs does NOT contain the string `ConcurrentQueue`
    - ProcessExecutionService.cs does NOT contain `using System.Collections.Concurrent;`
    - ProcessExecutionService.cs DOES contain `UseShellExecute = false`
    - ProcessExecutionService.cs DOES contain `CreateNoWindow = false`
    - ProcessExecutionService.cs DOES contain `OutputLines = [],` in the normal return path
    - ProcessExecutionService.cs DOES contain `ErrorLines = [],` in the normal return path
    - ProcessExecutionService.cs DOES contain `IProgress<string>? outputProgress` in the method signature
    - ProcessExecutionService.cs DOES contain `using System.Linq;`
    - `dotnet build AutoQAC/AutoQAC.csproj` exits 0 with no errors
  </acceptance_criteria>
  <done>ProcessExecutionService.ExecuteAsync no longer redirects stdout/stderr, no longer creates capture queues or event handlers, no longer calls BeginOutputReadLine/BeginErrorReadLine. Returns empty OutputLines/ErrorLines. All other process lifecycle behavior (PID tracking, timeouts, termination, slot management) is unchanged. Project builds cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Delete dead stdout capture test and verify all tests pass</name>
  <files>AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs</files>
  <read_first>
    - AutoQAC.Tests/Services/ProcessExecutionServiceTests.cs (current test file -- lines 132-155 are the test to delete)
    - AutoQAC/Services/Process/ProcessExecutionService.cs (the modified source from Task 1 -- verify the test deletion aligns with the implementation change)
  </read_first>
  <action>
Per D-03, delete the `ExecuteAsync_ShouldCaptureConcurrentStdoutAndStderrWithoutLoss` test method (currently lines 132-155) from `ProcessExecutionServiceTests.cs`. This test validates stdout/stderr capture behavior that no longer exists after Task 1.

Specific changes:

1. **Delete the test method** (lines 132-155):
   - Remove the entire `[Fact] public async Task ExecuteAsync_ShouldCaptureConcurrentStdoutAndStderrWithoutLoss()` method
   - This is inside the `#region Thread-Safety and Handle Lifecycle Tests` region

2. **Keep the region** (`#region Thread-Safety and Handle Lifecycle Tests`):
   - The region still contains `CleanOrphanedProcessesAsync_ShouldNotLeakProcessHandlesAcrossRepeatedRuns` (lines 157-193) which tests PID tracking handle cleanup -- still valid
   - Keep the region header and the remaining test

3. **Do NOT modify**:
   - The `ExecuteAsync_WhenProcessNotFound_ShouldReturnFailedResult` test -- still valid (tests startup failure path)
   - The `Dispose_ShouldPreventFurtherExecution` test -- still valid
   - The `CleanOrphanedProcessesAsync_ShouldNotLeakProcessHandlesAcrossRepeatedRuns` test -- still valid (tests PID tracking)
   - All orchestrator-level tests -- still valid (they use IProcessExecutionService mocks, not the real implementation)

4. **Verify**: Run `dotnet test AutoQACSharp.slnx` to confirm all remaining tests pass. The deleted test was the only one that spawned a real cmd.exe process for stdout capture -- removing it should not affect any other test.
  </action>
  <verify>
    <automated>cd J:/AutoQACSharp && dotnet test AutoQACSharp.slnx</automated>
  </verify>
  <acceptance_criteria>
    - ProcessExecutionServiceTests.cs does NOT contain the string `ShouldCaptureConcurrentStdoutAndStderrWithoutLoss`
    - ProcessExecutionServiceTests.cs DOES contain `ExecuteAsync_WhenProcessNotFound_ShouldReturnFailedResult`
    - ProcessExecutionServiceTests.cs DOES contain `Dispose_ShouldPreventFurtherExecution`
    - ProcessExecutionServiceTests.cs DOES contain `CleanOrphanedProcessesAsync_ShouldNotLeakProcessHandlesAcrossRepeatedRuns`
    - ProcessExecutionServiceTests.cs DOES contain `Orchestrator_StartCleaning_CallsCleanOrphanedProcessesAsync`
    - `dotnet test AutoQACSharp.slnx` exits 0 with all tests passing
  </acceptance_criteria>
  <done>The dead stdout capture test is deleted. All remaining tests (startup failure, disposal, handle lifecycle, orchestrator termination/orphan cleanup) pass. No test validates stdout/stderr capture behavior that was removed in Task 1.</done>
</task>

</tasks>

<verification>
1. `dotnet build AutoQACSharp.slnx` -- entire solution builds without errors or warnings
2. `dotnet test AutoQACSharp.slnx` -- all tests pass (the deleted test is the only one removed)
3. grep confirms no stdout/stderr redirect code remains in ProcessExecutionService.cs
4. grep confirms UseShellExecute = false is still present (per D-02)
5. grep confirms IProgress parameter is still in the method signature (per D-01)
</verification>

<success_criteria>
- PRC-01 satisfied: ProcessExecutionService no longer redirects stdout/stderr
- PRC-02 satisfied: MO2 wrapping works identically (UseShellExecute=false, PID tracking, WaitForExitAsync all preserved -- no stdout dependency in MO2 path)
- D-01 honored: Minimal scope -- only internal capture plumbing removed, interface fields and IProgress parameter kept
- D-02 honored: UseShellExecute stays false
- D-03 honored: Dead capture test deleted, no replacement test created
- D-04 honored: No additional MO2 verification or comments added
- All tests pass with `dotnet test AutoQACSharp.slnx`
- Solution builds cleanly with `dotnet build AutoQACSharp.slnx`
</success_criteria>

<output>
After completion, create `.planning/phases/02-process-layer-stop-stdout-capture/02-01-SUMMARY.md`
</output>
