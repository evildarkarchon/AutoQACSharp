---
phase: 04-cleanup-remove-dead-code
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AutoQAC/Services/Cleaning/IXEditLogFileService.cs
  - AutoQAC/Services/Cleaning/XEditLogFileService.cs
  - AutoQAC/Services/Cleaning/CleaningService.cs
  - AutoQAC.Tests/Services/CleaningServiceTests.cs
  - AutoQAC.Tests/Services/XEditLogFileServiceTests.cs
autonomous: true
requirements: [CLN-01, CLN-02, CLN-03]

must_haves:
  truths:
    - "No obsolete ReadLogFileAsync or single-arg GetLogFilePath method exists in the codebase"
    - "CleaningService no longer accepts IXEditOutputParser as a constructor parameter"
    - "No legacy timestamp-based log staleness detection code exists anywhere"
    - "All tests pass after removals"
  artifacts:
    - path: "AutoQAC/Services/Cleaning/IXEditLogFileService.cs"
      provides: "Clean interface with only offset-based API methods"
      contains: "GetLogFilePath(string xEditDirectory, GameType gameType)"
    - path: "AutoQAC/Services/Cleaning/XEditLogFileService.cs"
      provides: "Implementation without legacy methods"
    - path: "AutoQAC/Services/Cleaning/CleaningService.cs"
      provides: "CleaningService without IXEditOutputParser dependency"
    - path: "AutoQAC.Tests/Services/XEditLogFileServiceTests.cs"
      provides: "Test file without legacy test region"
    - path: "AutoQAC.Tests/Services/CleaningServiceTests.cs"
      provides: "Test file without _mockOutputParser field"
  key_links:
    - from: "AutoQAC/Services/Cleaning/CleaningService.cs"
      to: "AutoQAC/Infrastructure/ServiceCollectionExtensions.cs"
      via: "DI constructor resolution"
      pattern: "AddSingleton<ICleaningService, CleaningService>"
---

<objective>
Remove obsolete legacy log file methods and dead IXEditOutputParser dependency from CleaningService.

Purpose: Eliminate dead code that references the old timestamp-based log reading approach (replaced by offset-based reading in Phase 1) and remove the unused parser parameter deferred from Phase 3 (per D-01, D-02 from CONTEXT.md).

Output: Clean IXEditLogFileService interface and XEditLogFileService implementation with only the offset-based API. CleaningService constructor no longer takes IXEditOutputParser. All related test infrastructure removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cleanup-remove-dead-code/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete obsolete log methods and legacy tests (D-01, CLN-02)</name>
  <files>
    AutoQAC/Services/Cleaning/IXEditLogFileService.cs,
    AutoQAC/Services/Cleaning/XEditLogFileService.cs,
    AutoQAC.Tests/Services/XEditLogFileServiceTests.cs
  </files>
  <read_first>
    AutoQAC/Services/Cleaning/IXEditLogFileService.cs,
    AutoQAC/Services/Cleaning/XEditLogFileService.cs,
    AutoQAC.Tests/Services/XEditLogFileServiceTests.cs
  </read_first>
  <action>
**IXEditLogFileService.cs** -- Remove both `[Obsolete]`-marked method declarations and all their XML doc comments:
1. Delete the `[Obsolete("Use GetLogFilePath(string, GameType) instead. Will be removed in Phase 4.")]` attribute and the `string GetLogFilePath(string xEditExecutablePath);` declaration (current lines ~26-29).
2. Delete the `[Obsolete("Use ReadLogContentAsync instead. Will be removed in Phase 4.")]` attribute and the `Task<(List<string> lines, string? error)> ReadLogFileAsync(...)` declaration (current lines ~33-45).
3. Delete the "Legacy methods" section comment header and separator line (lines ~18-21: `// Legacy methods (preserved for CleaningOrchestrator compatibility)`).
4. Remove `using System.Collections.Generic;` from imports if it becomes unused after removing the `List<string>` return type (keep it if other declarations still need it -- check first).
5. The remaining interface should contain only: `GetLogFilePath(string, GameType)`, `GetExceptionLogFilePath(string, GameType)`, `CaptureOffset(string)`, `ReadLogContentAsync(...)`.

**XEditLogFileService.cs** -- Remove the entire legacy section (lines 171-223):
1. Delete the "Legacy methods" separator comment (lines ~171-173).
2. Delete the `[Obsolete] GetLogFilePath(string xEditExecutablePath)` method (lines ~176-184).
3. Delete the `[Obsolete] ReadLogFileAsync(string, DateTime, CancellationToken)` method (lines ~187-223).
4. Remove `#pragma warning disable/restore CS0618` that existed inside the deleted ReadLogFileAsync.
5. Verify no remaining code references these deleted methods.

**XEditLogFileServiceTests.cs** -- Delete the entire legacy test region (lines 512-585):
1. Delete the `#region Legacy methods (backward compatibility)` and matching `#endregion`.
2. Delete all 4 test methods inside:
   - `LegacyGetLogFilePath_StillWorks` (line ~514)
   - `LegacyReadLogFileAsync_FileNotFound_ReturnsError` (line ~527)
   - `LegacyReadLogFileAsync_StaleLog_ReturnsError` (line ~543)
   - `LegacyReadLogFileAsync_FreshLog_ReturnsLines` (line ~563)
3. Remove any `#pragma warning disable/restore CS0618` lines associated with these tests.
  </action>
  <verify>
    <automated>cd J:/AutoQACSharp && dotnet build AutoQACSharp.slnx 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - IXEditLogFileService.cs does NOT contain the string "Obsolete"
    - IXEditLogFileService.cs does NOT contain "GetLogFilePath(string xEditExecutablePath)"
    - IXEditLogFileService.cs does NOT contain "ReadLogFileAsync"
    - IXEditLogFileService.cs DOES contain "GetLogFilePath(string xEditDirectory, GameType gameType)"
    - IXEditLogFileService.cs DOES contain "CaptureOffset"
    - IXEditLogFileService.cs DOES contain "ReadLogContentAsync"
    - XEditLogFileService.cs does NOT contain the string "Obsolete"
    - XEditLogFileService.cs does NOT contain "ReadLogFileAsync"
    - XEditLogFileService.cs does NOT contain "processStartTime"
    - XEditLogFileService.cs does NOT contain "GetLastWriteTimeUtc"
    - XEditLogFileServiceTests.cs does NOT contain "LegacyGetLogFilePath"
    - XEditLogFileServiceTests.cs does NOT contain "LegacyReadLogFileAsync"
    - XEditLogFileServiceTests.cs does NOT contain "CS0618"
    - Solution builds with 0 errors
  </acceptance_criteria>
  <done>All obsolete log file methods and their tests are deleted. No timestamp-based staleness detection code remains. Solution compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Remove IXEditOutputParser from CleaningService and update tests (D-02, CLN-01, CLN-03)</name>
  <files>
    AutoQAC/Services/Cleaning/CleaningService.cs,
    AutoQAC.Tests/Services/CleaningServiceTests.cs
  </files>
  <read_first>
    AutoQAC/Services/Cleaning/CleaningService.cs,
    AutoQAC.Tests/Services/CleaningServiceTests.cs,
    AutoQAC/Infrastructure/ServiceCollectionExtensions.cs
  </read_first>
  <action>
**CleaningService.cs** -- Remove the dead `IXEditOutputParser outputParser` primary constructor parameter:
1. In the primary constructor (line 14-21), delete the `IXEditOutputParser outputParser` parameter. The constructor should become:
   ```
   public sealed class CleaningService(
       IGameDetectionService gameDetection,
       IStateService stateService,
       ILoggingService logger,
       IProcessExecutionService processService,
       IXEditCommandBuilder commandBuilder)
       : ICleaningService
   ```
2. Remove `using AutoQAC.Services.Cleaning;` import line ONLY if it was needed solely for `IXEditOutputParser` (it is also needed for `IXEditCommandBuilder` which is in the same namespace -- so this using is NOT needed at all since CleaningService is already in `namespace AutoQAC.Services.Cleaning`). Actually, check: all referenced types are either in the same namespace or imported elsewhere. No import changes should be needed here.
3. DI note: `ServiceCollectionExtensions.cs` registers `IXEditOutputParser` as a singleton AND `ICleaningService` as a singleton. The DI container resolves constructor params automatically. Since `CleaningService` no longer takes `IXEditOutputParser`, no DI registration changes are needed -- the container simply stops injecting it into CleaningService. The `IXEditOutputParser` registration must stay because `CleaningOrchestrator` still depends on it.

**CleaningServiceTests.cs** -- Remove dead `_mockOutputParser` field and all references:
1. Delete the `private readonly IXEditOutputParser _mockOutputParser;` field (line 20).
2. Delete `_mockOutputParser = Substitute.For<IXEditOutputParser>();` from the constructor (line 29).
3. In EVERY `new CleaningService(...)` call, remove the `_mockOutputParser` argument. There are 10 occurrences (one per test method). Each currently passes 6 arguments; after removal they pass 5:
   ```
   new CleaningService(
       _mockGameDetection,
       _mockState,
       _mockLogger,
       _mockProcess,
       _mockCommandBuilder)
   ```
4. Remove `using AutoQAC.Services.Cleaning;` import ONLY if it becomes unused (it won't -- `IXEditCommandBuilder` and other cleaning types still need it). Actually, check: `IXEditOutputParser` is in `AutoQAC.Services.Cleaning` namespace. The test file already needs that namespace for `CleaningService`, `ICleaningService`, `IXEditCommandBuilder`, `CleaningResult`, `CleaningStatus`. So no import removal.
  </action>
  <verify>
    <automated>cd J:/AutoQACSharp && dotnet test AutoQACSharp.slnx 2>&1 | grep -E "(Passed|Failed|Total tests)"</automated>
  </verify>
  <acceptance_criteria>
    - CleaningService.cs does NOT contain "IXEditOutputParser"
    - CleaningService.cs does NOT contain "outputParser"
    - CleaningService.cs constructor has exactly 5 parameters: IGameDetectionService, IStateService, ILoggingService, IProcessExecutionService, IXEditCommandBuilder
    - CleaningServiceTests.cs does NOT contain "_mockOutputParser"
    - CleaningServiceTests.cs does NOT contain "IXEditOutputParser"
    - All tests pass (0 failures)
    - ServiceCollectionExtensions.cs STILL contains "AddSingleton<IXEditOutputParser, XEditOutputParser>" (must NOT be removed -- orchestrator uses it)
  </acceptance_criteria>
  <done>IXEditOutputParser is removed from CleaningService constructor and all test mock infrastructure. DI registration remains for CleaningOrchestrator's dependency. All 680+ tests pass.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `dotnet build AutoQACSharp.slnx` succeeds with 0 errors
2. `dotnet test AutoQACSharp.slnx` -- all tests pass with 0 failures
3. No `[Obsolete]` attributes remain in `IXEditLogFileService.cs` or `XEditLogFileService.cs`
4. No `ReadLogFileAsync` method exists in the log file service
5. No `IXEditOutputParser` reference exists in `CleaningService.cs`
6. `IXEditOutputParser` registration still exists in `ServiceCollectionExtensions.cs` (needed by orchestrator)
</verification>

<success_criteria>
- Legacy timestamp-based log reading code is completely removed (CLN-02)
- Dead stdout parsing parameter removed from CleaningService (CLN-01)
- 4 legacy tests deleted, remaining tests all pass (CLN-03)
- Orchestrator's IXEditOutputParser dependency is preserved (it is actively used)
</success_criteria>

<output>
After completion, create `.planning/phases/04-cleanup-remove-dead-code/04-01-SUMMARY.md`
</output>
