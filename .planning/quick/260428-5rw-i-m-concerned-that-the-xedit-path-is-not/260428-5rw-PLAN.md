---
phase: 260428-5rw-i-m-concerned-that-the-xedit-path-is-not
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AutoQAC.Tests/ViewModels/MainWindowViewModelTests.cs
  - AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs
  - AutoQAC.Tests/Services/ConfigurationServiceTests.cs
autonomous: true
requirements:
  - QUICK-260428-5rw
must_haves:
  truths:
    - "When xEdit is changed and saved from Edit > Settings, the main-window runtime state immediately reflects the saved xEdit path."
    - "The saved xEdit path remains present when user configuration is flushed and loaded by a fresh configuration service."
    - "Existing main-window Browse xEdit path behavior remains covered and passing."
  artifacts:
    - path: "AutoQAC.Tests/ViewModels/MainWindowViewModelTests.cs"
      provides: "Regression coverage for Settings dialog path-to-state synchronization"
      contains: "UpdateConfigurationPaths"
    - path: "AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs"
      provides: "Post-settings-save bridge from reloaded configuration into AppState path fields"
      contains: "UpdateConfigurationPaths"
    - path: "AutoQAC.Tests/Services/ConfigurationServiceTests.cs"
      provides: "Flush/reload coverage proving XEdit.Binary disk persistence"
      contains: "XEdit.Binary"
  key_links:
    - from: "AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs"
      to: "IStateService.UpdateConfigurationPaths"
      via: "successful ShowSettingsAsync config reload"
      pattern: "UpdateConfigurationPaths\\(.*config\\.LoadOrder\\.File.*config\\.ModOrganizer\\.Binary.*config\\.XEdit\\.Binary"
    - from: "AutoQAC.Tests/ViewModels/MainWindowViewModelTests.cs"
      to: "CleaningCommandsViewModel.ShowSettingsCommand"
      via: "test invokes settings command and asserts refreshed path state"
      pattern: "ShowSettingsCommand.*UpdateConfigurationPaths"
    - from: "AutoQAC.Tests/Services/ConfigurationServiceTests.cs"
      to: "ConfigurationService.FlushPendingSavesAsync"
      via: "save, flush, fresh reload assertion"
      pattern: "FlushPendingSavesAsync.*XEdit\\.Binary"
---

<objective>
Fix and verify the suspected xEdit path persistence problem in the Settings dialog flow.

Purpose: The research indicates Settings saves `config.XEdit.Binary`, but the main-window runtime state is not refreshed afterward, making the path appear unsaved until another reload. This plan reproduces that failure first, fixes the Settings-to-state bridge, and hardens disk flush/reload coverage for the xEdit path.

Output: A focused regression test, a scoped ViewModel bridge fix, and configuration persistence coverage.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@.planning/quick/260428-5rw-i-m-concerned-that-the-xedit-path-is-not/260428-5rw-CONTEXT.md
@.planning/quick/260428-5rw-i-m-concerned-that-the-xedit-path-is-not/260428-5rw-RESEARCH.md
@AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs
@AutoQAC/ViewModels/MainWindow/ConfigurationViewModel.cs
@AutoQAC/ViewModels/SettingsViewModel.cs
@AutoQAC/Services/Configuration/ConfigurationService.cs
@AutoQAC.Tests/ViewModels/MainWindowViewModelTests.cs
@AutoQAC.Tests/Services/ConfigurationServiceTests.cs

<interfaces>
Research-verified contracts and flow to preserve:

- `ConfigurationViewModel.ConfigureXEditAsync` already assigns `XEditPath = path`, calls `_stateService.UpdateConfigurationPaths(LoadOrderPath, Mo2Path, path)`, then saves config. Do not regress this main-window Browse flow.
- `SettingsViewModel.SaveAsync` persists `config.XEdit.Binary = XEditPath`; keep SettingsViewModel scoped to settings configuration and do not inject `IStateService` into it.
- `CleaningCommandsViewModel.ShowSettingsAsync` success branch reloads `UserConfiguration`; fix belongs there by pushing reloaded path values into `IStateService`.
- `IStateService.CurrentState.XEditExecutablePath` is consumed by cleaning validation/start behavior, so YAML-only persistence is not enough for immediate UX correctness.
- `ConfigurationService.SaveUserConfigAsync` debounces disk writes; file-content persistence tests must call `FlushPendingSavesAsync` or dispose the service before constructing a fresh service.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing Settings xEdit path state-sync regression test</name>
  <files>AutoQAC.Tests/ViewModels/MainWindowViewModelTests.cs</files>
  <behavior>
    - Given the Settings interaction returns success and `LoadUserConfigAsync` returns a configuration containing a new `XEdit.Binary`, the main-window command layer must push that xEdit path into `IStateService.UpdateConfigurationPaths`.
    - The assertion must include load-order path and MO2 path from the same reloaded config so this hardens the whole relevant configuration-path bridge per CONTEXT Fix Breadth.
    - The test must fail before production changes because `CleaningCommandsViewModel.ShowSettingsAsync` currently updates only `Mo2ModeEnabled` and `CleaningTimeout`.
  </behavior>
  <action>Add a focused regression test to `MainWindowViewModelTests` using the existing test setup style and NSubstitute conventions. Invoke `MainWindowViewModel.Commands.ShowSettingsCommand` or the narrowest existing command surface that exercises `CleaningCommandsViewModel.ShowSettingsAsync`; arrange the settings interaction to return `true`; arrange the subsequent config reload to include distinct load-order, MO2, and xEdit paths; assert `IStateService.UpdateConfigurationPaths(loadOrder, mo2, xEdit)` is received. Preserve MVVM boundaries and do not touch production code in this task.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~MainWindowViewModelTests"</automated>
  </verify>
  <done>The new regression test is present and fails before the production fix for the missing `UpdateConfigurationPaths` call, while existing main-window xEdit path tests remain unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refresh runtime path state after successful Settings save</name>
  <files>AutoQAC/ViewModels/MainWindow/CleaningCommandsViewModel.cs, AutoQAC.Tests/ViewModels/MainWindowViewModelTests.cs</files>
  <behavior>
    - After Settings returns success, the reloaded configuration path values are copied into `AppState` through `IStateService.UpdateConfigurationPaths`.
    - Existing settings state updates for MO2 mode and cleaning timeout are preserved.
    - Main-window Browse path persistence behavior remains passing.
  </behavior>
  <action>In `CleaningCommandsViewModel.ShowSettingsAsync`, after `LoadUserConfigAsync` returns the saved config and before/alongside the existing settings-state update, call `_stateService.UpdateConfigurationPaths(config.LoadOrder.File, config.ModOrganizer.Binary, config.XEdit.Binary)`. Keep the fix in this ViewModel/state bridge per research; do not make `SettingsViewModel` manipulate `IStateService`; do not introduce blocking calls or unrelated cleanup. If adding a helper method, include a tight XML doc comment unless it is a trivial private passthrough.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~MainWindowViewModelTests"</automated>
  </verify>
  <done>The Task 1 regression test passes, existing `MainWindowViewModelTests` pass, and the Settings success path updates load-order, MO2, and xEdit runtime paths from the reloaded config.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Harden xEdit disk persistence coverage</name>
  <files>AutoQAC.Tests/Services/ConfigurationServiceTests.cs</files>
  <behavior>
    - Saving user configuration with `XEdit.Binary` set, flushing pending saves, and loading through a fresh `ConfigurationService` returns the same xEdit path.
    - The test accounts for debounced writes by explicitly calling `FlushPendingSavesAsync` before asserting fresh-service reload behavior.
  </behavior>
  <action>Add or strengthen a `ConfigurationServiceTests` regression test for disk persistence of `XEdit.Binary`. Use existing temporary-directory/configuration test patterns; save a config with a distinctive xEdit executable path, call `FlushPendingSavesAsync`, construct/load through a fresh service instance if that is the established pattern, and assert the reloaded `XEdit.Binary` exactly matches. If equivalent coverage already exists, extend it to explicitly include `XEdit.Binary` rather than duplicating broad save/reload tests.</action>
  <verify>
    <automated>dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~ConfigurationServiceTests"; dotnet test AutoQACSharp.slnx</automated>
  </verify>
  <done>Configuration persistence tests prove xEdit path survives save/flush/fresh reload, and the full solution test suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI/settings input → configuration state | User-selected filesystem paths enter app state and persisted YAML. |
| persisted YAML → runtime cleaning validation | Saved paths are loaded back into `AppState` and later used by cleaning validation/process launch code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260428-5rw-01 | Tampering | `CleaningCommandsViewModel.ShowSettingsAsync` | mitigate | Use values from `ConfigurationService.LoadUserConfigAsync` after the Settings save succeeds, not stale ViewModel properties. |
| T-260428-5rw-02 | Denial of Service | `ConfigurationService` debounced saves | mitigate | Regression test must call `FlushPendingSavesAsync` before fresh reload to prove persisted xEdit path survives debounce. |
| T-260428-5rw-03 | Elevation of Privilege | xEdit executable path launch surface | accept | This quick task only synchronizes already-saved user-selected paths; executable validation/launch policy is existing behavior and is not expanded. |
</threat_model>

<verification>
Run targeted tests first to demonstrate RED then GREEN, then run the full suite:

1. `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~MainWindowViewModelTests"`
2. `dotnet test AutoQAC.Tests/AutoQAC.Tests.csproj --filter "FullyQualifiedName~ConfigurationServiceTests"`
3. `dotnet test AutoQACSharp.slnx`
</verification>

<success_criteria>
- Settings-dialog xEdit path saves no longer leave `AppState.XEditExecutablePath` stale after the dialog closes successfully.
- The Settings success branch updates all relevant configuration paths: load order, MO2 executable, and xEdit executable.
- Disk persistence coverage explicitly proves `XEdit.Binary` survives save, flush, and fresh reload.
- No changes violate MVVM boundaries, no UI-thread blocking is introduced, and no files under `Mutagen/` are modified.
</success_criteria>

<output>
After completion, create `.planning/quick/260428-5rw-i-m-concerned-that-the-xedit-path-is-not/260428-5rw-SUMMARY.md`.
</output>
